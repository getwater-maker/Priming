node_modules/
output/
/dist/
lora-dataset/
*.log
*.vrew
*.vrew.debug.json
.DS_Store
Thumbs.db
# Kaggle LoRA 학습 데이터셋(대용량 zip 등) — GitHub 100MB 초과, 배포 제외
kaggle-lora/*.zip
kaggle-lora/joseon-krea-dataset.zip
# 별개 프로젝트(임베디드 git repo) — 배포 제외
usage-coach/
# Qwen3-TTS 보이스디자인 — 파이썬 venv(2.5GB)·테스트 산출물은 로컬 전용(커밋 제외)
qwen-design/venv/
qwen-design/__pycache__/
qwen-design/test.wav
tts/sample_voice/

# 화이트보드 렌더러 — 파이썬 venv(263MB)·바이트코드는 각 PC 가 만든다(커밋 제외)
#   ⚠ 앱이 whiteboard/py/prepare_env.py 로 자동 생성한다. 지우면 다음 실행에 다시 만든다.
whiteboard/.venv/
whiteboard/py/__pycache__/
