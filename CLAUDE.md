# Priming — 작업 컨텍스트 노트

> 다음 AI(또는 사람)가 5분 안에 컨텍스트를 복원하기 위한 노트. (PrimingFlow CLAUDE.md 스타일)

## 프로젝트 한 줄 요약
**롱폼(16:9) 대본(.md) → TTS·이미지·(선택)비디오 → Vrew 4.0.1 .vrew 자동 생성** Electron 앱
+ **출판(POD) PDF** 모드. 모드는 **롱폼/출판 둘뿐**이다(쇼츠·플리는 2026-08-22 제거 — 아래 항목).
⚠ 이 파일의 옛 항목들에 나오는 쇼츠·플리·BGM·ACE-Step 세부는 **폐기된 이력**이다 — 따라 하지 말 것.

## ✏🎬 화이트보드 손그림 애니메이션 — **1단계: 엔진 벤더링 + 렌더 배관** (2026-09-01, v0.3.90)
> 로이: "밴더링 해야지. 2번은 클로드가 결정해. 이미지는 로컬에서 만들자. 진행해"
> 설계는 `D:\화이트보드` 를 조사한 다른 세션(claude-6a)과 여러 라운드 주고받아 확정했다. **이번은 1단계뿐** —
> UI·장면 계획·이미지 생성·오디오 mux 는 아직 없다(아래 「남은 단계」).

### 무엇을 붙이는가 — 롱폼의 **출력 갈래**(3번째 모드가 아니다)
대본·TTS·자막까지 **롱폼 배관을 그대로 쓰고**, 마지막에 `.vrew` 대신 **화이트보드 MP4** 로 갈라진다.
「모드는 롱폼/출판 둘뿐」(2026-08-22 로이 확정)을 깨지 않는다.
🔑 **엔진은 그림을 만들지 않는다** — 완성된 선화를 미색 종이색(`#F5EBD7`)으로 덮고 펜이 지나간 자리만 걷어낸다.
마지막 프레임 = 원본 그림. ⇒ **넣는 그림이 결과물의 천장**이다.

### 🔑 실측으로 정한 것 (롱폼 스냅샷 320편 · 그룹 11,814개)
| | 값 |
|---|---|
| 그룹 TTS 길이 | 최소 1.7 · **중앙 31.6초** · p90 103 · 최대 935.5 (평균 48.0) |
| 짧은 쪽(10초↓) | 14.0% · 긴 쪽(45초↑) **33.5%**(100초↑ 10.4%) |
| 편당 | 22.2분 · 그룹 36개 · 문장/그룹 8개 · **이미지 보유 99.1%** |
- ⇒ **「장면 = 그룹」이 기본으로 작동한다.** element 예산(`(초-0.8)/3.0`, 상한 6)이 **76%에서 상한에 걸린다.**
  「인접 그룹 묶기」는 필수가 아니라 **14% 예외**다(element 1개 = 5초↓ 는 앞 그룹에 병합).
- 🔴 **진짜 문제는 긴 쪽** — 103초 장면에 element 6개면 하나당 17초(35px/s)라 **펜이 기어간다.**
  45초 초과는 **문장 경계에서 분할**한다(목표 25~30초 · element 4~5). 시뮬: 편당 그룹 36 → **장면 50**.
- 🔴 **`INTRO_VIDEO_MAX_SEC_DEFAULT = 10` 은 롱폼 그룹과 무관하다**(도입부 재배치 전용).
  롱폼 본문 그룹은 `buildGroupsHybrid` 가 **H3 섹션 1개 = 그룹 1개**로 만든다 → 상한이 없어 935초까지 나온다.

### 🔑 렌더 시간은 **장면 수가 아니라 총 영상 길이**에 비례한다
16초 예제가 1920 에서 83초 = **프레임당 0.173초**. 22분 = 39,960프레임:
| 해상도 | 총 렌더 | 4병렬 |
|---|---|---|
| 1920×1080 | 115분 | **29분** |
| 1080×600 | 15분 | **약 4분** |
⇒ **장면 수는 비용이 아니라 품질 결정이다.** 그리고 `--cap-long-edge` 가 **7.5배짜리 레버** —
**프롬프트 A/B 튜닝 중에는 반드시 낮춰서** 돌 것(안 그러면 한 번 고칠 때마다 29분).
⚠ `annotation.canvas` 는 원본 PNG 픽셀이고 렌더러가 내부 환산하므로 **같은 주석으로 1080·1920 둘 다** 나온다.

### 🔴 밟은 함정 넷 (전부 실측)
1. 🔴 **매니페스트가 `scripts` 라는 이름의 폴더를 통째로 거른다**(`EXCLUDE_DIR_NAMES` 는 **폴더 이름만** 본다).
   `whiteboard/scripts/` 로 뒀으면 **조용히 배포에서 빠져** 아내 PC 에만 파이썬이 없었다 → **`py/`** 로 뒀다.
2. 🔴 **`.venv` 를 막는 규칙이 없었다.** 디렉터리는 `EXCLUDE_FILE`(`/^\./`)을 안 거치므로 **263MB 가 매니페스트에
   섞여 GitHub 로 올라가고 모든 PC 가 받는다.** → `EXCLUDE_DIR_NAMES` 에 `.venv` 추가(테스트가 단언).
3. 🔴 **확인 그림이 한글을 못 그렸다** — `render_annotation_preview.py` 가 폰트를
   `msyh.ttc`(Microsoft YaHei = **간체 중국어**)로 하드코딩. `fontTools` cmap 실측 **한글 커버 False** →
   라벨(`사람의 요청`)이 전부 두부(□□□). 게다가 경로 하드코딩이라 그 폰트가 없는 PC 는 **확인 그림 자체가 안 나온다.**
   → 맑은 고딕 우선 후보 순회 + `load_default` 폴백(**fail-open** — 라벨이 작아지는 것보다 관문이 사라지는 게 나쁘다).
4. 🔴 **진행률 명세가 틀렸다** — `stream_render.py` 의 `선 긋기 진행: 45%` 는 **그 파일의 자체 진입점** 것이고,
   우리가 쓰는 `render_stream_whiteboard.py` 는 **자기 `RegionStreamRenderer`** 를 써서 **진행 표시가 0줄**이었다.
   → 상류에 `영역 진행: k/N` 한 줄 추가(PATCHES.md 7번). 안 넣으면 장면마다 수 분간 화면이 조용하다(v0.3.75 계열).

### 🔑 설계에서 중요한 것 넷
1. **「장면 = 그룹」이 불변식** — 깨면 ① `media-cache.imageKey(prompt, …)` 가 **프롬프트가 키**라 장면 경계가
   흔들리는 순간 **50장 전량 재생성**(「🗑 삭제 후 3장만」이 불가능해진다) ② `prompt-io` 왕복이
   `groups.find(x => x.num === num)` 이라 📤내보내기/📥가져오기가 깨진다 ③ 게이트·스냅샷·🔄 단건도 전부 그룹 번호 기준.
   ⇒ **분할·병합은 반드시 결정론**(같은 대본 → 항상 같은 장면). LLM 에게 분할을 맡기지 말 것.
2. **주석은 「있으면 건너뛴다」** — 로이가 `preview.html` 로 고친 `annotation.json` 을 다음 실행이 덮어쓰면
   그 수정이 조용히 사라진다(v0.3.86·v0.3.50 계열). `fillTts`·`runComfyImages` 와 같은 이어받기 규칙 +
   덮어쓸 땐 경고. ⚠ 「🗑 이미지 삭제」는 **주석도 함께** 지운다(그림이 바뀌면 좌표가 틀린 자리를 가리킨다).
3. **`canvas` ↔ PNG 크기 검사는 fail-closed** — 삭제 규칙만으론 새는 경로가 있다(손으로 그림 교체 ·
   워크플로 변경 · 남의 주석 복사 · **후처리가 크기를 바꿈**). 어긋난 채 렌더하면 **29분 태우고 틀린 영상**이
   나오는데 사람이 눈으로 봐야만 안다. ⚠ `readImageSize` 는 **`{w, h}`** 를 반환한다(`width` 아님 — v0.2.6 사고).
4. **`OUTPUT=` 이 성공 계약** — exit code 를 믿지 않는다. `[err]` 접두가 실패 사유고, 경로를 받아도
   **파일 존재까지** 확인한다(v0.3.61 은 0장 만들고 "완료"를 반환했다).

### 이번에 만든 것
- **`whiteboard/`** — 상류 `D:\화이트보드`(로이 소유 한국어판, MIT)의 사본. `py/`(파이썬 6) · `assets/`
  (손 이미지·영역 편집기) · `examples/`(주석 1개) · `LICENSE` · **`UPSTREAM.md`**(구조·동기화 규칙).
  ⚠ **여기서 고치지 않는다** — 상류에서 고치고 `PATCHES.md` 에 적은 뒤 복사한다.
- **`core/whiteboard-render.js`** — `ensureEnv`(격리 `.venv` 부트스트랩) · `checkCanvas` · `renderScene` ·
  `renderPreview` · `mergeScenes` · `checkUniform` · `makeProgressTracker`.
  ⚠ **`python.exe -u` + `PYTHONUNBUFFERED=1` 둘 다** 준다(벤더링본에 `flush=True` 가 0회 — 안 주면 진행률이
  덩어리로 늦게 온다). ⚠ **pythonw 금지**(콘솔이 없어 print 가 터져 조용히 죽는다 — v0.2.95·v0.3.30).
  ⚠ 자식 cwd 가 `py/` 라 **모든 경로를 `path.resolve`** 한다(상대경로를 그대로 넘겨 한 번 깨졌다).
- 상류 패치 2건: **폰트 후보 순회**(PATCHES.md 3-1) · **영역 진행 표시**(7번).

### 검증
`npm run test:whiteboard` **48/48** — 벤더링 구조 12(`scripts` 이름 금지 · `.venv` 제외 단언) · canvas
fail-closed 8 · 진행률 5 · 규격 일치 5 · **벤더링본 == 상류 해시 6** · 폰트 패치 4 · **실제 렌더 왕복**.
🔑 **A/B 역검증 2건**: ⓐ `checkCanvas` 를 `{ok:true}` 로 무력화하면 **8건 실패** ⓑ `RE_ELEM` 을 지우면 **3건 실패**.
**실측 왕복**: 확인 그림(한글 정상 · 302KB) → 장면 2개 렌더(640×360@60 · 4.1초) → 규격 검사 →
병합 **32.00초 h264 정상**(ffprobe). `.venv` 263MB 생성(opencv-python·numpy·av·Pillow).

### ⚠ 로이가 알아 둘 것
- **아직 앱에서 못 쓴다.** 이번은 배관만이고 UI 버튼이 없다. 다음 단계에서 붙는다.
- **이미지는 🖥 로컬 ComfyUI 로 확정**(로이 지정). 클라우드 대비 장당 12~18초 → **23.2초 · 동시 1**(v0.3.35)이라
  편당 50장이 **약 19분**이다. 렌더 29분과 합쳐 **편당 약 48분**(클라우드였으면 37분). 크레딧을 아끼는 대신 시간을 쓴다.
- **영역 편집기는 Electron 안**에 띄운다(로이가 위임 → 내가 결정). `preview.html` 을 **고치지 않고**
  `showDirectoryPicker` 를 preload 폴리필로 대체할 계획 — 브라우저로 열면 폴더 권한을 매번 사람이 골라야 한다.
- **대본 규약은 강제가 아니다** — 결정론적 자동 분할이 폴백이라 **레거시 468편이 그대로 돌고 `.vrew` 경로는
  한 글자도 안 바뀐다.** 「화이트보드로 낼 대본은 H3 를 25~35초로」는 그 대본에만 적용하는 **권장**이다.

### 남은 단계
2) 장면 계획 + `annotation.json` 생성(그룹 모델 → 장면 · OpenCV 연결요소로 영역 초안)
3) 🔴 **화이트보드 화풍 이미지**(미색 바탕 · 흑백 선화 · 강조색 1곳 · 글자 금지) — **여기가 진짜 위험구간, 하루이틀 튜닝 각오.**
   ⚠ **네거티브 프롬프트가 안 듣는다**(Krea2·Z-Image 셋 다 `cfg=1` + `ConditioningZeroOut`) → **positive 긍정 서술**로.
   후처리(배경 색 치환 · 선 이진화)를 **프롬프트 튜닝보다 먼저** — 프롬프트는 확률적이라 40장 중 3장을 못 막는다.
4) 헤더 「④ 출력」 드롭다운(**헤더 + 채널편집 2곳** — v0.3.76 계열) + `whiteboard` 레인(**`localGpu` 미점유**)
   + 게이트 재사용 + 관문 A(장면 계획 텍스트, 37분 지킴)/B(확인 그림, 29분 지킴 · ⚠ 이미지 19분은 이미 나간 뒤)
5) 오디오·자막 mux — 렌더러가 **무음**을 뱉고 `merge_scenes.py` 도 오디오를 안 다룬다. `tts-1/*.wav` +
   `writeSrt` 결과를 ffmpeg 로 얹는다(⚠ 큐는 로그만 · 단건만 확인 관문 — 무인 큐가 멈추면 손실이 더 크다).

## 🔴🔊 **「지지지」 — 리미터 자체가 파형을 왜곡했다** (2026-08-31, v0.3.89)
> 로이: "음량을 높여서 그런건가? 지지지 소리가 들리네" (v0.3.88 로 만든 음성. 클리핑은 0이었다)

### 🔑 클리핑이 아니라 **고조파 왜곡**이었다 — 대역별로 재서 갈렸다
게인은 +5.5dB 인데 **대역마다 다르게 올라 있었다**(균일하게 올라야 정상):
| 대역 | 정규화 전 | v0.3.88(리미터 O) | **v0.3.89(리미터 X)** |
|---|---|---|---|
| 0-500Hz | -31 | -24 (**+7**) | -26 (**+5**) |
| 0.5-1.5k | -42 | -26 (**+16**) | -37 (**+5**) |
| 1.5-4k | -63 | -45 (**+18**) | -58 (**+5**) |
| 4-8k | -69 | -58 (**+11**) | -64 (**+5**) |
🔑 **원인: `alimiter` 의 attack 기본 5ms 가 남성 저음의 한 주기(84Hz ≈ 12ms)보다 짧다.**
파형 한 주기 안에서 게인이 변하니 **파형 자체가 찌그러지고** 그게 고조파(「지지지」)로 나온다.
저음일수록 심하다 — 이 프로젝트는 84Hz 남성 목소리라 정면으로 걸렸다.

### ✅ 리미터를 아예 쓰지 않는다
`게인 ≤ (피크 상한 -1dBFS) − 측정 피크` — **피크가 넘지 않는 만큼만** 올리고 리미터·압축을 걸지 않는다.
왜곡이 원천적으로 없다(전 대역 +5dB 균일 · 클리핑 0 · 실측 8문장 전부).
- ⚠ **대가**: 피크 여유가 좁은 문장은 목표(-15dB)에 못 미친다. 실측 -15.0~-16.8dB(편차 1.8dB).
  **그래도 이게 맞다** — 문장 하나가 1dB 작은 것보다 왜곡이 비교할 수 없이 나쁘다.
- 원본 피크가 이미 상한보다 크면 **더 키우지 않고 그대로 둔다**(굳이 낮추지 않는다).

### 🔑 세 번 고쳐서야 맞췄다 — 무엇을 놓쳤나
1. v0.3.87 — 리미터를 걸었다. `level` 기본값이 auto 인 줄 몰랐다 → **클리핑 88개**(기계음)
2. v0.3.88 — `level=disabled` 로 클리핑은 잡았지만 **리미터 자체의 왜곡**은 남았다 → 「지지지」
3. v0.3.89 — **리미터를 뺐다**
⇒ 🔑 **음성 신호에 리미터·압축을 걸지 말 것.** 저음 주기보다 짧은 어택은 파형을 왜곡한다.
   음량이 부족하면 리미터로 밀어 넣지 말고 **참조음성을 더 크게 녹음**하는 것이 정답이다.
⇒ 🔑 **클리핑 0 = 왜곡 없음이 아니다.** 이번에 클리핑은 0인데 소리는 망가져 있었다.
   **대역별 에너지가 게인만큼 균일하게 올랐는지**를 봐야 왜곡이 드러난다.

### 검증
`test/audio-normalize.test.js` **65/65** — 리미터·압축 미사용 2 · 피크 상한 준수 4 ·
결과 피크가 원본보다 커지지 않음 · 대역 균일성(실측). 🔑 **A/B**: 리미터를 되살리면 **4건 실패**.
**실측 8문장**: 게인 +3.2~+5.9dB · 결과 -15.0~-16.8dB · **클리핑 0** · 전 대역 균일 상승.
회귀: makeall 18/18 · tts 전량.

### ⚠ 로이가 알아 둘 것
- **v0.3.87·v0.3.88 로 만든 음성은 둘 다 버려야 한다** — 「🗑 삭제」 후 다시 만들 것.
- 참조음성이 조용하면 게인이 커지고, 게인이 커도 **피크 여유가 없으면 목표에 못 미친다**.
  근본 해결은 **참조음성을 적당한 크기로 녹음하는 것**이다(지금 X1 은 -19.9dB 로 조용한 편).

## 🔴🔊 **정규화가 기계음을 만들었다** — alimiter 의 auto level (2026-08-31, v0.3.88)
> 로이: "새로운 대본에서 테스트해봤는데 기계음이 들리는데…" (v0.3.87 발행 직후)

### 원인 — `alimiter` 의 `level` 기본값이 **true(auto level)** 다
내 필터가 `volume=10dB,alimiter=limit=0.89` 였는데, alimiter 는 기본으로 **출력을 limit 까지 다시
끌어올린다.** volume 게인과 겹쳐 과증폭 → 실측:
| | RMS | 피크 | 클리핑 |
|---|---|---|---|
| 원본 | -20.5dB | -5.97dB | 0 |
| **auto level(사고 당시)** | -11.6dB | **0.00dB** | **88개** ❌ |
| level=disabled | -12.6dB | -1.01dB | **0** ✅ |
🔑 **ffmpeg 필터를 쓸 때 기본값을 확인할 것.** `-h filter=<이름>` 한 줄이면 보인다. 나는
"리미터는 피크만 잡는다"고 **가정**했고 그게 틀렸다.

### ✅ 두 겹으로 막았다
1. **`level=disabled` 명시** — 리미터가 피크만 잡게. (직접 원인)
2. 🔑 **피크 여유를 넘겨 올리지 않는다** — RMS 만 보고 올리면 피크가 큰 문장에서 리미터가 과하게
   물려 뭉개진다. `게인 ≤ (리미터상한 − 피크) + MAX_LIMITING_DB(3dB)`. 그 이상 필요하면
   **목표를 포기하고 덜 올린다** — 문장 하나가 조금 작은 것보다 왜곡이 훨씬 나쁘다.
   · 실측 예: RMS -20 · 피크 -2 → 목표는 +5dB 지만 **+4dB 만** 적용.

### 검증
`test/audio-normalize.test.js` **61/61**(+7) — auto level 금지 2 · 피크 여유 제한 4 ·
**클리핑 샘플 0 실측**(옛 코드는 88개). 🔑 **A/B**: `level=disabled` 를 빼면 **실패**.
**실제 TTS 결과물 8문장 재검증**: 게인 +4.9~+6.5dB · 결과 -15.0~-16.7dB · **클리핑 0개**.
회귀: makeall 18/18 · tts 전량.

### ⚠ 로이가 알아 둘 것
- **v0.3.87 로 만든 음성은 왜곡됐다** — 「🗑 삭제」 후 다시 만들어야 한다.
- 로그의 `· 음량 +5dB` 가 **+8dB 를 넘으면** 참조음성이 지나치게 조용하다는 신호다(녹음을 다시 볼 것).

## 🔊🎙 참조음성 재설계 + **TTS 결과물 음량 정규화** (2026-08-31, v0.3.87)
> 로이: "숨소리가 거슬릴 정도로 많이 나오네" → "환하게가 변환이 안 돼" → "결과_X1 목소리가 작지 않아?"
> 하루에 걸친 실측 연쇄. **참조음성이 결과의 거의 모든 것을 결정한다**는 것이 이번에 수치로 확정됐다.

### 🔑 이번에 확정한 복제 관계 — 참조음성 → 결과물 (전부 실측)
| 참조음성의 성질 | 결과물 |
|---|---|
| **억양 폭** 30Hz / 42Hz / 70Hz | → **31Hz / 47Hz / 69Hz** (거의 1:1) |
| **음량** -13.9dB / -18.9dB / -19.9dB | → **-14.6dB / -19.4dB / -20.2dB** (거의 1:1) |
| **숨 여유**(말−숨) 21dB / 42dB | → 결과 숨 **-34dB / -46dB** |
| **문장 끝 처리** | 뚝 끊기면 모든 문장 끝이 뚝 끊긴다(v0.3.3 계열) |
⇒ 🔑 **결과가 마음에 안 들면 엔진이 아니라 참조음성을 본다.** 억양·음량·숨·끝맺음 전부 여기서 온다.

### 🔴 숨소리 — 원인은 참조음성의 **녹음 자체**였다
`#01_득수_noBreath` 는 숨 여유가 **21.4dB** 로, breath.py 문서가 "지워도 결과에 남는다"고 적어 둔
최악 구간이었다. 실제로 합성 11문장 중 **7문장(64%)의 맨 앞에 숨**이 붙었다(발화 시작까지 평균 375ms).
- 🔑 **`--lead-ms` 가 결정적** — 기본 60ms 는 숨 꼬리를 경사에 남긴다. 반복 적용해도 -43dB 에서 수렴.
  **`--lead-ms 20` 으로 -37.5 → -63.3dB(25.8dB 개선).** breath.py 문서에 기록했다.
- 🔑 **덩어리만 잘라 재조립하는 방법**(로이 발상: "여기서 자르고 저기서 자르고") — 001 테이크는 숨이
  12개였는데 **전부 쉼 구간에 있었다.** 발화 덩어리 4개만 잘라 쉼을 무음 350ms 로 새로 넣으니
  **숨은 통째로 사라지고 억양은 남았다.** 이게 트레이드오프를 깼다:
  | | 억양폭 | 폭/피치 | 문장앞숨 | 숨레벨 |
  |---|---|---|---|---|
  | 007 그대로 | 69Hz | 62% | 3/8 | **-28.0dB** ❌ |
  | **X1 = 001 조립** | **67Hz** | **65%**(성우 64%) | **0/8** ✅ | -39.5dB |
  → **`#04_득수_낭독`** 으로 등록(서버 라이브러리). 참조텍스트·실합성 확인 완료.

### 🔊 그런데 **음량이 작았다** — 그래서 결과물 정규화를 넣었다
X1 은 조용히 녹음돼(-19.9dB) 결과도 -20.2dB 였다(기존 #01 은 -14.6dB).
- 🔴 **참조음성을 증폭하는 것으로는 못 고친다**(실측). 5dB 올리면 음량은 맞는데(-15.2dB)
  **문장 앞 숨이 0/8 → 3/8 로 늘었다** — 리미터가 말만 누르며 상대적으로 숨이 올라간 참조를
  모델이 그대로 학습한다. **결과물을 올리면 말과 숨이 같은 비율로 올라 그 문제가 없다.**
- ✅ **`core/audio-normalize.js` 신설** + `fillTts` 배선. 합성 직후 버퍼의 **말 구간 RMS**
  (20ms 프레임 중 피크 -20dB 이상인 것들의 중앙값)를 재서 목표(-15dB)까지의 게인을 계산하고,
  **기존 ffmpeg 호출(atempo)에 얹어** 처리한다 → **추가 프로세스 0**.
  · 필터 순서 **배속 → 증폭 → 리미터**(리미터는 **올릴 때만** — 낮출 땐 다이내믹을 안 건드린다)
  · 게인 상한 ±12dB(바닥 잡음까지 끌어올리지 않게) · 0.5dB 미만 차이는 손대지 않음(헛돌기 방지)
  · **다이내믹 무손실 확인** — 실제 ffmpeg 로 돌려 억양 유지(참조음성 실험에서도 57→56Hz)
- 🔑 **캐시 키에 목표를 넣었다**(`nd`) — 안 넣으면 목표를 바꿔도 **옛 음량의 캐시가 되살아난다**
  (v0.3.43 발음사전과 같은 계열). 로그에도 문장마다 `· 음량 +5dB` 로 남는다.
- **덤으로 해결되는 것**: 짧은 문장·의문문이 작게 나오던 것(실측 23개 표본 상관 r=0.675 ·
  「우리가 그 울새를 볼까?」 -17.9dB vs 다른 문장 -14.1dB).
- ⚙ **채널편집 → 🎙 음성 → 「🔊 음량 맞추기」**(기본 켜짐 · 목표 -15dB). 채널별로 끌 수 있다.

### 📖 발음사전 — `하얬고 → 하야코`
로이가 "환하게가 안 된다"고 했는데 실제로는 **「하얬고」를 잘못 들은 것**이었다(대본 전체에 1회).
후보 7종을 합성해 로이가 F(하야코)를 골랐다. 자막은 원문 그대로 나간다.
- ⚠ **ASR 전사로는 이런 판정을 못 한다**(이번에 확인) — 일부러 넣은 틀린 판(「화나게」)까지 ASR 이
  "환하게"로 정규화했다. v0.3.43 의 「판정력 검증」 조건을 통과하지 못하므로 **사람 청취가 유일한 판정**이다.

### 검증
`test/audio-normalize.test.js` **54/54 신설**(`npm run test:tts` 에 포함) — 말구간 RMS 측정 9 ·
게인 계산 6 · 필터 순서 6 · 채널 설정 6 · 캐시 키 3 · 배선 12 · **실제 ffmpeg 왕복**(목표 도달 ·
클리핑 없음 · **다이내믹 유지**) · 소스 위생.
🔑 **A/B**: `git show HEAD:core/pipeline.js` 로 되돌리면 **8/54 실패**.
회귀: makeall 18/18 · tts 18/31/59/30 · tsv 39 · remotion E2E 133 · merge E2E 18 · timestamps.
⚠ `tts-number` 의 캐시 키 단언이 세 번째 인자 확장으로 깨져 갱신했다(첫 인자가 keyText 인지만 본다).

### ⚠ 로이가 알아 둘 것
- **채널 참조음성을 `☁ #04_득수_낭독` 으로 바꿔야** 새 목소리가 적용된다(지금은 `#01_득수_noBreath`).
- 바꾸면 **캐시 키가 달라져 전량 재합성**된다. 음량 정규화도 함께 켜지므로 어차피 새로 만들어야 한다.
- ⚠ **기존 음성이 있으면 「🎤 TTS」가 건너뛴다** — 「🗑 삭제」를 먼저 해야 새 목소리로 만들어진다.
  (v0.3.86 에서 1단계 로그에 채널·목소리·시드·배속이 찍히므로 시작 한 줄로 확인할 수 있다.)
- ⏳ **`#01` 의 끝이 뚝 끊기는 것은 못 고친다** — -27dB 에서 80ms 만에 -61dB 로 급락한다(여운이 잘렸다).
  붙일 소재(같은 문장의 다른 테이크)가 없어 잘라 붙이기로도 안 된다. **X1 은 그 문제가 없다**
  (-27 → -37dB 로 서서히 맺는다).

## 🔴🎙 **고전서재 대본이 엉뚱한 목소리로 합성됐다** — 큐 항목에 채널이 없었다 (2026-08-31, v0.3.86)
> 로이: "고전서재 채널인데, 음성이 선택된 음성이 아닌 **다른 음성으로 TTS 변환**이 이루어졌어. 너무나도 심각한데."

### 🔑 원인을 가른 것은 **OmniVoice 서버 로그의 seed** 였다
앱 로그엔 채널명이 안 남아 아무 단서가 없었다. 서버 로그(`D:TTS_Modelomnivoiceserver.log`)를 보니:
```
TTS 합성 시작 [Voice Clone(lib)]: text=세 아이는 자두나무 아래로 의자를 끌어다 놓았습니다., ... seed=5697
```
그 텍스트는 「비밀의 화원」(고전서재)인데 **06_고전서재의 시드는 40469** 다. **5697 은 기본 채널
(04_역사이야기, `isDefault:true`, 목소리 `srv:02_저음 2단계`)의 시드**다. 즉 **기본 채널로 합성**됐다.
🔑 **다음에 "목소리가 다르다" 가 오면 서버 로그의 seed 를 채널 시드와 대조한다** — 한 줄로 갈린다.

### 원인 — 대본을 여러 개 열면 **마지막 하나만** 채널을 갖는다
`workspace.json` 실측(큐 4개, 전부 고전서재 대본):
| 파일 | settings |
|---|---|
| …8부 · 9부 · 10부 | **`{}` (비어 있음)** |
| …11부(마지막에 연 것) | `presetName:"06_고전서재"` |
- `open-script` 가 `addItem(S.parsed, S.scriptPath, S.outRoot)` 를 **settings 없이** 불렀다(→ `settings:null`).
- 채널을 채우는 건 렌더러의 **디바운스 자동저장(`set-queue-settings`)뿐인데 그건 활성 항목에만** 쓴다.
  다중 열기(v0.2.75)에서 활성은 **마지막에 연 것 하나**다 → 나머지는 영원히 빈 채로 남는다.
- 그 상태로 「만들기」 → run-batch 의 `presetName: s.presetName || null` → **null** →
  `runMakeAllCore` 의 `P.getPreset(null)` = **기본 채널**. **조용히** 엉뚱한 목소리로 나갔다.
- 🔑 **v0.2.76(영상 범위 전 그룹)·v0.2.77(스타일 누락 → 실사 이미지)과 같은 계열의 세 번째다.**
  그 둘은 「헤더(common) 우선」으로 고쳤는데 **채널만 안 고쳤다**(채널은 항목별이라는 판단 때문).
  항목별인 것은 맞지만 **값이 없을 때 기본 채널로 떨어지는 것**이 잘못이었다.

### ✅ 한 것 넷
1. **여는 즉시 채널을 박는다** — `addItem(..., preset.name ? { presetName: preset.name } : null)`.
   대본을 여는 시점의 채널이 그 대본의 채널이다(디바운스 저장을 기다리지 않는다).
   ⚠ 그래서 `addItem` 이 settings 를 **병합**하도록 함께 고쳤다 — 통째로 덮으면 **이미 저장된
   스타일·영상범위가 날아간다**(같은 대본을 다시 열 때. 테스트가 이걸 단언한다).
2. **run-batch 폴백** — `s.presetName || common.presetName || null`. 항목값이 없으면 **헤더 채널**을 쓴다.
   ⚠ 렌더러가 `common` 에 `presetName` 을 **실어 보내야** 이게 동작한다(안 보내면 무의미 — 테스트가 단언).
3. **같은 구멍이 둘 더 있었다** — `speed: s.ttsSpeed || null` · `aiNotice: !!s.aiNotice` 도 항목값만 봤다.
   배속은 null 이면 1 로 떨어지고(고전서재가 1이라 우연히 맞았다), AI 고지는 조용히 꺼진다. 둘 다 헤더 폴백.
4. 🔑 **로그로 드러나게** — 1단계에 **채널·목소리·시드·배속**을 찍는다:
   `🎙 1단계 — 음성(TTS) 일괄 변환… (채널 「06_고전서재」 · 목소리 ☁ #01_득수_noBreath · 시드 40469 · 배속 1)`
   채널을 못 찾으면 `⚠ 채널을 찾지 못했습니다`, 항목에 없어 헤더를 쓰면 `ⓘ … 헤더 채널 「X」 을 씁니다`.
   **이번에 서버 로그를 뒤져야 했던 이유가 이것이다.**

### 검증
`test/channel-voice.test.js` **30/30 신설**(`npm run test:tts` 에 포함) — `addItem` **원문 실행**
(병합·기존 설정 보존·**실사고 재현: 4개를 열면 4개 다 채널을 갖는다**) · open-script 배선 · run-batch 폴백 5 ·
렌더러 common 배선 · 1단계 로그 5 · 소스 위생.
🔑 **A/B 역검증**: `git show HEAD:main.js` 로 되돌리면 **14/30 실패**(병합 2 · 배선 7 · 로그 5).
회귀: **makeall 무음 E2E 18/18**(⚠ 첫 실행에서 「절전 차단 해제」 1건이 실패했는데 **로그 IPC 도착
타이밍 레이스**였다 — 2회 연속 재실행에서 18/18. 로그 파일엔 그 줄이 정확히 남아 있었다) ·
tts 18/31/59 · pipeline-mode 30 · img-regen 40 · merge E2E 18 · timestamps UI.

### ⚠ 로이가 알아 둘 것 — **이미 만들어진 음성은 버려야 한다**
- 오늘 큐로 만든 **[서재_0917] 8부 · [서재_0918] 9부** 의 음성은 **04_역사이야기 목소리**로 합성됐다
  (10부·11부는 중단돼 일부만). 그 대본들의 `tts-1` 폴더를 「🗑 음성 삭제」로 비우고 다시 만들어야 한다.
  ⚠ **TTS 캐시도 그 목소리로 남아 있다** — 다만 캐시 키에 `rn`(목소리)·seed 가 들어가므로
  올바른 채널로 다시 만들면 **키가 달라 새로 합성**된다(옛 음성이 되살아나지 않는다).
- 앞으로는 **대본을 열 때** 헤더 채널이 그 대본에 박힌다 → 여러 개를 한 번에 열어도 전부 같은 채널.
  ⚠ **채널을 고르고 나서 대본을 열 것.** 대본을 먼저 열고 채널을 바꾸면 그 항목(활성)만 바뀐다.
- 1단계 로그에 채널·목소리·시드가 찍히므로 **다음부터는 만들기 시작 한 줄로 확인**할 수 있다.

## ☁🎬 비디오에 **MiniMax H3 레퍼런스 (클라우드)** 추가 (2026-08-30, v0.3.85)
> 로이: "비디오생성을 컴피유아이클라우드를 활용한 minimax h3 레퍼런스기반 비디오 생성 도구를 추가해줘."

헤더 「③ 비디오」 → **`☁ MiniMax H3 레퍼런스 (클라우드)`**. v0.3.70 의 로컬판(RTX 3060 · 편당 47분)을
comfy.org 로 옮긴 것 — **같은 레퍼런스 방식인데 편당 약 5분**이다.

### 🔴 옛 기록 정정 — **클라우드에 minimax_h3 모델이 있다**
v0.3.70·v0.3.84 에 "comfy.org 엔 minimax_h3 모델이 없다"고 적어 뒀는데 **지금은 거짓이다**.
`/api/object_info` 실측(2026-08-30, 노드 클래스 3,699개):
| 자리 | 클라우드 실측 이름 |
|---|---|
| unet | `minimax_h3_ref2va_pruned_int8_convrot.safetensors` |
| clip | `qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors` (type `minimax`) |
| vae | `minimax_h3_video_vae_fp16` + `minimax_h3_audio_vae_fp32` |
| lora | `minimax_h3_turbo_v4_step600_ema_pruned_comfyui.safetensors` |
| 노드 | **`MiniMaxH3ReferenceToVideo`**(네이티브) · `MiniMaxH3ImageToVideo` · `MiniMaxH3AddGuide` |
- 🔑 **로컬과 노드가 다르다** — 로컬은 Deno 커스텀(`DenoMiniMaxH3Reference*`), 클라우드는 **ComfyUI 네이티브**.
  클라우드엔 `Deno*` 가 **하나도 없다** → 로컬 워크플로를 ☁ 로 보내면 실패한다(그 반대도 마찬가지).
- 🔑 **모델 유무는 로더 하나만 보지 말 것** — v0.2.x 때 `CheckpointLoaderSimple` 만 뒤져 "LTX 2.5 없음"으로
  오판한 적이 있다. `UNETLoader`·`CLIPLoader`·`VAELoader`·`LoraLoaderModelOnly` 의 옵션 배열을 다 훑는다.
- ⛔ **파트너 API 노드를 쓰지 않는다** — 템플릿 목록에 `api_minimax_h3_r2v`(유료 중계)와
  `video_minimax_h3_r2v`(로컬노드)가 둘 다 있다. **뒤엣것**을 썼으므로 구독 GPU 시간만 나가고 영상당 추가 과금이 없다.

### 워크플로 만든 법 — 공식 템플릿을 API 포맷으로 변환
`https://cloud.comfy.org/templates/video_minimax_h3_r2v.json`(UI 포맷) → **`scripts/comfy-ui2api.js`**
(v0.3.0 에서 만든 변환기) → `comfy/video_minimax_h3_ref2v_cloud.json`. 그다음 **우리 엔진 규약에 맞게 4가지**를 다듬었다:
1. 🔑 **LoadImage 를 1개로** — 템플릿은 레퍼런스가 2장이다. 엔진은 `LoadImage` 를 **전부** 업로드명으로 덮으므로
   2개를 두면 **같은 그림이 레퍼런스로 두 번** 들어간다. 두 번째 노드와 `ref_images.ref_image_1` 슬롯을 제거.
2. 🔑 **해상도를 리터럴로** — 템플릿은 `ResolutionSelector` 에서 링크로 받는다 → `_setVideoDims` 의 「같은 노드에
   width·height 가 **숫자**」 조건에 안 걸려 **해상도 주입이 조용히 무시**된다. 그 노드를 없애고 리터럴로 바꿨다.
3. 🔑 **Lightning LoRA 스위치 분기 제거** — 안 쓰는 가지의 모델도 **검증 대상**이라, 클라우드에 없는
   `minimax_h3_ref2v_turbo_4step_*`(로컬판 LoRA)를 참조한 채로 두면 큐 접수 단계에서 통째로 실패한다.
4. 제목을 못박았다(`Float (Duration)`·`Input Text (Prompt)`) — 엔진 탐지가 제목 기반이라 템플릿 개정에 흔들리지 않게.

### 🔴 밟은 함정 — `<Picture 1>` 접두사가 **클라우드 판에는 조용히 안 붙었다**
`_buildGraph` 의 레퍼런스 판정이 **`MiniMaxH3ReferenceImageLoader`(Deno 이름) 하나뿐**이었다. 클라우드 판은
네이티브 노드라 안 걸려서 접두사가 빠졌고, 그게 v0.3.70 이 「없으면 구도·화풍이 통째로 바뀐다」고 적어 둔 그 사고다.
→ **판정을 둘로 갈랐다**: `mmLoaderId`(Deno — 이미지를 `image_paths` 로 넣는 자리) / `mmRef`(접두사 판정, 로컬·클라우드 둘 다).
⚠ **이미지 주입까지 `mmRef` 로 하면 안 된다** — 클라우드 판엔 `image_paths` 가 없어 LoadImage 를 놓친다(테스트가 단언).

### 🔑 실측으로 정한 값 (comfy.org · 5초 · 같은 소스 이미지 · 총 30여 회 생성)
| | 시간 | 원본 구도·화풍 |
|---|---|---|
| steps **20**(템플릿 기본) · 1344x768 | **3.4분** | 3건 중 **1건**만 유지 |
| **turbo LoRA 4step** · 1344x768 | **57초** | 유지(아래 프롬프트 조건에서) |
| turbo4 · **1920x1088**(2.09MP) | **208초** | 2건 다 이탈 |
| turbo4 · 608x352(0.2MP) | 16초 | 유지 |
- 🔑 **스텝이 충실도를 좌우한다 — 많을수록 나쁘다.** 20스텝은 모델이 프롬프트를 따라 **장면을 다시 상상**한다
  (실측: 문 너머 새 구도 · 손 클로즈업 · 어두운 실사풍). 4스텝은 레퍼런스에서 멀리 못 간다.
  ⇒ **더 빠른 쪽이 더 충실하다** — 흔치 않은 경우라 헷갈리기 쉽다.
- 🔑 **해상도는 공식 0.98MP 격자를 넘기지 않는다.** 템플릿 표의 상한이 `1344x768 (official 768p)` 이고,
  1920x1088 은 규격 밖이라 **3.6배 느린데 이탈까지 심했다**. → 엔진에 **`_mmDims(aspect)`** 신설
  (16:9 `1344x768` · 9:16 `768x1344` · 1:1 `992x992`, 전부 32 배수). **레퍼런스 워크플로일 때만** 적용되고
  LTX2.5 는 1920x1088 그대로다(테스트가 단언).
- ⚠ **그래서 업스케일이 돈다** — 1920x1080 미만이라 `maybeUpscale` 이 내 PC GPU 로 올린다(영상당 30~40초).
  LTX2.5(1920x1088 직접 생성)는 그 단계가 생략된다 — 이 도구의 대가다.

### ⚠ 이건 i2v 가 아니다 — **컷이 튀는 것이 정상**이고, 프롬프트가 그걸 좌우한다
MiniMax H3 레퍼런스는 첫 프레임을 고정하지 않는다(v0.3.70 과 같은 성질). 1344x768·turbo4·접두사 고정 조건에서
**프롬프트만 바꿔 A/B** 한 실측:
| 프롬프트 | 원본 프레이밍 유지 |
|---|---|
| 짧은 기본값 `natural slow motion, subtle camera movement, cinematic` | **1/4** |
| 위 + 「카메라를 바꾸지 마라」 문구 추가 | 1/4 — **효과 없음** |
| **긴 서술형**(대본 `🎬 영상:` 에 장면을 풀어 쓴 수준) | **3/4** |
- ⇒ 🔑 **대본에 `🎬 영상:` 을 서술형으로 적어야 한다.** 없으면 앱 기본값(`…cinematic`)이 나가는데,
  그 짧고 막연한 지시가 이 모델에겐 「알아서 연출해라」로 읽혀 클로즈업·실사풍으로 튄다.
  ⛔ 접두사에 「카메라 고정」 문구를 더 넣는 안은 **실측으로 기각**했다(동률) — 다시 꺼내지 말 것.
- **첫 프레임을 꼭 고정해야 하는 컷은 LTX2.5 를 쓴다**(i2v). 이 도구는 「레퍼런스를 참고해 새 장면」이 목적이다.

### 검증
`test/comfy-video-minimax.test.js` **75/75**(+37) — 클라우드 워크플로 실재·API 포맷·네이티브 노드·Deno 없음 ·
**모델 4종 이름이 클라우드 실측과 일치** · LoadImage 1개 · 두 번째 레퍼런스 슬롯 없음 · 스위치 분기 없음 ·
steps 4 · euler · `_buildGraph` 원문 실행(업로드명·**`<Picture 1>` 접두사**·1344x768·길이·seed) ·
해상도 격자 3종 · **🔴 LTX2.5 회귀(1920 유지)** · 소스 위생 5.
🔑 **A/B 역검증 2건**: ⓐ 접두사 판정을 옛 `mmLoaderId` 로 되돌리면 **2건 실패** ⓑ `_mmDims` 를 빼면 **5건 실패**.
회귀: **`npm run test:comfy` 전량**(워크플로 31 · 모델 41 · perf 54 · 파이프라인 30 · GPU 레인 14 ·
남의 PC GPU 43 · **2분할 패널 E2E 92/92**(+1 — 실제 앱 드롭다운에 ☁ 항목이 뜨는지)).

### ⚠ 로이가 알아 둘 것
- **쓰는 법**: 헤더 「③ 비디오」 → `☁ MiniMax H3 레퍼런스 (클라우드)` → 범위 지정 → 「🎬 비디오」.
  ⚙ 설정 → 🎬 ComfyUI 비디오가 **클라우드 모드**여야 한다(로컬로 두면 그 모델이 없어 실패한다).
- **편당(영상 5개) 약 5분**(생성 57초 × 5 ÷ 동시 3 + 업스케일). 로컬 MiniMax 47분 · LTX2.5 2.5분 사이다.
- **오디오가 함께 생성된다**(aac 32kHz). 우리는 TTS 를 쓰므로 .vrew 에선 안 쓰인다(무해).
- ⛔ **드롭다운의 🖥 쪽으로 고르지 말 것** — 목록은 ☁/🖥 두 그룹에 같은 항목을 다 보여준다.
  이름의 **(클라우드)** 가 그 경고다(로컬판은 **(로컬)**).

## 🎬 비디오에 **Flow · Veo** 추가 — 그룹 이미지를 시작 프레임으로 i2v (2026-08-28, v0.3.71)
> 로이: "FLOW에서 비디오를 생성할수 있어. 이 기능을 활용해서 비디오를 만들고 싶어. 선택항목으로 넣어줘.
>   과정 분석하며 과정 만들어줘." (로이의 로그인된 Chrome 으로 Flow 를 열어 UI 를 실측하며 진행)

헤더 「③ 비디오」에 **`Flow · Veo (구독)`** 추가. Grok·Grok API·ComfyUI 옆의 네 번째 선택지다.
v0.1.71 에서 통째로 제거했던 경로를 되살렸는데, **엔진 코드가 남아 있어 그대로 쓸 수 있을 줄 알았지만
실측해 보니 거짓 가정 두 개가 박혀 있었다**(아래 함정).

### 🔑 Flow 웹 UI 실측 (2026-08-28 · 로그인 상태에서만 보인다)
프로젝트에 들어가 프롬프트 바의 **`동영상 · 720p · x1`** 칩을 누르면 설정 팝업이 뜬다. 전부 `role="tab"`:
| 줄 | 항목 |
|---|---|
| 1 | `이미지` \| **`동영상`** |
| 2 | **`프레임`** \| `애셋` ← **프레임이 i2v 진입점** |
| 3 | `9:16` \| `16:9` |
| 4 | 모델 드롭다운 — `Omni 1.1 Flash` · **`Veo 3.1 - Lite`(기본)** · `Veo 3.1 - Fast` · `Veo 3.1 - Quality` |
| 5 | `x1` \| `x2` \| `x3` \| `x4` · 하단에 **「생성 시 10크레딧이 사용됩니다」** |
- **`프레임` 탭을 누르면** 프롬프트 위에 **`시작` ⇄ `종료`** 버튼이 나타난다 — 이걸 안 누르면 프레임 UI 가
  아예 없어 **텍스트만으로 생성**된다(우리 이미지가 무시된다).
- `시작` → 미디어 선택창(`이미지`/`업로드` 탭 + `미디어 업로드`). **`input[type=file]` 이 `accept="image/*"`
  로 정확히 1개**(hidden) → `setInputFiles` 로 네이티브 대화상자를 우회할 수 있다(기존 코드 방식이 유효).
- 최종 제출 버튼은 **`arrow_forward 만들기`** — `_clickFinalCreateV2` 의 전략 1 이 그대로 맞는다.
- ⚠ **길이 옵션이 없다.** Veo 가 정하는 길이(약 8초)로 나온다 — 그룹 TTS 가 더 길면 .vrew 에서 뒷부분은
  이미지가 채운다(영상이 없는 그룹과 같은 처리). Grok 처럼 6/10초를 고를 수 없다.

### 🔴 밟은 함정 넷
1. 🔴 **`_selectModel` 이 동영상 모드에서 한 번도 동작한 적이 없다.** `button:has-text("Nano Banana")`
   **하나만** 찾는데 동영상 모드의 라벨은 `Veo 3.1 - Lite` 다 → 못 찾고 **조용히 넘어갔다**(로그도 없다).
   즉 Fast/Quality 를 골라도 언제나 Lite 로 만들어졌다. → `Veo`·`Omni` 라벨도 찾고, 실패하면 로그를 남긴다.
2. 🔴 **`defaultModel = 'Veo 3.1 - Fast'` 가 거짓이었다**(실제 기본은 **Lite**). 「기본값과 다를 때만 바꾼다」
   는 최적화가 **틀린 기준으로 판단**하고 있었다. → 하드코딩을 버리고 **드롭다운의 현재 라벨을 읽어 비교**한다
   (Flow 가 마지막 선택을 기억해도 정확하고, 기본값이 또 바뀌어도 안 깨진다).
3. 🔴 **프레임 첨부 실패를 무시하고 그대로 생성했다.** `_attachFrameImage` 의 반환값을 버리고 있었다 →
   첨부가 실패하면 **원본 이미지와 무관한 영상이 나오고 크레딧(10)만 나간다.** i2v 로 쓰는 우리 용도에선
   그건 성공이 아니다 → **그 컷을 만들지 않고 건너뛴다**(`requireFrame`). 화풍이 튀는 것보다 없는 게 낫다.
4. 🔴 **`failCount++` 를 썼다가 바로 잡았다** — 그런 변수가 없다(기존 카운터는 `consecutiveFails`·`_failedNums`).
   이 저장소 단골인 **미정의 식별자** 사고가 될 뻔했고, 테스트가 지금 이걸 금지한다.

### 🔑 설계에서 중요한 것 넷
1. **정규화에서 `'flow'` 를 빼야 한다** — App.jsx 2곳 + main.js run-batch 1곳이 `['flow','wan','grok10'] → 'grok'`
   으로 되돌리고 있었다(제거 시절 잔재). 그대로 뒀으면 **골라 저장해도 다음에 열면 Grok**이고 만들기도 Grok 으로
   나간다. v0.3.50 「이미지 순환이 되돌아가던 것」과 **같은 계열**이다. 테스트가 세 곳을 단언한다.
2. **파이프라인 병렬에 넣지 않는다** — Flow 비디오는 **이미지와 같은 크롬·같은 계정 프로필**을 쓴다.
   `grokVideoPipeline`·`comfyVideoPipeline` 어디에도 안 걸리므로 자동으로 **순차 3단계**로 간다(그게 맞다).
   `_vidUsesLocalGpu('flow')` 도 false — 브라우저라 GPU 레인과 무관하다(테스트가 단언).
3. **매수는 `x1` 고정.** 그룹당 1개만 쓰므로 x2~x4 는 **크레딧 배수**일 뿐이다.
4. **계정 순환은 이미지와 같은 스토어**(`core/flow-accounts`)를 쓴다 — 0개면 다음 계정, 구독 없음(`flowNoAccess`)이면
   12시간 휴식, 한도면 30분 쿨다운. 못 만든 그룹은 `videoStatus`를 **'fail' 로 정리**한다(스피너 고착 방지 — v0.2.62 계열).

### 🖼 붙이는 방식 두 가지 — **프레임 / 애셋** (2026-08-28, v0.3.72)
> 로이: "이 선택항목에서 에셋을 선택하고, VEO 3.1-LITE로 선택해서 만드는 방법도 추가해줘."

설정 팝업 2번째 줄의 **`프레임` \| `애셋`** 이 곧 「소스 이미지를 어떻게 붙일까」다. ⚙ 설정 →
🌐 브라우저 이미지·비디오 의 **「Flow 첨부 방식」** 에서 고른다(기본 = 프레임).

| | 프레임 (기본·권장) | 애셋 |
|---|---|---|
| 붙이는 자리 | 프롬프트 **위**의 `시작` ⇄ `종료` 버튼 | 프롬프트 바 **왼쪽**의 `+` 버튼 |
| 셀렉터 | `getByText('시작')` | **`button[aria-haspopup="dialog"]:has-text("add_2")`** |
| 의미 | **첫 프레임이 그 그림으로 고정** = 엄격한 i2v | **참조(레퍼런스)** — Veo 가 참고만 한다 |
| 결과 | 원본 그림이 그대로 움직인다(화풍 안전) | **구도·인물이 달라질 수 있다** |

- 🔑 **업로드 뒤는 둘이 똑같다** — 실측하니 **같은 미디어 다이얼로그**를 쓴다: hidden `input[type=file]`
  (`accept="image/*"`)에 넣으면 **자동 선택**되고 **`프롬프트에 추가`** 버튼이 나타난다. 그래서 그 부분을
  **`_uploadAndAddToPrompt(imagePath, num, label)` 공통 헬퍼**로 뽑았다(두 벌로 두면 반드시 어긋난다).
- 🔑 **애셋의 `+` 는 상단 툴바의 `+` 가 아니다.** 실측 텍스트가 각각 `add_2 만들기`(프롬프트 바,
  `aria-haspopup="dialog"`) · `add 미디어 추가`(툴바, `aria-haspopup="menu"`) 라 **haspopup 으로 갈린다.**
  기존 `_addCharacterReference` 가 이미 그 셀렉터를 쓰고 있었다(재사용).
- 🔴 **`attachMode` 를 `_runSequentialMode` 까지 넘겨야 한다.** run() 구조분해에만 넣고 그 아래
  인자 객체에 안 실으면 **애셋을 골라도 조용히 프레임으로** 동작한다(발행 전에 잡았다 · 테스트가 단언 ·
  A/B 로 확인). v0.3.50 계열의 「조용한 무시」 사고다.
- ⚠ **어느 쪽을 쓸까**: 대본 그림을 **그대로 움직이게** 하려면 프레임. 캐릭터·분위기만 참고시키고 새 구도를
  얻고 싶으면 애셋. v0.3.70 의 MiniMax H3 에서 배운 것과 같은 구조다 — **레퍼런스는 첫 프레임을 고정하지 않는다.**

### 📥 **1080p 로 받는다** — 재생 소스는 720p 원본이었다 (2026-08-28, v0.3.73)
> 로이: "영상을 만드는 방법에 오류가 있어. … 생성된 비디오를 **1080P로 다운 받아서** 그룹에 비디오를 첨부하면 되."

🔴 **실제 오류였다.** 엔진이 완성 영상을 **플레이어의 `video src` 를 fetch** 해서 가져왔는데, 그건
**720p 원본**이다. 1080p 는 **카드 메뉴의 다운로드로만** 받을 수 있다. 그래서 지금까지는 720p 를 받아
**우리 PC 의 Real-ESRGAN 으로 다시 업스케일**하고 있었다(장당 수 분 · v0.3.51 의 그 느린 경로).
Flow 에서 1080p 로 받으면 **`maybeUpscale` 이 해상도를 보고 스스로 건너뛴다** — 그 시간이 통째로 사라진다.

### 🔑 다운로드 메뉴 실측 (2026-08-28 · 로이 영상으로 확인 — 크레딧 0)
카드 hover → **`more_vert`** → 메뉴에서 **「다운로드」에 `hover`**(⚠ **클릭이 아니다**) → 서브메뉴:
| 항목 | 뜻 |
|---|---|
| `270p 애니메이션 GIF` | GIF |
| `720p 원본 크기` | 재생 소스와 같은 것 |
| **`1080p 업스케일`** | ← 우리가 받는 것 |
| `4K 업스케일` + `업그레이드` 배지 | 플랜 업그레이드 필요 |
- 메뉴 항목의 **접근성 이름이 비어 있다**(서브메뉴를 여는 항목이라) → `getByRole('menuitem')` 의 **이름 매칭은
  실패**한다. `filter({ hasText: '다운로드' })` 로 innerText(`download 다운로드`)를 봐야 한다.
- 받기는 `page.waitForEvent('download')` → `saveAs`. **1080p 는 업스케일이라 시간이 걸려** 대기를 180초로 뒀다.

### 🔑 「방금 만든 것」만 집는다 — 카드마다 `/edit/<uuid>` 가 있다
갤러리에서 최신 카드를 추측으로 집으면 **옛 영상을 받아 .vrew 에 싣는다**(Grok 에서 실제로 겪은 v0.3.20 사고).
→ **제출 전에** `a[href*="/edit/"]` 의 uuid 를 스냅샷(`_mediaIds`)하고, 생성 뒤 **새로 생긴 uuid** 의 카드만
집는다(`_newMediaCard`). ⚠ 스냅샷은 반드시 **제출보다 먼저** — 뒤에 찍으면 새 카드가 이미 포함돼 구분이 안 된다.

### 폴백 사슬 (조용히 실패하지 않는다)
`1080p 다운로드` → 실패하면 `네트워크 캡처(720p)` → 그것도 없으면 `재생 소스 버퍼`. 어느 단계로 떨어졌는지
**로그에 남는다**(`[DL 01] 1080p 다운로드 완료 (…KB)` / `재생 소스(720p 원본) 사용`).
⚙ 설정 → 🌐 브라우저 이미지·비디오 의 **「Flow 다운로드」** 에서 `1080p`(기본)·`720p`·`끔` 을 고른다.

### ⚠ 드래그앤드롭은 자동화에서 쓰지 않는다 (실측)
로이가 알려준 사람 방식은 **프롬프트 창에 이미지를 드래그**하는 것이다. 그런데 합성 `DragEvent`+`DataTransfer`
로 drop 을 쏴 보니 **`defaultPrevented`는 true 인데 썸네일이 붙지 않았다**(React 가 신뢰되지 않은 이벤트를
받지 않는 것으로 보인다). 반면 **hidden `input[type=file]` 에 `setInputFiles`** 하는 경로는 실측으로
「업로드 → 자동 선택 → `프롬프트에 추가` → 썸네일 부착」 까지 확인됐다. **결과가 같고 더 안정적이라 그쪽을 쓴다.**

### ✅ **실물 1개로 확인했다** — 1920x1080 · 8.00초 (2026-08-28, v0.3.74)
> 로이: "실제로 영상 1개 만들어서 확인해줘." → 계정 1(PRO)로 실제 대본 이미지(`[다산_0905] media-1/01.jpg`)를
>   시작 프레임으로 넣어 **크레딧 10을 써서** 만들었다. 출력은 임시 폴더 — 로이 작업 폴더는 읽기만 했다.

| 단계 | 실측 |
|---|---|
| 프레임 첨부 | 27.7s 업로드 → **37.0s 완료**(약 9.3초) |
| 생성 | 37.0s 시작 → 91.7s 완료(약 55초 · 13%→27%→40% 진행률 표시됨) |
| 네트워크 캡처(720p 원본) | 2,683KB |
| **1080p 다운로드** | 144.3s 완료 — **6,563KB**(2.4배) |
| **결과 mp4** | **1920x1080 · h264 · 24fps · 8.00초 · aac · 6.7MB** |
| 총 소요 | **144.3초** |
- ✅ **`maybeUpscale` 이 이걸 건너뛴다** — 판정이 `width >= 1920 && height >= 1080` 이라 로컬 GPU 업스케일
  (Real-ESRGAN · 장당 수 분)이 통째로 생략된다. 이게 1080p 로 받는 진짜 이유다.
- ✅ 길이는 예상대로 **8초 고정**(Flow UI 에 길이 옵션이 없다).

### 🔴 그 과정에서 잡은 실사고 — 「프롬프트에 추가」가 **disabled** 였다
첫 실행이 34.5초 만에 **아무것도 안 만들고** 끝났다. 원인은 **업로드 처리 시간**이었다:
- 옛 코드는 업로드 뒤 **고정 3.5초**만 기다렸는데, 실측하니 **약 9초**가 지나야 버튼이 활성화된다.
  (+3s `disabled:true` · +6s `disabled:true` · **+9s `disabled:false`, 타일 2개 등장**)
- 그 시점엔 버튼이 `disabled` 라 Playwright 의 actionability 검사(enabled 대기)에서 **3초 타임아웃**.
  28.0s(업로드) + 3.5s(대기) + 3s(클릭 타임아웃) = **34.5s** — 로그 시각과 정확히 맞는다.
- 🔑 **눈으로는 멀쩡해 보인다** — 버튼이 **보이고**(`visible:true`) **가려지지도 않았다**(`elementFromPoint`
  가 자기 자신). 그래서 처음엔 덤프에 찍힌 `동의함`/`나중에` 를 보고 **「업로드 동의 팝업이 덮었다」고 오진**했다.
  실측해 보니 그 두 버튼은 `visible:false`(DOM 에만 있는 잔재)였다. **덤프에 있다 ≠ 화면에 있다.**
  ⚠ 게다가 `_dumpFrameAttachUI` 는 `[...new Set()]` 로 **중복을 지운다** — 같은 이름 버튼이 2개여도 1개로 보인다.
- ✅ 고정 대기를 버리고 **`waitForFunction` 으로 enabled 될 때까지**(최대 40초) 기다린다. 고친 뒤 9.3초에 통과.
- ✅ **프레임 경로도 공통 헬퍼(`_uploadAndAddToPrompt`)로 통일**했다 — 두 벌로 뒀으면 애셋 쪽만 고쳐지고
  프레임은 그대로 깨져 있었을 것이다(테스트가 `setInputFiles` 구현이 1곳인지 센다).
- 🔑 **안전장치가 제 몫을 했다** — 첨부 실패를 감지해 **그 컷을 만들지 않았고 크레딧이 0** 이었다.
  그게 없었으면 원본과 무관한 영상에 10크레딧을 쓰고도 실패를 몰랐을 것이다.

### 🤫 「업스케일까지 갔는데 멈춘 것 같다」 — 멈춘 게 아니라 **조용했다** (2026-08-28, v0.3.75)
> 로이: "업스케일까지 진행했는데 그 뒤로 진행을 안하고 멈춘것 같아." (Flow 화면에 「업스케일링이 완료되었습니다」
>   토스트만 떠 있는 스크린샷)

**멈추지 않았다 — 그 시점에 이미 성공하는 중이었다.** 로이가 앱에서 v0.3.74 로 실제로 돌린 기록:
```
[23:47:56] 🎬 롱폼 비디오 생성 (Flow Veo 3.1 - Lite · 애셋(참조) · 1080p · 1개 그룹 · 계정 1)…
[23:51:01]   ✓ G2 → 02.mp4
[23:51:01] ⬆ 업스케일 생략 — 이미 1920x1080 이상인 영상 2개 (로컬 GPU 사용 안 함)
```
결과 파일 확인: `01.mp4`(7.9MB) · `02.mp4`(10.1MB) **둘 다 1920x1080 · 8.00초**. 의도대로 동작했다.

### 🔑 왜 멈춰 보였나 — 두 가지가 겹쳤다
1. **다운로드 구간이 통째로 침묵이었다.** 생성 중에는 15초마다 진행률(`13% → 27% → 40%`)이 찍히는데,
   **1080p 를 요청한 뒤 Flow 가 업스케일본을 만드는 1~3분 동안은 로그가 한 줄도 없었다.**
   화면에도 토스트 하나뿐이라 「멈췄다」로 보인다. 로이 실사용은 **총 3분 5초**였고 그중 2분 이상이 이 구간이다.
   → 시작을 알리고(`… 업스케일본을 만드는 중입니다(보통 1~3분 · 그동안 화면은 조용합니다)`)
     **20초마다 경과**를 남긴다. 완료 로그에도 걸린 시간을 붙였다. 대기 상한도 180초 → **300초**
     (로이 케이스가 2분을 넘겼다 — 옛 값이었으면 아슬아슬했다).
2. 🔴 **엔진 로그가 파일에 안 남았다.** `FlowAutomator.log()` 는 `send('log')` 로 **화면에만** 보낸다.
   앱 로그 파일(`~/.shots-maker/logs/`)에는 main 의 `log()` 를 거친 것만 남으므로, 나중에
   「그때 뭘 하고 있었나」를 되짚을 기록이 없었다(v0.3.15 가 로그 파일을 만든 바로 그 이유).
   ⚠ 엔진이 `run()` 안에서 쓰는 자체 `log.txt` 는 **임시 workDir** 이라 작업이 끝나면 함께 지워진다.
   → `runFlowVideos` 가 `eng.log` 를 감싸 **`logToFile('[Flow] …')`** 도 하게 했다.
     ⚠ `eng` 는 재사용되는 인스턴스라 **`_appLogHooked` 로 한 번만** 감싼다(안 그러면 로그가 2배·3배로 늘어난다).

### ⚠ 진행 상황을 볼 때 기억할 것
- Flow 비디오 1개 = **약 3분**(첨부 10초 + 생성 1분 + 1080p 업스케일·다운로드 1~2분). 그룹이 5개면 15분이다.
- 「업스케일링이 완료되었습니다」 토스트는 **끝났다는 뜻이 아니다** — 그 뒤에 파일을 받는 시간이 더 있다.
- 이제 로그창(과 로그 파일)에 20초마다 경과가 찍히므로, **그게 멈추면** 진짜 문제다.

### 🔴 채널 편집에는 Veo 가 없었다 — **비디오 엔진을 고르는 곳은 두 군데다** (2026-08-29, v0.3.76)
> 로이: "비디오 항목에 VEO가 없네" (⚙ 채널 편집 → 🖼 제작 도구 스크린샷)

헤더 「③ 비디오」만 고치고 **⚙ 채널 편집의 비디오 드롭다운을 빠뜨렸다.** 그래서 채널 기본값으로 Flow 를
지정할 수가 없었다(이미지 쪽은 `Flow (구독)` 이 이미 있어 더 눈에 띄었다).
- 이 저장소 단골인 **「진입점이 여러 곳」** 함정이다(v0.3.2 의 워크플로 select 이중화, v0.3.50 의 정규화 3곳과 같은 계열).
- ✅ 테스트가 이제 **개수를 센다** — `Flow · Veo (구독)` 항목이 **정확히 2곳**(헤더 + 채널 편집)에 있어야 통과한다.
  A/B 로 한쪽을 빼면 실제로 실패하는 것을 확인했다.
- 🔑 **비디오 엔진을 추가할 땐 App.jsx 에서 `<option value=` 를 검색해 두 곳을 다 고칠 것.**

### 🪟 창은 **작업 단위**로 열린다 — 대본 하나는 창 하나 (2026-08-29, v0.3.77)
> 로이: "비디오 몇 개 만들고 새 창을 열어서 만드는 기준점이 있어? 1개의 대본이라면 하나의 창에서 계속 해도 되는 것 아닌가?"

**개수와는 무관하다.** 새 창이 열리는 조건은 셋뿐이다:
| 조건 | 어디서 |
|---|---|
| **계정이 바뀔 때** | `getFlowEng` 의 프로필 비교 · 엔진의 `profileId 변경 감지` |
| **창이 죽었을 때** | 엔진 health check 실패 → 자동 재시작 |
| **작업(버튼 클릭)이 끝났을 때** | `closeFlowEng()` 가 닫는다 → **다음 클릭은 새 창** |
- `run()` 은 `paragraphs` 배열을 받아 **한 창·한 프로젝트**에서 순서대로 만든다(`_createNewProject` 도 **1회**).
  ⇒ **범위를 `G1~G5` 로 넓게 잡아 한 번에 만들면 창 하나로 끝난다.** 그룹을 하나씩 따로 만들면 매번
  새로 열려 **기동에만 약 20초씩** 버린다(브라우저 시작~프로젝트 진입 실측).
- anti-detect 는 **창을 새로 열지 않는다** — `기본` 프리셋 기준 컷 사이 평균 14초, **12개마다** 긴 쿨다운(30~60초)뿐.
- ✅ **로그로 보이게 했다**: `♻ Flow 브라우저 재사용` / `🔀 Flow 계정이 바뀌어 기존 브라우저를 닫습니다 (A → B)` /
  `🧹 Flow 브라우저를 닫습니다 (작업 완료 — 다음 작업은 새 창으로)`. 엔진 쪽 `[Flow] 기존 브라우저 재사용` 은
  **원래 있었지만 파일에 안 남아** 안 보였다(v0.3.75 에서 해소).

### ⚡ **동시 제출이 된다** — 실측 확인 + 두 번째 컷 클릭 버그 수정 (2026-08-29, v0.3.78)
> 로이: "비디오는 한번에 1개씩만 가능한가? 2개이상도 가능해?" → "네가 실측해줘" (크레딧 20 승인)

### 실측 결과 — 2개를 연달아 제출해 **병렬로 돌았다**
| 시각 | 관측 |
|---|---|
| 29.3s | 컷1 제출 |
| 31~39s | **제출 직후에도 `프롬프트바=편집가능 · 만들기=enabled`** ← Flow 는 동시 제출을 막지 않는다 |
| 51.2s | 컷2 제출 (간격 22초 = 프레임 첨부 9초 + 프롬프트 + 제출) |
| **61.3s** | **진행률 `["8%","28%"]` — 두 개가 동시에 돌고 있다** |
| **121.6s** | **둘 다 완성** |
- 순차였다면 대략 `29 + 52 + 22 + 52 ≈ 155초` → **약 22% 단축**(2개 기준). 컷이 많을수록 이득이 커진다.
- ⚠ **완전 병렬은 아니다** — 제출 자체가 순차(컷당 22초)라 그만큼 어긋나 시작한다.

### 🔴 그 과정에서 잡은 진짜 버그 — **두 번째 컷부터 첨부가 실패했다**
첫 시도에서 컷2 가 `locator.click: Timeout 5000ms exceeded` 로 죽었다. **스크린샷을 보니 그 버튼은
활성 상태로 멀쩡히 떠 있었다** — Flow 제약이 아니라 우리 클릭이 실패한 것이다.
- 원인 추정: 업로드 처리 중 **DOM 이 갈아끼워져 옛 핸들이 죽었거나**, `.first()` 가 보이지 않는 쪽을 집었다.
- ✅ **매 시도마다 다시 찾고**(`.filter({ visible: true })`) **2회 재시도**, 그래도 안 되면 **DOM 클릭으로 확정**.
  대기 상한도 40초 → 60초. 고친 뒤 재실측에서 **컷2 제출 성공**(51.2s).
- 🔑 **이건 동시 생성과 무관하게 중요하다** — 지금 구조에서도 한 대본에 그룹이 여러 개면 **2번째 컷부터
  같은 실패**가 났을 것이다. 로이가 어제 성공한 건 그룹이 **1개**짜리였다.

### ⛔ 동시 생성은 **하지 않기로 결정했다** (2026-08-29 로이 확정)
> 로이: "동시생성은 하지말자."

**Flow 가 받아 주는 것과 우리가 그렇게 쓰는 것은 별개다.** 기술적으로 가능함은 위에서 확인했지만
(진행률 2개 동시 관측 · 121.6초에 2개 완성) 다음 이유로 **순차를 유지한다**:
- 🔑 **anti-detect 와 정면으로 상충한다** — 지금은 컷 사이 평균 14초를 **일부러** 쉰다(봇 회피).
  동시 제출은 그 설계를 스스로 무너뜨린다. 얻는 22% 보다 **계정 차단 위험**이 크다.
- **매칭이 복잡해진다** — 지금은 제출 전후 `/edit/<uuid>` 비교로 **1:1 이 구조적으로 보장**된다.
  여러 개가 동시에 끝나면 어느 카드가 어느 컷인지 가려야 하고, 틀리면 **엉뚱한 영상이 그룹에 붙는다**
  (v0.3.20 Grok 「남의 영상」 사고와 같은 계열).
- Flow 는 크레딧을 쓰는 **대안 경로**다(주력은 ComfyUI 클라우드 LTX2.5 — 영상당 추가 과금 없음).
  주력이 아닌 경로를 위해 위험을 지는 것은 균형이 맞지 않는다.
- ⛔ **다음 세션이 이 제안을 다시 꺼내지 말 것.** 다시 볼 조건: 로이가 Flow 를 **주력**으로 전환하고,
  그때 계정 위험을 감수하기로 명시적으로 결정하는 경우.

### 🔴🎨 재시도로 만든 영상이 **실사로 튀었다** — 재첨부를 안 했다 (2026-08-29, v0.3.80)
> 로이: "재시도하여 생성된 동영상은 첨부한 이미지의 스타일에서 출발하는 것이 아니라 전혀 다른 스타일,
>   즉 실사로 비디오가 생성되었어." (수채 그림 원본 ↔ 실사 영상 프레임 두 장을 나란히 보여 줌)

**원인은 한 줄로 끝난다 — 재시도 경로가 소스 이미지를 다시 붙이지 않았다.**
```js
// 옛 재시도 블록
await this._typePrompt(simplifiedPrompt);
await this._clickFinalCreateV2();          // ← 이미지 첨부가 없다 = 텍스트만으로 생성(t2v)
```
🔑 **제출하면 프롬프트 바가 비워지면서 붙여 둔 이미지도 함께 사라진다.** 첫 시도는 첨부하고 제출하지만,
재시도는 프롬프트만 다시 넣고 제출하므로 **순수 t2v** 가 되어 Veo 가 자기 기본 화풍(실사)으로 그린다.
→ `_reattachSource(opts, i, num)` 신설. **재시도 경로 두 곳**(일반 5회 · rate-limit 1회)이 모두 부른다.
   설정한 방식(프레임/애셋)을 그대로 따르고, **재첨부에 실패하면 그 재시도를 하지 않는다**(첫 시도와 같은 원칙).

### 🔴 같은 사고의 다른 얼굴 — 재시도로 성공하면 **720p 가 저장됐다**
같은 로그에서 드러났다: `01.mp4` **3,260KB**(재시도 성공) vs `02·03.mp4` **8,621·8,090KB**(첫 시도 성공).
저장 지점이 **세 곳**(첫 시도 · rate-limit 재시도 · 일반 재시도)인데 **1080p 다운로드는 첫 시도에만** 있었다.
→ **`_saveResult` 로 저장을 하나로 모았다.** 어느 경로로 성공하든 1080p 를 먼저 시도한다.
- 🔑 **이 저장소 단골 패턴이다** — 「같은 일을 하는 코드가 여러 곳」(v0.3.76 드롭다운 2곳, v0.3.50 정규화 3곳).
  **성공 경로가 여러 개면 후처리도 그만큼 갈라진다** — 후처리는 한 함수로 모을 것.

### 🔴 왜 실패했는지 로그에 없었다
`_waitForImage` 가 실패를 감지하면 사유를 남기는데 그게 **`debug()`** 라 앱 로그 **파일에 안 남는다**
(v0.3.75 에서 `log` 만 파일에 연결했다). → `log()` 로 올려 **`⚠ 생성 실패 감지: <사유>`** 가 파일에 남는다.
- 덤: 생성이 실패했는데도 다운로드를 시도해 15초를 버리고 `결과 카드를 못 찾음` 이라는 **오해할 로그**를
  남겼다 → 성공 신호(버퍼·캡처)가 하나도 없으면 다운로드를 건너뛴다.

### 검증
`test/flow-video.test.js` **119/119**(+6) — 재첨부 2곳 · 실패 시 건너뜀 · 방식(프레임/애셋) 준수 ·
`_saveResult` 3곳(첫 시도+재시도 2) · 실패 사유 로그. 🔑 **A/B**: 재첨부를 빼면 **2건 실패**.
회귀: makeall 18/18.

### ⚠ 로이가 알아 둘 것
- **이미 만들어진 `01.mp4` 는 실사다** — 그 그룹만 「🎬 비디오」로 다시 만들면 된다(앱 재시작 후).
- 재시도는 **프롬프트를 단순화**한다(모더레이션 회피용). 이미지가 다시 붙으므로 **화풍은 유지**되지만
  모션 지시는 약해질 수 있다. 결과가 밋밋하면 그 컷만 다시 만드는 편이 낫다.

### 🔴🪟 **돌고 있는 Flow 창을 남의 작업이 닫았다** — 비디오가 통째로 죽음 (2026-08-29, v0.3.81)
> 로이: "VEO로 비디오를 만들 때 창이 자꾸 닫히는 현상이 발생하는데 원인과 처방을 진행해줘."

로그가 원인을 그대로 보여 줬다 — **이미지 작업이 비디오 창을 닫는다**:
```
[11:22:04] ↔ 대본 선택: [성경_0905] …          ← 큐가 다음 대본으로 넘어감
[11:22:04] 🖼 롱폼 이미지 생성 (comfy)…         ← ComfyUI 이미지(Flow 와 무관!)
[11:22:05] ✓ 롱폼 이미지 완료
[11:22:05] 🧹 Flow 브라우저를 닫습니다 (작업 완료)  ← ⚠ 이게 남의 창을 닫았다
[11:22:05] [Flow] [04] 오류: Target page, context or browser has been closed
[11:22:05] [Flow] 계정 1 영상 매핑 3/5           ← 5개 중 3개만 만들고 죽음
```
- 🔑 **`closeFlowEng()` 가 「누가 쓰고 있는지」를 보지 않았다.** 이미지 핸들러(`image-build`)도 끝날 때
  이걸 부르는데(Flow 이미지를 쓸 수도 있으니), 그 시점에 **Flow 비디오가 8분째 돌고 있었다.**
  이미지 엔진이 comfy 여서 Flow 와 아무 상관이 없는데도 닫았다.
- 로그에서 **3초 만에 열었다 닫는 사이클이 반복**된 것도 같은 원인이다(비디오가 시작하자마자 다음 대본의
  이미지 작업이 끝나며 닫음 → 계정 순환 → 또 열림 → 또 닫힘).

### ✅ 처방 둘
1. **쓰는 중이면 닫지 않는다** — `S.flowBusy` 카운터. `closeFlowEng()` 가 0 보다 크면 건너뛰고
   `⏸ Flow 브라우저를 다른 작업이 쓰는 중이라 닫지 않습니다` 를 남긴다. 쓰던 작업이 끝나면 그쪽이 닫으므로
   창이 남지 않는다. ⚠ 카운터는 **`finally` 로** 되돌린다(예외에 새면 영영 안 닫힌다 — 테스트가 단언).
2. **Flow 브라우저 전용 레인**(`_LANES.flowBrowser`) — Flow 이미지와 Flow 비디오는 **같은 크롬 창·같은 page**
   를 쓴다(`getFlowEng` 가 인스턴스를 재사용). 둘이 동시에 돌면 서로의 페이지를 조작해 깨진다 → 직렬화한다.
   호출부는 `_withFlowBrowser(label, fn)` 하나로 감쌌다(이미지 1곳 · 비디오 1곳).
   ⚠ image/localGpu 와 **다른 레인**이라 교착이 없다(Flow 는 브라우저라 GPU 와 무관).

### 검증
`test/flow-video.test.js` **125/125**(+6) — 레인 정의 · `_withFlowBrowser` 3곳(정의+호출2) · busy 가드 ·
finally 복원 · 로그. 🔑 **A/B**: 가드를 빼면 **실패**. 회귀: makeall 18/18 · comfy 전량(2분할 91/91).
⚠ `gpu-lanes.test.js` 가 **레인 개수를 못박고** 있어 4→5 로 갱신했다(v0.3.51 때와 같은 정비).

### ⚠ 로이가 알아 둘 것
- 이 사고로 **[성경_0905] 는 5개 중 3개만** 만들어졌다. 앱 재시작 뒤 그 대본의 남은 그룹을 다시 만들면 된다.
- 이미지와 비디오를 **같은 큐에서 연달아** 돌릴 때 잘 드러난다 — 이제 서로의 창을 닫지 않는다.

### 🔴📥 업스케일이 도는 이유 — **1080p 항목이 계정에 따라 잠긴다** (2026-08-29, v0.3.82)
> 로이: "플로우로 비디오를 만들었을경우 1080P로 다운받는것 맞지? 그러면 업스케일이 필요없지 않나. 지금 보니 업스케일을 하고 있네…왜그러지?"

**설계는 맞다 — 1080p 로 받으면 업스케일은 생략된다.** 이번엔 **1080p 다운로드가 실패해 720p 로 떨어진 것**이고,
그 실패 원인이 **계정**이었다. 로그가 계정별로 정확히 갈린다:
| 계정 | 1080p 다운로드 |
|---|---|
| **계정 1**(flow-mr2xrx49) | 성공 **8건**(39~72초) → `⬆ 업스케일 생략` |
| **flow1**(flow-mt6g8s2p) | 실패 **2건**(11:39 · 13:22) → `1280x720` → **로컬 GPU 업스케일 36.8초** |

### 🔑 실패 로그가 원인을 그대로 말하고 있었다 — `element is not enabled`
```
[DL 01] 1080p 다운로드 실패: locator.click: Timeout 5000ms exceeded.
  - locator resolved to <button role="menuitem" data-disabled="" aria-disabled="true" …>
  - 2 × waiting for element to be visible, enabled and stable
    - element is not enabled          ← 셀렉터가 틀린 게 아니라 **항목이 잠겨 있다**
```
- 🔑 **Flow 는 잠긴 항목을 숨기지 않는다** — `4K 업스케일 + 업그레이드 배지`와 같은 방식으로 **비활성 상태로
  남겨 둔다.** 그래서 `waitFor({state:'visible'})` 은 **통과하고** `click` 에서만 5초 뒤에 터진다.
  UI 가 바뀐 것으로 오해하기 딱 좋다(실제로 그렇게 보인다).
- ⚠ **왜 그 계정만 잠겼는지는 Flow 쪽 사정**이다(플랜 등급·크레딧으로 추정 — flow1 은 v0.3.36 에서 구독이
  없던 그 계정이다). 앱이 고칠 수 있는 문제가 아니다. **고칠 수 있는 건 「조용히 느려지는 것」뿐이다.**

### ✅ 한 것 — 원인이 로그에 남고, 같은 계정에서 반복 낭비하지 않는다
1. **클릭 전에 잠김을 판정한다**(`_menuItemDisabled`) — `aria-disabled` · `data-disabled` · `el.disabled`.
   ⚠ **못 읽으면 막지 않는다(fail-open)** — 판정 때문에 되던 다운로드를 포기하는 쪽이 더 나쁘다.
2. **사람 말로 알린다**: `⛔ 이 계정에서는 「1080p」 항목이 비활성입니다 — Flow 플랜/크레딧 제한으로 보입니다` +
   `메뉴 상태: 270p 애니메이션 GIF · 720p 원본 크기 · 1080p 업스케일(잠김) · 4K 업스케일(잠김)`(`_dumpDownloadMenu`) +
   `→ 720p 원본으로 받습니다. 받은 뒤 내 PC GPU 로 업스케일하므로 영상당 수 분이 더 걸립니다`.
3. **그 계정에서는 다시 시도하지 않는다**(`_hiResBlockedFor = this.profileDir`) — 안 그러면 **컷마다 5초씩**
   메뉴를 여닫으며 버린다. ⚠ 키를 profileDir 로 잡아 **계정이 바뀌면 다시 시도**한다(계정 1 은 되므로).
4. **실패 로그는 첫 줄만** — Playwright 는 Call log 를 20줄씩 붙여 **진짜 원인 한 줄이 묻힌다**(이번에 실제로 묻혔다).

### 검증
`test/flow-video.test.js` **135/135**(+10) — 클릭 전 판정 · 세션 캐시 · 메뉴 덤프 · 사람 말 로그 · 첫 줄만 ·
**`_menuItemDisabled` 원문 실행 4케이스**(실사고 DOM 의 `aria-disabled="true"` · `data-disabled` · 정상 항목 ·
읽기 실패 시 fail-open). 🔑 **A/B**: 판정을 `if (false)` 로 무력화하면 **실패**(134/135). 회귀: makeall 18/18.

### ⚠ 로이가 알아 둘 것
- **계정 1 을 쓰면 1080p 로 받아 업스케일이 통째로 생략된다**(오늘 8건 전부 그랬다). flow1 로 만들면 720p 를
  받아 **영상당 30~40초 로컬 GPU 업스케일**이 붙는다 — 결과 화질은 비슷하지만 시간이 더 든다.
- flow1 의 1080p 를 살리려면 그 계정의 Google AI 구독 등급을 올려야 한다(앱에서 못 고친다). 확인법:
  Flow 에서 영상 카드 `⋮` → 「다운로드」에 마우스를 올려 **1080p 항목이 회색인지** 보면 된다.
- ⏳ **다른 720p 폴백 원인도 로그에 2건 있다** — `⚠ 새로 생긴 결과 카드를 못 찾음`(11:15·11:45, **계정 1**).
  이건 잠김과 무관한 별건(제출 전후 uuid 비교가 새 카드를 못 잡은 경우)이고 아직 원인 미확정이다.

### 🪟 비디오 만들다 **빈 창(about:blank)이 자꾸 늘던 것** — 다운로드가 새 탭에서 시작된다 (2026-08-29, v0.3.83)
> 로이: "비디오 생성하다가 이렇게 빈창을 여는 경우가 자주 발생하는데 이건 왜 생기는거고 해결책을…"
>   (Flow 탭 1개 + `about:blank` 탭 2개 스크린샷)

### 🔑 원인 — 다운로드 1건당 빈 탭 1개
**Flow 의 다운로드는 새 탭에서 시작된다.** 서명 URL 을 새 탭으로 열고 파일 전송이 시작되면 그 탭에는 그릴
문서가 없어 **`about:blank` 로 남는다**(크롬의 정상 동작이라 오류 로그도 안 남는다).
- 🔑 **숫자가 정확히 맞았다** — 스크린샷 시점에 살아 있던 창(계정 1 · 13:30:57 시작)의 로그를 보면
  **1080p 다운로드가 2건 완료**(13:33:16 · 13:35:19)였고, 화면의 빈 탭도 **정확히 2개**였다.
  12:47 세션은 5건 받았으니 그때는 5개였을 것이다 — 로이가 「자주」라고 느낀 이유가 이것이다.
- ⚠ 무해하지 않다: **탭 하나 = 렌더러 프로세스 하나**다. 그룹이 30개인 대본이면 탭 30개가 쌓인다.
- ⛔ **처음엔 「프로필 점유 상태에서 launch 하면 기존 창에 about:blank 가 열린다」를 의심했는데 아니었다.**
  그 경로는 로그에 `⚠ 정식 Chrome 실행 실패` 가 반드시 찍히는데 오늘 로그에 **한 건도 없다**(전부 `브라우저 엔진: Chrome`).
  (다만 그 현상 자체는 실재한다 — 진단하려고 같은 프로필로 Playwright 를 띄웠더니 크롬이 기존 인스턴스에
  `about:blank` 를 넘기고 자기는 종료했다. **앱 밖에서 그 프로필을 열지 말 것.**)

### ✅ 한 것 — `_closeBlankTabs()` 로 빈 탭만 치운다
- ⚠ **반드시 전송이 끝난 뒤에 닫는다** — 진행 중에 닫으면 다운로드가 취소된다. 그래서 `saveAs` **뒤**에 부른다
  (테스트가 이 순서를 단언한다).
- ⚠ **메인 페이지(`this.page`)와 URL 이 있는 탭은 건드리지 않는다.** `about:blank` 와 빈 URL만 닫는다(보수적).
  남의 탭을 지우는 쪽이 빈 탭을 남기는 것보다 나쁘다.
- 부르는 곳 **4곳**: 1080p 다운로드 성공 · 실패(폴백 경로에서도 탭이 생겼을 수 있다) · 고해상도 이미지 다운로드 ·
  **컷 종료 시**(다운로드 경로가 1080p·네트워크 캡처·재생 소스로 갈라지므로 마지막 안전망).
- 어떤 이유로 실패해도 **던지지 않는다** — 탭 정리 때문에 생성이 멈추면 본말전도다.

### 검증
`test/flow-video.test.js` **145/145**(+10) — 배선 3(헬퍼 존재 · 호출 4곳 · saveAs 뒤 순서) +
**`_closeBlankTabs` 원문 실행 7**(빈 탭 2개만 닫음 · **메인 페이지 보존** · 내용 있는 탭 보존 · 이미 닫힌 탭 ·
context 없음 · close 예외). 🔑 **A/B**: 정리를 no-op 으로 만들면 **실패**(143/145).
회귀: makeall 18/18 · grok 81 · browser-recovery 37/37.

### ⚠ 로이가 알아 둘 것
- 이제 컷마다 빈 탭이 정리되므로 창에 **Flow 탭 하나만** 남는다. 이미 떠 있는 빈 탭은 손으로 닫으면 된다.
- 빈 탭이 다시 보이면 그건 **다른 원인**이다(위 ⛔ 항목처럼 앱 밖에서 같은 프로필을 연 경우).

### 💳 Flow **크레딧 소진**을 못 알아채고 컷마다 헛돌던 것 (2026-08-29, v0.3.84)
> 로이: "비디오 생성중인데 크레딧을 다썼어. 이제 이것에 대한 대안 방법을 찾아야겠는데"
>   (Flow 화면 상단 배너: 「Google Flow 크레딧이 소진되었습니다. 크레딧이 갱신될 때까지 기다리거나…」)

### 🔑 앱이 그 배너를 못 봤다 — 이유가 둘이다
1. **패턴에 「크레딧」이 없었다.** `_detectRateLimitText` 는 `너무 빨리`·`일일 한도`·`rate limit` 만 봤다.
2. 🔴 **폭 상한 1200px 에 걸렸다.** 그 배너는 **판면 전폭**(실측 스크린샷 ≈1280px)이라, 작은 토스트를 겨냥한
   `rect.width > 1200 → continue` 필터가 **그것만 골라서 걸러 냈다.** 셀렉터가 맞아도 못 잡는 구조였다.
- 그래서 크레딧이 0인 채로 컷마다 제출 → 생성 안 됨 → 타임아웃까지 대기를 반복했다.

### ✅ 한 것 — 「기다리면 되는 것」과 「기다려도 안 되는 것」을 가른다
- **`credit-exhausted` 타입 신설.** rate-limit 은 60초 뒤 재시도가 의미 있지만 **크레딧은 구독 주기로 갱신**되므로
  기다려도 그 자리에서 회복되지 않는다 → **대기 0초로 즉시 다음 계정**(`isCredit` 분기).
- **크레딧 패턴은 폭 무관하게 먼저 본다**(그 밖의 토스트는 기존 폭 필터 유지 — 페이지 전체 오탐 방지).
  ⚠ **「생성 시 10크레딧이 사용됩니다」(설정 팝업)와 섞이지 않게** 「소진/부족/모두 사용」을 함께 요구한다.
- **안전망**: 셀렉터에 안 걸려도 **본문에서 크레딧 문구만** 한 번 더 본다(다른 패턴은 본문 검사를 하지 않는다 — 오탐이 크다).
- **main 은 그 계정을 `FLOW_CREDIT_REST_MIN`(6시간) 쉬게 한다.** 30분 쿨다운(rate-limit 용)은 의미가 없다.
  ⚠ 12시간이 아닌 이유: 일일 보너스 크레딧이 붙는 기간이 있어 그날 안에 회복되기도 한다.
- **무엇을 하면 되는지 로그가 알려준다** — 비디오는 `→ 헤더 「③ 비디오」를 ☁ LTX2.5 로` , 이미지는 `→ 🖥 ComfyUI 로`.

### 🔑 크레딧이 없을 때의 대안 (실측 근거)
| 경로 | 크레딧 | 편당 5개 소요 | 해상도 |
|---|---|---|---|
| **☁ ComfyUI LTX2.5**(주력) | **0** — 구독 GPU 시간만 | **약 2.5분**(동시 3개) | 1920x1088 → 업스케일 생략 |
| 🖥 MiniMax H3 turbo4(로컬) | 0 | 약 47분 | 1312x736 → 업스케일 필요 |
| ☁ MiniMax H3 레퍼런스(클라우드) | **0** — 구독 GPU 시간만 | 약 5분 | 1344x768 → 업스케일 필요 |
| Flow · Veo | **10크레딧/컷** | 약 15분 | 1080p(구독 계정만) |
- 확인함: `https://cloud.comfy.org/api/system_stats` **200**(cloud v0.235.5) · 설정에 API 키 있음 ·
  워크플로 `LTX2.5` 등록됨 · 동시 3. **LTX2.5 는 파트너 API 노드를 쓰지 않는 로컬노드 워크플로라 영상당 추가 과금이 없다.**

### 검증
`test/flow-video.test.js` **159/159**(+14) — 배선 7 + **`_detectRateLimitText` 원문 실행 7**:
**실제 배너 문구(폭 1280)** 감지 · 「생성 시 10크레딧」 오탐 금지 · 본문 안전망 · 영어 배너 ·
**rate-limit·비정상활동 회귀** · 평범한 화면에서 무반응. 🔑 **A/B**: 폭 필터를 옛 값(1200)으로 되돌리면
**실패**(157/159) — 그 한 줄이 실사고의 원인이었음을 확인. 회귀: makeall 18/18.

### ⚠ 별건 — `img-rotate-resume` 이 이미 깨져 있다 (이번 수정과 무관)
`npm run test:visual` 의 **27/34**. **내 수정 전(HEAD 상태)에도 같은 실패**임을 `git stash` 로 확인했다.
증상: v0.3.67 의 「한도가 풀리면 같은 대본 도중에 Genspark 로 되돌아간다」가 **동작하지 않는다**
(Genspark 호출 1회 · 5장 중 1장). 그 뒤 어떤 커밋이 `runRotatingImages` 의 복귀 경로를 깬 것으로 보인다.
⏳ **원인 미확정 — 다음에 따로 잡을 것.** 지금은 이미지 순환이 한 바퀴만 돌고 끝난다(복귀 없음).

### 쓰는 법
헤더 「③ 비디오」 → **`Flow · Veo (구독)`** → 범위 지정 → 「🎬 비디오」.
모델은 **⚙ Veo** 버튼(또는 ⚙ 설정 → 🌐 브라우저 이미지·비디오)에서 Lite/Fast/Quality 중 고른다.
**이미지가 있는 그룹만** 대상이다(i2v 라 시작 프레임이 필요하다). 이미 영상이 있으면 건너뛴다(이어받기).

### 검증
- **`test/flow-video.test.js` 65/65 신설**(`npm run test:flow` 에 포함) — 실측값 대조(모델 라벨·프레임 탭) ·
  **`runFlowVideos` 원문 실행**(대상 선정 · `NN_본문.mp4` → `media/GG.mp4` 매핑 · 범위 · 이어받기 ·
  계정 순환 · status 고착 없음 · 대상 0이면 브라우저 미기동) · 배선 대조 12 · `_vidUsesLocalGpu` 실행 ·
  image-rotation 왕복(임시 HOME — 실제 설정 무변경) · **번들 반영 확인**(소스만 고치고 빌드를 잊으면 화면은 옛것).
- 🔑 **A/B 역검증 3건**: ⓐ `_selectModel` 을 옛 셀렉터로 되돌리면 **실패** ⓑ `frameImages` 를 빼면
  (=t2v 로 퇴행) **실패** ⓒ `attachMode` 전달을 빼면(=애셋을 골라도 프레임) **실패**. 셋 다 실제로 돌렸다.
- v0.3.72 에서 **79/79**(애셋 모드), v0.3.73 에서 **93/93**(1080p 다운로드) 로 늘었다.
  A/B 역검증도 늘렸다: ⓓ `attachMode` 전달을 빼면 **실패** ⓔ `videoDownload` 전달을 빼면 **실패** —
  둘 다 「설정을 골라도 조용히 무시」 되는 사고라 배선 단언이 특히 중요하다.
- 회귀: **makeall 무음 E2E 18/18**(main.js 를 만졌으므로 필수) · flow-account 46/46 · grok 81 ·
  browser-recovery 37/37 · comfy 전량(2분할 패널 E2E 91/91).

### ⚠ 로이가 알아 둘 것
- **크레딧을 쓴다** — Veo 3.1 Lite · x1 · 720p = **10크레딧**(Flow 화면 표시). 화질을 올리면 더 나간다.
  ComfyUI 클라우드 LTX2.5 는 구독 GPU 시간이라 영상당 추가 과금이 없다 — **비용만 보면 LTX2.5 가 유리하다.**
  Flow 의 이점은 Veo 품질과, ComfyUI 가 막혔을 때의 대안이라는 점이다.
- **길이가 약 8초로 고정**이다(Flow UI 에 길이 옵션이 없다). 긴 그룹은 뒷부분을 이미지가 채운다.
- ✅ **`프롬프트에 추가` 버튼은 v0.3.72 에서 실측으로 확인했다** — 더미 PNG 를 실제로 업로드해
  「업로드 → 자동 선택 → `프롬프트에 추가` → 프롬프트 바에 썸네일로 붙음」 까지 눈으로 봤다
  (확인 뒤 그 애셋은 휴지통으로 정리). 프레임·애셋이 같은 다이얼로그를 쓰므로 양쪽 다 검증된 셈이다.
- ✅ **실물 생성까지 확인 완료**(v0.3.74 — 위 항목). 로그에서 볼 것: `[i2v 01] 첨부 완료 ✓` →
  `[DL 01] 1080p 다운로드 완료 (…KB)`. 실패하면 그 컷을 건너뛰며 `⏭ 시작 프레임 첨부 실패` 와
  `[i2v DUMP]` 로 버튼 후보를 남긴다.
- ⚠ 조사·검증 중 로이 계정에 **Flow 프로젝트가 여러 개** 생겼다(8월 28일 오후 10:31 · 10:49 · 11:3x 대 몇 개 —
  진단 스크립트가 실행마다 `_createNewProject` 를 탔다). 앱도 실행마다 하나씩 만들므로
  무해하지만, 정리하려면 Flow 홈에서 삭제하면 된다.

## 🎬🖥 비디오 로컬 항목 — MiniMax H3 turbo4 (2026-08-28, v0.3.70)
> 로이: "로컬 turbo 4step로 하는 항목 만들어줘. 여유 있을때 사용하곤 할께. **클라우드는 비용이 너무 비싸다**는 큰 단점이있어."

헤더 「③ 비디오」에 **`🖥 MiniMax H3 turbo4 (로컬)`** 추가. 크레딧 0 · 오디오 동시 생성 · 원본 이미지 유지.
⚠ **기본 활성은 LTX2.5 그대로다**(로이: 여유 있을 때만) — 테스트가 이걸 단언한다.

### 🔑 이건 i2v 가 아니다 — 「레퍼런스 참조 생성」이다
MiniMax H3 Ref2VA 는 LTX 처럼 **첫 프레임을 고정하지 않는다.** 레퍼런스를 「참고」만 하므로 그냥 넣으면
**구도·인물·화풍이 통째로 바뀐다**(실측: 원본 「활 든 왕 + 신하 5명 롱샷」 → 「얼굴 클로즈업 1명」).
- ✅ 해법 = **`<Picture 1>` 태그 규약**(워크플로 공식 노트 #116): *"reference the inputs by tag, in the exact
  order they were connected … then describe the target scene"*. 태그로 **역할을 지정**해야 레퍼런스가 산다.
- 🔑 **대본은 한 글자도 안 고친다** — 엔진이 `MM_REF_PREFIX` 한 줄을 앞에 붙인다. 워크플로에도 태그를 안 박았다
  (테스트가 단언). ⛔ **대본에 이 태그를 쓰게 하지 말 것** — LTX 로 되돌리면 대본 468편이 그 문법에 묶인다.
- 효과(같은 seed·같은 대본): 구도·신하 옷 색·흙더미·억새·**좌측 자막 여백**·수채 화풍이 전부 복원됐다.

### 🔴 밟은 함정 넷 (전부 실측으로 잡았다)
1. **turbo LoRA 를 AccLoader 로 붙이면 실패한다** — `MiniMax H3 Acc metadata 'pdd_num_steps' must be an integer`.
   정품 Acc LoRA 는 **PDD(단계별 증류)** 라 스텝마다 다른 블록을 쓰고 메타데이터를 요구한다.
   `minimax_h3_ref2v_turbo_4step` 은 그 규격이 아니라 **표준 `LoraLoaderModelOnly`** 로 붙여야 한다.
2. 🔴 **정품 Acc-8Step 을 4스텝으로 줄여도 이득이 없다**(110초 vs turbo 89초). HF 문서의 "4 and 8 NFE" 는
   diffusers 스크립트 기준이고 ComfyUI PDD 구현과 다르다. ⛔ 8Step 파일을 4로 쓰지 말 것.
3. 🔴 **`ref_image_size: max` 는 오히려 나쁘다**(105초 · 구도 이탈). 노트의 "정체성 보존이 강해진다" 는
   **인물 얼굴·디테일** 얘기지 구도 보존이 아니다. → **`match` 고정.**
4. 🔴 **기본 타임아웃 600초로는 실제로 터진다** — 0.98MP 실측 **563초**라 여유가 없다(E2E 에서 601초 타임아웃).
   → `MM_MIN_TIMEOUT_SEC = 1500`, **그 워크플로일 때만** 올리고 `finally` 로 되돌린다(인스턴스 재사용 누수 방지).

### 🔑 해상도는 0.98MP 로 고정 — 2.0MP 는 VRAM 한계로 비효율
| 해상도 | 시간 | 직전 대비 지수 |
|---|---|---|
| 0.2MP (608x352) | 89초 | — |
| **0.98MP (1312x736)** | **563초** | 1.22 |
| 2.0MP (1888x1056) | 2063초 | **1.79** ← VRAM 여유 270MB, 스왑 진입 |
- 🔑 **지수가 1.22 → 1.79 로 악화되는 지점이 VRAM 한계선이다.** 픽셀당 비용이 계산이 아니라 스왑으로 정해진다.
  ⇒ **VRAM 여유가 1GB 아래로 떨어지는 해상도는 쓰지 않는다**(다른 모델에도 적용되는 규칙).
- 🔑 **해상도 주입이 자동 무시되게 설계했다** — `DenoResolutionSetup.mode='Preset Ratio'` 라 엔진이 1920x1088 을
  넣어도 `megapixels=0.98` 이 이긴다. 모르고 34분짜리를 돌리는 사고를 **구조로** 막았다(테스트가 단언).

### 실측 A/B 9회 (RTX 3060 · 5초 · 같은 대본·같은 이미지)
| | LoRA | 스텝 | 해상도 | 시간 | 원본 유지 |
|---|---|---|---|---|---|
| A | Acc-8Step | 8 | 0.2MP | 121초 | ✗ |
| B3 | + sageattention | 8 | 0.2MP | 122초 | ✗ |
| C | turbo→AccLoader | — | — | **실패** | — |
| D | turbo→표준LoRA | 4 | 0.2MP | 81초 | △ |
| **E** | **turbo + `<Picture 1>`** | **4** | 0.2MP | **89초** | **✅** |
| F | E + `max` | 4 | 0.2MP | 105초 | ✗ |
| **G** | E | 4 | **0.98MP** | **563초** | ✅ ← 채택 |
| H | E | 4 | 2.0MP | 2063초 | ✅ |
| I | 정품 Acc 4step | 4 | 0.2MP | 110초 | ✅ |

### ⚠ sageattention 은 효과가 없다 (설치했으나 무의미)
`sageattention 1.0.6` + `triton-windows 3.7.1`(cp313 wheel, 컴파일 불필요)을 ComfyUI venv 에 설치했지만
**121초 → 122초**. 서버 로그: `Found comfy_kitchen backend triton: {'available': True, **'disabled': True**}` ·
`Using pytorch attention`. 게다가 **원본 워크플로의 sage 노드가 `BasicScheduler`(시그마 계산)로만 가고
실제 디노이징을 하는 `BasicGuider` 는 우회**한다(Deno 배선 실수). → 번들 워크플로에서 **뺐다**.

### 검증
- **`test/comfy-video-minimax.test.js` 38/38 신설**(`npm run test:comfy` 에 포함) — 워크플로 최적값 대조 ·
  **`_buildGraph` 원문 실행**(image_paths 주입 · 접두사 · 길이 · 해상도 무시 · seed) · **🔴 LTX2.5 회귀**
  (LoadImage 유지 · 접두사 안 붙음) · 타임아웃 하한 · 소스 위생.
  🔑 **A/B 역검증 2건**: 접두사를 빼면 **2건 실패**, `image_paths` 주입을 빼면 **1건 실패**.
- **실제 앱 엔진 E2E** — `imageToVideo()` 원문으로 실제 대본 이미지를 넣어 mp4 생성까지 확인.
- 회귀: comfy-workflows 31 · comfy-models 41 · comfy-perf 54 · pipeline-mode 30 · gpu-lanes 14 · gpu-foreign 43.

### ⚠ 로이가 알아 둘 것
- **편당 영상 5개면 약 47분**이다(클라우드 LTX2.5 는 **2.5분** — 앱 로그 실측 개당 1분 17초·동시 3개).
  크레딧을 아끼는 대신 시간을 쓰는 선택이라 **여유 있을 때** 쓰는 게 맞다.
- 산출물은 **1312x736**(.vrew 규격 1920x1088 보다 작다). 필요하면 업스케일 1.46배 — Deno 방식(RealESRGAN).
- **오디오가 함께 생성된다**(aac 32kHz). 우리는 TTS 를 쓰므로 .vrew 에서는 영상 오디오가 안 쓰인다(무해).
- ⚠ **이 워크플로를 클라우드(☁)로 고르면 실패한다** — Deno 커스텀 노드를 쓰는데 comfy.org 엔 그 노드가 없다.
  이름의 **(로컬)** 이 그 경고다. 🔴 **다만 「모델이 없어서」는 이제 틀린 설명이다** — 2026-08-30 실측으로
  minimax_h3 모델이 클라우드에 올라온 것을 확인했고, 네이티브 노드용 **☁ 클라우드 판을 따로 추가**했다
  (맨 위 v0.3.85 항목). 클라우드에서 MiniMax 를 쓰려면 그쪽을 고른다.
- ⏳ **더 빨라질 여지**: Deno 가 영상에서 "터보/디스틸 LoRA 로 최대 5배" 를 예고했다. 새 LoRA 가 나오면
  `comfy/video_minimax_h3_turbo4_i2v.json` 의 `lora_name` 과 `steps` 만 바꾸면 된다.

## 🖼 리모션 — 그림 TSV 를 제 폴더에서 열고, **대본 없이 그림만** 만든다 (2026-08-28, v0.3.69)
> 로이: "설정창에서 그림목록폴더를 설정했는데, 그림목록버튼을 눌렀을때 **다른 폴더가 나오네**." +
>   "TSV열기 버튼은 「대본 TSV열기」, 그림목록은 「그림 TSV열기」로 라벨을 변경" +
>   "**그림만 만들어야 하는 상황**도 있어. 그러니 그림만 만들수 있는 환경도 구축해줘."

### 🔴 ① 그림 TSV 대화상자가 **음성 TSV 폴더**를 열고 있었다
`remotion-open-image-tsv` 의 `defaultPath` 가 `preset.scriptFolder`(=「TSV 폴더」)였다. 채널에 「그림목록
폴더」(`imgTsvFolder`)를 넣어도 그 값을 **아무도 안 읽었다** — 자동 연결(v0.3.66)만 쓰고 대화상자는 안 썼다.
→ `preset.imgTsvFolder || preset.scriptFolder`. ⚠ **설정을 추가했으면 그 값을 읽는 곳을 전부 세어 볼 것** —
읽는 곳이 하나면 나머지 화면은 조용히 옛 폴더를 연다(사용자에겐 「설정이 안 먹는다」로 보인다).
· 실측으로 자동 연결 자체는 정상이었다: `001·002·003_….tsv` ↔ `001·002·003_그림목록.tsv` 세 쌍 존재.

### ② 라벨 — 「📄 대본 TSV 열기」 · 「🖼 그림 TSV 열기」
버튼이 「TSV 열기」·「그림목록」이라 무엇이 대본이고 무엇이 그림인지 이름만으로 안 갈렸다. 대화상자 제목도 같이.

### ③ 🖼 **그림만 만드는 강** — 대본 없이 그림 TSV 만 열면 된다
그전엔 그림 TSV 를 열어도 **활성 강이 없으면 화면에 아무것도 안 떴다**(`_remotionDto` 의 `active` 가 null →
표도 버튼도 죽는다). 이제 그림 TSV 열기가 **큐에 강을 만든다**:
- **여러 개** 고를 수 있다(음성 TSV 와 같은 자연정렬).
- 🔑 **어디에 붙일지는 앞 숫자가 정한다** — 자동 연결과 **같은 열쇠**를 쓴다:
  ① 같은 번호의 강이 열려 있으면 거기에 붙인다 ② **번호가 없는 파일**만 활성 강에 붙인다(수동 보정)
  ③ 번호가 있는데 맞는 강이 없으면 **그림만 있는 강**(`ttsRows: []`)을 새로 만든다.
  ⛔ ②를 「번호가 안 맞아도 한 개면 활성에」로 넓히면 `777_그림목록` 을 열었을 때 **003 강에 조용히 붙어**
  엉뚱한 강의 그림을 만든다(첫 구현에서 이 함정을 밟았고 테스트가 지금 이걸 단언한다).
- 그림만 있는 강은 칩에 **`🎤—`**, 오른쪽 요약에 **`(그림만)`** 으로 표시되고 「🎤 mp3 만들기」는 비활성이다.
  **「▶ 전체 만들기」는 그대로 동작한다** — 음성 패스에서 걸러지고 그림 패스만 돈다(`withTts`/`withImg`).
- ⚠ **그림 TSV 열기는 열린 강들을 지우지 않는다**(더한다). 큐를 새로 만드는 건 「대본 TSV 열기」뿐이다.

### 검증
- `npm run test:remotion` = tsv-tts 39 + tsv-images 64 + **앱 E2E 126/126**(+11) —
  원문 대조 5(기본 폴더가 `imgTsvFolder` · 다중 선택 · 그림만 강 생성 · `!num` 조건 · 큐 미삭제) +
  **실제 앱 E2E 6**(대본 없이 `777_그림목록.tsv` 만 열어 「🖼 그림 만들기 (3)」 활성 · `(그림만)` 표시 ·
  **003 강에 안 붙었는지** · 칩 생성 · mp3 만들기 비활성).
  ⚠ 이 블록은 **맨 뒤**에 뒀다 — 큐를 늘리므로 앞 테스트의 상태를 흔든다(v0.3.66 과 같은 이유).
- 회귀: **makeall 무음 E2E 18/18**(main.js 를 만졌으므로 필수).

## 🖥 ComfyUI 를 「수시로」 쓰는 법 — Desktop 앱 대신 **웹 UI** (2026-08-28, v0.3.68)
> 로이: "컴피유아이를 부팅시 자동실행되어서 컴피유아이를 켜지 말라고 했었는데, 난 컴피유아이를 **수시로 켜야하는
>   상황**이야. 이 문제를 어떻게 해결해야 할까?" → 용도는 **커스텀 노드·모델 설치 / 버전 업데이트·설치 관리 /
>   생성 진행·큐 확인**. 로이 선택: **웹 UI 로 전환.**

### 🔑 실측이 옛 경고를 정정했다 — 이번엔 「서버 두 벌」이 아니었다
| | 실측(2026-08-28 12:00) |
|---|---|
| 8188 을 쥔 프로세스 | **pid 19048 — 부모가 Comfy Desktop**(`--enable-manager` 플래그, 11:08 시작) |
| 우리 부팅 런처 | 10:40 에 정상 기동(0.33.4 · `0.0.0.0:8188`)했으나 **지금은 없다** |
| 8189 | **없음** — 서버는 한 벌 · VRAM 1,593MB · RAM 여유 21.9GB |
→ Desktop 을 열면 **8189 로 밀리는 게 아니라, 우리 서버가 사라지고 Desktop 것이 8188 을 가져간다.**
  서버가 한 벌이라 v0.3.35 의 「장당 498초」 사고 조건이 **아니고**, 우리 앱도 8188 로 보내니 그대로 동작한다.
  ⇒ v0.3.30·v0.3.49 의 **「Comfy Desktop 창을 열지 말 것」은 그때 관측 기준**이었다. 지금 버전은 이렇게 움직인다.

### 그래도 남는 손해 셋 (이게 진짜 문제였다)
1. 🔴 **아내 PC 원격 생성이 죽는다** — 우리 런처는 `--listen 0.0.0.0`, Desktop 서버는 **127.0.0.1 바인딩**.
   Desktop 이 8188 을 쥔 동안 v0.3.46 이 무력화된다(밖에서 접속 불가).
2. Desktop 창을 닫으면 **서버도 함께 꺼진다**(`settings.json` 의 `onAppClose: "quit"`) → 다음 생성 첫 장에 1분 추가.
3. 타이밍이 어긋나 우리 서버가 안 죽으면 그때는 **진짜 8189 두 벌** = v0.3.35 재발.

### ✅ 해결 — 웹 UI 는 **같은 서버의 같은 화면**이다
- **런처 argv 에 `--enable-manager` 추가**(`comfy/comfy-server.pyw`). 이게 없으면 웹 UI 에 **커스텀 노드 매니저가
  안 보여** 노드를 깔려면 Desktop 을 열 수밖에 없었다 — 위 손해 셋의 출발점이 이 한 줄이었다.
  · ComfyUI **내장** 플래그(`comfy/cli_args.py:161`). 패키지가 없으면 서버가 **스스로 끄고 정상 기동**한다
    (`main.py:126` `args.enable_manager = False`) → 넣어도 위험이 없다.
- **`comfy/comfy-open.pyw` 신설 + 바탕화면 바로가기 2개**:
  · **「ComfyUI (웹)」** — 서버가 없으면 띄우고(최대 180초 대기) **크롬 앱 모드**로 연다(주소창 없는 창이라
    Desktop 창과 체감이 같다). 이미 떠 있으면 그대로 쓴다.
  · **「ComfyUI 서버 재시작」**(`--restart`) — 8188 을 쥔 프로세스를 끄고 **우리 런처로** 새로 띄운다.
    쓸 때: **커스텀 노드를 설치한 뒤** · **Desktop 이 포트를 가져갔을 때**(원격을 되살린다).
- ⚠ **Desktop 앱은 「버전 업데이트·인스턴스 관리」할 때만** 연다. 그 작업이 끝나면 **「재시작」을 한 번** 눌러
  우리 서버(0.0.0.0 + manager)로 되돌린다.

### 검증
- 🔑 **운영 서버를 안 건드리고 확인했다** — `PRIMING_COMFY_PORT=8197` 로 런처를 임시 기동 →
  로그에 **`[START] ComfyUI-Manager`** · `To see the GUI go to: http://0.0.0.0:8197` 확인 후 종료.
  8188 은 내내 정상이었다(v0.3.48 에서 OmniVoice 를 9899 로 띄워 검증한 것과 같은 패턴).
- `comfy-open.pyw` 원문 실행: `alive()`=True · **`listener_pids()`=[19048]**(지금 8188 주인을 정확히 집는다) ·
  크롬 경로 탐지. 바로가기를 **실제로 클릭**해 로그 `서버가 이미 떠 있습니다 — 그대로 씁니다` → 앱 모드 창까지 확인.
- `npm run test:comfylaunch` **52/52**(+6 — `--enable-manager` · `--listen 0.0.0.0` · comfy-open 존재 ·
  `--restart` · 같은 런처를 쓰는지 · print 금지).

### ⚠ 알아 둘 것
- **pythonw 는 콘솔이 없어 `print` 가 무효다**(v0.2.95 함정) → comfy-open 은 파일 로그
  (`~/.shots-maker/logs/comfy-open.log`)에 쓰고, 실패는 **메시지박스**로 알린다.
- 바로가기 대상은 `D:\miniconda3\pythonw.exe` — comfy-open 은 표준 라이브러리만 쓰므로 **ComfyUI 인스턴스
  경로가 바뀌어도 안 깨진다**(런처가 스스로 `.venv` 로 재실행한다).
- 이 릴리스 시점의 이 PC 는 **Desktop 서버가 8188 을 쥔 상태**였다(VRAM 9.7GB 점유 · 큐 비었음).
  원격을 되살리려면 「ComfyUI 서버 재시작」을 한 번 누르면 된다.

## 🔁 한도가 풀리면 **같은 대본 도중에도** Genspark 로 되돌아간다 + 「무료」 표기 정정 (2026-08-27, v0.3.67)
> 로이: "젠스파크로 이미지 생성을 더 이상 못하면 … Flow로 변경되어서 이미지를 만들던데, **제한시간이 지난
>   다음에는 다시 젠스파크로 돌아가** 이미지 생성을 할수 있도록 해줘. 지금은 **대본 1편을 기준**으로 진행되는
>   것 같은데, **같은 대본에서라도** 진행되면 좋겠어." + "젠스파크는 무료가 아니라 **구독요금제**야."

### 🔑 원인 — 순환이 **단일 패스**였다
`runRotatingImages` 가 `for (const engineId of order)` 한 바퀴로 끝났다 → Genspark 한도 → Flow 로 넘어가면
그 호출 안에서는 **두 번 다시 Genspark 를 보지 않는다.** Flow 가 30장을 만드는 20분 사이에 Genspark 쿨다운
(보통 5시간, 침묵 정체는 30분)이 풀려도 남은 이미지는 전부 Flow 가 만들었고, 복귀는 **다음 대본(다음 호출)**
에서나 일어났다 — 이게 로이가 본 「대본 1편 기준」이다.

### ✅ 라운드 반복 — 한 바퀴 뒤 「그새 풀린 엔진」이 있으면 되돌아간다
- `imgEngineReady(engineId)` 신설 — **「지금 되나 · 아니면 언제 풀리나」**를 가른다:
  `{ok:true}` / `{ok:false, until:ts}`(쿨다운 = 재시도 후보) / `{ok:false, until:0}`(일일 캡 소진 = 기다려도 안 풀림).
  Genspark·Flow 계정 스토어를 각각 읽고, **조회 실패는 fail-open**(판정 때문에 생성이 막히면 본말전도).
- 🔑 **후보(`cooledOut`)는 각 엔진을 시도한 「직후」 기록한다.** 라운드가 끝난 뒤 한꺼번에 재면, 그 사이 이미
  풀린 엔진은 `ok:true` 라 후보에서 빠져 **영영 복귀하지 못한다**(첫 구현에서 실제로 이 함정을 밟았다).
- ⛔ **기다리지 않는다.** 쿨다운이 5시간일 수 있고, 그동안 TTS·GPU 레인을 쥔 채 앱이 멎는 쪽이 더 나쁘다.
  아직 안 풀렸으면 미생성으로 두고 **`— T+N 이후에 「🖼 이미지」를 다시 누르면 genspark 로 이어서 만듭니다`** 를 남긴다.
- 멈추는 조건 셋: 남은 게 없음 / 풀린 엔진이 없음 / **복귀했는데 진전 0**(헛돌기 방지). 상한 `MAX_ROUNDS=8`.
- 평상시(한도 없음)엔 **1라운드로 끝나 기존 동작과 완전히 같다** — 테스트가 Flow 미호출을 단언한다.

### 💰 「무료」 표기 정정 — Genspark·Flow **둘 다 구독제다**
로이는 Genspark 만 지적했지만 **Flow 도 무료가 아니다** — v0.3.36 실측: Google AI 구독(Pro/Ultra)이 없는 계정은
Flow 앱이 아니라 **소개 페이지만 열린다.** 그래서 둘 다 고쳤다(라벨이 과금 구조를 오해시키면 안 된다).
- 드롭다운(헤더·채널편집) `Flow (무료)`·`Genspark (무료)` → **`Flow (구독)`·`Genspark (구독)`**
- ⚙ 설정 탭 **`🆓 무료 이미지` → `🌐 브라우저 이미지`**(탭 id `free` 는 그대로 — 진입점 6곳을 안 건드린다)
- 탭 안내문에 「Genspark 구독 · Flow 는 Google AI Pro/Ultra 구독」 명시 + 복귀 동작 설명.
- 쿨다운 배지 title 도 「이 시각이 지나면 만들던 대본 도중이라도 자동으로 Genspark 로 되돌아갑니다」로.
- ⚠ 남겨 둔 「무료」 2곳은 **진짜 무료**다 — Ollama 프롬프트 작성(로컬), main.js 의 Ollama 주석.

### 검증
- **`test/img-rotate-resume.test.js` 36/36 신설**(`npm run test:visual` 에 포함) — main.js **원문에서
  `imgEngineReady`·`runRotatingImages` 를 뽑아 가짜 시계·가짜 계정 스토어로 실행**(실제 `~/.priming-maker/*.json`
  무변경): 판정 9 · **실사고 재현**(Flow 가 25분 도는 사이 Genspark 20분 쿨다운이 풀림 → 5장 전부 생성 ·
  Genspark 2회 호출 · 순서까지) · 대기 안 함(3초 내 종료) · 진전 0 종료 · 평상시 1라운드 · 중단 · 소스 위생 5.
- 🔑 **A/B 역검증**: `git show HEAD:main.js` 의 옛 함수로 같은 시나리오를 돌리면 **Genspark 1회 호출 · 3/5장**
  (복귀 없음 = 로이가 본 그 증상), 새 코드는 **2회 · 5/5장**.
- 회귀: **makeall 무음 E2E 18/18**(main.js 를 만졌으므로 필수) · visual 14/10/13/7/40 ·
  **2분할 패널 E2E 91/91**(+5 — 드롭다운에 「무료」 표기 0 · 두 항목 다 「구독」 · 탭 안내문 정정·복귀 안내).

### ⚠ 로이가 알아 둘 것
- 복귀는 **다른 엔진이 일하는 동안 시간이 흘러 한도가 풀렸을 때** 일어난다. 두 엔진이 동시에 한도면 그 자리에서
  끝나고(기다리지 않는다) 로그가 **언제 다시 누르면 되는지** 알려준다 — 그 시각 뒤 「🖼 이미지」를 누르면 빠진 것만 만든다.
- 복귀했다는 사실은 로그의 **`🔁 genspark 한도 재설정 시각이 지났습니다 — 남은 N장을…`** 한 줄로 확인된다.

## 🖼 리모션 그림 — 그림목록 TSV → 로컬 ComfyUI 일괄 생성 (2026-08-27, v0.3.65)
> 로이: "이미지 생성 기능도 만들어줘, 일단 로컬로 12장 뽑아보자. 프롬프트는 비즈니스PT세션에서 만들어
>   제공할꺼야. … 이미지 생성이니 로컬컴피유아이를 활용하자."

강의 76강(`D:\비즈니스PT`)이 글자뿐이라 지루하다는 판정이 나와 장면마다 선그림을 넣기로 했다. 그동안
로이가 ComfyUI 데스크탑에서 따로 만들어야 했는데, **음성과 같은 화면에서 버튼 한 번으로** 끝나게 했다.

### 쓰는 법
리모션 화면에 **「🖼 그림목록」 → 「🖼 그림 만들기 (N)」 → 「📁 그림 폴더」**. 음성 TSV 와 그림 TSV 를
**둘 다 열어 두고 탭으로 전환**한다(🎤 음성 N행 / 🖼 그림 N장).
채널 편집 → 📁 폴더 에 **「이미지 출력」** 칸이 생겼다 — 한 번 넣으면 끝이다.

### 형식 — 음성 TSV 와 다르다(**헤더 한 줄 + 5칸**)
`경로 · 장면번호 · 화면 한글(참고) · positive · negative`
- 🔑 **헤더는 라벨 문구가 아니라 「첫 칸이 이미지 확장자인가」로 판별한다.** 「경로」·「path」 같은 목록을
  못박으면 상대가 라벨을 바꾸는 순간 **첫 그림이 조용히 사라진다.**
- 🔑 **1번 칸이 곧 저장 경로다**(하위 폴더 포함). 음성처럼 「TSV 이름으로 폴더를 만드는」 방식이 아니다 →
  `<이미지 출력>/<1번 칸>`. `..`·절대경로는 거부하고, **중복 경로는 세운다**(조용히 덮으면 그 장면이
  영상에서 사라지고 렌더 단계에서야 드러난다 — 음성 TSV 와 같은 정책).

### 🔑 설계에서 중요한 것 넷
1. **프롬프트를 손대지 않는다.** 스타일까지 포함된 완성 프롬프트를 그대로 넘긴다. 앱이 화풍을 덧붙이면
   그림체가 **두 곳에서 관리**되어 76강을 가는 동안 반드시 어긋난다(로이 결정: 스타일은 대본 쪽이 관리).
   테스트가 그림 경로에 `stylePrompt`·`buildImagePrompt` 가 없는지 단언한다.
2. **시드 고정 `20260826`** · **1024×1024**. 같은 그림을 다시 뽑을 수 있게. ⚠ 뒤집어 말하면 **프롬프트를
   바꾸지 않으면 같은 그림이 나온다** — 「같은 지시문으로 다시 뽑아 다른 걸 고르기」는 안 된다.
3. **이어받기** — 이미 있으면 건너뛴다. 마음에 안 드는 그림만 지우고 다시 돌리면 그것만 만들어진다
   (실측: 12장 중 3장만 재생성 · 9장 건너뜀).
4. **같은 GPU 레인**(`enqueueImageJob(..., 'comfy')`) — 로컬이면 TTS·롱폼 이미지와 자동으로 순차 처리된다.
   ⚠ 엔진 인자를 안 넘기면 레인을 안 잡아 TTS 와 겹친다(테스트가 단언).

### 🔴 밟은 함정 둘
1. **자리표시 파일을 「이미 있음」으로 봤다.** 상대 세션이 폴더 구조를 잡으려고 **70바이트 png 12개**를
   미리 놓아 두었는데, 크기를 안 보는 게이트가 그걸 전부 완성본으로 보고 **한 장도 안 만든 채 "완료"** 라고
   보고했다. → **4KB 미만이면 없는 것으로 본다**(`MIN_IMG_BYTES`). 1024×1024 PNG 는 아무리 단순해도 그보다 크다.
2. 🔴 **부정 프롬프트는 이 워크플로에서 무효다.** Krea2 Turbo 계열은 KSampler 의 negative 가
   `ConditioningZeroOut` 이고 **cfg=1** 이라 **넣을 자리도 없고 있어도 안 듣는다**(v0.2.83 과 같은 사실).
   → `_findNegativeNode` 가 자리를 찾아보고, 없으면 **한 번만 로그로 알린다**(조용히 버리면 "negative 를
   적었는데 왜 글자가 나오지?" 를 영원히 못 푼다). ⚠ 글자·잡물은 **positive 에 긍정 서술로** 적어야 듣는다.
   · 실제로 negative 가 안 들어갔는데도 **12장 전부 글자가 없었다** — positive 의 스타일 서술이 일하고 있다.

### 엔진 확장(`core/comfy-image.js`)
`textToImage({ prompt, negative, seed, dims, ... })` — 셋 다 **옵션**이라 롱폼 경로는 지금까지와 똑같이 동작한다
(negative 없음 · 시드 매번 랜덤 · aspect 로 해상도). `_buildWorkflow(positive, aspect, opts)` 에서 주입.

### 검증
- **`test/tsv-images.test.js` 57/57 신설**(`npm run test:remotion` 에 포함) — 원문 모듈 실행:
  헤더 판별 5(라벨이 영어여도 · 헤더가 없어도) · 경로 안전 14(`..`·절대경로·확장자·역슬래시 정규화) ·
  중복 2 · 프롬프트 무가공 4 · 일괄 생성 18(이어받기 · **자리표시 재생성** · force · 실패 격리 ·
  연속 실패 상한 · negative 경고 1회 · 중단) · **실제 강의 파일 회귀 5** · 소스 위생 2.
- **앱 E2E 104/104**(+18) — dialog 스텁으로 임시 그림목록을 열어 표·탭 전환·버튼 활성/비활성 확인.
  ⚠ 실제 그림 생성은 E2E 에서 하지 않는다(GPU · 장당 18초).
- **실측**: 3강 12장 전부 생성 · 실패 0 · **장당 20.0초** · 재생성 3장 17.7초. 그림은 상대 세션이 판정했고
  9장 확정 · 3장은 지시문을 고쳐 다시 뽑았다.
- 회귀: makeall 18/18 · comfy 전량(31·41·**54**·30·14·43·2분할 86) · visual 14/10/13/7/40 · tsv 39 · remotion E2E.
  ⚠ `comfy-perf` 의 「진단을 부르는 곳」 단언이 2 → **3** 이 됐다(그림도 로컬 GPU 라 같은 진단을 탄다).

### 🎬 강 여러 개를 한 번에 + 그림목록 자동 연결 (v0.3.66)
> 로이: "tsv 불러와 작업하는것이 1개만 할수 있네, 여러개도 할수 있도록 수정해줘." +
>   "대본TSV파일을 불러오면 그와 관련된 이미지 파일도 같이 불러와서 작업이 순차적으로 한번에 같이
>   진행되도록 되어 있는거지?" + "이미지 생성프롬프트가 있는 폴더를 설정할수 있게"

**질문의 답은 「아니오」였다** — v0.3.65 까지는 음성 TSV 와 그림목록을 **각각 손으로** 열어야 했고,
한 번에 한 강씩만 됐다. 이번에 셋 다 고쳤다.

### 쓰는 법
- **「📄 TSV 열기」에서 음성 TSV 를 여러 개 고른다** → 강마다 칩이 생기고, 칩을 누르면 표가 바뀐다.
- **「▶ 전체 만들기 (N강)」** — 열어 둔 전부를 순서대로. **전 강 음성 → 전 강 그림** 2패스다.
  🔑 강마다 음성·그림을 번갈면 **GPU 에서 모델이 그만큼 스왑**된다(OmniVoice ↔ ComfyUI) —
  롱폼 큐가 「전 항목 이미지 → 전 항목 비디오」로 가는 것과 같은 이유(v0.1.68).
- 개별 버튼(🎤·🖼·🎧)은 **지금 고른 강**만 대상으로 한다.

### 🔗 그림목록 자동 연결 — 앞 숫자가 열쇠
채널 편집 → 📁 폴더에 **「그림목록 폴더」**(`imgTsvFolder`)를 넣으면, 음성 TSV 를 열 때 그 폴더에서
**같은 번호**의 파일을 찾아 붙인다: `003_돈이_만들어지는_구조.tsv` ↔ `003_그림목록.tsv`.
- 짝이 없으면 그 강은 **음성만** 만든다(칩에 `🖼—` 로 표시). 막지 않는다.
- 「🖼 그림목록」 버튼은 남아 있다 — **활성 강에 수동으로 붙이는** 보정용(짝을 못 찾았거나 다른 목록을 쓸 때).
- 파일 선택 순서는 OS 마다 제각각이라 **이름 자연정렬**로 큐 순서를 정한다(대본 열기와 같은 정책).

### 🔑 설계에서 중요한 것 셋
1. **`S.tsv`·`S.imgTsv` 는 「활성 강」을 가리키는 창구로 남겼다.** 미리듣기·만들기·폴더열기가 전부 이 둘만
   보므로, 큐를 얹으면서도 그 코드를 한 줄도 안 고쳤다. 큐를 건드릴 땐 **반드시 `_remotionActivate`** 로 맞춘다.
2. **`remotion-run-all` 은 스스로 레인을 잡지 않는다.** 각 단계가 `enqueueTtsJob`/`enqueueImageJob` 을 탄다 —
   여기서 잡으면 **자기 자신을 기다려 교착**된다(`enqueueImageJob` 과 같은 규칙). 테스트가 단언한다.
3. **화면이 진행을 따라간다** — run-all 이 강을 바꿀 때마다 `remotion-active` 로 DTO 를 보내 표가 함께 움직인다.
   끝나면 원래 보던 강으로 되돌린다.

### 검증
- `test/tsv-images.test.js` **64/64**(+7 — 짝 찾기: 같은 번호 · 없는 번호 · 번호 없는 파일 · 빈 폴더 ·
  없는 폴더에도 안 던짐. **main.js 원문에서 `_lecNum`·`_pairImageTsv` 를 뽑아 실행**).
- 앱 E2E **122/122**(+18) — 임시 강 3개(짝 2개 + 짝 없는 1개)를 **한 번에 열어** 자연정렬·자동 연결·
  강 전환·짝 없는 강의 버튼 비활성까지 확인. ⚠ 실제 생성은 안 한다(GPU).
  🔴 **테스트 순서에 발이 걸렸다** — 다중 강 블록을 앞에 두니 뒤의 그림목록 테스트가 쓰던 큐를 덮어
  「음성 탭」이 실패했다. 블록을 뒤로 옮겨 해결. **상태를 바꾸는 E2E 는 그 상태를 쓰는 테스트 뒤에 둔다.**
- **실제 강의 파일 확인**: `001·002·003_*.tsv` → 각각 `001·002·003_그림목록.tsv` 정확히 연결(음성 820행 · 그림 39장).
- 회귀: makeall 18/18 · comfy 전량 · visual 40/40.
  ⚠ `remotion-run-images` 를 함수로 뽑으면서 E2E 의 `}, 'comfy'));` 원문 단언이 깨져 갱신했다.

### ⚠ 알아 둘 것
- 76강 전체(약 900장)면 장당 18초 기준 **4시간 반**이다. 나눠 돌리는 편이 좋다.
- 그림 뿌리는 **채널의 「이미지 출력」** 이다(음성의 「MP3 출력」과 다른 폴더). 안 정하면 만들기가 막힌다.
- 만들기가 끝나도 **탐색기를 자동으로 열지 않는다**(v0.2.99 정책) — 「📁 그림 폴더」로 연다.

## 🎧 리모션 — 몇 문장만 먼저 만들어 들어본다 (2026-08-27, v0.3.62)
> 로이: "이곳에서 특정 문장 몇개만 먼저 빠르게 변환해서 들어볼수 있는 기능이 있으면 좋겠어."

355문장을 15분 돌린 **뒤에야** 목소리·배속·사전이 틀린 걸 아는 구조였다. 그런데 그 넷은 **캐시 키**라
하나만 바꿔도 전량 재합성이다 — 확인이 늦을수록 손해가 크다.

### 쓰는 법
표에 **🎧 고르기 열**이 생겼다. 체크(**Shift 클릭 = 범위**) → 「🎧 미리듣기 (N)」 → 만들어지는 대로
**자동 재생**. 행마다 ▶ 로 다시 듣고, 「▶ 이어 듣기」로 고른 것들을 순서대로 듣는다.
· 한 번에 **12개까지**(`REMOTION_PREVIEW_MAX`) — 실수로 355개를 고르는 것을 막는다.
· 순서는 **고른 순서가 아니라 TSV 순서** — 이어 듣기가 대본 흐름대로 들린다.

### 🔑 설계에서 중요한 것 셋
1. **임시 폴더에 만든다**(`%TEMP%/priming-tsv-preview/<TSV이름>/`). MP3 출력 폴더에 부분 결과를 넣으면
   그 폴더의 `_manifest.json` 에 섞여 **나중 전체 만들기가 그걸 「건너뜀」으로 잡는다** — 무엇이 최신인지
   알 수 없게 된다.
2. **그래도 헛수고가 아니다** — 전용 캐시(`~/.shots-maker/tsv-tts-cache`)는 전체 만들기와 **같은 것**을 쓴다.
   미리듣기로 만든 문장은 전체 만들기에서 **0초에 재활용**된다. ⇒ **들은 그 소리가 결과물에 그대로 들어간다**
   (다시 합성되지 않으므로 달라질 수가 없다). 그래서 `force` 를 **켜지 않는다** — 켜면 캐시를 무시해
   「빠르게」도 아니고 재활용도 안 된다(테스트가 preview 본문에 `force:` 가 없는지 단언).
3. **`_remotionVoiceCfg` 로 채널 설정을 읽는 곳을 하나로 모았다.** 목소리·배속·사전을 두 곳에서 각자 읽으면
   반드시 어긋나고, 그러면 **미리듣기로 들은 소리와 결과물이 달라져** 이 기능의 존재 이유가 사라진다.
   (전체 만들기 쪽 중복 코드도 이 헬퍼로 교체했다 — 테스트가 호출이 정확히 2곳인지 센다.)
- ⚠ **같은 TTS 큐**(`enqueueTtsJob`)를 탄다 — 전체 만들기와 같은 GPU 라 동시에 돌면 v0.2.57 계열 사고가 난다.
  전체 만들기 중에 미리듣기를 누르면 그게 끝난 뒤 실행된다.
- TSV 를 다시 열면 고른 것·미리듣기 결과를 **비운다**(옛 TSV 의 음성을 듣는 일이 없게).
- 재생은 **`read-audio`(base64)** — 렌더러에서 `media://` fetch 가 막히는 문제를 이 앱의 다른 미리듣기들이
  이미 이 방식으로 우회하고 있다(App.jsx 와 같은 경로).

### 검증
`npm run test:remotion` = tsv 39/39 + **remotion-ui E2E 59/59**(+21). E2E 는 **dialog 를 스텁해 임시 TSV 15행**을
열고(사용자 파일을 건드리지 않는다) 고르기 열 · 버튼 개수 표시 · **Shift 범위 선택** · **상한 13개 → 합성하지 않고
안내**(실제 IPC 왕복으로 확인) · 선택 해제 · 미리듣기 전 ▶ 없음 을 본다. 원문 대조는 임시 폴더 사용 ·
force 미사용 · 같은 큐 · 헬퍼 2곳 호출 · preload 배선.
회귀: **makeall 무음 E2E 18/18**(main.js 를 만졌으므로 필수) · tsv 39/39 · img-regen 40/40.

### 🔴 발행 직후 잡은 것 둘 (v0.3.63)
> 로이: "체크 박스를 클릭해도 체크 표시가 표시 안되었다가 다른곳을 클릭하면 그때서야 이전에 체크한
>   체크박스가 체크되네." + "작업마치고 파일 저장된 폴더를 열던데 열지말고, 윗부분에 저장 폴더로 가는
>   버튼을 하나 만들어줘."

**① 체크가 한 박자 늦게 나타났다 — 원인은 `preventDefault`.**
`onClick` 에서 `e.preventDefault()` 로 제어 checkbox 의 **기본 토글을 막고** state 로만 그리려 했는데,
그러면 React 가 다음 렌더에서 **DOM 에 새 checked 를 쓰지 않고 넘어간다**(직전 커밋값과 같다고 보고 스킵).
그래서 state 는 맞는데(버튼의 개수는 바로 늘었다) **화면의 체크 표시만 다음 상호작용까지 밀린다.**
· ✅ 해법 = **click 에서 Shift 여부만 `shiftRef` 에 받아 두고, 토글은 `onChange` 에서** 한다.
  checkbox 는 `click → checked 토글 → change` 순서라 이 방법이 안전하고, 기본 토글을 막지 않으므로
  DOM 과 state 가 어긋나지 않는다.
· 🔑 **A/B 역검증으로 실사고를 재현했다** — 옛 코드로 되돌리면 E2E 의 「클릭 즉시 체크 표시가 보인다」가
  **실제로 실패**한다(범위 선택된 중간 행은 통과 = 클릭한 그 행만 어긋난다. 로이 증상과 정확히 일치).
· ⛔ **제어 checkbox·radio 의 click 에서 preventDefault 를 하지 말 것.** 테스트가 이걸 금지한다.

**② 만들기가 끝나면 탐색기를 자동으로 열던 것 → 「📁 출력 폴더」 버튼.**
v0.2.99 에서 롱폼은 이미 폐지한 정책인데(큐를 돌리면 창이 쌓인다) 리모션은 나중에 만들어져 남아 있었다.
`remotion-open-out` IPC 신설 — **TSV 를 열었으면 그 TSV 폴더로, 아직 안 만들어졌으면 MP3 출력 뿌리로**
연다(없는 폴더를 열면 오류만 난다). 검증: E2E 가 버튼 존재를, 원문 대조가 run-tts 본문에 `shell.openPath`
가 없는지 단언한다.

검증: `npm run test:remotion` **69/69**(+10) · makeall 무음 E2E 18/18.

### 🔴 두 번 눌러야 들리던 것 — 만든 직후 재생이 **옛 state** 를 읽었다 (v0.3.64)
> 로이: "미리듣기 해봤는데 소리 잘 나오네. 그런데 미리듣기 버튼을 2번 눌러야 음성을 들을수 있네."

`setPreviews(...)` 바로 다음 줄에서 `playOne(name)` 이 **`previews[name]`** 을 찾았다. 그런데 그 `previews` 는
**이번 렌더가 캡처한 옛 값**(setState 는 비동기)이라 첫 클릭에서는 `undefined` → 조용히 return.
두 번째 클릭 때는 첫 호출의 setState 가 반영돼 있어 들린다. 정확히 「2번 눌러야 들린다」.
· ✅ **재생 대상을 파일 객체로 다룬다** — 만든 직후엔 응답의 `r.files` 를 **그대로** 재생(`playFiles`).
  이름으로 state 를 되찾는 경로(`playOne`)는 없앴다.
· 🔑 **같은 함정이 하나 더 있었다** — `ensureAudio` 의 `onended` 클로저는 **한 번만** 만들어지므로 state 를
  읽으면 **첫 렌더 값에 영원히 묶인다**(이어 듣기가 둘째 곡부터 멎는다). 그래서 대기열도 파일 객체를 담는다.
· 🔑 **A/B 역검증 + 실제 합성으로 확인**: 옛 방식으로 되돌리면 E2E 의 「한 번 눌러 재생이 시작된다」와
  「⏹ 로 바뀐다」가 **실제로 실패**한다(결과 줄은 뜨고 파일도 만들어진다 = 재생만 안 되는 그 증상 그대로).
· ⛔ **`setState` 직후 그 state 를 읽어 무언가 하지 말 것.** 방금 받은 값을 직접 넘긴다.

**실제 합성·재생 E2E 를 넣었다**(위 v0.3.62 의 ⏳ 미검증 해소): 임시 TSV 1행을 **한 번만** 눌러 만들고
「재생 중 —」 표시·⏹ 전환·임시 폴더 생성·정지·▶ 잔존까지 본다.
🔑 **서버가 바쁘면(`/busy`) 그 부분만 건너뛴다** — 로이 작업 중에 GPU 를 뺏으면 그쪽이 1.7배 느려진다(v0.3.60 실측).
⚠ 주소는 `~/.flow-app/tts-config.json` 의 **`omnivoice.baseUrl`**, 키는 **`tts/secret-store`** 에서 읽는다
(provider 와 같은 출처). 처음에 `c.providers.omnivoice.apiKey` 로 찾다가 401 → 늘 건너뛰는 헛테스트가 됐었다.

검증: `npm run test:remotion` **72/72**(+3).

### ✅ 실제 합성·재생 — 확인됨(v0.3.64 E2E 로 자동 검증)
(아래는 그때의 기록) **실제 합성·재생 왕복**. 검증 시점에 OmniVoice 서버가 `busy:true`(로이 작업 중)라 **GPU 를 뺏지 않으려고
합성을 돌리지 않았다**. 앱에서 문장 2~3개를 골라 🎧 를 눌러 보면 된다(문장당 2.4~2.7초).
⚠ 만약 소리가 안 나면 로그창의 `🎧 리모션 미리듣기` 줄과 그 아래를 보면 이유가 나온다.

## 🔴🔄 영상이 있는 그룹은 「🔄 이미지 재생성」이 **원리적으로 안 되고 있었다** (2026-08-26, v0.3.61)
> 로이: "왜 이미지 생성이 안되는거지? 직전 이미지 생성은 잘 되었는데."
> ⚠ 로그가 원인을 **한도로 오도**한다(`엔진 한도·오류·결제 확인`) — 실제로는 한도와 무관하다.

### 🔑 로그 두 줄이 갈랐다 — 걸린 시간이 30초 vs **1초**
| 시각 | 대본 | media-1 | 결과 |
|---|---|---|---|
| 23:36:00 | [다산_0829] | `01.jpg` | ✓ 성공(브라우저 띄우고 **30초**) |
| 23:36:38 | [다산_0827] | `01.jpg` + **`01.mp4`** | ✗ 실패(**1초**) |
```
[23:36:38] 🔄 이미지 순환: genspark → flow
[23:36:38] ✅ 순환 이미지 생성 완료      ← 0장 만들고 "완료"
[23:36:39] ✗ G1 재생성 실패 — … (엔진 한도·오류·결제 확인)
```
실패 쪽엔 `[Genspark] 브라우저 시작`·`남은 N장 생성 시도` 가 **한 줄도 없다.** 대상이 0개라 그냥 빠져나온 것.
🔑 **다음에 "이미지가 안 만들어진다" 가 오면 걸린 시간부터 본다** — 1~2초면 엔진 문제가 아니라 **대상 0개**다.

### 원인 — 자산 판정이 영상까지 보는데, 재생성은 이미지만 지운다
`hasVisual(g)` 는 `imagePath || videoPath` 다. 그런데 재생성 경로는 **`g.imagePath = null` 만** 지운다
→ 영상이 있는 그룹은 「이미 자산 있음」 → **대상 0개 → 아무것도 안 만들고 성공 반환** → 호출부는
이미지가 없으니 「실패」. 즉 **영상이 만들어진 뒤에는 그 그룹 이미지를 영영 다시 못 만든다.**
· 같은 필터를 쓰는 곳이 **4개**였다(순환·ComfyUI·Flow·Gemini) — 한 곳만 고치면 순환이 다음 엔진에서 또 막힌다.
· 🔑 **Genspark(`core/pipeline.generateImagesGenspark`)만 원래부터 이미지 유무만 봤다** — 나머지를 그쪽에 맞췄다.
· 같은 계열로 조용히 막혀 있던 곳 둘: **영상 단건 재생성 전 이미지 선행 생성**(`이미지 생성 실패 — 영상 생략`)과
  **검정·노이즈 복구**(이미지만 이상하고 영상은 멀쩡한 그룹 = 그 코드가 막으려던 바로 그 막다른 길).

### ✅ 한 것
- **`imgDone(g, force)` 신설** — `force` 면 **이미지 유무만**, 아니면 기존 `hasVisual`. 필터 4곳을 여기로 통일.
  🔑 **평소 경로(만들기 2단계)는 그대로 둔다** — force 를 거기까지 켜면 영상 있는 그룹에 쓸데없이 이미지를 만든다.
  force=true 를 켜는 곳은 **명시적 재생성 4곳뿐**: 🔄 단건 · 영상 단건 재생성 전 선행 이미지 · 검정 복구 2곳.
- **`noTargetMsg` — 대상 0개면 조용히 넘어가지 않는다**(`⏭ 이미지 생성 대상 없음 (그룹 1) — 이미 이미지·영상이 있습니다`).
  🔑 **이 사고가 로그에 안 보였던 이유가 이것이다.** 앞으로 같은 일이 다른 원인으로 나도 한 줄로 드러난다.
- 🔴 **부수 피해도 막았다 — 🔄 가 실패하면 원래 이미지 참조가 끊긴 채로 남았다**(파일은 폴더에 그대로인데
  그룹은 이미지 없음 → .vrew 게이트에 막힌다). 실패 시 이전 경로를 되돌린다(`↩ G1 기존 이미지를 그대로 둡니다`).

### 검증
`npm run test:visual` 에 **`test/img-regen.test.js` 40/40 신설**(main.js **원문에서 `hasVisual`·`imgDone`·
`noTargetMsg` 를 뽑아 실제 파일로 실행** · **실사고 재현**(01.jpg+01.mp4 → 옛 판정 0개 / force 1개) ·
배선 대조(시그니처 4 · 필터 4 · force 전파 4 · 대상 0개 로그 4 · force 켜는 곳 4 · **평소 경로는 force 없음** 3) ·
실패 복원 3 · Genspark 회귀 2 · LF·NUL).
🔑 **A/B 역검증 2건**: ⓐ `git show HEAD:main.js` 로 되돌리면 함수가 없어 즉시 실패 ⓑ `imgDone` 을
`return hasVisual(g)` 로 무력화하면 **핵심 2건이 실패**(force 판정 · 실사고 재현).
회귀: **makeall 무음 E2E 18/18**(main.js 를 만졌으므로 필수) · visual 14/10/13/7 · comfy 전량(31·41·54·30·14·43·
2분할 86) · tts(게이트 18 · fs-retry 31 · 숫자 59).

### ⚠ 로이가 알아 둘 것
- 이제 **영상이 있는 그룹도 🔄 이미지 재생성이 된다**(예전엔 1초 만에 실패했다).
- 어제 실패한 [다산_0827] G1 은 **`01.jpg` 가 폴더에 그대로 있다** — 앱에서 그 대본을 다시 열면 이미지가 보인다.
  마음에 안 들면 이제 🔄 가 실제로 동작한다.
- ⚠ **영상은 그대로 남는다** — 이미지를 새로 만들어도 그 그룹의 기존 `01.mp4` 는 안 바뀐다(i2v 를 다시 돌리려면
  「🎬 비디오」 단건 재생성). .vrew 에서는 **영상이 있으면 영상이 쓰인다**.

## ⏱ 리모션 RTF 표시 + WAV 출력 — "왜 롱폼보다 느린가" 의 답 (2026-08-26, v0.3.60)
> 로이: "롱폼에서처럼 RTF 표시해줘. 그런데 롱폼 대비 속도가 너무 느린 것 같은데 이유가 있어?"
>   + "파일이 꼭 MP3 가 아니어도 되는거지? MP3 변환과정 때문에 RTF 가 1을 넘기는 것 같아서."

### 🔑 느린 게 아니었다 — 문장이 짧아서다 (실측)
단계 분해(실제 355문장 작업): `문장당 2.43초 = 합성 2.37 · 트림 0.00 · mp3 0.06 · 기타 0.00`.
「mp3 인코딩이 병목」이라는 내 첫 가설은 **틀렸다.** 병목은 합성 자체다.
길이가 다른 8문장을 재서 회귀를 뽑으면:
```
생성시간 ≈ 2.42초(고정비) + 0.08 × 오디오길이
```
- **문장 길이와 거의 무관하게 문장당 2.4~2.7초**다. 오디오 1초당 추가 비용은 0.08초뿐.
- 그래서 RTF 는 문장이 짧을수록 나빠진다(순전히 산수):
  | 문장 오디오 | RTF |
  |---|---|
  | 1.0초 | 2.63 |
  | 2.4초(강의 평균) | **1.05** |
  | 6.2초(롱폼 평균) | **0.47** ← 롱폼 실측 0.455 와 일치 |
- 절대 속도는 오히려 지금이 빠르다(문장당 2.37초 vs 롱폼 측정 당시 2.83초).
- ⇒ 🔑 **총 GPU 시간은 오디오 길이가 아니라 「문장 수」로 정해진다.** 대본을 잘게 쪼개면 그만큼 늘어난다.
- ⚠ **GPU 경합 실측**: 로이 작업 중에 내가 CLI 로 4문장을 합성했더니 **2.63 → 4.53초(1.7배)**.
  앱의 `enqueueTtsJob` 큐는 **앱 안에서만** 작동한다 — CLI 는 그 밖이다. 남이 돌리는 중엔 CLI 를 쓰지 말 것.

### ✅ RTF 표시 — 진행 중에도 보인다
`76 / 355 · ⏱ RTF 1.15 · 문장당 2.51초 · 남은 시간 약 12분` (진행률 줄) + 완료 후 결과 박스 + 로그.
단계별 평균(합성·트림·mp3)도 함께 찍어 **병목이 로그만 보고 드러나게** 했다.
⚠ RTF 는 **새로 만든 것만**으로 잰다 — 캐시 재활용분(0초 복사)이 섞이면 0 에 가깝게 왜곡된다.

### ✅ WAV 출력 — **TSV 의 확장자가 정한다**
로이 직감이 맞았다: mp3 를 빼면 RTF 1.05 → **0.98** 로 1 아래가 된다(다만 절약은 2.5% = 355문장에 21초).
🔑 **앱 설정으로 포맷을 정하지 않는다** — `001.mp3` 인데 내용이 wav 가 되거나, 이름을 `001.wav` 로 바꿔
  리모션이 파일을 못 찾게 된다. **이름을 정하는 곳을 TSV 한 군데로** 유지한다.
- `001.wav` 라고 적으면 wav, `001.mp3` 면 mp3. 한 TSV 안에 섞여도 된다.
- **WAV + 배속 1 이면 ffmpeg 를 아예 안 부른다** — 모델 출력이 이미 24kHz 모노 WAV 라 트림한 버퍼를
  그대로 쓴다. 배속이 걸리면 그때만 ffmpeg(`pcm_s16le`).
- ⚠ **포맷은 캐시 키에 들어간다** — 바꾸면 그 TSV 가 전량 재합성된다. 로이 결정: 001(mp3 355개)은
  그대로 두고 **이후 작업부터 wav**.
- ⚠ 파일 크기는 wav 가 약 2배(24kHz 모노 기준 25시간이면 4.3GB vs 2.1GB).

### 🔴 밟은 함정 둘
1. **`encodeMp3` 인자 이름을 바꾸면서 본문에 옛 이름(`mp3Path`)이 남아** mp3 출력이 통째로 실패했다
   (`mp3Path is not defined`). wav 만 테스트했으면 못 봤다 — **한 TSV 에 wav·mp3 를 섞어** 돌려서 잡았다.
   테스트가 `mp3Path` 잔존을 단언한다.
2. **단계별 평균을 성공 문장 수로 나누고 있었다** — 실패가 섞이면 `합성 4.04 > 문장당 2.75` 같은
   모순이 나온다. 시도 횟수(`stageN`)로 나누게 고쳤다.

### 검증
`npm run test:tsv` **39/39**(+4 — 포맷이 캐시 키에 들어감 · 확장자가 포맷을 정함 · wav 지름길 배선 ·
`mp3Path` 잔존 금지). 실측: 한 TSV 에 `.wav` 2 + `.mp3` 1 을 섞어 **3/3 성공**, wav 는 24000Hz/1ch/16bit.
회귀: remotion-ui.smoke 36/36 · makeall 18/18.
⚠ **덤으로 조용히 죽어 있던 회귀 2건을 살렸다** — 실제 입력 회귀가 `003.tsv` 파일명을 못박고 있었는데
그 파일이 개명돼 skip 되고 있었다(35→33). 폴더에서 아무 `.tsv` 나 찾도록 고쳤다.

## 🎬 리모션 모드 — 세 번째 모드(롱폼·출판 옆) (2026-08-26, v0.3.55)
> 로이: "채널을 하나 더 추가할게… 시작화면 항목에 리모션을 하나 추가해줘. 여기서 음성 선택하고 배속도
>   내가 설정할게. 리모션이라 자막·분할은 필요없겠지? 제작도구도 필요없겠네. 폴더 경로중 「롱폼 출력」이
>   아니라 MP3 출력폴더로 바뀌어야겠어. 이렇게 셋팅하고 TSV파일 불러와서 tts변환하면 되겠지?"
- **모드가 셋이 됐다**: 롱폼 · **🎬 리모션** · 📖 출판. 리모션은 **음성(mp3)만** 만든다 —
  자막·이미지·비디오·.vrew 가 전부 없다(영상은 리모션이 만든다).
- **`core/mode-profiles.js` 에 `remotion` 추가** — `parser:'tsv'` · `noCaption`·`noVisuals`·`noVrew`·`audioOnly` 플래그.
- **화면**: `renderer/src/RemotionView.jsx` 신설. 「📄 TSV 열기」 → 표로 확인 → 「🎤 mp3 만들기」(진행률·실패 목록).
  목소리·배속·시드는 **채널이 정한다**(헤더 ⚙). 출력은 `MP3 출력/<TSV 이름>/`.
- **채널 편집**: 시작 화면에 「🎬 리모션 (음성만)」. 고르면 **「자막·분할」·「제작 도구」 탭이 사라지고**
  폴더 탭이 「TSV 폴더 / **MP3 출력**」으로 바뀐다.
- **IPC**: `remotion-open-tsv`(파싱해서 표로) · `remotion-run-tts`(**`enqueueTtsJob` 큐를 탄다** — 롱폼 TTS 와
  같은 GPU 를 쓰므로 동시 실행되면 v0.2.57 계열 사고가 난다) · 진행률 `remotion-progress`.
- 🔴 **밟은 함정 — 정규화가 선택을 되돌린다(v0.3.50 과 같은 계열, 이번엔 세 곳).**
  `openChannelEditor` · `switchModeForChannel` · `switchMode` 가 전부 `=== 'book' ? 'book' : 'longform'` 이라,
  그대로 뒀으면 **리모션을 골라 저장해도 다음에 열면 롱폼**이고 **모드 버튼을 눌러도 안 바뀐다.**
  테스트가 세 곳을 원문으로 단언한다. ⚠ **모드를 추가할 때 이 세 곳을 먼저 찾을 것.**
- 🔑 **헤더 숨김은 플래그 하나로 묶었다** — `noProduction = isBk || isRx`. 예전엔 `!isBk` 가 세 곳에
  흩어져 있어서, 모드를 늘릴 때 **반드시 한 곳을 빠뜨린다**(실제로 E2E 가 3건 잡았다).
- 🔑 **배속의 정본은 `speedLong`** 이다(`speed` 는 옛 단일 필드 = 방치값 — v0.3.25 와 같은 함정).
  실제로 로이 채널이 `speedLong=1 / speed=1.25` 로 **서로 다르다.** `speed` 를 읽었으면 1.25 로 나갔을 것이다.
- 검증: **`test/remotion-ui.smoke.js` 25/25 신설**(실제 앱 부팅 — 모드 토글·화면 렌더·탭 사라짐·라벨 변경 ·
  헤더에 이미지/비디오/.vrew 없음 · **정규화 3곳 원문 대조** · 미정의 식별자 `baseNameOf` 없음 · 렌더러 오류 0).
  회귀: makeall 18/18 · tsv 35/35 · 2분할 패널 86/86 · timestamps UI.
- ⚠ **로이가 할 일**: 채널 「로이의유튜브강의」의 시작 화면을 **🎬 리모션**으로 바꾸고, 폴더 탭에서
  **TSV 폴더·MP3 출력**을 정하면 된다. 목소리(`srv:#01_득수_noBreath`)·배속(1)·시드(20260826)는 이미 맞다.
- 🔴 **발음사전 칸을 빠뜨렸다가 바로 잡았다 (v0.3.56).** RemotionView 에 `dictPath` **상태만 있고 입력 UI 가
  없어** 사전이 **절대 적용되지 않았다**(항상 빈 값). 화면상으로는 멀쩡히 돌아가므로 조용히 틀리는 종류다.
  → **채널에 저장한다**(`preset.dictPath`, 폴더 탭). 매번 손으로 고르게 하면 언젠가 한 번 빠지고,
    **사전 없이 합성된 것은 캐시 키가 달라 나중에 물릴 때 그 TSV 전체가 재합성**된다.
  · 화면에 「📖 발음사전 <파일명>」을 항상 띄우고, 없으면 **주황 띠 + 만들기 전에 한 번 확인**한다.
  · ⚠ `openChannelEditor` 에 `dictPath: p.dictPath` 를 **안 실으면 저장할 때 빈 값으로 덮인다**
    (이 저장소의 단골 사고 — v0.3.8 「채널 참조음성이 통째로 지워짐」과 같은 계열). 테스트가 단언한다.
  · 강의용 사전: `D:\비즈니스PT\_강의안기획\08_발음사전.md`
- ⏳ **아직 안 한 것**: 롱폼 대본을 리모션용으로 내보내는 `export-remotion`(음성 1개 + 타임라인 JSON)은
  **IPC·모듈(`core/remotion-export.js`)만 만들고 UI 버튼을 안 달았다.** TSV 경로가 먼저라 뒤로 미뤘다.

## 🎙 TSV 일괄 TTS + 앞뒤 무음 트림 — 리모션(Remotion) 파이프라인용 (2026-08-26, v0.3.53)
> 로이: "리모션으로 영상을 만들 계획… 대본을 tts로 읽고 1개 파일로 다운받는 단계가 있어야 해."
>   + 별도 세션(`pt-f6`, D:\비즈니스PT)이 강의 208강(16,000문장·25시간)용 TSV 변환을 요청.
>   로이 결정: **공통 하부 → 강의 TSV → Remotion 통합음성** 순서 · **무음 트림은 신규 기능에만**.

### 🔑 실측이 먼저였다 — OmniVoice 는 문장마다 앞 0.2초를 붙인다
로컬 TTS 캐시 300개(24000Hz/1ch/16bit · 길이 중앙 3.24초):
| 임계 | 앞 무음(중앙/평균/p90/최대) | 뒤 무음 | 문장당 합 |
|---|---|---|---|
| -34dB | 0.200 / 0.198 / **0.210** / 0.410 | 0.150 / 0.164 / 0.220 / 0.240 | 0.362초 |
| -40dB | 0.200 / 0.193 / **0.200** / 0.330 | 0.140 / 0.152 / 0.200 / 0.230 | 0.345초 |
- 앞 무음이 **거의 항상 정확히 0.200초**(p90=0.20) = 모델이 붙이는 **고정 패딩**이다.
- 🔑 **이건 강의만의 문제가 아니다** — 로이 롱폼도 그대로 갖고 있다: 빨간머리앤 통합본 1(4,182문장)에
  **약 24분**, 지성의서재 1편(575문장)에 3.3분이 무음이다. 리모션처럼 **오디오 길이로 화면 길이를
  정하는** 파이프라인에서는 그만큼 화면이 늘어진다.
- ⚠ **다만 양날이다** — 0.2초 간격이 오디오북에서는 자연스러운 호흡일 수 있다. 소리 판정은 로이 몫이라
  **기존 롱폼·.vrew·공용 캐시는 건드리지 않았다**(로이 결정).

### ✅ 만든 것
- **`core/audio-trim.js`** — `analyze` · `trimSilence`. `core/wav-slice.js` 위의 **무손실** 절단.
  🔑 **ffmpeg silenceremove 를 쓰지 않는다**: ① 재인코딩 0(PCM 프레임 경계로 자르고 헤더만 다시 쓴다)
  ② 임계 근처에서 안 흔들린다 ③ 자식 프로세스 0(16,000문장에서 기동 비용이 사라진다).
  ⚠ **어떤 경우에도 던지지 않는다** — 실패하면 원본 + `changed:false` + `reason`(조용히 성공한 척 안 함).
  ⚠ **padSec 기본 40ms** — 0 으로 두면 첫 음절 파열음(ㅍ·ㅌ·ㅋ)이 잘린다.
- **`core/tsv-tts.js`** — `parseTsv` · `parseDictMd` · `runTsvBatch` · 전용 캐시 · `encodeMp3`.
- **`tsv-tts.js`(루트 CLI)** — `node tsv-tts.js <입력.tsv> --out <폴더> [--speed 0.9] [--dict x.md] …`
- 출력 규격 **24000Hz · 모노 · mp3**. ⚠ 44100Hz 로 올리지 않는다 — 원본이 24kHz라 정보가 안 늘고 파일만 커진다
  (리모션은 길이만 재므로 표본율과 무관 — pt-f6 확인).

### 🔑 설계에서 중요한 것 셋
1. **공용 TTS 캐시를 쓰지 않는다(전용 `~/.shots-maker/tsv-tts-cache`).** 공용에 트림된 파일을 넣으면
   **롱폼이 그걸 재활용**해 기존 .vrew 와 타이밍이 어긋난다. 로이의 "신규 기능에만"을 **주석이 아니라
   구조로** 지킨다. ⛔ 여기서 `core/tts-cache` 를 require 하지 말 것(테스트가 단언).
2. **처리 순서 = 합성 → 트림 → 배속 → mp3.** 배속을 먼저 걸면 무음도 늘어나 임계 판정이 흔들린다.
3. **파일명을 절대 바꾸지 않는다.** 리모션은 파일명이 화면과 음성을 잇는 유일한 키다. 중복이면
   덮어쓰지 않고 **에러로 세운다**(조용히 덮으면 그 문장이 영상에서 사라지고 렌더 단계에선 안 보인다).

### 🔴 밟은 함정 넷 (전부 실측으로 잡았다)
1. **시드를 안 박으면 같은 문장도 매번 다른 음성이 나온다.** 배속 A/B 를 뽑았더니 **0.9(느리게)판이
   1.0판보다 짧았다**(3.26 vs 3.64초). 배속이 아니라 **원본 합성이 서로 달랐던 것**. → `DEFAULT_SEED`
   고정. 고친 뒤 0.9판이 **전부** 길고 비율 1.106 ≈ 1/0.9 로 정확했고, **무음 제거량이 소수점까지 동일**
   (0.29·0.25·0.43·0.22·0.33)해 같은 원본임이 확인됐다. `--force` 로 두 번 뽑아 **바이트 동일**도 확인.
2. 🔴 **발음사전 md 를 통째로 긁으면 대본이 파괴된다.** 문서에 숫자 빈도표(`| 만 | 967 | …`)·원칙표·
   시험 구성표가 섞여 있어 **167개**가 잡혔고 `만`→`967`(→"백만 명"이 "백967 명") 같은 항목이 나왔다.
   → **헤더가 두 칸이고 좌측이 「표기」로 끝나고 우측에 「읽기」가 있는 표만** 연다(38→**44개** 정상).
   ⛔ `| 금지 | 대신 |`(약어표)는 일부러 제외 — 좌측 `A/B 테스트` 가 슬래시로 쪼개진다.
   ⚠ 한 글자 치환(`만`·`수`)도 버린다 — 문장 아무 데나 걸린다.
3. 🔴 **Write 도구로 쓴 파일에 NUL 바이트(0x00)가 들어갔다.** `' '`(공백)이 `'\0'` 로 저장돼
   `src + '\0' + right` 가 됐고, grep 이 "Binary file" 이라 하고 **Edit 이 계속 미끄러졌다**(원인 불명).
   → 테스트가 **소스에 NUL 이 없는지 단언**한다. 새 파일을 만들면 `node -e` 로 한 번 검사할 것.
4. **ffmpeg 를 두 번 부르고 있었다**(인코딩 + 길이 측정) → 문장당 3.54초. stderr 의 `time=` 을 읽어
   **인코딩하면서 길이까지** 얻게 고쳐 **2.4초**로. 16,000문장이면 **약 1.8시간** 차이다.

### 🔴 OmniVoice 의 `speed` 파라미터는 **느린 방향으로 듣지 않는다** (2026-08-26 실측)
서버(`api.py`)에 `speed` 가 있어서 「모델이 실제로 천천히 발화하는 쪽이 운율이 살아 원리적으로 낫다」고
보고 `--speed-mode server` 를 넣었는데, **실측이 그 판단을 뒤집었다.**
| 서버 speed | 길이(같은 문장·트림 끔) |
|---|---|
| 0.5 | **3.02초** ← 늘어나기는커녕 짧아진다 |
| 1.0 | 3.86초 |
| 2.0 | **2.01초** ← 절반. 빠른 방향은 정상 작동 |
- ASR 전사로 붕괴인지 확인: **0.5 도 문장이 온전하다**(「받을 것이 준비되어 있지 않으면 그냥 지나갑니다.」).
  즉 소리가 깨진 게 아니라 **그냥 느려지지 않는다.** 반대로 2.0 은 끝이 잘렸다(「그냥」에서 끝).
- 실사용 확인: `SRV-0.9-*` 5개가 원속판과 거의 같았다(R-04-5 는 **1.27초로 소수점까지 동일**, R-06-5·R-10-2 는
  오히려 더 짧다). ⇒ **`--speed-mode server --speed 0.9` 는 사실상 원속이다. 쓰지 말 것.**
- 🔑 **느리게 하려면 atempo 뿐이다** — 이 앱의 롱폼이 v0.1.10 부터 atempo 를 써 온 선택이 옳았다는 근거다.
- ⚠ 재사용할 측정법: 극단값(0.5·2.0)으로 **길이가 예상대로 움직이는지** 먼저 보고, 이상하면 **ASR 전사**로
  「깨진 것인지 안 듣는 것인지」를 가른다. 0.9 같은 미세한 값만 보면 랜덤 변동과 구분되지 않는다.

### 실측 결과 (실제 강의 3강)
- **77문장 + A/B 25문장 + 배속비교 10 = 112개 생성 · 실패 0** · 무음 **26.5초** 제거.
- **문장당 2.4초** → 16,000문장 ≈ **10.5시간 GPU**(예측 11시간과 일치).
- 캐시 재활용은 **0.0초**(5개 즉시 복사) — 대본을 고쳐도 안 바뀐 문장은 재합성되지 않는다.
- ⚠ **목소리·배속·발음사전·시드를 바꾸면 전량 재합성**된다(캐시 키에 들어간다). 시험에서 확정할 것.

### 검증
`npm run test:tsv` = **`test/tsv-tts.test.js` 35/35 신설**(원문 모듈을 require 해 실행 — TSV 파싱 8 ·
사전 표 게이팅 8 · 트림 6(합성 WAV 로 실행, fail-safe 포함) · 캐시 키 4 · 구조 5(공용 캐시 미사용·NUL 없음·
시드 고정 배선) · mp3 인코딩 2(**atempo 방향 확인**) · 실제 입력 회귀 2).
🔑 **A/B 역검증**: 사전 표 게이팅을 무력화하면 **5건 실패**(35→30), 복원하면 35/35.
회귀: tts(게이트 18 · fs-retry 31 · 숫자 59) · **makeall 무음 E2E 18/18**.

### ⏳ 아직 안 한 것
- **Remotion 통합 음성**(로이 순서상 다음) — 문장 음성들을 **1개로 병합 + 타임라인 JSON**.
  wav 무손실 병합이라 이음새 갭이 없고, 문장·자막 줄·장면(이미지/영상 경로)·챕터 타이밍을 실측 오프셋으로 낸다.
- **앱 UI·IPC** — 지금은 CLI 뿐이다. 로이가 앱에서 쓰려면 버튼이 필요하다.
- ⚠ pt-f6 대본은 **이미 띄어 쓴 표기**(`세부 정보`·`브루로`)라 **사전 적용 0행**이었다. 즉 TERM 시험은
  "사전이 듣는지"가 아니라 **"띄어 쓴 표기를 모델이 제대로 읽는지"** 검증이다.

## 🌐 브라우저 자동화 실패 복구 3종 — 아내 PC 로그에서 나온 것들 (2026-08-26, v0.3.52)
> 로이가 아내 PC 로그를 붙여 주면서 드러난 문제들. ⚠ **아내 PC 로그는 그 PC 에만 있다** — 지금은
> 로이가 로그창의 「📁 파일」로 열어 붙여 주는 방식이 유일하다(원하면 공유 폴더 자동 복사를 만들 수 있다).

### ① Chrome 이 시작하자마자 닫힘 → 프로필 정리 후 재시도 + 사람 말 오류
- 그 PC 로그: `⚠ 정식 Chrome 실행 실패 (Target page, context or browser has been closed)` →
  번들 Chromium 폴백도 `Executable doesn't exist at ...chromium-1223` 로 죽어 **G2 영상이 통째로 실패**.
  화면엔 영어 스택트레이스(`npx playwright install`)만 나와 무엇을 해야 하는지 알 수 없었다.
- 🔑 **락만 지우면 부족하다** — 크롬이 비정상 종료하면 `Preferences.exit_type='Crashed'` 가 남아 다음 실행 때
  복원 안내가 뜨고 자동화 컨텍스트가 그대로 닫힌다. **Flow 경로만** 이걸 고치고 있었고(main.cleanChromeProfile)
  **Grok·Genspark 는 Singleton 락만** 지웠다.
- ✅ **`core/chrome-profile.js` 신설** — `cleanProfile`(락 + 크래시 표시) · `explainLaunchError`(사람 말) ·
  `installBundledChromium`(앱이 직접 설치). Grok·Genspark 가 **① Chrome → ② 정리 후 1회 재시도 →
  ③ 번들 Chromium → ④ 사람 말 오류** 순으로 간다.
- 🔴 **터미널에서 `npx playwright install chromium` 을 돌리면 소용없다(실측)** — npx 가 **최신 playwright**
  (1.62.1)를 임시로 받아 그쪽 revision(**chromium-1234**)을 설치하는데, 앱은 **playwright 1.60.0 → chromium-1223**
  을 찾는다. 아내 PC 가 실제로 그렇게 헛설치했다(게다가 1208·1217 을 지웠다).
  → ⚙ 설정 → 👤 계정 의 **「⬇ 브라우저 설치」** 버튼을 쓴다. 앱 안의 playwright CLI 로 돌려 **버전이 맞는다**.
  · Electron 에서 node 스크립트를 돌리는 법: `process.execPath` + `ELECTRON_RUN_AS_NODE=1`(출판 vivliostyle 과 같은 패턴).
  · ⚠ `require.resolve('playwright/cli.js')` 는 package.json `exports` 에 막힌다 → **파일 경로로 직접** 찾는다
    (개발 저장소는 `playwright-core/cli.js`, 설치본은 `playwright/cli.js` — 둘 다 시도).

### ② Genspark — 팝오버가 안 열려 「확인만」 못 한 것을 설정 오류로 오판
- 그 PC 로그: `⚠️ 설정 팝오버 옵션이 안 보임` → `종횡비 '16:9' 못 찾음` → 3회 재시도 후
  `❌ 설정 적용 실패 — 이 배치를 생성하지 않음` → **G8·G9 통째로 실패**.
  🔑 **그런데 같은 세션 앞부분에서는 `종횡비: 16:9 선택 (현재 선택=16:9)` 로 성공했었다** — 간헐적 타이밍 문제다.
  (`크기=(크기옵션없음)` 은 정상이다 — 새 UI 엔 크기 옵션이 없어 `sizeOk` 는 기본 true. 실패한 건 비율 쪽뿐.)
- ✅ 비율 적용에 성공하면 `this._ratioConfirmed` 에 기억하고, 나중에 **크기는 맞는데 비율만 확인 실패**면
  「이 세션에서 이미 16:9 로 맞춘 적이 있어 그대로 진행합니다」로 넘어간다.
  Genspark 는 고른 비율을 세션 내내 유지하므로 **확인 실패 ≠ 설정 틀림**이다.
  ⚠ 안전장치의 취지(잘못된 크기 방지)는 그대로 — 한 번도 못 맞춘 세션에서는 여전히 배치를 만들지 않는다.

### ③ 모더레이션 순화가 헛수고면 그 실행에서는 건너뛴다
- 그 PC 로그: G5·G6·G7 이 **순화로도** 막혔고, 그 뒤 **Flow 가 5장을 전부 만들었다**(성공 5/5).
  즉 결과 손실은 없었고 **컷당 약 25초씩 시간만** 버렸다.
- ✅ `softenFutile` 카운터 — **실제로 순화를 해본 뒤 실패한** 경우만 세고, 2번을 넘으면 이후 NSFW 컷은
  순화 재시도 없이 바로 다음 엔진에 넘긴다. 순화가 통하는 대본에서는 지금과 똑같이 동작한다.
  ⛔ **모더레이션 자체는 프로그램으로 못 뚫는다** — 뚫으려 해서도 안 된다. 순환(다음 엔진)이 정답이다.

### 검증
- **`test/browser-recovery.test.js` 37/37 신설**(`npm run test:grok` 에 포함) — `cleanProfile` 을 **실제 임시
  프로필 폴더로** 실행(락 삭제 · `exit_type` 복구 · 다른 설정 보존 · 없는 경로에도 안 던짐) · 사람 말 오류 4종 ·
  두 엔진 배선 대조(옛 락-only 코드가 사라졌는지 포함) · 비율 완화 배선 · 순화 건너뛰기 배선 ·
  **미정의 식별자 방지**(`softenFutile`·`SOFTEN_GIVEUP`·`didSoften` 선언 확인 — 이 저장소 단골 사고).
- 회귀: grok 81 · tts(게이트 18 · fs-retry 31 · 숫자 59) · makeall 무음 E2E 18/18 · comfy 전량(2분할 86/86).

### ⚠ 작업 중 밟은 함정 (다음 사람도 밟는다)
- 🔴 **힙독 안 JS 에서 백슬래시가 사라진다** — `\/`·`\n`·`` \` `` 가 통째로 먹히거나 해석된다.
  이번에도 `split('\n')` 앵커가 안 맞아 교체가 실패했다. **JS 를 힙독으로 쓸 땐 백슬래시·백틱을 쓰지 말 것**
  (경로는 슬래시, 개행은 `String.fromCharCode(10)`, 정규식 대신 `includes`).
- 🔴 **줄 인덱스로 블록을 자를 땐 시작·끝을 둘 다 단언할 것.** 이번에 끝 줄을 하나 잘못 세어 가드가 잡았다
  (가드가 없었으면 조용히 코드가 깨졌다).
- 🔴 **삽입한 줄을 다시 매치하는 루프**를 만들면 무한히 늘어나 OOM 으로 죽는다(실제로 한 번 죽었다).
  `splice` 로 삽입할 땐 **찾자마자 break** 하거나 인덱스 기반으로 한 번만.

## ⬆ 영상 업스케일 — 중복·동시 실행 차단 + 느린 PC 자동 폴백 (2026-08-26, v0.3.51)
> 로이: "비디오 업스케일은 내 PC에서 진행하는거지? 속도가 이상할정도로 느린데…"
>   → 이어서 "아내 PC에서 업스케일한다고 장시간 진행하고 있어."
- 🔑 **내 PC 는 원래 업스케일을 안 한다** — 로그 실측(8/25·8/26 전량 `⬆ 업스케일 생략 — 이미 1920x1080 이상`).
  ☁ LTX2.5 가 **1920x1088** 로 뽑기 때문(v0.2.99). 느린 건 **아내 PC** 였다.
- **아내 PC 가 느린 이유 3가지**(그 PC 로그 실측):
  ① 비디오 엔진이 **Grok** → 1280x704·1264x720 → **업스케일 대상**
  ② **NVIDIA GPU 가 없다**(`🌡 GPU 온도 기록 불가(nvidia-smi 응답 없음)`) → Real-ESRGAN(ncnn-vulkan)이
     `361프레임 (24fps)` 을 한 장씩 확대 → 내장 그래픽에서 수십 분
  ③ 🔴 **같은 영상을 중복·동시 업스케일**했다 — `⬆ [1/3] G1` 이 도는 중에 `⬆ [1/4] G1` 이 또 시작되고
     `⬆ 업스케일 완료 (1/3)`·`(1/2)`·`(1/4)` 가 겹쳐 찍혔다.
- 🔑 **③의 원인**: 경로 교체(`NN.mp4`→`NN_1080.mp4`)를 **끝에 한 번만** 한다(썸네일 깜빡임 방지 v0.2.65).
  앞 호출이 끝나기 전에 다음 호출이 들어오면 **아직 원본 경로**라 같은 그룹이 또 대상이 된다.
  게다가 `maybeUpscale` 에 **뮤텍스가 없었다** — 수동 「🎬 영상」(`video-group`)도 끝에 부르는데
  **Grok 은 로컬 GPU 엔진이 아니라 레인을 안 잡아** 파이프라인 업스케일과 그대로 겹쳤다.
- ✅ **① 업스케일 전용 레인** — `_LANES` 에 `upscale` 추가, `maybeUpscale` 이 `_runOnLanes(['upscale'])`.
  직렬화만으로 중복도 사라진다(앞 호출이 끝나면 경로가 `_1080.mp4` 라 다음 대상 필터에서 빠진다).
  ⚠ **`localGpu` 를 잡지 않는다** — make-all 이 그 레인을 쥔 채 내부에서 부르므로 **자기 자신을 기다려 교착**된다
  (`enqueueImageJob` 과 같은 규칙 — 테스트가 단언한다).
- ✅ **② 방식 선택 + 자동 강등** — `core/upscale-config.js`(`~/.priming-maker/upscale-config.json`):
  **auto(기본) · ai · fast · off**. ⚙ 설정 → 🎬 ComfyUI 비디오 에 드롭다운.
  🔑 **하드웨어를 탐지하지 않고 실제 걸린 시간으로 판단한다** — `auto` 는 AI 로 시작해 **한 영상이 5분(기본)을
  넘으면** 남은 영상을 ffmpeg 로 낮추고 이유를 로그로 남긴다. 느린 PC 가 스스로 빠져나온다.
  (Vulkan 은 AMD/Intel 에서도 돌아 "NVIDIA 없음 = 느림" 이 항상 참은 아니다 — 그래서 실측으로 가른다.)
  `upscaler.upscaleVideo` 에 **`method:'fast'`** 추가 — AI 실행파일을 쓰지 않고 **내려받지도 않는다**.
- 로그에 **장당 시간**도 남는다(`⬆ G1 업스케일 완료 (4분 11초)` — v0.3.49 의 `_dur` 재사용).
- ⚠ **업스케일은 원격화되어 있지 않다** — TTS(9881)·이미지(8188)는 내 PC 서버로 보내지만 Real-ESRGAN 은
  **그 PC 로컬 실행파일**이라 아내 PC 에서 그 PC 하드웨어로 돈다. 이번 수정은 원격화가 아니라 **중복 제거 +
  느릴 때 낮추기**다. 근본 해법은 **아내 PC 비디오 엔진을 ☁ LTX2.5 로** 바꾸는 것(1920x1088 → 업스케일 자체가 생략).
- 검증: `comfy-perf` **54/54**(+6 — 레인 배선 · localGpu 미사용 · off 처리 · 자동 강등 · upscaler fast · 설정 기본값)
  · 2분할 패널 E2E **86/86**(비디오 탭에 업스케일 select). 회귀: makeall 무음 E2E 18/18.
  ⚠ `gpu-lanes.test.js` 가 **레인 3개를 정규식으로 못박고** 있어 함께 갱신했다(정규식 → 문자열 포함 — 레인이
  늘어도 백슬래시 이스케이프에 휘둘리지 않는다). `comfy-panes.smoke` 의 `selects === 0` 도 탭별 기대값으로 바꿨다.

## 🎛 이미지 엔진 UI 정리 — 순환 모달 제거 · Flow/Genspark 분리 · 버튼 1개 (2026-08-26, v0.3.50)
> 로이: "이미지 순환 선택이 있는데 이제 이미지의 창을 없애고, 선택창으로 분리하자. 순환(무료) 항목을
>   Flow(무료), 젠스파크로 분리하자. 로라 학습이미지 섹션은 설정의 하나 섹션으로 옮기고,
>   [⚙ · ⚙ComfyUI]는 1개의 버튼으로 통합하자."
- **드롭다운**: `순환(무료)` 1개 → **`Flow (무료)` · `Genspark (무료)` 2개**.
  🔑 **이어받기는 그대로 유지**(로이 확정) — `activeOrder(고른엔진)` 이 고른 쪽을 앞에 두고 나머지를 뒤에 붙인다.
  즉 드롭다운은 「어디서 **시작**할지」를 고르는 것이고, 한도(Genspark 휴식/한도 메시지 · Flow 계정 한도)에
  걸리면 남은 이미지를 **다른 쪽이 이어 만든다**. ⇒ **엔진 코드(main.js·image-rotation.js)는 한 줄도 안 고쳤다.**
- 🔴 **가장 위험했던 것 — 정규화가 선택을 되돌린다**: App.jsx 두 곳이 `['genspark','flow'] → 'rotate'` 로
  되돌리고 있었다(단일 항목 시절 잔재). 그대로 뒀으면 **Flow 를 골라 저장해도 다음에 열면 순환으로 돌아간다**
  (v0.3.8 「채널 참조음성이 통째로 지워짐」과 같은 계열의 조용한 되돌림). → 방향을 뒤집어 **옛 `'rotate'` →
  `'genspark'`** 로만 이관한다. 기본값 3곳(`useState` · 채널 기본 2곳)도 함께 바꿨다.
- **버튼 통합**: 이미지 줄의 `⚙`(순환) + `⚙ ComfyUI` → **`⚙` 하나**. 지금 고른 엔진에 맞는 탭으로 연다
  (comfy → ComfyUI 탭 · 나노바나나 → 🔑 API 키 · Flow/Genspark → 🆓 무료 이미지).
  ⚠ 그 줄엔 **Ollama 프롬프트 작성용 `⚙` 가 원래 따로 있다** — 테스트는 **버튼 제목으로** 구분해 센다
  (라벨 `⚙` 로 세면 2개가 나와 헛실패한다. 실제로 한 번 그랬다).
- **⚙ 설정에 「🆓 무료 이미지」 탭 신설** — Flow 이미지 모델(Nano Banana 2 / Lite) · 👤 계정 관리 링크 ·
  **LoRA 학습용 이미지 수집 섹션(이동)** · 한도 시 이어받기 안내. 순서 ↑↓·체크박스는 없앴다(분리했으므로 무의미).
- 🔴 **숨은 의존을 먼저 확인하고 지웠다** — 옛 `openImgRotation` 이 **나노바나나 키·설정(`giKey`/`giCfg`)까지
  로드**하고 있었고, 그 값은 **🔑 API 키 탭이 쓴다.** 그냥 지웠으면 키 탭이 빈 채로 열렸을 것이다.
  `openSettings` 가 이미 같은 것을 로드하는 것(App.jsx 1825~1826)을 확인한 뒤 삭제했다.
  🔑 **함수를 지울 땐 「그 함수가 무엇을 로드하는가」를 먼저 볼 것** — 모달만 보면 안 보인다.
- 제거: 순환 모달 JSX **53줄** · `openImgRotation` · `toggleRotEngine` · `moveRotEngine` ·
  `imgRotOpen`(상태·ESC·deps 배열) · `ENGINE_META`.
  남긴 것: `imgRot`·`saveImgRot`(Flow 모델 저장용) · `lora`·`saveLora`·`pickLoraDir`.
  · `core/image-rotation.js` 는 **무수정** — `order`/`enabled` 는 이제 UI 가 없지만 현재 값이 기본(둘 다 true)이라 무해하다.
- 검증: `comfy-panes.smoke` **85/85**(+16 — 드롭다운에 Flow·Genspark 있고 `rotate` 없음 · 설정 ⚙ 1개 ·
  옛 「⚙ ComfyUI」 라벨 없음 · 🆓 탭 존재 · LoRA 섹션 이동 · Flow 모델 select · **정규화 되돌림 금지 4건**).
  회귀: makeall 무음 E2E 18/18 · timestamps UI · styles 17/17 · merge E2E 18/18 · test:comfy 전량.
- ⚠ **로이가 알아 둘 것**: 옛 채널·작업항목의 `'rotate'` 는 열 때 **Genspark 로 표시**된다(동작은 동일 —
  Genspark 먼저, 한도면 Flow). Flow 로 시작하고 싶으면 드롭다운에서 고르면 되고 **그 선택이 이제 유지된다**.
  순환 순서·체크 조정 UI 는 사라졌다(설정 파일 `~/.priming-maker/image-rotation.json` 에는 값이 남아 있다).

### ⏱ 장당 생성 시간을 로그에 남긴다 (2026-08-26, v0.3.49)
> 로이: "내가 보고 싶은 건 이미지 생성에 몇 초가 걸리는지 확인하고 싶은 거야."
- 그전엔 `✓ G1 → 01.png` 만 남아 **시간을 알 수 없었다**(느려져도 로그로는 안 보였다 — v0.3.35 사고 때
  서버 로그를 따로 뒤져야 했던 이유). 이제 앱 로그만 보면 된다.
- **한 줄마다**: `✓ G1 → 01.png (13.5초)` · 실패도 `✗ G3 실패 (300.0초): 타임아웃` (타임아웃 진단에 필요).
- **끝에 요약**: `⏱ 이미지 22장 — 장당 평균 13.8초 (최소 12.1초 · 최대 18.2초) · 전체 5분 4초 · 동시 2장`
  · 영상도 동일(`⏱ 영상 5개 — 개당 평균 …`). i2v 는 건당 수 분이라 특히 유용하다.
  · ⚠ **동시 생성이면 합계 > 전체**다(그래서 평균과 전체를 함께 찍는다).
- `_dur(sec)` 공용 헬퍼 — 60초 미만은 `23.2초`, 그 이상은 `4분 11초`.
- 🔑 **앱 로그는 파일로도 남는다**(v0.3.15, `~/.shots-maker/logs/YYYY-MM-DD.log`) → 나중에 되짚어 볼 수 있다.
- 💡 **더 자세히 보고 싶으면 ComfyUI 웹 UI** — `http://127.0.0.1:8188`(다른 기기는 `http://desktop-cbqlolj:8188`).
  ⛔ **Comfy Desktop 창을 열지 말 것** — 우리 런처가 8188 을 쥐고 있어 그쪽이 다른 포트로 밀리고 **서버가 두 벌**이
  된다(v0.3.35 실사고: RAM 여유 2.2GB → 장당 498초 = 17.6배 → 타임아웃 실패). **웹 UI 는 같은 서버라 비용 0.**
- 검증: `test:comfy` 의 `comfy-perf` **48/48**(+5 — `_dur` 원문 추출 실행 7케이스 · 성공/실패 로그에 시간 ·
  요약 줄 · 계측이 생성 호출을 감싸는지). 회귀: makeall 무음 E2E 18/18 · test:comfy 전량(2분할 E2E 69/69).

## 🖧 로컬 ComfyUI 를 다른 PC 도 쓰게 개방 — TTS 와 같은 구조 (2026-08-26, v0.3.46)
> 로이: "아내가 로컬 선택하면 TTS 처럼 지금 PC 에서 만들어 보내주면 되는거 아니야?"
> → **맞다.** 원리적으로 TTS 와 같은 구조인데 런처가 `--listen` 을 안 넘겨서 못 하고 있었다.
- 🔑 **원인은 실측 한 줄로 드러난다**:
  ```
  TCP  0.0.0.0:9881    LISTENING   ← OmniVoice(TTS)  — 모든 랜카드 = 아내 PC 접속 가능
  TCP  127.0.0.1:8188  LISTENING   ← ComfyUI         — 이 PC 안에서만 = 접속 불가
  ```
  `comfy-server.pyw` 의 argv 가 `--port … --disable-auto-launch` 뿐이었다(v0.3.30 에 런처를 만들 때
  "이 PC 전용" 으로만 생각한 누락. 설계 판단이 아니었다).
- ✅ **argv 에 `--listen 0.0.0.0` 추가.** 재기동 실측: `0.0.0.0:8188 LISTENING` · 로그
  `To see the GUI go to: http://0.0.0.0:8188` · 기동 72초 · ComfyUI 0.33.4 · **모델 목록 유지**(UNET 4 · LoRA 3) ·
  `127.0.0.1` / `100.112.7.63`(Tailscale) / `DESKTOP-CBQLOLJ` 모두 200.
- 🔑 **실제 관문은 Windows 방화벽이다** — 바인딩만 열려도 인바운드 규칙이 없으면 밖에서 못 들어온다(**기본은 안전**).
  · 실측: 이 PC 의 **이더넷·Tailscale 인터페이스가 둘 다 `Private`** → **Private 규칙 하나가 LAN·Tailscale 을 다 커버**한다.
  · 본보기가 이미 있다: `OmniVoice TTS (PrimingFlow)` · Inbound · Allow · **Private**(9881). 8188 은 규칙이 없었다.
  · ⚠ **규칙 추가는 로이가 관리자 권한으로** 한다(내가 시스템·보안 설정을 바꾸지 않는다).
    일반 창에서 실행하면 `New-NetFirewallRule : Access is denied` 가 난다(실제로 한 번 났다).
  · ⚠ ComfyUI 에는 **인증이 없다** — 공용 와이파이(Public 프로필)에서는 규칙이 안 걸려 자동으로 막힌다.
- **주소는 이름으로 쓴다.** tailnet 실측: `desktop-cbqlolj`(100.112.7.63, 이 PC) · `desktop-c4knmhu`(100.90.175.26,
  **온라인**) · `desktop-i6hhnot`(오프라인).
  ⚠ **CLAUDE.md v0.3.7 의 "아내 PC 엔 Tailscale 이 설치된 적 없음" 은 낡았다** — 지금 tailnet 에 다른 PC 가 온라인이다.
  LAN IP 는 DHCP 로 바뀌어 실제 사고를 냈으므로(v0.3.7) **`http://desktop-cbqlolj:8188`** 처럼 이름을 쓴다.
  Tailscale 로 붙으면 **집 밖에서도 그대로 동작**한다(메인 PC 가 켜져 있어야 한다).
- ⚠ **GPU 경합은 코드로 못 막는다** — 앱의 GPU 레인(`_runOnLanes`)은 **같은 프로세스 안에서만** 작동한다.
  아내 PC 의 이미지 요청은 그 레인 밖이라 이 PC 의 TTS 와 3060 을 동시에 다툰다.
  실측 근거: 로컬 GPU 점유 중 TTS **RTF 0.70→1.26**(1.8배), 심하면 60초 타임아웃 3회 → **반쪽 .vrew**(v0.3.22).
  → **운영으로 피한다**: 아내 PC 기본은 ☁ 클라우드, int4 가 필요할 때만 🖥 로컬.
  · ✅ 이미지끼리는 안 다툰다 — ComfyUI 가 큐 서버라 두 PC 요청을 **순차 처리**한다.
  · ✅ 아내 PC 앱이 **남의 서버를 켜려 들지 않는다** — `ensureLocalComfy` 는 127.0.0.1/localhost/::1 일 때만 spawn.
  · ✅ 아내 PC 도 생성 후 `freeMemory()` 를 부른다(`!cfg.cloud`) → 오히려 이 PC TTS 자리를 비워 준다.
- 「Krea2 int4 Turbo **(로컬)**」 라벨은 **유지**한다 — 드롭다운은 ☁/🖥 두 그룹에 같은 워크플로를 모두 나열하므로,
  ☁ 쪽에서 고르면 실패한다는 경고로 여전히 유효하다(클라우드엔 그 모델이 없다).

### 🚦 1단계 — TTS 시작 전에 「남의 PC 가 이 PC GPU 를 쓰는 중인가」 확인 (2026-08-26, v0.3.47)
> 로이: "누군가 먼저 작업중인데, 다른 사람이 작업요청하면 그 요청 자체를 순차적으로 진행하기 위해 대기
>   하도록 할수는 없는거야?"
- **이미 되던 절반**: 이미지끼리는 **ComfyUI 큐가 순차 처리**한다(누가 보냈든). 두 PC 가 동시에 보내도 안 다툰다.
- **안 되던 절반**: TTS(9881 OmniVoice)와 이미지(8188 ComfyUI)는 **서버가 둘**이라 서로를 모른다.
  앱의 GPU 레인은 같은 프로세스 안에서만 작동하므로 남의 PC 요청은 레인 밖이다.
- ✅ **`awaitForeignComfyIdle(label)` 신설** — `enqueueTtsJob` 이 레인을 잡은 뒤 **TTS 를 시작하기 전에**
  로컬 ComfyUI `/queue` 를 3초 간격으로 폴링해 빌 때까지 기다린다.
  · 🔑 **큐가 진실이다** — 누가 보냈든(아내 PC·외부 노트북) 다 보인다. 별도 조율 서버가 필요 없다.
  · 🔑 **교착 없음** — 우리 앱의 로컬 이미지·비디오도 `localGpu` 레인을 잡으므로, 이 함수가 도는 시점에
    큐에 있는 건 **남의 것뿐**이다. ⚠ `enqueueImageJob` 에는 **넣지 않는다**(자기 자신을 기다린다 — 테스트가 단언).
  · ⚠ **fail-open** — 클라우드·서버 꺼짐·HTTP 오류·주소 없음은 전부 그냥 진행. 상한 **10분**을 넘으면
    경고만 남기고 시작한다(무한 대기가 더 나쁘다).
  · 로그: `⏳ 전체 TTS 변환 — 다른 PC 가 이 PC 의 ComfyUI 로 이미지를 만드는 중입니다(2건). 끝나면 시작합니다`
    → `▶ … 로컬 ComfyUI 가 비었습니다. 시작합니다 (37초 대기)`. 평소(큐가 빔)엔 **로그를 남기지 않는다**.
- 🔑 **이게 실사고 유형을 정확히 막는다** — v0.3.22 의 반쪽 .vrew 는 「이미지가 GPU 점유 → TTS 60초 타임아웃
  3회」 순서였다. 반대 순서(TTS 먼저 → 이미지 나중)는 이미지가 느려질 뿐 타임아웃이 300초라 여유가 있다.
- ✅ **2단계 완료(v0.3.48)** — 아래 항목 참조.
- 검증: **`test/gpu-foreign.test.js` 21/21 신설** — main.js 원문에서 **async 함수를 뽑아 실행**(큐 바쁨→대기 ·
  비면 즉시 진행 · 대기 로그는 1회만 · fail-open 4종 · 상한 · 꼬리 슬래시 정리 · 배선 대조).
  🔑 **A/B 역검증**: `enqueueTtsJob` 배선을 빼면 **2건 실패**.
  ⚠ `gpu-lanes.test.js` 의 단언이 콜백 모양(`label, fn`)까지 못박고 있어 함께 갱신했다 — **레인만 확인**하도록.
  회귀: **makeall 무음 E2E 18/18**(실제 경로에서 이 함수가 돈다) · test:comfy 전량(2분할 E2E 69/69) · test:tts.

### ✅ 2단계 — 진행 중인 TTS 를 남의 이미지 요청으로부터 보호 (2026-08-26, v0.3.48)
> 로이: "작업 끝났어. 2단계 지금 바로 진행하자."
- **서버(OmniVoice `api.py`)에 `GET /busy` 신설** — `{"busy": bool, "n": int}`.
- 🔴 **그냥 붙였으면 소용없었다** — 합성이 **이벤트 루프를 막아** 정작 바쁠 때 대답을 못 한다.
  실측(수정 전): 합성 중 `/health` 가 **초반 약 5초 동안 3초 타임아웃**, 그 뒤엔 2ms.
  → 🔑 **TTS 엔드포인트 3개를 `async def` → `def` 로 바꿔 FastAPI 스레드풀에서 돌린다.**
    · 셋 다 `await` 를 쓰지 않는 것을 **원문에서 먼저 확인**하고 바꿨다(안 그러면 즉사).
    · 동시 실행은 **`_GPU_LOCK`(threading.Lock)** 으로 **지금과 똑같이 한 번에 하나**로 유지.
    · 들여쓰기를 다시 쓰지 않으려고 **`@_tts_busy` 데코레이터**로 감쌌다(`functools.wraps` 라
      FastAPI 의 시그니처 추론도 그대로 동작한다). 카운터는 GPU 락을 **잡기 전에** 올린다(대기 중도 busy).
  · 수정 후 실측: 합성 내내 `/busy` 가 **1~4ms** 로 `{"busy":true,"n":1}` 을 정확히 반환하고 끝나면 false.
- 🔑 **검증을 운영 서버 건드리지 않고 했다** — 임시 런처로 **포트 9899 · 127.0.0.1 · 임시 data 폴더**
  (FLOW_DATA_DIR/DICT/STYLE 격리)에 두 번째 인스턴스를 띄워 확인하고 종료. 9881 은 내내 정상이었다.
  ⚠ 이 방법은 VRAM 여유가 있어야 한다(당시 8.5GB 여유 · OmniVoice 모델 약 2~3GB).
- ✅ **덤 — v0.3.29 참조음성 오염의 뿌리가 사라진다.** 그 사고는 「합성 중 5초 GET 이 죽어 `listServerVoices`
  가 빈 목록을 받고 → 전 목소리 재업로드 → 바이트 동일 사본 24개」였다. 이제 합성 중에도 `/ref-voices`·
  `/dict` 가 정상 응답한다.
- **앱(`main.js`)**: `awaitForeignTtsIdle(label, logger)` 신설 — `runComfyImages`·`runComfyVideos` 가
  **`!cfg.cloud` 일 때만** 생성 직전에 호출(클라우드는 이 GPU 와 무관).
  · fail-open 전부: **구버전 서버 404/401 · 못 닿음 · `busy` 필드 없는 응답 · 주소 없음 → 그냥 진행.**
    ⇒ **서버를 재시작하기 전까지는 무동작**이라 배포만으로는 아무것도 바뀌지 않는다(안전).
  · 상한 10분 · `X-API-Key` 동봉(없으면 401) · 대기 로그는 1회만.
- 🔴 **미정의 식별자 사고를 발행 전에 잡았다** — 호출부에 `label` 을 썼는데 `runComfyImages`·
  `runComfyVideos` 어디에도 그 변수가 없었다(이 저장소 세 번째가 될 뻔했다: `imgEngine`·`onPickImgEngine`).
  → 리터럴(`'로컬 이미지 생성'`·`'로컬 비디오 생성'`)로 바꾸고 **테스트가 `await awaitForeignTtsIdle(label`
  을 금지**한다. ⚠ 함수 **선언부**도 `awaitForeignTtsIdle(label,` 을 포함하므로 단언에 `await ` 접두가 필수다.
- 검증: `test/gpu-foreign.test.js` **43/43**(1단계 21 + 2단계 22). 🔑 **A/B 역검증**: 호출부를 `label` 로
  되돌리면 **2건 실패**. 회귀: **makeall 무음 E2E 18/18** · test:comfy 전량(2분할 E2E 69/69).
- ⚠ **서버 파일은 저장소 밖**(`D:\TTS_Model\omnivoice\api.py`, 백업 `api.py.bak-20260826`)이라
  라이트 업데이트로 배포되지 않는다. **관리자 PowerShell 로 예약작업을 재시작해야** `/busy` 가 켜진다:
  `Stop-ScheduledTask -TaskName "OmniVoice_Backend"` → `Start-ScheduledTask -TaskName "OmniVoice_Backend"`
  (모델 로딩 약 1분 뒤 `/health` 200). 그전까지는 앱이 404 를 받고 그냥 진행한다.

### 💡 "OmniVoice 를 ComfyUI 에서 돌리면?" — 방향은 정확하지만 지금은 비용이 이득보다 크다
> 로이: "어차피 컴피유아이를 이용해 이미지를 만들 거란 말이지. 그러면 옴니보이스를 컴피유아이에서 돌리는 건 어떨까?"
- 🔑 **진단은 정확하다** — 큐가 하나면 GPU 조율 문제가 **원리적으로 사라진다**(1·2단계가 아예 필요 없어진다).
- 그런데 비용이 크다:
  · OmniVoice 용 **ComfyUI 커스텀 노드를 직접 만들어야** 한다(공식 노드 없음).
  · 앱의 TTS 경로가 OmniVoice **HTTP API 에 깊게 얹혀 있다** — `/tts`·`/tts-batch`·`/upload-ref-audio`·
    `/ref-voices`(서버 공용 목소리)·`/dict`(발음사전)·`/asr-upload`(STT) + `ref_name`·seed·cfgValue·
    inferenceTimesteps·speed. 이걸 전부 워크플로/노드로 옮겨야 하고, 아내 PC 도 그 API 로 TTS 를 쓴다.
- 🔴 **오히려 느려질 수 있다** — ComfyUI 는 작업마다 모델을 올렸다 내린다. 지금 OmniVoice 는 모델을 **상주**
  시켜 둔다(유휴 시 약 2~3GB). 이미지 모델(약 10GB)과 번갈아 돌면 **문장마다 모델 스왑**이 붙는다.
  문장 수백 개짜리 롱폼에서 이건 치명적이다.
- ⇒ **1·2단계가 같은 결과를 훨씬 싸게 얻는다.** 지금은 하지 않는다.
  ⏳ 다시 볼 조건: OmniVoice 용 ComfyUI 노드가 공식으로 나오고, ComfyUI 가 모델 상주(캐시)를 보장할 때.

## 🖼 Krea2 int4 판 추가 — "새 LoRA" 인 줄 알았던 게 본체 모델이었다 (2026-08-26, v0.3.44)
> 로이: "이미지 생성하는데 로컬 krea2 를 이용하는 경우 새로운 로라를 다운받았는데, 새로운 로라로
>   이미지 생성을 하려면 어떻게 해야해?" → 파일은 `krea2Int4Convrot_v10Turbo.safetensors`.
- 🔑 **LoRA 가 아니라 본체 확산 모델(UNET)이었다.** 구분법(다음에도 이 순서로 본다):
  ① **위치** — LoRA 는 `…/ComfyUI-Shared/models/loras/`, 본체는 `…/models/diffusion_models/`
  ② **크기** — LoRA 는 수백MB(`krea2_darkbrush` 469MB), 본체는 수~십GB(이건 6.9GB)
  ③ **결정적** — 로컬 `/object_info/LoraLoaderModelOnly` 와 `/object_info/UNETLoader` 중 어디에 잡히는가
  · 모델 폴더의 정본은 `%APPDATA%\Comfy Desktop\instance-model-paths\<id>.yaml`(base_path + `loras:`/`unet:`).
- **앱에는 LoRA 선택 UI 가 없다** — 엔진(`core/comfy-image.js`)은 프롬프트·해상도·seed 만 주입하고
  로더는 워크플로 JSON 이 정한다. Krea2 워크플로의 로더 4개:
  `30:10` UNETLoader · `30:11` CLIPLoader · `30:12` VAELoader · `30:15` LoraLoaderModelOnly(0.8).
  → 모델이든 LoRA 든 **워크플로에서 지정하는 것이 유일한 방법**이다.
- ✅ **`comfy/image_krea2_int4_turbo.json` 신설** — 원본에서 **UNETLoader 하나만** 바꿨다(CLIP·VAE·LoRA 동일).
  `comfy-image.js` BUNDLED 에 「Krea2 int4 Turbo」로 등록 → 헤더 「② 이미지」 드롭다운에 나온다.
  🔑 **기본값은 그대로 Krea2 Turbo** — 화질 판정은 로이 몫이라 자동으로 갈아타지 않는다.

### A/B 실측 (RTX 3060 · 같은 프롬프트 · 같은 seed · 같은 LoRA · 1344x768)
조건: 중복 ComfyUI 0 · VRAM 여유 11.2GB · RAM 여유 21.9GB · 큐 비어 있음(`comfy-perf.scanRivals` 로 확인).
seed 는 **`Math.random` 을 고정**해 두 판이 같은 값을 받게 했다(엔진 코드 무수정).

| | 1장째(모델 로딩 포함) | 2장째(warm) | 파일 |
|---|---|---|---|
| 현재 = fp8 요청 → **int8_convrot 자동 교체** | 30.4s (서버 29.9s) | **20.3s** | 1171·1073KB |
| 신규 **int4** | 18.2s (서버 17.2s) | **13.5s** | 1162·1238KB |

→ **1.50배 빠름.** ⚠ VRAM 점유는 둘 다 약 10GB 로 **차이가 없었다**(파일이 6.9GB vs 13.5GB 인데도 —
  ComfyUI 가 그만큼 잡아 두는 듯. "파일이 작으니 VRAM 도 준다" 고 기대하지 말 것).
⚠ **화질은 내가 판정 못 한다** — 4장(같은 seed 짝)을 로이에게 보냈다.

### 🔑 모델 자동 대체가 int4 에는 개입하지 않는다 (실측)
- `baseKey('krea2Int4Convrot_v10Turbo')` = `"krea2int4convrot v10turbo"` 로 `"krea2 turbo"` 와 달라
  **`pickSubstitute`·`pickFasterQuant` 둘 다 null** → **요청한 그대로 int4 가 쓰인다.**
  (정체성이 바뀌는 대체는 거부 = v0.3.22 의 의도된 동작. 이름이 camelCase 라 토큰 분해가 안 되는 것도 한몫한다.)
- ⚠ 반대로 **지금 Krea2 워크플로는 fp8 을 요청하지만 3060 에서 실제로 쓰이는 건 int8_convrot 이다**(v0.3.35).
  로그에 `⚡ … fp8 을 하드웨어로 못 돌립니다 — … → krea2_turbo_int8_convrot` 가 매번 찍힌다.

### 검증
- `npm run test:comfy` 에 **`test/comfy-workflows.test.js` 31/31 신설** — 번들 파일 실재 + **API 포맷**
  (UI 포맷이면 앱이 못 읽는다) · **int4 는 UNETLoader 하나만 다르다** · LoRA/CLIP/VAE 동일 ·
  자동 대체 무개입 4건 · fp8→int8 기존 동작 보존 · `loadConfig()` 목록에 실제 등록.
  🔑 **A/B 역검증**: int4 워크플로의 CLIP 을 일부러 바꾸면 **2건 실패**(31→29).
- 회귀: comfy 전량 통과(모델 · perf · pipeline-mode · gpu-lanes · **2분할 패널 E2E 69/69**).

### ⚠ 로이가 알아 둘 것
- 쓰려면 헤더 **「② 이미지」 → 🖥 Krea2 int4 Turbo** 를 고른다. 기본값은 안 건드렸다.
- 아내 PC 에도 **항목은 보이지만** 그 PC 에 `krea2Int4Convrot_v10Turbo.safetensors` 가 없으면 실패한다
  (v0.3.22 의 사람 말 오류로 "그 서버에 있는 것: …" 을 알려준다). 클라우드(☁)를 쓰면 무관.
- 💡 파일명을 `krea2_turbo_int4_convrot.safetensors` 로 바꾸면 앱이 **같은 Krea2 Turbo 계열**로 인식한다
  (클라우드↔로컬 왕복 시 자동 대체 대상이 됨). 선택 사항 — 지금 이름 그대로도 정상 동작한다.
- **진짜 LoRA** 를 받았을 땐: 파일을 `…/models/loras/` 에 넣고 워크플로 `30:15` 의 `lora_name`·
  `strength_model` 을 바꾼다. LoRA 를 여러 개 쌓으려면 ComfyUI 웹UI 에서 편집 후 **「API 포맷으로 저장」**
  (⚠ 일반 저장은 앱이 못 읽는다) → ⚙ 설정 → 🖼 ComfyUI 이미지 → 워크플로 **＋추가**.

### ✅ 기본값 전환 + 클라우드 실측 (2026-08-26, v0.3.45)
> 로이: "int4 화질 괜찮네, 기본값으로 바꿔줘. 아내PC에서도 파일이 있어야 한다는게 무슨 말이지?"
- **활성 워크플로 = 「Krea2 int4 Turbo (로컬)」** 로 전환(`~/.priming-maker/comfy-image-config.json`).
- 🔴 **채널 프리셋이 옛 워크플로를 못박고 있었다** — `06_고전서재` 의 `imgEngine` 이
  `comfy::…\image_krea2_turbo_t2i (2).json` 로 **절대경로 고정**이라, 설정만 바꿨으면 그 채널은 계속 옛
  모델로 그렸을 것이다(엔진 값이 생성 때 이긴다 — v0.3.1). → **bare `'comfy'`**(= 앱 활성 워크플로 따름)로
  바꿨다. 나머지 comfy 채널 3개는 이미 bare 라 자동으로 따라온다.
  🔑 **워크플로를 바꿀 땐 채널 고정값(`preset.imgEngine`)을 함께 볼 것.** 한 곳만 바꾸면 조용히 옛 모델로 그린다.
  ⚠ 절대경로 고정은 PC 마다 설치 경로가 달라 깨진다 — 특별한 이유가 없으면 bare `'comfy'` 가 낫다.
- 🔴 **클라우드엔 int4 가 없다(실측)** — `GET /api/object_info`(9.5MB · 2초)로 확인. UNET **376개** 중 krea
  계열 9개(`krea2_turbo` fp8/bf16/int8_convrot/mxfp8/nvfp4 · `krea2_raw` 3종 · flux1-krea-dev)이고
  **`krea2Int4Convrot_v10Turbo` 는 없다.** (LoRA `krea2_darkbrush` 는 클라우드에도 있다.)
  · 드롭다운 이름을 **「Krea2 int4 Turbo (로컬)」** 로 바꿔 잘못 고르는 것을 줄였다.
  · ⛔ `DEFAULT_ACTIVE_FILE` 은 **z-image 그대로** 둔다 — 설정이 비었을 때의 폴백이라 int4 로 두면
    클라우드 PC 가 없는 모델로 떨어진다.
  · 🔑 **클라우드 모델 목록을 보려면 `/api/object_info` 를 통째로 받는다**(`/api/object_info/<노드>` 는 404).
    9.5MB 라 생성 경로에서는 쓰지 말 것(v0.3.22 가 에러 응답의 목록을 쓰는 이유).
- 🔑 **아내 PC 는 ComfyUI 를 설치할 필요가 없다** — 그 PC 는 ☁ 클라우드(comfy.org)로 보내므로 GPU 도 모델도
  전부 서버 것이다. 「Krea2 Turbo」를 그대로 쓰면 되고 **「(로컬)」이 붙은 항목만 안 고르면 된다.**
  🖥 로컬을 쓰려면 그 PC 에 ComfyUI + 모델 파일이 있어야 하지만, 지금 구조상 그럴 이유가 없다.
- ✅ **로이 결정(2026-08-26): 파일명을 바꾸지 않는다 — "로컬에서만 int4 를 이용하면 된다."**
  즉 int4 는 **로컬 전용**으로 두고, 클라우드는 지금처럼 「Krea2 Turbo」를 쓴다. 아래 안은 폐기.
  ⛔ 다음 세션이 이 제안을 다시 꺼내지 말 것.
- 💡 (폐기된 안) 모델 파일명을 `krea2_turbo_int4_convrot.safetensors` 로 바꾸면
  `baseKey` 가 `krea2 turbo` 가 되어 **클라우드에서 자동으로 int8_convrot 으로 대체**된다(v0.3.22).
  그러면 워크플로 하나로 로컬=int4 · 클라우드=int8 이 다 돌아가고 「(로컬)」 표시도 필요 없어진다.
  6.9GB 파일 이름을 바꾸는 일이라 손대지 않았고, 로이가 위와 같이 결정했다.

## 🔢 자막은 숫자 · TTS 는 발음 — 대본에 숫자를 한글로 쓸 필요가 없었다 (2026-08-25, v0.3.43)
> 로이: "자막 만드는 텍스트하고 TTS 변환하는 텍스트가 서로 다르게 관리되고 있지? TTS 때문에 숫자를
>   한글로 작성하고 있었는데, 자막이 너무나도 어색하고 이상하게 읽히는 부분이 있어. TTS 변환용으로는
>   한글로 숫자를 작성하고, 자막용으로는 숫자로 작성하도록 하자."
- 🔑 **구조는 이미 그렇게 되어 있었다** — 두 경로가 갈리는 지점은 하나뿐이다:
  · 자막 = **`s.text` 원문 그대로** (`vrew-builder.js:964` `splitCaptionLines(s.text)`)
  · TTS  = **`processForTTS(s.text)`** (`tts-manager.synthesize`)
  → 그래서 **대본에 숫자로 쓰면 그것으로 끝**이다. 자막은 숫자로 나가고 발음만 앱이 손댄다.

### 🔴 전제가 거짓이었다 — OmniVoice 는 아라비아 숫자를 정확히 읽는다
- 옛 코드(`_convertSinoNumbersBeforeUnits`)의 근거는 "모델이 숫자를 「십 십이년」처럼 읽는다"였는데,
  현재 서버(`100.112.7.63:9881`)로 재니 **거짓**이었다. 모델·서버가 바뀐 것으로 보인다
  (`D:\TTS_Model\omnivoice` 이관 + omnivoice 0.1.2/0.1.5 충돌 이력 참조).
- 🔑 **측정법(재사용할 것)** = **합성 → ASR 전사 비교**. 23조 · 약 50클립 · 목소리 `02_저음 2단계`:
  ① 대본 문장을 숫자 표기 / 정답 한글 / 자리읽기 3종으로 합성 ② `asr-client.transcribe` 로 전사
  ③ **숫자 표기의 전사가 정답 한글의 전사와 같은가**를 본다.
  · 🔑 **판정력 검증이 핵심이다**(이게 없으면 위 표는 헛단언) — ASR 이 숫자를 다시 아라비아 숫자로
    정규화하기 때문에 "같다"가 의미를 가지려면 **다른 것은 다르게 나온다**를 먼저 보여야 한다:
    `일영영영`→`1-0-0-0…` · `일이년`→`1, 2년` · `이삼 살`→`2, 3살` 로 **자리읽기를 구분해 냈다.**
  · ⚠ **길이 비교는 쓰지 말 것** — 프로소디 노이즈가 커서 판정이 안 된다(같은 문장이 ±7% 흔들리고,
    `1000명` 숫자판은 1.07s 로 정답 1.56s 보다 오히려 짧게 나와 오히려 오판을 유도했다).
- **실측 결과**:
  | 확인 | 값 |
  |---|---|
  | 옛 화이트리스트 단위 11개(회·차·등·호·위·도·분·초·세기·세대·세) 전수 | 숫자 = 정답 한글 **11/11 일치** |
  | 고유어까지 문맥으로 스스로 가린다 | `5개`→「다섯 개」 · `23살`→「스물세 살」 · `2시 30분`→「두 시 삼십 분」 |
  | 큰 수·쉼표·날짜·기호 | `1000명`→「천 명」 · `1,000명`→「천 명」 · `1592년 4월 13일` · `10%` · `3.5배` 정확 |
- 🔴 **강제 변환은 오히려 해롭다**: 한자어로 바꾼 `오개를 받았다` → 들림 **「옥외를 받았다」**.
  `3번`→`삼번` 으로 고정하면 문맥상 「세 번」을 고를 여지까지 없앤다(모델이 판단하는 게 낫다).
- 🟠 **딱 하나는 앱이 해야 한다 — 물결표**: `50~60명` 그대로는 「50**,** 60명」으로 끊어 읽어 **오독**이고,
  `50에서 60명` 은 정확했다. → `~`→`에서` 변환만 남겼다.

### 🔴 게다가 옛 정규화기는 절반만 동작하고 있었다
- lookahead 가 `년\b` 인데 **`\b` 는 ASCII 경계**라 한글 조사 앞·문장 끝에서 **무조건 실패**했다.
  `12년 전`(뒤가 공백) 은 변환되는데 **`1418년에`·문장 끝 `13일`·`1등`·`5호실`·`2위`·`36도`·`19세기`·
  `40세` 는 그대로**였다. 즉 로이가 한글로 쓰게 된 진짜 이유는 이 반쪽 동작이다.
- **A/B 역검증(git 원본 실행)**: `git show HEAD:tts/text-pronouncer.js` 로 옛 모듈을 그대로 돌리니
  회귀 목록 24건 중 **8건을 변형**(나머지 16건은 위 버그로 놓치던 것). 새 구현은 **0건**.

### ✅ 한 것
- `tts/text-pronouncer.js` — `_convertSinoNumbersBeforeUnits` **제거**, `~`→`에서` 유지.
  **`processForTTS(text, dict)` 신설 = TTS 텍스트 가공의 유일한 문**(사전 → 정규화 순서).
  `numToHangulSino` 는 순수 유틸로만 남겼고, **테스트가 `normalizeForTTS` 에 다시 배선되지 않는지 단언**한다.
- `tts/tts-manager.js` — synthesize 가 `this.processText()` 를 쓴다. **`processText(text)`**(sync, 합성될
  최종 문자열) · **`prepareDict()`**(사전 동기화) 신설.
- `docs/대본-작성-가이드.md` — 공통 원칙 #4 「**숫자는 아라비아 숫자로 쓴다**」 + 체크리스트 1줄.
  예외 ① 고유어로 읽혀야 하는 곳만 한글(「세 번」 — `3번`은 「삼 번」으로 읽힌다) ② 범위는 `50~60명` 그대로.

### 🔴 덤 — 캐시가 옛 발음을 되살리고 있었다(기존 잠재 버그)
- TTS 캐시 키가 **원문 기준**(`TtsCache.keyFor(s.text, …)`)이라, 정규화기나 **발음사전을 고쳐도 같은 키**가
  나와 **옛 음성이 그대로 재활용**됐다. 즉 📖 발음사전에 항목을 추가해도 이미 합성된 문장은 안 바뀌었다.
- ✅ 키를 **`ttsMgr.processText(s.text)`**(= 실제 합성될 문자열) 기준으로 바꿨다. 루프 전에 `prepareDict()`
  로 사전을 맞춘다(안 하면 첫 문장만 옛 사전으로 키가 계산돼 어긋난다).
  · 🔑 **통째 무효화가 아니다** — 가공이 필요 없는 문장은 `processText(t) === t` 라 **키가 그대로** 유지된다.
    사전·물결표에 걸리는 문장만 새 키가 되어 한 번 재합성된다(그게 맞는 동작이다).
  · 🔑 **가공을 `processForTTS` 한 곳으로 모은 이유가 이것이다** — 키와 합성 텍스트를 두 곳에서 각자
    계산하면 반드시 어긋난다. ⛔ 앞으로 TTS 텍스트 가공을 다른 곳에 추가하지 말 것.

### 검증
- `npm run test:tts` = 게이트 18 + fs-retry 31 + **`test/tts-number.test.js` 59/59 신설**
  (원문 모듈을 require 해 실행 · 실측 표 24건을 「변형되지 않는다」 회귀로 박음 · 물결표 6건 ·
  사전 우선순위 · **자막 빌더가 TTS 가공 함수를 쓰지 않는지** · pipeline·tts-manager 배선 원문 대조 ·
  캐시 키 3건(사전 걸리면 키 변경 / 안 걸리면 키 유지 / 사전 수정 시 키 변경) · 가이드 문구).
- 🔑 **A/B 역검증 2건**: ⓐ git 원본 모듈로 돌리면 회귀 24건 중 **8건 실패** ⓑ 옛 함수를 되살리면
  소스 가드 2건(`_convertSinoNumbersBeforeUnits` 없음 · 화이트리스트 없음)이 즉시 실패.
- **실경로 확인**: `makeTtsManager` 가 돌려주는 `mgr` 이 `TTSManager` 이고 `processText`·`prepareDict` 를
  갖는지 확인했다(⚠ `makeTtsManager` 는 **`{mgr, ok}`** 를 반환한다 — 이걸 매니저로 착각해 한 번 헛짚었다).
  사전 17항목이 정상 적용됨(`정약용→정야굥` 등).
- 회귀: **makeall 무음 E2E 18/18** · caption 102 · timestamps 53+UI · visual 14/10/13/7 · delmedia 27 ·
  merge 104+E2E 18/18.

### 🔴 예외를 하나 찾았다 — **「숫자+번」은 횟수 자리에서 틀린다** (2026-08-26 실측)
강의 프로젝트(pt-f6) 대본 규칙을 정하다가 같은 방법(ASR 전사 + **틀린 판 판정력 검증**)으로 재서 나왔다.
| 대본 표기 | 실제 읽기 | |
|---|---|---|
| `1번만 눌러보십시오` | 「**일 번만**」 | ❌ (「한 번만」이어야) |
| `2번 눌러야 열립니다` | 「**이 번**」 | ❌ (「두 번」이어야) |
| `1번부터 차례로` | 「일 번부터」 | ✅ |
| `3번째 영상부터` | 「**세 번째**」 | ✅ |
- 🔑 **모델이 `번`의 자리를 가리기는 한다 — 다만 순서는 맞히고 횟수는 한자어로 떨어진다**(문맥이 짧을수록).
- 판정 근거: A(숫자)의 전사가 **B(정답 한글)와 다르고 W(틀린 한글)와 같았다.** 05 는 반대로 W(`삼 번째`)만
  "3번째"로 갈려 A=B 를 확정했다. ⚠ **틀린 판이 다르게 전사될 때만 「일치」가 의미를 갖는다.**
- **함께 확인된 것**: `3가지`→「세 가지」 ✅(전사로 확정).
- 🔴 **길이 비교로 판정하지 말 것 — 이번에 그걸 어겼다가 틀렸다.**
  `4편`(AB2-08)을 「A 2.08초 = B 2.08초(소수점까지 일치), 틀린판 2.13초」라는 근거로 **A 가 맞다**고
  보고했는데, **로이가 들으니 B 가 맞았다**(A 는 「사 편」으로 읽힌다). **길이가 같아도 발음은 다르다.**
  같은 방식으로 판정한 `4강` 도 근거가 무너져 **철회하고 재판정 대기**로 돌렸다.
  ⚠ 이 파일 v0.3.43 항목에 「길이 비교는 쓰지 말 것(프로소디 ±7%)」이라고 이미 적어 두고 어긴 것이다.
- **판정 신뢰도는 방법으로 갈린다**:
  | 방법 | 신뢰 |
  |---|---|
  | 전사 비교 + **틀린 판이 다르게 전사됨** | ✅ 확정 — `3가지`·`3번째`·`1번부터`(A OK) · `1번만`·`2번`(A 틀림) |
  | 사람 청취(로이) | ✅ 확정 — `8자`·`4편`(A 틀림 → 한글로) |
  | 길이 비교 | ❌ **쓰지 않는다** — 위 실패 |
- ✅ **`강` 재판정 결과(로이 청취) — 예측대로 「사 강씩」으로 들렸다.** `번` 과 **구조가 같다**:
  | | `번`(측정) | `강`(청취) |
  |---|---|---|
  | 명칭·순서 | `1번부터` → 「일 번부터」 ✅ | `2강에서`·`76강을` → 「이 강에서」·「칠십육 강」 ✅ |
  | 수량 | `1번만` → 「일 번만」 ❌ | `2강씩` → 「이 강씩」 ❌ |
  🔑 **자리로 갈린다 — 명칭은 한자어가 맞고 수량만 고유어다.** 실제 1강에서 `숫자+강` 22곳 중
  **명칭이 18곳**이라 「전부 한글」로 하면 오히려 나빠진다(`2강에서` 를 「두 강에서」로 읽게 된다).
- ⇒ **한글로 써야 하는 단위: `번`(횟수) · `자` · `편` · `강`(수량 자리만)**.
  8/25 에 확인한 `개`·`살`·`시`·단위 11종과 `만`·`원`·`년`·`주`·`분`·`%`·`명`·`회`·`배` 는 그대로 유효하다.
- 🔑 **로이 롱폼 영향은 사실상 없다(실측)** — 대본 468편 · 문장 **73,223개** 중 「숫자+번」은 **46개(0.063%)**,
  그나마 10개는 순서형(맞게 읽힘)이고 대부분 제작메모·표·검산 줄이라 낭독되지 않는다. 실제로 걸리는 것은
  `기록에 2번뿐`(제목) 정도다. **기존 대본을 고칠 규모가 아니다.**
- ⇒ 앞으로 대본에 **횟수의 `번`은 한글로**(「한 번」·「두 번」·「세 번」) 쓰면 된다. 순서(`3번째`)는 숫자 그대로 무방.

### ⚠ 로이가 알아 둘 것
- **이제 대본에 숫자를 그대로 쓰면 된다**(`1592년`·`3만 명`·`10%`). 자막이 숫자로 나가고 발음도 정확하다.
  ⚠ **단 횟수의 `번`만 예외**다(바로 위 항목).
  「세 번」처럼 고유어로 읽혀야 하는 곳만 한글로 쓰면 된다(자막에도 「세 번」이 나오는데 어색하지 않다).
- **이미 만든 대본·자막은 자동으로 안 바뀐다.** 한글로 써 둔 숫자를 숫자로 고쳐야 자막이 바뀐다. 그 문장은
  텍스트가 달라지므로 **TTS 도 다시 합성된다**(캐시 키가 달라진다).
- **📖 발음사전은 그대로 유효하다** — 숫자에도 걸린다(`10월→시월` · `6월→유월` · `1천→천` 같은 항목이
  실제로 일하고 있다. 한자어 불규칙 읽기는 모델이 틀리므로 이건 남겨 두는 게 맞다).
  이제 사전을 고치면 **그 문장이 실제로 재합성된다**(예전엔 캐시가 막았다).
- ⚠ **이 결론은 목소리 1개·현재 서버 기준이다.** 다른 목소리나 서버 교체 후 숫자 발음이 이상하면
  **위 측정법을 그대로 다시 돌려** 판단할 것(길이 비교 금지, ASR 전사 비교 + 판정력 검증).

### ⚠ 곁가지 정정 — 줄끝(CRLF) 관련 기존 메모가 오해를 부른다 (2026-08-25 실측)
- 이 저장소는 `.gitattributes` 에 **`* text=auto eol=lf`** 가 있어 **git 에 저장되는 blob 은 전부 LF** 다.
  실측: `HEAD:main.js` · `HEAD:flow-engine.js` · `HEAD:core/pipeline.js` · `HEAD:tts/tts-manager.js`
  모두 **crlf=0**. 즉 작업 사본이 CRLF 여도 git 이 커밋할 때 정규화하므로 **diff 가 부풀지 않는다.**
  → 위 항목들의 "CRLF 로 저장하면 diff 가 4천 줄로 부푼다" 는 경고는 **사실이 아니다.**
  실제로 신경 쓸 것은 **CRLF 를 전제한 테스트**(원문을 `\n` 으로 잘라 찾는 단언)뿐이다.
- 🔴 **`grep -c $'\r' <파일>` 로 줄끝을 재지 말 것.** 이 셸(Git Bash)에서 `$'\r'` 이 전개되지 않아
  **글자 `r`** 을 세고, **어떤 파일이든 "CRLF = 전체 줄 수"** 라는 그럴듯한 거짓을 준다.
  이번에 그 거짓을 믿고 **LF 파일 3개를 CRLF 로 바꿔 썼다**(내용은 같아 커밋엔 영향 없었지만 헛일).
  → 반드시 node 로 잰다: `(s.match(/\r\n/g)||[]).length` 와 `(s.match(/(?<!\r)\n/g)||[]).length`.
- `scripts/gen-manifest.js` 는 **LF 정규화 후 sha1**(라인 53 `normalizeLF`)을 낸다 → 작업 사본 줄끝이
  무엇이든 매니페스트 해시는 같다. **매니페스트를 검증할 때도 LF 기준으로 비교할 것**(raw 바이트로
  비교하면 멀쩡한 매니페스트를 "불일치" 로 오판한다 — 이번에 한 번 그랬다).

## 📕 출판 판권지 = 실물 단행본 판권 레이아웃 (2026-08-24, v0.3.42)
> 로이: "출판 PDF 에서 판권을 `D:\책스캔\하루한장고전수업\20260824_110032.jpg` 처럼 만들고 싶어."
>   (비즈니스북스 『하루 한 장 고전 수업』 판권 실물 스캔)
- 🔑 **레퍼런스에서 읽어낸 구성**(위→아래): 책제목(볼드) → **발행 이력**(`1판 1쇄 발행  2022년 11월 22일`,
  라벨 안 굵고 구분선 없음) → **「굵은 라벨 ｜ 값」 행**(지은이·발행인·편집인·발행처·등록·주소·전화·팩스·
  대표메일·홈페이지·블로그·페이스북) → **ISBN**(구분선 없이 값 + 부가기호를 띄워 붙임) →
  **별표 고지문**(`* 잘못된 책은 …`) → **테두리 박스**(투고 안내). 판면 **상단**에서 시작.
  · 2글자 라벨(등록·주소·전화·팩스)은 **3글자 폭에 균등 분산**(`등  록`) — flex 로 쪼개 글꼴·자간과 무관하게 정확히 맞춘다.
- **원고 규약(추가 규약 최소)**: 새 메타 라벨 `편집인·대표메일·블로그·페이스북·인스타그램`(→ extra 로 새지 않는다) ·
  **발행일에 `;`** 를 쓰면 쇄 이력이 여러 줄(`> 발행일: 1판 1쇄 발행 2022년 11월 22일; 1판 6쇄 발행 …`,
  라벨/값은 **첫 연도 4자리 앞**에서 가른다. 라벨을 안 쓰면 「초판 1쇄 발행」) ·
  `[판권]` 자유문의 **목록/별표 줄 = 고지문**, **인용(`>`) 블록 = 테두리 박스**.
  · 🔑 **판권 안의 목록을 불릿(ul)으로 조판하면 점이 붙어 본문처럼 보인다** → 별표를 글자로 찍고 내어쓰기.
- **조판 패널에 「판권 배치」(위/아래) 신설** — 기본 **위**(레퍼런스·실물 단행본 다수). 구 앱 최종본
  스타일(하단, `margin-top:44%`)이 필요하면 「아래」로. `colophonAlign` → pageCss 가 `.cp-wrap` 을 덮어쓴다.
- 🔴 **덤으로 잡은 것 — 판권 첫 쪽에 러닝헤드·쪽번호가 찍히고 있었다.**
  · 🔑 **`@page display` 는 vivliostyle 에서 「조각의 첫 쪽에 한 쪽 늦게」 적용된다**(실측으로 확정 —
    `@page display { background: #ffdddd }` 를 넣고 쪽별 픽셀을 찍어 보니 **판권 둘째 쪽부터만** 분홍).
    특이도 문제가 아니다(`@page display:left/:right` 를 함께 적어도 그대로) — 이름+의사클래스는
    다른 이유로 필요하니 남겨 뒀다. 문서 첫 쪽(반표제지)은 앞에 이름 변경이 없어 정상 적용된다.
  · ✅ 우회 = **러닝헤드 문자열을 비운다** — 판권 맨 앞에 `<span class="cp-rhclear">`(display:none)를 두고
    `string-set: chapter-title "", book-title "", book-subtitle "", sec-title ""`. 헤더 밑줄은 v0.2.11 에서
    **글자 밑줄(text-decoration)** 로 바꿔 뒀으므로 글자가 비면 줄도 안 그려진다.
  · ⚠ **뒤 판권에만 넣는다** — 앞 판권(속표지 뒷면)에 넣으면 **본문 러닝헤드가 통째로 사라진다**(테스트가 단언).
  · ⏳ **쪽번호는 남는다**(같은 원인, 문자열로는 `counter(page)` 를 못 지운다). 쪽번호가 거슬리면
    조판 패널에서 쪽번호를 「숨김」으로 두거나, 판권을 「아래」 배치로 두면 덜 눈에 띈다.
    ⚠ 같은 계열로 **`@page :blank` 도 안 먹는다**(recto 강제로 생긴 백면에 러닝헤드가 찍힌다) — 별건.
- **검증**: `npm run test:book` = 파서 44 + **book-md 114**(+35: 새 메타 라벨 5 · 발행 이력 4 · 라벨 행 순서 4 ·
  2글자 분산 3 · ISBN 3 · 별표 고지문 3 · 테두리 박스 2 · 러닝헤드 억제 2 · 배치 2 · @page 1 · 배선 5) +
  PDF 스모크 39쪽 + 앱 E2E.
  🔑 **A/B 역검증 3건**: ⓐ 목록→고지문 변환을 빼면 **#100 실패** ⓑ 2글자 분산을 빼면 **#94 실패**
  ⓒ 러닝헤드 비우기를 빼면 **#105 실패**.
  **실측 조판**(레퍼런스 원고를 그대로 옮겨 46판으로 PDF 생성): 러닝헤드 0 · 발행 이력 2줄 ·
  라벨 행 13 · 별표 고지문 3 · 테두리 박스 1 — **레퍼런스와 같은 그림**(눈으로 대조).
  회귀: makeall 무음 E2E 18/18(main.js 수정) · caption 102 · timestamps 53+E2E · 실원고(서론_고린도전서) 조판 무변화.
- ⚠ **로이가 알아 둘 것**: ① 판권 항목이 아주 많으면(46판 + 전 항목 + 박스 + ⓒ) **ⓒ 두 줄이 다음 쪽으로
  넘어간다** — 신국판처럼 큰 판형이면 여유가 있고, 넘치면 `> 저작권:` 을 짧게 쓰거나 고지문을 줄이면 된다.
  ② ⓒ 블록은 레퍼런스엔 없지만 **유지**했다(편역서 편집 저작권 표기 — v0.2.11 목표 최종본에 있던 것).
  ③ ePub 판권은 이 조판을 쓰지 않는다(리플로우 형식이라 자유문 그대로 — 의도).

## ✂ 자막 「끊어 읽기」 — 「것」으로 시작하는 줄 + 너무 짧은 줄 (2026-08-24, v0.3.39 → v0.3.41)

### 🟠 v0.3.41 — 2차 신고 「너무 짧은 자막이 생성된다」
> 로이: "다만, 살림, 여러분, 예, 그리고" + "287줄은 문장의 끝부분만 남았네. 의미전달에 약점이 발생하잖아."
- ⚠ **로이 설정은 14자가 아니라 20자다.** 스크린샷의 가장 긴 줄로 역산해 확인했다(`어느 날부터 여러분은
  사람이 아니라 손이 됩니다.` = 20자). 앞선 v0.3.39 측정의 「14자」는 그 시점 이미지 기준이었다.
  **다음에 자막 신고가 오면 설정값부터 역산할 것** — 폭이 다르면 재현이 안 된다.
- 🔑 **원인은 「하한이 없다」 하나**였고, 옛 규칙 두 개가 그걸 증폭했다(둘 다 v0.1.x 부터 있던 것):
  · **접속부사는 무조건 단독 줄** → `그리고`·`다만`·`그래서`·`그런데` 가 통째로 한 줄
  · **쉼표에서 무조건 세그먼트 분리** → `살림,` `내외 사이,` `그 집 문,` 이 각각 한 줄
  두 규칙 다 **분리를 강제**해서 DP 가 합칠 방법이 없었다(세그먼트를 먼저 쪼개고 각각 DP 를 돌렸다).
- 🔑 **287줄 `하셨습니다.` 는 계산이 아슬아슬해서 진 것이다** — `…끌었다고`(18자)/`하셨습니다.`(5자) 가
  `…아버지 소를`(14자)/`끌었다고 하셨습니다.`(9자) 를 **0.8점 차로** 이겼다(`고$` 연결어미 +3 때문).
  `TAIL`(0.3) 만으로는 못 막는다. → 하한 페널티가 있어야 뒤집힌다.
- ✅ **해결 = 세그먼트 분리를 걷어내고 문장 전체를 한 번에 DP + 하한 페널티**
  · `minCharsFor(maxChars) = max(4, round(maxChars×0.4))` — 20자→8 · 14자→6 · 7자→4.
  · `SHORT: 6` → 하한보다 d 글자 모자라면 `6d²`. `그리고`(3자, 하한 8) = **150 > 줄 하나(100)** →
    한 어절짜리 줄을 만드느니 붙인다.
  · 쉼표·접속부사는 **강제 분리 → 선호(점수 4)** 로 바뀌었다. 짧아지지 않는 한 그 자리에서 끊는다.
  · ⚠ **하한은 지향이지 강제가 아니다** — 문장 자체가 짧으면(`서운해합니다.` 6자) 그대로 한 줄이다.
- **실측(20자 = 로이 설정, 6개 폴더 대본 · 31,472문장)**:
  | | v0.3.38(원래) | v0.3.40 | **v0.3.41** |
  |---|---|---|---|
  | 자막 줄 수 | 57,688 | 57,699 | **49,966 (-13.4%)** |
  | 🟠 짧은 줄(8자 미만) | 8,595 | 10,205 | **1,476 (-86%)** |
  | 🔴 금지 위반 | 1,081 | 0 | **0** |
  · 상한 위반 0 · 어절 손실 0. 남은 1,476건은 **문장 자체가 짧은 경우**다.
  · 🔑 **짧은 줄은 원래도 8,595건 있었다** — v0.3.40 이 만든 문제가 아니라, 끊어읽기 규칙이 들어가며
    분리가 늘어 **눈에 띄게 된 것**이다(10,205 로 19% 증가).
- 검증: `test:caption` **102/102**(`[1-b]` 2차 사고 회귀 13건 신설 — 접속부사·쉼표 열거문·고아 꼬리·
  「짧은 문장은 그대로 둔다」). 🔑 **A/B ⓓ**: `SHORT` 를 빼면 **2건 실패**(짧은 줄 2.9%→4.8%).
  회귀: timestamps·makeall 18/18·merge 18/18·book 전체 · 앱 부팅 `v0.3.41` 오류 0.
- ⚠ **자막 줄이 13% 줄어든다** — 한 줄이 더 오래 떠 있고 덜 자주 바뀐다(상한 20자는 그대로라 줄이
  길어지는 게 아니라 **짧은 줄이 합쳐진 것**). 너무 안 바뀐다고 느끼면 자막 글자수를 낮추면 된다.

### (1차) 의존명사 「것」으로 시작하는 줄 — v0.3.39 → v0.3.40
> 로이: "vrew에서 자막을 생성하게 되는데, 끊어읽기가 너무 안된다… 대표적으로 「것」, 85번 줄을 보면
>   '것'으로 시작하잖아. 이러면 자막을 보는 사람들에게 긍정적인 피드백을 받기가 어렵겠지."
> ⚠ **TTS 아니다 — 자막 줄바꿈만의 문제다.** 음성은 문장 단위로 합성되고, 자막 줄은 그 하나의 음성을
>   `sourceIn/sourceOut` 으로 **시간만 잘라** 보여준다(vrew-builder:994, 글자수 비율 분배).
>   그래서 이 수정으로 **음성은 하나도 안 바뀐다** — 💾 .vrew 만 다시 뽑으면 새 자막이 나온다(재합성 없음).

### 🔑 원인 — 목적함수가 「길이 균형」이었다
- 옛 알고리즘이 아는 것은 **① 어절 안 쪼갬 ② 줄 길이를 고르게(최대 줄길이 최소화)** 둘뿐. 문법은 안 봤다.
- 로이가 신고한 그 분할이 왜 하필 거기였나 — 실측: `셈하지 않게 되면 다음부터는 받는`(**14자**) /
  `것이 아니라 가져가는 것이 됩니다.`(**14자**). **균형이 완벽해서 최적해로 뽑힌 것이다.**
  → **설정값(자막 글자수)으로는 못 고친다.** 10·12·13·14·15·16 전부 돌려봤고 12자에서는 오히려 더 나빠졌다.
- 🔴 **구현이 두 벌이었다** — `core/caption-splitter.js`(.vrew·SRT·프리미어)와
  `renderer/src/lib/captions.js`(앱 화면·미리보기)가 같은 알고리즘의 **복사본**. 한쪽만 고치면
  「화면에서 본 줄」과 「실제 .vrew 에 들어간 줄」이 조용히 갈라진다.

### ✅ 해결 — 경계마다 금지/점수를 매겨 DP 목적함수에 넣는다
| | 규칙 | 사례 |
|---|---|---|
| **금지** | 다음 줄이 **의존명사**로 시작(것·데·수·때·줄·뿐·만큼·대로…) | **`받는 ｜ 것이`** |
| **금지** | 앞 줄이 **관형사**로 끝남(그·이·두·모든·어떤) | `그 ｜ 조건을` |
| **금지** | **보조용언** 사이(`-아/-어/-게/-지/-고` + 보다·주다·하다…) | `맡아 ｜ 본` |
| **금지** | **부정부사**(안·못) 뒤 ｜ **수사 + 단위명사** 사이 | `꿈쩍도 안 ｜ 하는` · `관직 네 ｜ 자리` |
| **강한 감점** | **관형절 + 꾸밈받는 명사**(8×15=120 > 줄 하나 100) | **`본 ｜ 사람에게서`** |
| **약한 감점** | **부사** 뒤(8×3=24 < 줄 하나 100 → 다른 자리가 있을 때만 피한다) | `바로 ｜ 셈을` |
| **선호** | 쉼표·접속부사(4) > 연결어미·부사격조사(3) > 주격·목적격(1) | |

### 🔑 v0.3.40 — 「이걸로 어색함이 다 해소되나?」 에 답하며 찾은 것
- ⚠ **v0.3.39 의 「사고 39건」은 순환 논리였다** — **내가 만든 규칙으로 잰 값**이라 규칙 밖은 안 보인다.
  그래서 **다른 잣대**(부정부사·수사+단위·부사·한음절 종결)로 다시 재 규칙 밖 잔여를 찾았다. 그 결과:
  · **부정부사 13건**·**수사+단위 4건** → 명백해서 **금지**로 추가.
  · **보조용언 `있던`·의존명사 `동안` 누락** 발견(`와 ｜ 있던`, `여덟 해 ｜ 동안`) → 목록 보강(-던 형 일괄).
  · **부사 493건** → 금지는 과하다(`함께 ｜ 읽어보겠습니다` 는 자연스럽다) → **약한 감점**.
- 🔴 **부사가 오히려 우대받고 있었다** — `바로·서로·스스로`는 `로$`, `함께`는 `께$` 라 **부사격조사 규칙
  (RE_GOOD3)에 걸려 +3점**을 받았다. 493건의 진짜 원인이 이것. → **부사 검사를 RE_GOOD3 앞에** 둔다.
- 🔑 **감사기를 「금지 위반」과 「감점 자리」로 분리**했다(`auditCaptionLines` 가 `banned` 를 함께 반환).
  섞어 세면 부사 감점이 늘어난 것을 「사고가 늘었다」로 오독한다(실제로 39→108 로 보여 한 번 헷갈렸다).
- 가중치 `{VIOL:250, LINE:100, GOOD:8, BAL:0.5, TAIL:0.3}` — **금지를 피하려면 줄 하나를 더 쓴다.**
  `TAIL` 은 마지막 줄에도 약한 균형 페널티(없으면 `됩니다.` 만 남는 고아 줄이 생긴다 — 실측).
- **렌더러를 core 로 통합**: `renderer/src/lib/captions.js` 는 이제 **re-export 4줄**뿐.
  vite root 가 `renderer` 라 root 밖 CJS 를 쓰려면 **두 설정이 다 필요**하다 —
  `build.commonjsOptions.include: [/node_modules/, /[\\/]core[\\/]/]`(⚠ include 를 쓰면 기본값을 덮으므로
  node_modules 를 **함께** 나열) + `server.fs.allow: ['..']`(dev 서버).

### 🔴🔴 발행 직전에 잡은 사고 — **앱 화면이 백지였다**
- core 파일 끝의 「직접 실행 시 자기검사」 블록이 **렌더러 번들에 실렸고**, commonjs 변환이 그 CJS 런타임
  참조를 브라우저로 내보내 `require is not defined` → **`#root` 가 비어 h1 이 0개**(실측).
  `typeof require !== 'undefined'` 가드를 넣어 뒀는데도 **소용없었다**(변환기가 그 앞에서 이미 참조를 만든다).
- 🔑 **빌드 통과·단위테스트 통과로는 안 잡힌다** — `test:timestamps` 의 **앱 E2E 가 h1 을 못 찾아** 드러났다.
  → 자기검사 블록을 **삭제**하고, 테스트가 **번들과 core 양쪽에 그 참조가 없는지** 단언한다(#5).
  ⚠ core 를 렌더러가 쓰는 한 이 규칙은 계속 유효하다 — **core/*.js 에 CJS 런타임 참조를 두지 말 것.**

### ⚠ 이 방식의 본체는 「오탐 관리」다 — 실제로 다섯 번 밟았다
1. `에게서` 가 조사 목록에 없어 **`본 ｜ 사람에게서` 를 못 잡았다**(`에서$`·`에게$` 만 있었다).
2. 보조용언 어간을 `^보` 로 찾아 **활용형 `본`**(보+ㄴ, 한 음절로 합쳐짐)을 놓쳤다.
3. 그래서 초성+중성만 비교했더니 **`침대`→「치우다」·`죽을`→「주다」·`말도`→「말다」·`이름만`→「있다」** 로 오판.
   → **활용형 명시 목록(`AUX_FORMS`)** 으로 바꿨다. 못 잡는 쪽이 엉뚱한 곳을 묶는 것보다 낫다.
4. `조건을`(ㄹ받침)을 **관형형으로 오판** → `을/은` 끝은 제외 + ㄴ/ㄹ 경로는 1~2음절로 좁힘.
5. **쉼표 검사가 관형사·보조용언보다 뒤**라 `네, ｜ 빨간색이에요`·`백여 년 전, ｜ 캐나다` 를 사고로 신고.
   → **쉼표·접속부사 뒤는 어떤 판정보다 먼저** 「정당」으로 반환한다.
- 자립 동형어가 흔한 의존명사(바·등·판·적·지·편·쪽·성·양·체·만·차)와 관형사 `네` 는 **일부러 뺐다.**
- ⛔ **형태소 분석기(mecab-ko 등)는 안 쓴다** — deps 가 늘면 **라이트 업데이트가 막히고 설치본 재배포**가
  필요하다(아내 PC 포함). 아래 수치가 규칙 기반으로 충분함을 보여준다.

### 검증
- `npm run test:caption` = **`test/caption-break.test.js` 83/83 신설**(core 원문을 require 해 실행).
  실사고 2문장 회귀 · **금지 16건** · **오탐 회귀 17건**(위 함정이 전부 단언으로 박혀 있다) ·
  **부사는 금지가 아닌 약한 감점** + **조사 규칙보다 먼저 검사되는지** · 기존 규칙 보존(어절 무손실·
  글자수 상한·쉼표·접속부사·기본값 7) · 단일 구현 배선 · 번들 검사.
- 🔑 **A/B 역검증 3건**: ⓐ `boundaryAt` 을 무력화하면 **20/66 실패** ⓑ 렌더러를 옛 복사본으로 되돌리면
  **5건 실패** ⓒ 부사 검사를 RE_GOOD3 뒤로 옮기면 **6건 실패**.
- **실측 — 5개 채널 대본 72편 · 문장 31,472개** (`[6]` 이 매번 다시 잰다. 대본 폴더가 없는 PC 는 건너뜀):
  | 자막 글자수 | 🔴 금지 위반 | 🟡 감점 자리 | 자막 줄 수 |
  |---|---|---|---|
  | **14자(로이 설정)** | 2,471 → **0 (-100%)** | 2,521 → 108 | 72,166 → 72,519 (**+0.49%**) |
  | 7자(기본값) | 6,910 → 610 (-91.2%) | 6,256 → 2,195 | 127,735 → 130,819 (+2.41%) |
  · 어절 손실·변형 0 · 글자수 상한 위반 0.
  · **14자에서 금지 위반이 0**이다. 남은 108건은 전부 감점 자리(관형절 41 · 부사 67) — **다른 선택지가
    없을 때만 밟는 곳이라 결함이 아니다.**
  · 7자가 덜 개선되는 건 **폭이 좁아 피할 자리가 없어서**다. 로이 설정은 14자다.
- 회귀 전량 통과: timestamps 53+E2E · **makeall 무음 E2E 18/18** · tts 18+31 · visual 14/10/13/7 ·
  delmedia 27 · comfy 41/43/30/14/69 · flow 46 · grok 81 · styles 40/40/22/17 · merge 104+18 · book 전체.
- **앱 실부팅 확인**: `h1` 1개 · `#root` 정상 · **화면 오류 0건** · 헤더에 `v0.3.39`.

### ⚠ 로이가 알아 둘 것
- **이미 만든 .vrew 는 안 바뀐다.** 그 편을 열고 **💾 .vrew 를 다시 뽑으면** 새 자막으로 나온다.
  **TTS 재합성은 필요 없다**(음성 파일을 그대로 쓴다).
- 자막이 편당 **약 0.5% 늘어난다**(더 자주 바뀐다). 그 대신 「것」·「사람에게서」로 시작하는 줄이 사라진다.
- 남은 108건은 **금지가 아니라 감점**이다 — 다른 선택지가 없을 때만 밟는다. 더 줄이려면 자막 글자수를
  조금 올리면 된다(폭이 넓을수록 좋은 자리를 고를 여유가 생긴다).
- ⚠ **규칙 기반의 한계는 남는다** — 목록에 없는 형태의 어색함은 계속 나온다. 로이가 자막을 보다 어색한
  곳을 발견하면 그 유형을 규칙에 추가하는 방식으로 늘려 간다(오탐 회귀를 반드시 함께 박을 것).
  · 판정 자료를 만드는 법: `OLD.splitCaptionLines` vs 현재로 **바뀐 문장만** 전/후 표로 뽑아 보여준다
    (2026-08-24 에 다산_0830 으로 328문장 대조표를 만들어 로이에게 전달했다).

## 🔴🔢 「무제한」으로 바꿔도 45에서 막힌 것 — 한도 판정이 **두 곳**이고 카운터도 **두 개**였다 (2026-08-24, v0.3.38)
> 로이: (0=무제한으로 바꾼 뒤에도) `🛑 계정 flow-mr2xrx49 오늘 45회 — 계정당 하루 한도(45회) 도달` / "왜 안되는거지?"
> 로그 안에 **모순**이 그대로 찍혀 있었다: 앱 스토어는 `오늘 3/45`, anti-detect 는 `이 계정 오늘 45회`.

### 🔎 실측 — 어느 카운터가 맞나 (앱이 틀렸다)
| | 값 |
|---|---|
| `~/.priming-maker/flow-accounts.json` (앱) | **3** |
| `~/.flow-app/anti-detect-state.json` (엔진) | **45** |
| 오늘 출력폴더에 실제로 생긴 이미지 파일 | **68장**(Flow + Genspark) |
- 앱 로그의 매핑 실적을 전부 세면 `1/1 · 1/20 · 1/21 · 0/21 …` = 합계 3. **그런데 그 실행의 출력폴더에는 21개가 있다.**
- 🔑 **범인은 `mapOnce` 의 반환값이었다.** 2.5초 폴링(`setInterval`)이 먼저 복사해 두면 최종 호출에서는
  `if (g.imagePath && …) return;` 로 **스킵**되므로, `mapOnce(true)` 는 **"마지막 호출에서 새로 복사한 수"**
  만 돌려준다. 그 값을 `markUsed(acc.id, made)` 에 넣고 있었다 → 실제 20장을 만들어도 **1로 기록**.
  · 그래서 **앱의 일일 한도가 사실상 작동하지 않았고**(3으로 세니 45에 영원히 안 닿는다) UI 도 「오늘 3/45」로 거짓을 보여 줬다.
  · ✅ `copiedTotal` 을 따로 누적해 `markUsed` 에 넘긴다(폴링이 복사한 것까지 포함). 로그도 그 값을 쓴다.
  · ⚠ v0.2.20 의 「실제 성공분만 카운트」 수정은 방향은 맞았지만 **폴링과의 상호작용을 못 봤다.**
- 🔑 **anti-detect 의 45가 맞다** — `registerGenerationSuccess()` 는 `_saveImage` 에서 **파일을 쓴 직후** 불린다.
  즉 계정 1은 오늘 진짜 45장을 만들었고, **하루 한도 도달은 정당했다.**

### 🔴 판정이 두 곳이라 UI 값이 무시됐다
- `flow-engine.js` 의 **`PER_PROFILE_DAILY_CAP = 45` 하드코딩**이 진짜 차단선이었다(2곳: run 진입 · 작업 중간).
  `main` 은 `antiDetect: { enabled:true, preset:'기본' }` 만 넘기고 **한도를 안 넘겼다** → 엔진은 자기 상수를 썼다.
  → ⚙ 설정의 「일일 한도」를 0(무제한)으로 바꿔도 **45에서 막힌다.** 한도 칸은 하나인데 판정이 두 곳 = 반드시 어긋난다.
- ✅ **`_perProfileCap()` 신설** — `this.antiDetect.dailyLimit`(= UI 값)이 유효하면 그것, 없으면 상수 폴백.
  `main` 이 `dailyLimit: cap` 을 넘긴다. **0이면 무제한**(판정 자체를 건너뛴다).
  · ⚠ 상수는 **폴백 한 곳에서만** 값으로 쓰인다 — 테스트가 `PER_PROFILE_DAILY_CAP > 0` 같은 **직접 비교가
    다시 생기지 않는지** 단언한다(#44).
- 🔑 **`anti-detect.js` 의 `dailyLimit`(기본 50)은 차단선이 아니다** — `onLimitReached` 기본이 `'warn'` 이라
  경고만 한다. 그래서 v0.3.37 에서 "50은 안 막는다" 고 한 것은 맞았지만, **flow-engine 의 별도 상수 45를
  놓쳤다**(그게 실제로 막던 것). 이제 둘 다 같은 UI 값을 본다.

### 🔴 로그가 거짓을 말했다 — `res` 가 undefined 였다
- 하루 한도 도달 경로가 `this.send('done', …); return;` 으로 **빈 return** 이었다 → `main` 의 `res` 가 `undefined`
  → `res && res.rateExhausted` 가 false → `made === 0` 분기로 떨어져
  **`⚠ 생성 0장 — 다음 계정으로 (Flow UI 문제일 수 있음…)`** 이 찍혔다. 실제로는 정당한 하루 한도였다.
  (이 문구 때문에 "UI 가 바뀌었나" 를 또 의심하게 된다 — v0.3.36 과 같은 계열의 오도.)
- ✅ **객체를 반환한다**: `{ rateExhausted:true, reason:'daily-limit', profileCount:_pc, dailyCap:_cap, … }`.
  main 이 `reason === 'daily-limit'` 을 따로 잡아
  `🛑 Flow 계정 "계정 1" 오늘 45장 — 계정당 하루 한도(45장) 도달. 자정에 초기화됩니다.` +
  「한도를 늘리려면 ⚙ 설정 → 👤 계정 … 다만 과하게 쓰면 구글이 계정을 차단할 수 있습니다」를 함께 남긴다.

### 검증
- `npm run test:flow` = **46/46**(+11). `[2-b]` 한도 출처 5건(UI 20→20 · **0→0** · 값 없으면 폴백 45 ·
  비숫자 폴백 · 큰 값 존중) + `[3]` 배선 6건(UI 값 전달 · 누적 카운트 · daily-limit 로그 구분 ·
  **객체 반환** · 상수 직접 비교 없음 · flow-engine CRLF 유지).
  🔑 **A/B 역검증 2건**: ⓐ `_perProfileCap` 을 상수만 보게 되돌리면 **#26·#27·#30·#44 실패**(42/46)
  ⓑ `made` 를 `mapOnce(true)` 로 되돌리면 **#41 실패**.
- 회귀: **makeall 무음 E2E 18/18**(main.js 를 만졌으므로 필수).
- ⚠ `flow-engine.js` 는 CRLF 파일이라 **넣는 개행도 CRLF** 로 맞췄다(4187/4187 확인).
  ⚠ 패치 중 main.js 에 CRLF 6개가 섞여 들어간 것을 발견해 LF 로 정규화했다(main.js 는 전체 LF 규약 — 테스트가 단언).

### ⚠ 로이가 알아 둘 것
- **계정 1은 오늘 진짜 45장을 썼다.** 하루 한도 도달은 정상이고 **자정에 초기화**된다.
  더 쓰고 싶으면 ⚙ 설정 → 👤 계정에서 한도를 올리거나 0(무제한)으로 두면 이제 **실제로 먹는다.**
  ⛔ 다만 그 45는 임의의 숫자가 아니라 **구글 차단 예방선**이다(flow-engine 주석: 한 계정을 하루에 과하게 쓰면
  「비정상 활동」으로 차단된다). 무제한으로 두면 계정을 잃을 위험을 사용자가 지는 것이다.
- **앱 UI 의 「오늘 N/한도」는 오늘 것만 어긋나 있다**(3으로 기록됐다). 새 코드는 정확히 세므로 다음 실행부터
  맞고, 자정에 어차피 리셋된다. 굳이 손으로 고칠 필요는 없다.
- Flow 를 오늘 더 쓰려면: 한도를 올리거나 · 구독 있는 계정을 추가하거나 · **🖥 ComfyUI 로컬(장당 23.2초, 무료)**.

## 🔢 일일 한도 「0=무제한」이 Flow 에서만 안 먹던 것 — 라벨이 거짓이었다 (2026-08-24, v0.3.37)
> 로이: "일일한도 「0=무제한」 표시가 있는데 0 입력이 안되."
- 🔑 **UI 는 정상이었다** — `⚙ 설정 → 👤 계정` 의 입력은 `<input type="number" min="0">` + 「0=무제한」이고,
  IPC(`set-flow-cap` 등)도 값을 **그대로** 스토어에 넘긴다(clamp 없음). 문제는 스토어였다.
- 🔴 **Flow 만 독립 구현이라 0 을 이중으로 막고 있었다**(`core/flow-accounts.js`):
  ```js
  c.dailyCap = Math.max(1, parseInt(n, 10) || 45);   // ①  parseInt('0') || 45 → 0 은 falsy 라 45
                                                     // ②  통과해도 Math.max(1, 0) = 1
  ```
  실측: `flow: 0 입력 → 45` / `genspark: → 0 ✅` / `grok: → 0 ✅`.
  Genspark·Grok 은 **`core/accounts.js` 공용 스토어**(`makeAccountStore`)를 쓰고 그쪽은 v0.2.20 에
  이미 0 을 허용하도록 고쳤다. **Flow 는 그때 손대지 않았고**, v0.3.21 에서 세 서비스를 한 탭으로 통합하며
  「0=무제한」 라벨이 Flow 에도 붙어 **라벨이 거짓이 된 순간**이 생겼다.
- 🔴 **저장만 되게 고치면 무제한이 아니라 「전부 차단」이 된다** — 두 번째 결함:
  `_available` 이 `countToday(c,id) < c.dailyCap` 이라 **dailyCap 0 이면 모든 계정이 한도 초과**가 된다.
  → `if (_cooling) return false; if (!(c.dailyCap > 0)) return true;` 로 캡을 아예 보지 않게 했다.
  ⚠ **무제한이어도 쿨다운은 존중한다** — 그건 앱이 정한 캡이 아니라 **엔진이 Flow 의 실제 한도·차단
  메시지를 본 뒤** 쉬는 것이다(테스트 #10 이 이걸 단언).
- **로그도 함께 고쳤다**(0 이면 거짓을 말한다): `오늘 3/0` → **`오늘 3/무제한`**(`capTxt`) ·
  소진 요약의 `a.used >= cap` 은 cap=0 일 때 **항상 참**이라 「오늘 한도 도달」로 오판했다 → `cap > 0 &&` 가드.
- 🔎 **덤 — anti-detect 의 50회는 차단선이 아니다**(확인함). `anti-detect.js` 의 `dailyLimit` 기본 50 이지만
  `onLimitReached` 기본이 **`'warn'`**(막지 않음)이고 main 도 `'stop'` 을 넘기지 않는다. 그래서 앱 캡을 0 으로
  두면 **실제로 무제한**이고, 50회를 넘으면 `⚠️ 이 계정 일일 한도 50회 초과 … 계정 안전 주의` 가 **한 번만**
  뜨고 계속 진행한다. (로그의 `남은 한도 50회` 는 그 경고선까지 남은 횟수 표시일 뿐이다.)
- **검증**: `npm run test:flow` = **35/35**(+15). `[1-b]` 무제한 8건(0 저장 · 999회 써도 살아 있음 ·
  쿨다운은 존중 · 문자열 `'0'` · 음수/문자/빈값은 45 로) + `[1-c]` **세 서비스 정책 일치**(flow·genspark·grok
  각각 `setCap(0)` → 0 그리고 999회 후에도 활성).
  🔑 **A/B 역검증 2건**: ⓐ `setCap` 을 옛 코드로 되돌리면 **#8·#9·#11·#12·#14 실패**(30/35)
  ⓑ `_available` 의 무제한 한 줄을 빼면 **#9·#15·#34 실패**(32/35).
  회귀: **makeall 무음 E2E 18/18**(main.js 를 만졌으므로 필수).
- ⚠ **한도를 0 으로 두면 앱은 더 이상 막지 않는다** — 계정 순환이 「실제로 실패할 때까지」 한 계정을 계속 쓴다.
  Flow 쪽 한도·차단 메시지를 만나면 그때 30분 쿨다운으로 넘어간다(기존 동작). 계정을 아껴 쓰고 싶으면
  숫자를 넣는 편이 안전하다.

## 🔴🔑 Flow 계정을 추가했는데 이미지가 안 나온 것 — 구독 없는 계정은 **앱 화면 자체가 안 열린다** (2026-08-24, v0.3.36)
> 로이: "flow에서 1번계정 이미지 생성을 다해서 더이상 못만든다하여 2번계정을 추가했는데, 진행이 안되네."
> 로그가 `[01] 오류: locator.waitFor: Timeout 10000ms exceeded ... div[role="textbox"]` 뿐이라
> **"Flow UI 가 바뀌었나" 를 의심하게 된다 — 아니었다.** 계정 권한 문제였고, 앱이 그걸 말해 주지 않았다.

### 🔎 실측으로 갈라낸 것 (전제부터 틀렸다)
- 🔴 **1번 계정은 한도를 다 쓴 게 아니었다** — `flow-accounts.json` 실측 `n=3 / dailyCap=45`.
  **30분 쿨다운** 중이었다(08:31 에 0/21 실패 → `cooldown(30)`). 그런데 로그는 그냥
  `⚠ 모든 Flow 계정 시도/소진` 이라 적어 **"한도 소진"으로 읽힌다.**
- 🔴 **2번 계정은 Flow 앱에 들어가지도 못한다.** 앱이 쓰는 프로필로 Chrome 을 띄워 확인:
  | | 화면 | 「새 프로젝트」 | 입력칸 |
  |---|---|---|---|
  | 1번(PRO 배지) | 프로젝트 목록 | ✅ 있음 | 프로젝트 진입 후 1개 |
  | 2번(신규) | **소개(마케팅) 페이지** | ❌ 없음 | **0개** |
  2번 화면 실측 문구: `Try in Google Flow`(버튼 9개) · `Pricing` · **"Google AI 구독을 살펴보세요"** ·
  `Features may vary by Google AI subscription tier`. 즉 **Google AI 구독(Pro/Ultra)이 없으면**
  `labs.google/fx/ko/tools/flow` 가 앱이 아니라 소개 페이지로 열린다. **로그인은 성공한다**(쿠키도 저장된다)
  — 그래서 앱은 "이 계정으로 이미지 생성 가능합니다" 라고 잘못 알렸다.
- ✅ **Flow UI 는 안 바뀌었다.** 1번 계정으로 프로젝트에 들어가면 `div[role="textbox"][contenteditable="true"]` 1개,
  에이전트 버튼, `🍌 Nano Banana 2 Lite`, `만들기` 버튼 모두 정상. **셀렉터는 손댈 필요가 없었다.**
- 🔑 **다음에 "Flow 가 안 된다" 가 오면 이 순서로 본다**: ① `~/.priming-maker/flow-accounts.json` 의
  `n`·`dailyCap`·`cooldownUntil` ② 그 계정 프로필(`~/.flow-app/profiles/<id>`)로 Chrome 을 띄워
  **홈에 「새 프로젝트」가 있는지** ③ 있으면 눌러 들어가 입력칸이 있는지. ②에서 소개 페이지면 구독 문제다.

### 🔴 진짜 결함 — 앱이 조용히 실패했다
- `flow-engine._createNewProject()` 가 **버튼을 못 찾으면 아무것도 안 하고 넘어갔다**(로그도 없다):
  ```js
  if (btn && await btn.isVisible()) { await btn.click(); ... }   // else 가 없다
  ```
  그러면 홈에 머문 채 다음 단계로 가고, 홈에는 입력칸이 없으니 **10초 기다렸다 타임아웃**한다.
  로그의 `[Flow] 에이전트 버튼 못 찾음` 도 같은 원인이었다(홈이라 그 버튼도 없다) — **결정적 단서인데
  "UI 변경 시 셀렉터 점검 필요" 라고 적혀 있어 오히려 오도했다.**
- ✅ **`_diagnoseNoProjectButton()` 신설** — 버튼이 없으면 화면을 읽어 이유를 갈라 **던진다**:
  · 소개 페이지 신호(`Try in Google Flow|Create with Google Flow|구독을 살펴보|Pricing`)이고 「새 프로젝트」가
    없으면 → `err.flowNoAccess = true` + `이 계정은 Flow 를 쓸 수 없습니다 — Google AI 구독(Pro/Ultra)이 없어
    소개 페이지만 열립니다. 계정을 더 추가해도 구독이 없으면 생성되지 않습니다. …🖥 ComfyUI 로컬(무료)·Genspark 로 바꾸세요`
  · 로그인 화면이면 → `⚙ 계정에서 「🔑 로그인」을 다시` (구독 문제로 **오진하지 않는다**)
  · 낯선 화면이면 → `UI 변경 가능` + **URL·화면 텍스트를 함께** 남긴다(다음 사람이 셀렉터를 고칠 근거)
  · 🔑 클릭에 성공해도 `/project/` 로 안 넘어가면 경고를 남긴다(**막지는 않는다** — 느린 전환일 수 있다).
- ✅ **main 이 `flowNoAccess` 를 구분해 잡는다** → 그 계정을 **12시간** 쉬게 하고(`cooldown(12*60)`)
  `⛔ Flow 계정 "flow1" — 이 계정은 Flow 를 쓸 수 없습니다 …` 를 찍는다.
  ⚠ 예전 문구 `⚠ 생성 0장 — 다음 계정으로 (Flow UI 문제일 수 있음…)` 는 **이 경우엔 거짓**이라 안 쓴다.
  ⚠ 계정을 **지우지는 않는다** — 구독을 넣으면 그대로 다시 쓰인다.

### 🐞 덤 — 로그가 거짓을 말하고 있었다
- `🔑 Flow 계정: 계정 1 (오늘 **undefined**/100)` — `pickNext()` 가 원본 `{id,label}` 을 그대로 돌려줘
  `acc.used` 가 없었다(`list()` 만 used 를 붙였다). → pickNext 도 `used`·`available`·`coolUntil` 을 붙인다.
- 계정이 다 소진되면 **각 계정이 왜 못 쓰이는지** 함께 찍는다:
  `· 계정 1: 쉬는 중 — 오전 9:01:13 이후 재사용 (오늘 3/45)` / `· flow1: 이번에 시도했으나 0장 (오늘 0/45)`.
  이게 없어서 로이가 "한도를 다 썼다" 로 읽었다.

### 검증
- `npm run test:flow` = **`test/flow-account.test.js` 20/20 신설.**
  🔑 **원문 메서드를 실제로 돌린다** — `Object.create(FlowAutomator.prototype)` 로 인스턴스를 만들고
  `page.evaluate(fn)` 을 「가짜 document 를 심고 콜백을 그대로 실행」으로 스텁했다(브라우저 없이 원문 판정 실행).
  계정 스토어는 **USERPROFILE 을 임시 폴더로 갈아끼워** 실제 `~/.priming-maker/flow-accounts.json` 을 건드리지 않는다.
- 🔑 **A/B 역검증 2건**: ⓐ `pickNext` 를 옛 코드로 되돌리면 **#2 실패** ⓑ `_createNewProject` 를 「조용히 넘어감」
  으로 되돌리면 **#9·#10·#11·#18 실패**(16/20).
- 🔑 **실제 브라우저 E2E**(둘 다 돌렸다): 새 계정 → **0.0초**에 `flowNoAccess=true` + 그 안내문(예전엔 10초 타임아웃) ·
  기존 계정 → 정상으로 `/project/423a6180…` 진입.
- 회귀: **makeall 무음 E2E 18/18**(main.js 를 만졌으므로 필수) · comfy 41/43/30/14/69 · grok 81/0.
- ⚠ **flow-engine.js 는 전체가 CRLF 다**(main.js 와 다르다). 라인 기반으로 교체하며 **줄끝을 보존**했다
  (4167/4167 확인). 이 파일을 편집할 때 LF 로 저장하면 diff 가 4천 줄로 부푼다.

### ⚠ 로이가 알아 둘 것 (코드로는 해결 못 하는 부분)
- **Flow 멀티계정으로 한도를 늘리려면 각 계정에 Google AI 구독이 있어야 한다.** 무료 계정을 몇 개 추가해도
  소개 페이지만 열린다. 지금 2번 계정(`flow1`)은 그 상태다 — 구독을 넣거나 계정을 지우는 게 맞다.
- **1번 계정은 멀쩡하다**(쿨다운만 걸렸다). 다만 오늘 아침 실적이 **20장 중 1장 · 21장 중 1장**으로
  거의 실패했는데, 그 상세는 **파일 로그에 안 남아 원인을 확정하지 못했다**(엔진 상세 로그가 화면에만 나간다).
  Flow 크레딧 소진이 유력하나 **추론이다**(홈에 "일일 보너스: 8월 31일까지 추가 크레딧 50개" 배너가 있다).
  ⏳ 다음에 재발하면 그때 화면 로그를 보내 주면 갈린다.
- **로컬 ComfyUI 가 지금 장당 23.2초로 정상**(v0.3.35)이라 급할 때 대안이 된다 — 무료·한도 없음.
- ⚠ 조사 중 1번 계정에 **Flow 프로젝트 2개가 새로 생겼다**(`18d76f50…`, `423a6180…`). 앱도 실행마다 하나씩
  만들므로 무해하지만, 정리하려면 Flow 홈에서 「프로젝트 삭제」로 지우면 된다.

## 🔴🐌 이미지 1장이 28초 → **498초(17.6배)** · 타임아웃 실패 — 원인 2개 (2026-08-24, v0.3.35)
> 로이: "이미지생성에 갑자기 너무나도 오랜시간이 소비되고 있는데, 원인 설명과 해결해줘."
> 앱 로그엔 "느리다"는 흔적이 없다(성공/실패만 찍힌다). **ComfyUI 서버 로그 + 포트 + RAM** 을 봐야 갈렸다.
- 🔑 **다음에 "이미지가 느리다" 가 오면 이 순서로 본다**(이번에 이 순서로 30분 안에 갈렸다):
  ① `~/.shots-maker/logs/comfy-server.log` 의 `Prompt executed in N seconds` 와 **스텝 시간(`s/it`)**
  ② `netstat -ano | grep :818` → **8188 말고 다른 포트에 ComfyUI 가 또 떠 있나**
  ③ `Get-CimInstance Win32_OperatingSystem` 의 **FreePhysicalMemory**(여유 RAM) ④ `nvidia-smi`.
  ⚠ 앱 로그만 보면 `✗ G1 실패: 타임아웃 (300초)` 뿐이라 "엔진 한도·오류·결제" 를 의심하며 헛다리를 짚는다.

### 🔴 원인 ① ComfyUI 가 **두 벌** 떠 있었다 — 놀면서 RAM 12.4GB 점유
- 실측: `8188` = 우리 런처(pid 21732, 07:21 부팅 기동) · **`8189` = Comfy Desktop 이 11:29 에 띄운 것**(pid 12140).
  8188 이 이미 점유돼 있어 그쪽이 **8189 로 밀렸다**(`portConflict:auto`) — v0.3.30 에 적어 둔 경고 그대로다.
- 🔑 **그 서버는 큐가 `running 0 · pending 0` 인데 RAM 12.36GB · VRAM 7.3GB 를 붙들고 있었다**(= 놀면서 점유).
  → 시스템 RAM 여유 **31.9GB 중 2.2GB**(페이지파일 7.4GB 사용).
- 그래서 느려진 메커니즘: Krea2 는 `12,530MB Staged` **dynamic VRAM loading** — 모델을 RAM 에 올려두고 VRAM 으로
  흘려보낸다. 올릴 RAM 이 2.2GB 뿐이니 **페이지파일로 스왑** → 스텝 2.48s/it → **27.61s/it**, 첫 스텝 **299.97초**.
  `Prompt executed in 498.28 seconds` → 앱의 300초 타임아웃에 걸려 실패.
- ✅ 조치: 8189 서버 + Comfy Desktop 종료. **RAM 여유 2.2 → 16.8GB · VRAM 11,513 → 1,696MB · 온도 72 → 49°C.**
  생성이 **타임아웃 실패 → 42초 성공**으로 즉시 복귀.

### 🔴 원인 ② fp8 파일이 로컬에 생기자 **자동 대체가 멈춰** 에뮬레이션 경로로 떨어졌다
- v0.3.22 의 모델 자동 대체는 **"그 서버에 파일이 없을 때"** 만 동작한다(`pickSubstitute` 첫 줄이
  `if (allowed.includes(current)) return null`). 그런데 로컬 UNET 목록에 **`krea2_turbo_fp8_scaled` 가 새로 생겼다**
  (예전엔 `int8_convrot` 뿐이었다 — v0.3.22 기록) → **대체가 조용히 멈추고** 워크플로가 요구한 fp8 을 그대로 썼다.
- 🔑 **"파일이 있다 ≠ 이 GPU 에서 빠르다."** 서버 로그가 근거를 그대로 찍는다:
  `Native ops: asym_w4a8_int8, int8_tensorwise, convrot_w4a4` / **`emulated ops: mxfp8, float8_e5m2, float8_e4m3fn, nvfp4`**
  → 3060(Ampere)엔 fp8 텐서코어가 없다(Ada/RTX40 부터). **fp8 은 소프트웨어 에뮬레이션.**
- **A/B 실측**(같은 프롬프트·워크플로·2장씩): `fp8_scaled` **44.0초** vs `int8_convrot` **25.4초** = **1.73배**.
  (int8 의 25.4초는 v0.3.22 기록 28.7초와 일치 = 원래 성능)
- ✅ `core/comfy-models.js` 에 **`isFp8NativeGpu` · `pickFasterQuant` · `applyFasterQuant`** 신설 —
  로컬이고 GPU 가 fp8 네이티브가 **아닐 때만** 같은 모델의 int8/convrot 판으로 바꾼다.
  · 🔑 **기존 `baseKey`/`precToks` 를 재사용**한다 — `z_image_turbo_fp8 → z_image_int8` 같은 **모델 정체성이
    바뀌는 대체는 여전히 거부**한다(turbo·distilled 는 정밀도 토큰이 아니다. 테스트 #16 이 이걸 단언).
  · 🔑 **GPU 이름을 모르면 아무것도 안 한다**(fail-open). ComfyUI 가 native/emulated 목록을 API 로 안 주므로
    이름으로 판단할 수밖에 없다 — 낯선 이름에 손대면 "조용히 다른 모델로 그린다" 가 된다.
  · **클라우드에선 하지 않는다**(최신 GPU 는 fp8 이 더 빠르다). 되돌리려면 설정에 `preferFastQuant:false`.
  · 판단은 **세션당 1회** — `/object_info/<노드클래스>`(수 KB, 전체 목록은 수 MB 라 안 받는다) + `/system_stats`
    를 첫 장에서만 조회하고 기억한다(두 번째 장부터 왕복 0).
  · 실측 로그: `⚡ NVIDIA GeForce RTX 3060 는 fp8 을 하드웨어로 못 돌립니다 — 같은 모델의 빠른 판으로: … → int8_convrot`
    ⚠ ComfyUI 는 GPU 이름을 `cuda:0 NVIDIA GeForce RTX 3060 : cudaMallocAsync` 로 준다 → 사람이 읽을 부분만 남긴다.

### ✂ 덤 — 이미지에 안 쓰이는 LLM 프롬프트 확장 가지를 걷어낸다
- Krea2 워크플로 `image_krea2_turbo_t2i (2).json` 은 프롬프트를 **`TextGenerate`(Qwen3VL 4B, 512토큰 자기회귀)**
  로 부풀려 CLIP 에 넣는 체인을 갖는다. 그런데 `_buildWorkflow` 는 **`CLIPTextEncode.text` 를 리터럴로 덮어쓰므로**
  그 결과는 **아무도 쓰지 않는다.** 그런데도 **`PreviewAny`(출력 노드)** 가 그 가지를 붙들어 ComfyUI 가 실행한다.
- ✅ `pruneToImageOutputs(graph)` — **이미지 출력 노드(SaveImage·PreviewImage)에서 역방향 도달 가능한 노드만** 남긴다.
  실측: 22노드 → **12노드**(TextGenerate·PreviewAny·StringConcatenate·ResolutionSelector 등 10개 제거),
  CLIP·VAE·UNET·LoRA·KSampler·SaveImage 는 전부 보존.
  · 🔑 **제목·클래스 이름 추측에 기대지 않는다** — 이 워크플로의 스위치 제목은 `Boolean (Refine Prompt?)` 라
    비디오 쪽 `prompt enhance` 정규식(v0.2.63)에 **안 걸린다**. 도달 가능성만 본다.
  · 🔑 **이미지 출력 노드를 못 찾으면 아무것도 안 한다**(fail-open — 낯선 워크플로를 망치지 않는다).
  · **프롬프트를 우리가 덮어쓴 경우에만** 걷어낸다(안 덮어썼다면 그 체인이 실제로 프롬프트를 만든다).
- ⚠ **내 첫 진단 정정**: "장당 14초 낭비" 라고 했는데 **틀렸다.** 서버 로그를 보면 `Generating tokens` 는
  **첫 장에만** 찍힌다 — TextGenerate 의 **입력이 매번 같아서**(우리가 CLIP 을 덮어쓰므로 그 앞단 문자열은 불변)
  **ComfyUI 노드 캐시가 적중**한다. 즉 실제 이득은 **첫 장 약 14초 + 캐시 메모리**뿐이다.
  프루닝 전후 warm 시간이 42.5초로 같은 것이 그 증거다(프루닝만으로는 안 빨라진다 — 원인 ②가 본체였다).

### 🔎 재발 방지 — `core/comfy-perf.js` 신설(로이 승인)
- 로컬 생성 직전(`ensureLocalComfy` 뒤, VRAM 정리 앞)에 **이미지·비디오 두 경로 모두** 진단해 로그로 알린다:
  · **`scanRivals`** — 우리 포트 +1~+6 에 ComfyUI 가 또 떠 있는지(`/system_stats` 의 `comfyui_version` 으로 확인).
    ⚠ **우리 포트는 스캔하지 않는다**(자기 자신을 신고하면 안 된다 — 테스트 #32).
  · **RAM 여유 8GB 미만** 경고(실측 기준선: 2.2GB=17.6배 느림 / 16.8GB=정상).
  · 문구는 **무엇을 해야 하는지**까지 적는다(`… 로컬로 만들 거라면 그 창을 닫으세요`).
- 🔑 **절대 막지 않는다 — 경고만 한다.** 오판으로 작업을 멈추게 하는 것이 더 나쁘다. 진단이 터져도 `try/catch` 로 삼킨다.

### 검증
- `npm run test:comfy` = 모델 41 + **`test/comfy-perf.test.js` 43 신설** + 파이프라인 30 + GPU 레인 14 + 2분할 E2E 69.
  🔑 **A/B 역검증 2건**(둘 다 실제로 돌렸다): ⓐ `isFp8NativeGpu` 를 항상 null 로 무력화하면 **#1·#10·#19 실패**
  ⓑ `pruneToImageOutputs` 를 no-op 으로 만들면 **#23·#26·#27·#28 실패**(39/43).
- 회귀: **makeall 무음 E2E 18/18**(main.js 를 만졌으므로 필수) · comfylaunch 46/46 · visual 14+10+13+7.
- **실측 전후**(같은 PC·같은 워크플로·warm):
  | 상태 | 장당 |
  |---|---|
  | 사고 당시(ComfyUI 두 벌) | **300초 타임아웃 실패** |
  | 중복 서버만 제거 | 42.5초 |
  | + fp8 → int8 자동 교체 | **23.2초** |
- ⚠ **로이가 알아 둘 것**: 로컬로 이미지를 만들 거면 **Comfy Desktop 창을 열지 않는다.** 열면 8189 로 서버가 하나 더
  떠 RAM·VRAM 을 먹는다(부팅 자동 실행이 이미 8188 을 쥐고 있다). 이제는 앱이 그 상황을 로그로 알려 준다.
- ⚠ **설정 확인 권장**: `comfy-image-config.json` 의 `activeServer` 가 `"comfy.org"` 인데 `cloud:false`·
  `baseUrl:127.0.0.1:8188` 이라 **실제로는 로컬로 나간다**. `activeServer` 는 v0.3.22 이후 **표시용 라벨일 뿐**이고
  실제 전송은 `cloud` 가 정한다. 클라우드로 보내려면 헤더 「② 이미지」 드롭다운에서 **☁** 를 고를 것
  (클라우드는 장당 12~18초 — 로컬 23.2초보다 빠르지만 크레딧을 쓴다).

## 💬 출판 — HTML 주석이 본문에 인쇄되던 것 + "내용이 끝까지 안 나온다" 조사 (2026-08-23, v0.3.34)
> 로이: 「고린도전서 주해」 원고(`주해_고린도전서.md`) 미리보기 캡처 + "내용이 끝까지 나오지 않네".
- 🔴 **고친 것 — `<!-- … -->` 가 본문 문단으로 인쇄됐다.** 이 원고의 **마지막 줄**이
  `<!-- ↓↓↓ 여기서부터 페리코페 주해를 순서대로 누적한다 (① 1:1-9 부터) ↓↓↓ -->` 라
  책의 마지막 쪽에 그대로 찍혔다. 마크다운 규약상 주석은 출력되지 않는다.
  · 파서에서 줄 단위로 건너뛴다(여러 줄 주석은 `-->` 나올 때까지). `inlineMd` 에도 안전망 —
    **esc 보다 먼저** 지운다(esc 뒤엔 `&lt;!--` 가 되어 못 잡는다).
  · 🔑 **코드펜스 처리 뒤에 둔다** — 이 원고의 §2 「페리코페별 주해 작성 양식」은 `````` 안에 양식 예시를
    담고 있고 그 안에도 주석이 있을 수 있다. 예시 안의 주석은 **예시의 일부**라 보존해야 한다(테스트가 단언).
- 🔎 **"내용이 끝까지 안 나온다" — 조사 결과 누락이 아니었다(사실).**
  · 원고 = 장 2개(§1 일러두기 18블록 · §2 양식 3블록). **앱 미리보기에서 §2 까지 정상 조판**된다
    (실측: 13쪽, 마지막 두 쪽이 §2). PDF 도 마찬가지 — **§2 에 문단 40개를 부풀리니 12쪽 → 23쪽**
    으로 늘어 §2 가 실제로 조판되고 있음을 확증했다(누락이면 쪽수가 안 변한다).
  · 🔑 **끝처럼 보이는 이유**: §1 이 `---`(✻ 장면 전환)로 끝나고, `chapterStart: 'recto'` 기본값 때문에
    §2 는 **백면(빈 쪽) 하나를 건너뛰고** 홀수쪽에서 시작한다 → 빈 페이지를 보고 문서가 끝났다고 읽기 쉽다.
    백면이 싫으면 조판 패널의 「장 시작」을 `다음 쪽`(page)으로 바꾸면 된다.
  · ⚠ 로이 화면의 쪽번호(§1 끝이 13쪽)와 내 재현(§1 끝이 10쪽)이 다르다 = **조판 설정이 다르다**.
    쪽수가 갈리면 원인 추적이 어긋나므로, 다음에 같은 신고가 오면 **미리보기 상단 「N / M쪽」의 M** 을 먼저 묻는다.
- **검증**: `npm run test:book` = 파서 44 + **book-md.test.js 79**(+5) + PDF 스모크 + 앱 E2E.
  🔑 A/B 역검증: 주석 제거를 무력화하면 **#75 실패**. 실원고 재조판 — 주석 잔재 0 · 12쪽 · 양식 예시 보존.

## 🗂 출판 — 작업용 장을 **원고는 두고 책에서만** 뺀다 + ePub 이 조판 옵션을 무시하던 것 (2026-08-23, v0.3.33)
> 로이: "출판용이야" → 바로 위 항목의 ⏳ 로 남겨 뒀던 5번(제작메모 필터)을 진행.
> 설교 원고는 33회 강해 내내 갱신되는 **작업 문서**라 사람이 매번 손으로 지우는 방식은 지속 불가능하다.
- 🔑 **자동 판별하지 않는다 — 사용자가 고른다.** "이건 작업용이네" 하고 앱이 알아서 지우면 **조용히 틀린다**
  (이 저장소가 가장 싫어하는 실패). 이미 있던 「구조 패널 체크 해제 = 원고 보존한 채 출력에서만 제외」를
  **본문 장까지** 넓혔다 — 추가 규약 0, 원고 수정 0, 클릭 한 번.
- **① 본문 장 출력 제외** — 구조 패널의 장 목록에 체크박스. 해제하면 본문·목차에서 빠지고(취소선 표시)
  다시 체크하면 복원된다. `layout.excluded` 에 저장되므로 원고별로 유지된다.
  · 🔑 **키는 장 번호가 아니라 `ch:<장 제목>`** — 번호로 잡으면 앞에 장을 하나 추가하는 순간 **엉뚱한 장이
    사라진다**. 제목이 바뀌면 제외가 풀려 다시 나오는데, 그건 **눈에 보이는 실패**라 조용히 사라지는 것보다 낫다.
  · 부(部) 안의 장이 전부 빠지면 **부 표제지도 생략**(빈 표제지만 남지 않게).
  · 🔑 **원고(BookModel)는 불변** — 조판이 원고를 깎으면 다음 조판이 어긋난다. 테스트가 이걸 단언하고,
    E2E 는 체크 해제 후 **실제 .md 파일에 그 장이 남아 있는지** 확인한다.
- **② 작업용 파일 경로 → 파일명만**(`hidePaths`, 조판 패널, **기본 OFF**) —
  `../참고문헌/지도교수/오광만_박사논문2008_….pdf` 처럼 종이에선 찾아갈 수 없는 경로가 한 줄을 통째로 잡아먹는다.
  폴더만 떼고 파일명은 남긴다(무엇인지는 알 수 있게).
  · 🔑 **슬래시 + 확장자로 끝나는 코드스팬만** 건드린다 — `A/B 실험` 같은 평범한 글자를 자르면 안 된다.
    외부 주소(http)도 그대로 둔다. **기본 OFF** 인 이유: 내용을 바꾸는 일은 사용자가 켜서 하게 한다.
- 🔴 **작업 중 드러난 기존 결함 — ePub 은 조판 옵션을 아예 안 받고 있었다.** `main.js` 의 `book-build-epub` 이
  `bookLayoutOpts` 를 호출조차 안 해서 **구조 패널 제외·영상 대본 모드가 ePub 에서 전부 무시**됐다
  (= 종이책과 전자책 내용이 갈린다). `epub-builder` 도 `excluded` 를 몰랐고 `blocksXhtml` 이 `scriptFilter` 를
  안 태웠다. → `chapterExcluded`·`scriptFilter` 를 html-builder 에서 **export 해 공유**하고 main 이 옵션을 넘긴다.
  ⚠ 두 렌더러가 각자 판정하면 반드시 갈린다 — 판정 함수는 **한 곳에** 둘 것.
- **검증**: `npm run test:book` = 파서 44 + **book-md.test.js 74**(+26) + PDF 스모크 + 다중파일 + **앱 E2E**.
  · E2E 신설: 실제 앱에서 **장 체크박스를 눌러 14쪽 → 12쪽**, 다시 체크하면 복원, **원고 .md 는 불변**,
    경로 축약 체크박스 존재·기본 OFF.
  · 🔑 **A/B 역검증 2건**: ⓐ 장 제외를 무시하게 되돌리면 **#53 실패** ⓑ hidePaths 를 무력화하면 **#65 실패**.
  · 실원고 실측(§0 진행 현황 + ✅ 체크리스트 제외 · 경로 축약 ON):
    **장 14→12 · 표 7→6 · 목차 14→12줄 · 내지 45쪽 → 39쪽 · 진행표 흔적 0 · 긴 경로 0 · 원고 불변** ·
    **ePub 도 장 12개로 같은 내용**(zip 을 열어 확인 — 이게 ①②가 ePub 에 실제로 먹는다는 증거).
- ⚠ **§11 「본 권 강해 주요 주석 목록」은 안 뺐다** — 참고문헌 성격이라 출판물에도 의미가 있다.
  그 안의 파일 경로만 ② 로 줄어든다. 장째로 빼고 싶으면 구조 패널에서 체크를 해제하면 된다.

## 📖 출판 조판이 표·목록·링크를 몰랐다 + 일반 원고를 「회차 파일」로 오판 (2026-08-23, v0.3.32)
> 로이: 설교 원고 `D:\말씀이있는교회\설교준비\서론_고린도전서.md` 를 출판 탭에서 열었더니 첫 쪽이 이상하다 →
>   진단해 보니 "불필요한 게 섞인" 정도가 아니라 **조판이 구조적으로 어긋나** 있었다. 원인 2개를 고쳤다.
- 🔴 **① 파일 종류 오판 — 모든 헤딩이 한 단계 밀렸다.** `detectBookFileKind` 가 규칙에 하나도 안 걸리면
  마지막 폴백으로 `'chapter'`(구 앱 회차 파일)를 돌려줬고, `normalizeChapterFile` 이 **헤딩을 전부 +1** 밀었다.
  실측 결과: **장 1개 · meta 빈 객체 · 35KB 원고 전체가 한 장**.
  · `# 책 제목` → **장 제목**이 되고(스크린샷의 그 큰 글씨) · `## 0.~12.` 14개 섹션 → **소제목(h3)** 으로 강등 ·
    `meta.title` 이 비어 **반표제지·속표지에 파일명 `서론_고린도전서`** 가 찍히고 저자·출판사는 공백 ·
    장이 1개뿐이라 **목차·짝수쪽 러닝헤드가 통째로 무너진다**.
  · ✅ 규칙 신설: **`# 제목` + `## 섹션` 2개 이상 = native.** 구 앱 회차 파일은 H1 이 `제N회`(그 위 규칙에서
    이미 걸린다)이거나 H1 이 아예 없으므로(평문 제목 승격) 이 규칙에 안 걸린다 → **기존 삼국지 세트 무영향**.
    head 스캔도 4000 → 20000자(표가 긴 원고는 H2 가 4000자 밖으로 밀린다).
  · 실측: 수정 후 **장 14개 · 제목 「고린도전서 강해 — 서론」 복구 · 목차·러닝헤드 정상**(PDF 로 눈으로 확인).
  · 🔑 **덤 — 제작 메타가 저절로 본문에서 빠진다.** `> 작성일:`·`> 신학 기준:` 같은 줄이 이제 첫 헤딩 앞의
    메타 영역에 들어가 `meta.extra` 로 흡수된다(본문 인쇄 안 됨). 그 값은 **판권 페이지에만** 나가는데
    이 원고엔 `## [판권]` 이 없어 판권 페이지 자체가 생성되지 않는다 → 아무 데도 안 나온다.
- 🔴 **② 조판기가 표·목록·링크를 몰랐다 — 원고 317줄 중 207줄(65%)이 깨졌다.**
  `blockHtml` 에 `table`·`list` 케이스가 없고 `inlineMd` 가 링크를 몰라서: **표 109줄**이 공백으로 이어붙은
  한 문단이 되고(33행 진행표가 통짜 덩어리), **불릿 85 + 번호 13줄**도 한 문단, **링크 7줄**은
  `[텍스트](주소)` 원문 그대로 인쇄됐다.
  · **표**: `| a | b |` + **구분행 `|---|---|` 이 있을 때만** 표로 본다(파이프 든 평문 오인 방지).
    정렬행 `:---:` 해석 · 이스케이프 `\|` 는 셀 구분 아님 · 셀 수가 모자란 행은 머리행 기준으로 채움.
    🔑 **`thead { display: table-header-group }` 으로 쪽을 넘으면 머리행을 반복** — 33행 계획표가 여러 쪽에
    걸쳐도 어느 열인지 읽을 수 있다. **vivliostyle 이 실제로 지원한다**(40행 표로 실측: 7쪽 17행 → 8쪽에
    머리행 재출력 후 18행부터). `tr { break-inside: avoid }` 로 행이 쪼개지지 않게.
  · **목록**: `- / * / + / 1. / 1)`, **들여쓰기 2칸 = 한 단계**(중첩 `ul/ol`), `- [x]` 체크박스(☑/☐),
    들여쓴 비항목 줄은 앞 항목에 이어붙임. 체크박스 항목은 `li.task { list-style:none }` — **☑ 옆에 불릿까지
    찍히면 두 겹으로 보인다**(첫 실측에서 실제로 그렇게 나왔다).
  · **링크**: 종이책이라 주소는 버리고 **글자만** 남긴다. 외부 주소(http)만 글자와 다를 때 괄호 병기.
    🔴 **주소에 괄호가 든다** — `[` + 백틱경로 + `](../개역개정4판(구약+신약).txt)` 가 실제로 있다.
    `\(([^)\s]*)\)` 로 짜면 첫 `)` 에서 끊겨 **`.txt)` 가 본문에 남는다** → 한 겹 중첩까지 받는 정규식으로.
  · 🔑 **`span.code` 스타일이 아예 없었다** — 생성은 하는데 CSS 가 없어, 긴 파일 경로가 body 의
    `word-break: keep-all` 때문에 안 끊겨 **그 줄이 양쪽정렬 대공백으로 찢어졌다**(실측 확인) →
    `overflow-wrap: anywhere`.
  · ePub(`epub-builder.js`)도 같은 블록을 렌더한다 — 안 맞추면 종이책과 전자책 내용이 갈린다.
- **검증**: `npm run test:book` = 파서 44 + **`test/book-md.test.js` 48 신설** + PDF 스모크 + 다중파일 +
  **앱 E2E**(부팅 → 조판 → 목차 쪽번호 ?? 없음 → 클릭-편집 왕복). 신설 테스트는 **모듈을 직접 require** 해
  앱 원문을 그대로 돌린다(복사본을 두면 앱과 갈라져도 통과한다).
  · 🔑 **A/B 역검증 2건**: ⓐ `git stash` 로 수정 전 코드를 돌리면 **#1 단언부터 실패**한다
    ⓑ `case 'table'` 한 줄만 빼면 **#36 「표가 <table> 로 나간다」가 실패**한다(§6 이 헛단언이 아님을 확인).
  · 실원고 실측: **표 7개(102행) · 목록 18개(98항목, 중첩 20 · 체크박스 9) · 파이프 남은 문단 0 · 링크 원문 0 ·
    내지 PDF 45쪽**. 롱폼(영상) 경로는 `stripBookSections`/`isReservedHeading` 무변경이라 무영향(확인).
- ⏳ **안 한 것(로이 결정 대기)**: 「출판용 제작메모 필터」 — §0 진행 현황(33행 트래커·상태 전부 '예정')·
  ✅ 새 권 시작 체크리스트·로컬 파일 경로 같은 **작업용 내용**은 조판이 아니라 **원고의 성격** 문제라 손대지 않았다.
  영상 대본 모드의 `scriptFilter`(🎯📝🎨 제작메모 제거)와 같은 방식으로 옵션을 넣을 수 있다.
  ⚠ 원고에서 직접 지우는 방식은 권하지 않는다 — 이 문서는 33회 강해 내내 갱신되는 작업 문서다.

## 📥 통합본 「자산 이어받기」 — 이미 만든 TTS·이미지·비디오를 물려받는다 (2026-08-22, v0.3.31)
> 계획서: `docs/통합본-자산이어받기-계획.md`(실측 근거 전부 거기 있다). 대상: 고전서재 빨간머리앤 14부 → 통합본 3개.
- **문제**: 통합대본(각 부 본문을 **글자 그대로** 이어붙인 .md)을 그냥 「만들기」하면 **12.3시간 분량을 통째로 재합성**한다
  (RTF 0.455 → GPU 약 5.6시간). 파일 12,304개는 각 부 `tts-1/` 에 멀쩡히 있는데 **앱이 그것을 가리킬 방법이 없었다.**
  · 🔴 **TTS 캐시로는 안 된다** — v0.3.29 가 키에 `rn`(refName)을 넣어 08-21 이전(=14부 생성 시점) 캐시가 전부 자연 무효화됐고,
    애초에 「🗑 TTS 삭제」 한 번에 `clearAll` 되는 저장소라 정본이 될 수 없다. 실측 적중률 1부 9% · 2부 0.8% · 배속 1.15 로는 **0**.
  · 🔑 **이미지는 사정이 다르다** — media-cache 가 기존 프롬프트 **40/40 = 100%** 적중이라 이어받기 없이도 앱이 복원한다.
    **이 기능의 본질은 TTS** 이고, 이미지·비디오는 캐시가 못 잡을 때의 안전망이다.
- ✅ **`core/merge-assets.js` 신설 + 「📥 이어받기」** — 대본에 `> 📥 자산출처: <소스.md> | <소스 출력폴더>` 줄을 적으면
  소스의 문장·그룹 자산을 통합본 작업폴더로 **복사**하고 경로·길이를 주입한다. **그 뒤는 기존 코드가 무수정으로 다 한다**
  (fillTtsList 는 음성이 있으면 건너뛰고, 이미지·비디오 엔진은 hasVisual 이면 건너뛰고, .vrew 게이트는 절대경로 실존만 본다).
  · `> 📥 …` 은 '>' 인용이라 **기존 파서가 조용히 버린다** → 구버전 앱에서 열어도 대본이 안 깨진다(하위호환).
  · **대본 열 때 자동 실행**(음성 없는 문장이 절반 넘을 때만 — 4천 건 재복사 방지) + 작업바 「📥 이어받기」 버튼으로 재실행.
  · ⛔ **.vrew 를 자르거나 합치지 않는다.** 그 수술은 files↔tracks↔assets↔clips id 재매핑·zIndex 재발번호·AI고지 절대시각
    재계산·40bit id 충돌 검사를 전부 손으로 재구현하는 일인데, **빌더는 문장 배열을 통째로 받아 그 불변식을 공짜로 보장**한다.
- 🔴 **밟기 쉬운 함정 4개 — 전부 실측에서 나왔고 테스트가 지킨다**:
  ① **원본을 참조하지 않고 복사한다.** `sweepBadVisuals` 가 이상 판정 파일을 `fs.rmSync` 로 **실제로 지운다** →
     원본을 가리키면 14부 산출물이 사라진다. 테스트가 「모든 경로가 작업폴더 안」 + 「원본 폴더 무변경」을 단언한다.
  ② **`ttsDurationSec` 를 반드시 함께 주입한다.** 없으면 빌더가 mp3 를 `size/6000` 으로 **약 4배 과대** 추정하고
     그룹 길이 합산에서는 **아예 빠진다** → 타임라인이 통째로 왜곡. 스냅샷 값 우선, 없으면 ffprobe. **길이를 못 구하면 연결하지 않는다**(재합성 유도).
  ③ **14부 스냅샷은 이름도 경로도 옛 것이다**(회차 대본이 개명됐다 — `[서재_0815] 1부` ↔ 현재 `[서재_0823] 1부`) →
     스냅샷은 **문장 지문**으로 찾고(raw 문자열 1차 거르기 후 JSON.parse — 스냅샷 271개), 경로는 **basename 만 취해 현재
     출력폴더로 재기반 + 실존 확인**. 실측: 소스 7개 중 5개가 지문 매칭으로 살아났고 94개 자산 경로가 전부 복원됐다.
  ④ **그룹 매칭은 num 이 아니라 `imagePrompt`** — 도입부 재배치(regroupIntro)가 그룹 번호를 재발번호한다. 문장은 텍스트 매칭.
- 🔑 **매칭은 「전방 커서」다** — 소스를 순서대로 이어붙인 평탄 배열 위에서 앞으로만 나아간다(이진탐색 위치색인).
  통합본은 소스 순서를 따르고 중간 섹션(`도입부`·`지난 이야기`·`마무리` 끝 2문장)만 빠지므로 선형·비모호하고,
  **같은 문장이 여러 번 나와도 서로 다른 음성에 순서대로** 붙는다. ⚠ 매칭 실패한 항목은 **커서를 안 움직인다**
  (실패 하나가 뒤 전체를 밀어내지 않게 — 테스트가 이걸 단언한다).
- 🔑 **매칭률 게이트 95%** — 미만이면 **멈추고 미매칭 목록을 팝업**으로 보여 준다. 이게 없으면 통합대본의 사소한 오탈자가
  **조용히 수 시간짜리 재합성**으로 이어진다(계획서 §5-1). 정규화는 「공백 접기 + trim」까지만 — 더 관대하게 하면
  서로 다른 문장이 같은 키가 되어 **엉뚱한 음성이 붙는다**(조용히 틀리는 쪽이 새로 만드는 것보다 나쁘다).
- 🔑 **부분 합성 상태를 덮어쓴다** — 이어받기 전에 새로 합성된 음성이 있으면(실제로 통합본1 폴더에 16개 있었다)
  「있으면 스킵」으로 만드는 순간 그 문장은 **영원히** 새 음성을 쓴다(F1 때문에 fillTts 도 건너뛴다). **무조건 복사** +
  확장자가 다른 옛 파일(`2.mp3` 옆의 `2.wav`)까지 정리한다.
- 🔑 **스냅샷에 경로가 빈 문장이 실제로 있다**(자동저장이 마지막 문장 합성 직전에 찍힌 경우 — 「통합본 1 문」 6번째).
  그때만 **번호 폴백**(`tts-1/<순번>`)을 쓰되, **번호가 순번과 일치하는 스냅샷에서만** 쓴다(합성 뒤 재분할돼 번호가
  밀린 스냅샷이면 엉뚱한 음성이 붙는다 — `_ordinalMatchesFilename` 이 막고 테스트가 확인한다).
- **검증(§7-0 시뮬레이션 재현 + §7-1 단위/E2E)** — `npm run test:merge`:
  · 🔑 **실데이터 회귀 통과**: 통합대본 `[서재_0822] 빨간머리앤 통합본 1 · 1~5부.md` →
    **문장 4,182/4,182(100%) · 그룹 이미지 80/150 · 비디오 6** = 계획서 §7-0 기대값과 **정확히 일치**.
    이어받는 음성 총 **4.52시간**. (G: 미연결·다른 PC 면 이 섹션만 자동으로 건너뛴다.)
  · 단위 **104/104**(`test/merge-assets.test.js` — 픽스처로 개명 스냅샷·옛 경로·중복 문장·오탈자·번호 폴백·번호 밀림 안전장치·
    복사 멱등·게이트를 전부 재현. main.js·preload.js·App.jsx 배선은 **원문 문자열 대조**).
  · 앱 **E2E 18/18**(`test/merge-assets.smoke.js` — 실제 Electron 부팅 → 임시 채널·임시 대본으로 **자동 이어받기가 실제로
    도는지** · DTO 까지 길이가 오는지 · 버튼 재실행 · 화면 오류 0).
  · 🔑 **A/B 역검증 2건**(둘 다 실제로 돌려 확인): ⓐ main.js 의 **게이트를 지우면** 테스트가 실패한다(103/104)
    ⓑ 복사를 **원본 참조로 바꾸면** 「작업폴더 안」·「부분 합성분 덮어쓰기」 단언이 실패한다.
  · 회귀: pipeline-mode 30 · visual-defect 14 · gpu-lanes 14 · delmedia 27 · channel-styles 40 · tts-gate 18 ·
    fs-retry 31 · timestamps 53 · **makeall 무음 E2E 18/18**.
- ⏳ **아직 안 한 것(로이가 할 차례)**: 계획서 §7-2·§7-3 — 실제 통합대본을 앱에서 열어 이어받기 → 💾 .vrew →
  **Vrew 에서 열기·재생·MP4 내보내기**. 통합본은 문장 4천/클립 8천 규모로 **이 앱 최대 실적(1,030문장)의 4배 = 미답 영역**이다
  (adm-zip 전량 메모리 · ZIP64 없음 4GB 한계 · 메인 프로세스 동기 → 빌드 중 화면이 멈춘 것처럼 보인다). 먼저 「문 + 1부」만으로
  작은 통합본을 만들어 Vrew 로드를 확인한 뒤 전체로 가는 것이 계획서 순서다.
- ⏳ **일부러 안 넣은 것**: 계획서 §5-2 의 「이어받기 전에 `regroupIntroByTtsDuration` 을 자동 실행」. 실측에서 **없이도
  기대값(비디오 6개 전부 매칭)이 나왔고**, 그룹 경계를 말없이 바꾸면 유튜브 챕터(⏱)가 달라진다. 필요하면 기존
  「🎬 도입부 TTS+10초 재배치」 버튼이 그대로 한다.
- ⚠ **통합대본 파일명은 소스와 겹치면 안 된다** — 스냅샷(.smproj)이 basename 키라 서로 덮어쓴다. 겹치면 로그로 경고한다.

## 🔴🖥 부팅 자동 실행이 **한 번도 서버를 띄운 적이 없었다** — Comfy Desktop 이 런처로 바뀜 (2026-08-22, v0.3.30)
> v0.3.27 의 ⏳「실제 재부팅 후 자동 실행 확인」을 이번에 실측했다. **실패였다.**
- 🔑 **증상은 "실행 안 됨"이 아니라 "실행됐는데 서버가 없음"이었다** — 여기서 헷갈리면 엉뚱한 곳을 고친다:
  · 부팅 **18분 뒤** `Comfy Desktop` 프로세스가 **8개 살아 있고** 창도 응답(v1.0.39)한다. 바로가기·대상 파일 정상.
  · 그런데 **그 PID 들이 연 포트가 0개**다(8188 리스너 없음). 열려 있던 9881·9893·8765 는 전부 무관한 pythonw.
  · 앱 로그는 `App started` → `user-tier` → `git bootstrap` **4줄에서 멈춘다**(오류조차 없다).
  · 결정적 증거: `%APPDATA%\Comfy Desktop\last-session.json` = **`{"kind":"dashboard"}`**.
    → v1.0.39 는 **대시보드(런처)** 로 뜨고, ComfyUI 서버는 **사람이 인스턴스를 클릭할 때만** 시작된다.
  · 즉 v0.3.27 의 전제(「앱을 띄우면 서버가 뜬다」)가 **버전 업으로 조용히 거짓이 됐다.** 방어선 2겹이 **둘 다** 무력:
    앱 쪽 `comfy-launch` 도 이미 떠 있는 앱을 또 spawn 할 뿐이라 150초 타임아웃으로 끝난다.
- ✅ **해결 = 앱 UI 에 의존하지 않고 서버를 직접 띄운다** — `comfy/comfy-server.pyw` 신설(로이 확정).
  Comfy Desktop 의 UI 가 또 바뀌어도 안 깨지고, OmniVoice·보이스디자인이 쓰는 **검증된 패턴과 같다**.
  · 시작프로그램 = 「**ComfyUI 서버 (Priming 이미지용).lnk**」 → `<install>\ComfyUI\.venv\Scripts\pythonw.exe -s <앱>\comfy\comfy-server.pyw`.
    옛 「ComfyUI (Priming 이미지용).lnk」(Comfy Desktop.exe)는 **제거**했다 — 두 개면 8188 을 서로 다툰다.
  · 앱 쪽(`core/comfy-launch.js`)도 **같은 런처**를 spawn 한다(진입점이 둘로 갈리면 다음 사람이 엉뚱한 쪽을 고친다).
    `findInstance`/`instancePython`/`launcherScript`/`resolveLaunch` 를 export 해 테스트가 원문을 직접 돌린다.
  · **경로 하드코딩 안 함** — `%APPDATA%\Comfy Desktop\installations.json` 이 정본이다(클라우드 항목 제외,
    `ComfyUI\main.py` 실재 확인, `lastLaunchedAt` 최신 우선). 다시 설치하거나 드라이브를 옮겨도 따라간다.
- 🔴 **함정 ① 실행 파이썬은 `ComfyUI\.venv` 다 — `standalone-env` 가 아니다.** 실측: standalone-env\pythonw.exe 로
  돌리면 `ModuleNotFoundError: No module named 'torch'` 로 즉사한다(그건 기반 인터프리터일 뿐이고 torch 는
  uv 가 만든 `.venv` 안에 있다 — `pyvenv.cfg` 의 `home` 이 standalone-env 를 가리킨다).
  `.venv\Scripts\pythonw.exe` → **torch 2.12.1+cu130 · cuda True**. 런처는 잘못된 파이썬으로 불려도
  **스스로 `.venv` 로 한 번 재실행**해 낫는다(`PRIMING_COMFY_REEXEC` 로 무한루프 방지) — 실측 확인.
- 🔴 **함정 ② pythonw 는 콘솔이 없어 `sys.stdout` 이 무효다.** 그 상태로 ComfyUI 의 tqdm·logging 이 출력하면
  **포트조차 안 열리고 조용히 죽는다**(v0.2.95 OmniVoice 에서 이미 밟은 그것). → **main.py 를 불러오기 전에**
  진짜 파일 핸들로 바꾼다(`~/.shots-maker/logs/comfy-server.log`, 5MB 넘으면 새로 씀).
  ⚠ 그 로그 파일은 앱의 7일 보관 청소에 **안 걸린다**(정규식이 `YYYY-MM-DD.log` 만 지운다 — 확인함).
- 🔴 **함정 ③ 모델 경로 yaml 을 반드시 넘긴다** — `instance-model-paths\<id>.yaml`(Comfy Desktop 이 만든다).
  안 넘기면 모델 목록이 비어 「로컬 ComfyUI 에 모델 'krea2_turbo…' 가 없습니다」로 생성이 통째로 실패한다.
  `--disable-auto-launch` 도 필수(없으면 뜰 때마다 **브라우저가 열린다**). 입·출력 폴더는 settings.json 값을 따라간다.
- 🔑 **셸을 안 거친다**: `runpy` 로 **같은 프로세스에서** main.py 를 돌린다(자식 프로세스 0). bat 을 끼우면
  cmd.exe 가 자식을 기다려 **검은 창이 서버가 사는 내내 남는다**(v0.2.94 실측). 런처는 포트를 먼저 확인해
  **이미 떠 있으면 아무것도 안 한다**(두 번 띄우면 뒤엣것이 다른 포트로 밀려 앱이 8188 에서 못 찾는다).
- **검증(실측 — 전부 이 PC 에서 직접 돌렸다)**:
  · 런처로 기동 → **약 55초**에 `Starting server` · 8188 LISTENING · ComfyUI **0.33.3** · RTX 3060 VRAM 11,245/12,287MB.
  · **재실행 자동교정 확인** — 일부러 standalone-env pythonw 로 띄웠더니 `.venv` 로 다시 실행해 정상 기동.
  · 모델 목록 정상: UNET 2개(`krea2_turbo_int8_convrot`·`z_image_turbo_bf16`) · CLIP 7 · VAE 3 · LoRA 3.
  · 🔑 **앱의 실제 엔진 코드(`core/comfy-image.js`)로 1장 생성 성공** — `🔁 모델 자동 대체:
    krea2_turbo_fp8_scaled → krea2_turbo_int8_convrot`(v0.3.22)까지 그대로 통과, **47.5초 · 1344x768 ·
    1202KB · 노이즈/검정/글자 없는 정상 그림**(눈으로 확인). = Desktop 이 띄운 서버와 기능상 동일.
  · `npm run test:comfylaunch` **46/46**(신설 [2b] 인스턴스 선택 5·[2c] 런처 원문 함정 7·[11] 바로가기 대상 4 포함).
    **A/B 역검증**: 옛 `core/comfy-launch.js` 에는 `resolveLaunch`·`findInstance` 가 없어 [2b] 가 즉시 실패한다.
  · 회귀: `test:comfy`(모델 41·파이프라인 31·GPU 레인 14·2분할 E2E 69) · `test:makeall` 무음 E2E 18/18.
- ⏳ **미검증(이것만 남았다)**: **실제 재부팅 후** 자동 기동. 다음에 PC 를 켜고 1분쯤 뒤 8188 이 응답하면 성공이고,
  안 되면 `~/.shots-maker/logs/comfy-server.log` 의 `[priming-launcher]` 줄이 이유를 말해 준다.
- ⚠ **알아 둘 것**: 이제 8188 은 우리 서버가 쥔다. 그 상태에서 **Comfy Desktop 창으로 인스턴스를 열면** 그쪽이
  다른 포트로 밀리고(`portConflict:auto`), 두 서버가 뜨면 **모델이 VRAM 에 두 벌** 올라가 3060 에선 위험하다.
  로컬로 직접 작업할 일이 있으면 우리 서버를 먼저 끄는 편이 안전하다(작업관리자에서 그 pythonw 종료).
  · Comfy Desktop 은 오늘 ComfyUI 를 **v0.32.0 → v0.33.3** 로 자동 업데이트했다(`autoUpdateComfyUI:true`).
    UNET 목록은 그대로라 앱 동작에는 영향이 없었다.

## 🌡☁ GPU 온도 기록·자동 알림 + 공유(스타일·참조음성) 실결함 5건 수리 (2026-08-22, v0.3.29)
> 로이: "열 제안 진행해줘. 몇 주 데이터가 쌓이면 나에게도 알려줄 장치 — 나도 분명 잊어버릴 것 같아. 그때 대책도."
>   + "이미지 스타일과 참조음성 공유가 깔끔하게 잘 운영되는지 점검하고 수리해줘." (감사 워크플로 3방향 실측)
- **🌡 `core/gpu-temp.js` 신설** — 작업(절전 차단 withAwake)이 도는 동안만 60초마다 nvidia-smi 를 **비동기**로
  읽어 `~/.shots-maker/logs/gpu_날짜.csv`(KST, 90일 보관)에 기록. ⚠ 2026-07-22 의 옛 로거는 돌지 않는 서버
  파일에 심어져 한 번도 기록된 적 없음 — 이번엔 앱 코드라 **라이트 업데이트로 두 PC 에 배포**된다.
  · **자동 알림 장치**: 앱 시작 때 14일치 이상 쌓였으면 요약 팝업(최고온도·83°C 이상 비율·**수치별 대책**:
    양호/주의(83°C 20%+)/대책필요(90°C 근접 — 팬 육안확인·환기·청소)) → 30일마다 반복. 87°C 넘는 순간은 로그에 즉시.
  · 검증 `npm run test:gputemp` **29/29**(파싱·통계·90일 경계·14일/30일 판정·문구 3단계·main.js 배선·비동기 확인).
- **☁ 공유 감사에서 나온 실결함 수리(전부 실측 증거 있음)**:
  ① 🔴 **서버 참조음성 라이브러리가 이미 오염돼 있었다** — 62개 중 **24개가 바이트 동일 `_2`/`_3` 사본**
    (2026-08-14 같은 초에 23개 + 08-20 재발 1개). 원인 = `listServerVoices()` 가 타임아웃·404·네트워크 실패를
    전부 **빈 배열**로 돌려줘 "조회 실패"를 "빈 라이브러리"로 오인 → 로컬 전 목소리 재업로드. TTS 생성 중엔
    서버 이벤트루프가 막혀 5초 GET 은 죽고 60초 POST 는 살아남는 창이 실재했다.
    → **실패 = null 반환**(빈 목록과 구분), sync 는 null 이면 건너뜀 + **single-flight**(재진입 금지) +
    **참조텍스트(.txt) 없는 wav 는 업로드 안 함**. 서버(api.py)도 같은 이름 **바이트 동일이면 멱등 반환**(dedupe).
  ② 🔴 **TTS 캐시 키에 refName 이 없었다** — srv: 목소리는 refAudioPath·refText 가 둘 다 undefined 라
    **다른 목소리끼리 캐시 교차 적중**(엉뚱한 목소리 재활용) 가능. → `tts-cache.keyFor` 에 `rn` 추가(기존 캐시는
    키가 달라져 자연 무효화 — 첫 실행은 전부 새로 합성).
  ③ 🔴 **빈 참조텍스트 목소리는 Auto(엉뚱한 목소리)로 200 성공** — api.py `/tts`·`/tts-batch`·`/tts-batch-save`
    3곳에 「.txt 비면 400」 게이트 추가 + `/save-ref-voice` 가 빈 text 저장 거부.
  ④ **스타일: push 실패분이 다음 pull(서버 정본 교체)에서 소리 없이 소실** → `style-sync.json` 에 **dirty** 표시,
    pull 이 dirty 면 통째 교체 대신 **합쳐서 되올림**, rev==known+dirty 면 그 자리에서 재push(☁ 버튼이 약속대로 "받기+올리기").
  ⑤ **rev 모르는 PC 의 첫 push 가 baseRev -1(무검사 덮어쓰기)** — 서버의 남의 스타일을 지울 수 있었다 →
    **pull-union 먼저** 한 뒤 확보한 rev 로만 push(무검사 경로 제거).
  · 검증 `test/sync-guard.test.js` **22/22** — 스텁 서버 실행: 실패≠빈목록 5케이스 · 오프라인 편집 보존 ·
    첫 push 서버 보존 + baseRev -1 미사용 · main.js 가드 원문 대조 · 실제 홈 파일 무변경. (test:styles 에 포함)
- ⚠ **api.py 는 저장소 밖**(D:\TTS_Model\omnivoice, 백업 api.py.bak-20260822) — **관리자 PowerShell 로
  OmniVoice_Backend 예약작업 재시작해야** 새 게이트·dedupe 가 켜진다. 그전까지 앱 쪽 방어(①②)만 동작.
- ✅ **사본 24개 정리 완료(2026-08-22, 로이 지시 「1개만 남기고 제거」)** — 라이브러리 **wav 62 → 38**.
  🔑 **규칙을 보수적으로 잡았다: `<원본>_2`·`_3` 이고 `<원본>.wav` 가 실재하며 바이트가 동일할 때만** 치운다.
  · ⛔ **이름이 다른 동일 내용은 남겼다** — 실측하니 사본 말고도 **바이트 동일한데 이름이 다른 3쌍**이 있었다:
    `01_저음 1단계 ≡ 1nd_고전_남성` · `바리톤1 ≡ 테너2` · `역사자취 ≡ 역사자취 (2)`.
    채널은 목소리를 **이름으로** 가리키므로(`srv:<이름>`) 내용이 같다고 지우면 **그 이름을 고른 PC 의 채널이 깨진다**
    (특히 `1nd_고전_남성` 은 v0.3.8 사고 때 실제로 채널에 박혀 있던 이름). 그래서 남은 wav 38 · 고유 내용 35 가 정상이다.
  · ⛔ **`조용한승리_03`·`조용한승리_일단_01` 은 사본이 아니다** — 원본이 없어 규칙이 알아서 걸렀다(꼬리 숫자 오탐 방지).
  · 🔑 **지우지 않고 격리했다** → `D:\TTS_Model\ref-audio-dupes-20260822`(48개 = wav 24 + .txt 24).
    아내 PC 채널이 `_2` 이름을 골랐을 가능성이 아직 안 지워졌기 때문. 그 PC 확인 뒤 폴더째 삭제하면 끝이고,
    문제가 나면 **파일을 ref-audio 로 되옮기면 즉시 복구**된다. (되살아나지 않는다 — 서버 dedupe 가 켜져 있어
    바이트 동일 재업로드는 멱등이다. 로컬 `~/.flow-app/ref-audio` 12개에 `_N` 이름은 없음을 확인했다.)
  · 검증(정리 뒤 서버 실측): `/ref-voices` **38개** · `_N` 꼬리는 위 2개만 · **채널 참조 4개 전부 있음**
    (`고전_ok`·`02_저음 2단계`·`2nd_성경`·`#01_득수_noBreath`) · 이름 다른 동일내용 3쌍 양쪽 다 살아 있음 ·
    **참조텍스트 빈 목소리 0**. ⚠ PowerShell 5.1 은 UTF-8 을 ANSI 로 읽어 **한글 이름 비교가 거짓으로 실패**한다
    (`고전_ok → ❌ 없음` 오보를 한 번 봤다) — 한글 파일명 검증은 **node 로** 할 것.
- ⏳ **이름 같은 스타일 2개(「따듯한 회화풍 일러스트」)는 손대지 않았다 — 동일하지 않기 때문이다.**
  실측: `user_1785726628730_2tn8` 은 **106자**, `user_1784618892499_qtnb` 는 **96자**로 프롬프트가 다르다
  (긴 쪽에 `featuring` 이 더 붙어 있다). 어느 쪽이 정본인지는 그림이 갈리는 문제라 **로이가 🎨 에서 골라 지워야** 한다.
- 🧰 `scripts/breath.py` 를 저장소에 넣었다(2026-08-17 작성, 그동안 미추적) — 참조음성의 숨소리를 **제자리에서 눕히는**
  도구(잘라내면 참조텍스트 대응이 흔들린다). `scripts/` 는 매니페스트 제외라 **배포·버전과 무관**하다.

## 🧹 쇼츠·플리·ACE-Step·BGM **전면 제거** (2026-08-22, v0.3.28)
> 로이: "쇼츠와 Ace-step의 흔적을 모두 지워줘." + "이 프로그램에서 쇼츠를 안만들꺼야."
> 플리 모드 통째 제거·BGM 전체 제거·내부 배관 폴더명 유지 — 전부 로이 확정.
- **지운 것(파일째)**: `build-shorts.js`(쇼츠 CLI) · `core/cut-script-parser.js`(쇼츠 파서) ·
  `core/parsers/playlist-parser.js` · `core/playlist-video.js` · `core/ace-step.js` · `ace-step/` 폴더 ·
  `test/parser.test.js` · `vrew/dummy/shape-square.vbin`(제목 도형) · 플리 docs 3개 · 로컬 `Ace Step/`·`playlist/` 폴더.
- **지운 것(기능)**: 모드 토글 쇼츠/플리(→ 롱폼/출판 2-way) · `mergeGroupsByTts`(8초 재구성)+🔗합치기 ·
  제목/도형 트랙(`addTitleTrack`/`addShapeTrack`+TitleEditor+set-title IPC) · BGM 전부(체크박스·3.5단계·
  `deriveBgmMood`·`addBgmTrack`·`loopAudioTo`·파서 `> 🎵 배경음악:` 메타) · `set-aspect`/aspect 셀렉터 잔재 ·
  채널편집의 쇼츠 열(배속·자막·화풍·출력폴더) · `detectScriptMode`(대본 자동 모드감지 — 이제 항상 롱폼).
- 🔑 **이름만 쇼츠인 공유 배관은 유지**(매핑 워크플로 5개 에이전트로 전수 분류 후 결정 — 바꾸면 기존
  작업물·스냅샷·IPC 가 깨진다): `shortsDirs`(media-N/tts-N/subtitles-N 폴더 구조 그 자체) · `shortsNum`
  (편 주소 키, 롱폼=1 — 모든 IPC·스냅샷·gemini-batch 키) · `~/.shots-maker/` 홈 폴더 · grok-engine 의
  `_shorts` 분기(롱폼 16:9 칩 선택 코드가 그 안에 있다) · vrew-builder 의 비율(9:16/1:1) 블록(죽은 가지지만
  16:9 배선과 얽혀 있어 손대면 위험 — v0.2.x 세로 열림 사고 전례).
- 🔑 **옛 저장 데이터는 읽을 때 정규화**: `normalizeMode`/`normMode` 폴백을 'shorts'→**'longform'** 으로 뒤집었다
  (이걸 프로필 삭제보다 먼저 안 하면 `getModeProfile()` undefined 로 즉사). 옛 쇼츠 .smproj 는 이어받지 않고
  새로 파싱, load-project 는 제거된 모드 파일을 명시적으로 거부. workspace.json 의 shorts/playlist 큐는 조용히 소멸.
- 🔑 **채널 프리셋의 쇼츠 필드(capShort·speedShort·styleShort·outShort)는 파일에 남긴다** — UI 만 걷어냈고
  saveChannel patch 에서 빼기만 한다(preset-store 가 병합 저장이라 무해. 마이그레이션 삭제는 v0.3.8 계열 사고).
  **`channel-styles.json` 의 `short` 필드 내보내기도 유지** — 아도나이로이 대시보드(8765)와의 외부 계약 + 테스트 40개.
- ⚠ **함정 회피 기록**: `gpuBusyReason` 이 플리 섹션 한가운데 정의돼 있었다 — 섹션 통삭제 전에 함수를 살려
  옮겼다(안 그러면 tts-build·runMakeAllCore 가 런타임 즉사 = 이 저장소 3번째 미정의 식별자 사고가 될 뻔).
  App.jsx 의 bgm deps 배열(디바운스 effect)도 state 삭제와 동시에 정리(남기면 렌더 즉시 ReferenceError → 백지).
- **테스트 정비**: parser.test.js 삭제(test:parser 스크립트 제거) · timestamps 쇼츠 블록 삭제(53단언) ·
  pipeline-mode 의 regroup 인자·단언 삭제(30/30) · style-share.smoke 화풍 칸 2→1 · makeall-dry 의 outShort 제거.
  라이브 업데이트는 **삭제를 전파하지 않는다** — 아내 PC 에 옛 파일이 남지만 아무도 require 안 해서 무해.
- **검증(전 스위트 통과)**: makeall 무음 E2E 18/18(runMakeAllCore 실제 완주 — 수술 후 핵심 증거) · tts 18+31 ·
  comfy 41+30+14+69 · visual 14+10+13+7 · delmedia 27 · grok 81 · timestamps 53+UI · styles 40+40+17 ·
  comfylaunch 29 · book 전체. vite build 통과 · main.js LF 유지.
- deps 무변경 → **라이트 업데이트로 배포**. 쇼츠 흔적: 코드 443곳(27파일) + ACE 117곳 + BGM 152곳 정리.

## 🖥⚡ ComfyUI 로컬을 부팅 때 자동 실행 + **생성 뒤 VRAM 반납**(TTS 보호) (2026-08-22, v0.3.27)
> 로이: "PC 가 켜질 때 컴피유아이 데스크탑이 자동으로 켜지고 셋팅되어 이미지를 로컬에서 즉시 생성하게.
>   이것 켜는 것을 매번 깜빡하는 바람에 왜 안되지 하는 상황이 계속 발생하네.
>   이렇게 켜놓는다고 VRAM 점유율이 올라가 TTS 변환에 문제가 생기지는 않겠지?"
- **방어선 두 겹으로 만들었다.**
  ① **부팅 자동 실행** — `shell:startup` 에 **「ComfyUI (Priming 이미지용).lnk」**(대상 `C:\Program Files\Comfy Desktop\Comfy Desktop.exe`,
     창 스타일 최소화). 앱이 아니라 윈도우가 켜 주므로 Priming 을 안 켜는 날에도 서버가 떠 있다. **지우면 자동 실행이 멈춘다.**
  ② **앱이 스스로 켠다** — `core/comfy-launch.js` 신설. 로컬 생성 직전에 `/system_stats` 로 찔러 보고, 꺼져 있으면
     Comfy Desktop 을 **띄우고 떠오를 때까지 기다린다**(기본 150초, 2초 간격 폴링). 바로가기를 지웠거나 창을 닫았거나
     부팅 직후 아직 안 올라온 경우까지 덮는다.
  · 🔑 **로컬 주소(127.0.0.1·localhost·::1)일 때만** 손댄다 — 클라우드·아내 PC 주소를 켤 수는 없다(`isLocalUrl`).
  · 🔑 **single-flight** — 이미지 여러 장이 동시에 들어와도 실행은 **한 번**(동시 4건 요청 → spawn 1회 검증).
  · 🔑 **이미지는 못 켜면 한 번만 명확히 실패**시킨다(`throw`) — 안 그러면 40장이 각각 `fetch failed` 로 죽어 로그만 더럽다.
    **비디오는 던지지 않고 그 편만 건너뛴다**(영상이 없어도 이미지+켄번스로 .vrew 가 나가므로 — 의도된 차이).
  · 설치를 못 찾으면 **조용히 실패하지 않는다**: 찾아본 경로 + 「☁ 클라우드로 바꾸세요」를 함께 알려 준다.
- 🔴 **VRAM 질문에 대한 실측 답 — "켜 두는 것"은 무해하고, 문제는 "생성한 뒤"였다.**
  | 상태 | GPU 사용(12,288MB 중) |
  |---|---|
  | ComfyUI 모델 미로드(부팅 직후·`/free` 뒤) | **3,480 / 4,657MB** (= OmniVoice + 윈도우 데스크탑) |
  | 이미지 생성 뒤 **큐가 비어 있는데도** | **10,110 / 10,571MB** ← ComfyUI 가 **약 6GB 를 계속 붙들고 있다** |
  · 두 번 재 봤고 둘 다 같았다(`/free` 로 6.6GB·5.9GB 반납). 즉 **켜 두기만 하면 0에 가깝고**, 한 번 그리면
    그 모델이 남아 **TTS 가 쓸 자리가 2GB 대로 좁아진다**(그 상태를 로이의 실제 큐 실행 중에도 관측: `vram_free 2,234MB`).
  · 우리 앱은 예전엔 로컬 생성 **직전에만** `/free` 를 불렀다(OOM 방지) → **끝난 뒤에는 안 불러서 남아 있었다.**
  · ✅ **그래서 `runComfyImages`·`runComfyVideos` 끝에 `freeMemory()` 를 추가**했다(`🧹 로컬 VRAM 반납` 로그).
    이제 켜 놓아도 TTS 에 영향이 없다. 대가는 다음 생성 때 모델 재로딩(장당 첫 장만 몇 초) — OOM·느려짐보다 싸다.
  · 참고: 동시 실행 자체는 이미 **GPU 레인**(v0.3.22)이 막고 있다 — TTS 와 로컬 이미지는 절대 같이 돌지 않는다.
    이번 것은 "동시 실행"이 아니라 **"점유한 채 놀고 있는 VRAM"** 문제라 레인으로는 해결되지 않던 부분이다.
- 검증 `npm run test:comfylaunch` **29/29** — `isLocalUrl` 7케이스 · 설치 후보 · ping(살아있음/연결거부) ·
  이미 켜져 있으면 spawn 0 · 원격은 손 안 댐 · **꺼져 있으면 켜고 1.2초 뒤 준비되면 성공**(spawnFn 주입으로 실제 앱은
  안 띄운다) · 타임아웃 안내 · 미설치 안내 · **동시 4건 → spawn 1회** · main.js 원문 배선(이미지 throw / 비디오 건너뜀 /
  **반납 2곳** / 「켜기 → VRAM 정리」 순서) · 시작프로그램 바로가기 존재. 회귀: comfy 41/31/14.
- ⏳ **미검증**: 실제 재부팅 후 자동 실행(로이가 다음에 PC 를 켤 때 확인 — 8188 이 응답하면 성공).
  ⚠ **Comfy Desktop 창을 닫으면 서버도 함께 꺼진다**(Electron 앱). 최소화해 두는 것이 맞다.

## 🔢 버전 역행을 **배포 단계에서 차단**했다 — 하루에 두 번 났다 (2026-08-22, v0.3.27)
- 사고: `0.3.25 → 0.2.99`(커밋 7781473), 고친 뒤 또 `0.3.26 → 0.3.0`(커밋 8565380). 둘 다 **다른 세션이 낡은
  컨텍스트로 "내가 아는 값 + 1"** 을 썼다. 코드는 멀쩡하지만 로그의 「v0.x.x 시작」이 어느 코드인지 못 가리킨다.
- ✅ **`scripts/gen-manifest.js` 가 막는다**: 지금 배포된 `update-manifest.json` 의 버전보다 낮으면
  **에러 + 종료코드 1**(`❌ 버전이 거꾸로 갑니다: 지금 배포본 v0.3.0 → package.json v0.2.5`).
  `npm run update:publish` 가 이걸 거치므로 **역행한 채로는 발행이 안 된다.** 정말 되돌릴 땐 `ALLOW_VERSION_DOWNGRADE=1`.
  🔑 A/B 확인: 0.2.5 로 낮추면 종료코드 1 로 막히고, 같은 값·올린 값은 통과한다.

## 🔢 버전이 거꾸로 갔다(0.3.25 → 0.2.99) + main.js 가 CRLF 로 저장돼 테스트 2개가 깨졌다 (2026-08-21, v0.3.26)
- 🔴 **다른 세션이 낡은 컨텍스트로 `package.json` 버전을 0.3.25 → 0.2.99 로 내려 썼다**(커밋 7781473).
  코드는 멀쩡했지만 로그의 「v0.x.x 시작」이 **어느 코드인지 가리키지 못하게** 된다 → **0.3.26 으로 앞으로** 올렸다.
  🔑 **버전은 되돌리지 않고 항상 앞으로만** 간다(과거 최고값보다 큰 값). 세션이 여러 개 도는 저장소에서
  버전을 "내가 아는 값 + 1" 로 쓰면 이렇게 역행한다 — **고치기 전에 `package.json` 을 먼저 읽을 것.**
- 🔴 **같은 커밋이 `main.js` 를 통째로 CRLF 로 저장했다**(4,900줄). 이 저장소가 이미 기록해 둔 단골 사고다.
  결과: main.js 원문을 `
` 으로 찾는 테스트가 **거짓으로 실패**한다 — `visual-defect` 14개가
  `❌ main.js 에서 판정 블록을 못 찾음` 으로 죽고, `channel-styles` 배선 단언 6개가 실패했다(코드는 정상).
  → main.js 를 **LF 로 되돌렸다**(318,711 → 313,811 바이트, 내용 동일 · 문법 확인).
  → 덤으로 `channel-styles.test.js` 가 **읽을 때 줄끝을 정규화**하게 고쳤다(다음에 또 CRLF 가 되어도 헛실패 없음).
  ⚠ `visual-defect` 같은 다른 테스트는 여전히 LF 를 전제한다 — **python 으로 main.js 를 저장했으면 LF 로 되돌릴 것**
  (git 은 정규화해 커밋하므로 **diff 로는 안 보인다** = 눈으로 못 잡는다. 검사: `node -e "…match(/
/g)…"`).

## 🎨📤 채널 화풍을 파일로 내보낸다 — 아도나이로이 대시보드(8765)의 썸네일 프롬프트용 (2026-08-21, v0.3.25)
> 로이: "채널마다 이미지화풍이 다르게 설정되고, 이것을 localhost:8765 대시보드에서도 공유해야 해.
>   Priming 에서 셋팅된 채널의 화풍을 그대로 가져가서 썸네일 이미지 프롬프트에 조합해야 하거든."
- 🔑 **조사에서 나온 사실 — 두 시스템은 이미 이 값을 공유하고 있었다. 손으로 복사하는 방식으로.**
  `D:\## 아도나이로이\01_고전\02_고전서재\지침\config.md:429` 에 그대로 적혀 있다:
  *「이 문자열은 Priming의 「채널 스타일」 칸에 넣는다. 앱이 `<채널 스타일>, <대본 프롬프트>, no text, no watermark` 로 감싼다」*
  성경 채널 config 의 고정 프리픽스는 **Priming 기본 스타일 `webtoon-illust` + 공통보정 문구와 글자까지 같다**(실측 대조).
- 🔴 **그리고 이미 어긋나 있었다** — 고전서재 config 의 화풍은 「짙은 수채」인데 앱의 롱폼 화풍은
  `Cozy Textured Digital Gouache` 다. 손복사는 반드시 어긋난다 → **정본을 앱 하나로.**
  · ✅ **로이 확정(2026-08-21)**: 지금 작업 중인 **빨간머리앤 = `Cozy Textured Digital Gouache`** 가 맞다
    (= 앱 값이 정본. config 의 「짙은 수채」는 옛 값이므로 아도나이로이 세션이 그 블록을 지운다).
  · 🔑 **고전서재는 작품마다 화풍이 조금씩 달라질 수 있다**(로이). 그래도 **채널 단위 한 칸으로 충분하다** —
    이 채널은 한 작품을 연재하는 단위라, **새 작품을 시작할 때 채널 화풍을 그 작품 것으로 바꾸면**
    내보내기가 갱신되고 대시보드도 따라온다(추가 코드 0).
    ⚠ 대신 대시보드가 보는 값은 **언제나 「지금」의 화풍**이다 — 옛 작품 편의 썸네일을 나중에 다시 만들면
    새 화풍이 붙는다. 그래서 JSON 의 `updatedAt`·`rev` 를 대시보드 화면에 함께 띄우게 했다.
    ⛔ 작품별 화풍을 아도나이로이 config 에 또 적지 않는다 — 그게 방금 고친 그 사고(두 벌)다.
- ⚠ **내가 처음 잘못 읽었다(정정 기록)**: `preset.styleId` 만 보고 "채널 화풍이 거의 다 치비" 라고 했는데,
  실제 채널 화풍은 **모드별 필드 `styleLong`·`styleShort`** 다(`styleId` 는 옛 단일 필드 = 사실상 방치값).
  실측: 고전 따듯한 회화풍 · 승리 수채화 · 역사 일러스트 · 성경 웹툰 일러스트 · 서재 Cozy Gouache
  (쇼츠는 대부분 시네마틱). **채널별로 이미 제대로 설정돼 있었다** → 로이가 다시 세팅할 필요 없다.
- **`core/channel-styles.js` 신설 → `~/.flow-app/channel-styles.json`**:
  · `{note, updatedAt(KST), rev, combineRule, channels[], styles[]}` ·
    channels[] = `{channel, group, long, short, thumb}` 이고 각 화풍은 `{styleId, styleName, prompt, from, isBuiltIn}`.
  · 🔑 **prompt 는 `style-store.getPrompt()` 값 그대로** — 기본 스타일의 공통보정 문구까지 포함해 **앱이 실제로
    전송하는 문자열**이다. 여기서 다시 조합하면 또 두 벌이 된다(그게 지금까지의 사고 원인).
  · 🔑 **`from` 을 함께 적는다**(`styleLong`/`styleShort`/`styleId`/`styleThumb`/`long(폴백)`) — 소비자가 폴백
    규칙을 또 구현하지 않게. 스타일이 지워진 채널은 **`missing:true`**(빈 문자열을 조용히 내보내지 않는다).
  · 🔑 **내용이 그대로면 다시 쓰지 않는다**(rev 헛증가 방지) · 저장은 tmp→rename **원자적**(대시보드가 반쪽 파일을
    읽지 않게) · 스토어가 터져도 **예외를 던지지 않는다**(화풍 내보내기가 채널 저장을 막으면 본말전도).
  · 시각은 **KST(+09:00)** — UTC 로 적으면 대시보드가 하루 밀린 값을 보여 준다(전역 지침).
- **내보내는 시점 7곳**: 앱 시작(**스타일 서버 동기화 뒤** — 받은 스타일이 반영돼야 한다) · 채널 저장·추가·
  순서변경·이름변경·삭제 · 스타일 추가/수정/삭제/순서변경. IPC `export-channel-styles` 로 수동 호출도 가능.
- **채널편집 ⚙ → 🖼 제작 도구에 「🖼 썸네일」 화풍 칸 신설**(`preset.styleThumb`). 비우면 **롱폼과 같게**.
  왜 따로 두나: 썸네일은 인물이 주인공이라 본문 화풍과 다를 수 있다(고전서재 config 이 실제로 ①본문 ②썸네일
  ③네거티브로 갈라 두었다). 이제 그 두 문자열이 **둘 다 Priming 에 있고**, 아도나이로이는 "어느 것을 쓰나"만 정한다.
- 검증 `npm run test:styles` = 공유 40 + **채널화풍 40/40**(`test/channel-styles.test.js` — 원문 실행 ·
  **prompt 가 `getPrompt()` 와 글자까지 같은지** · 모드별/옛 필드 폴백 · 썸네일 지정·폴백 · missing 표시 ·
  KST 경계(15:00Z → 다음 날 00:00) · rev 무증가/증가 · tmp 잔재 없음 · 스토어 예외 삼킴 · **main.js·preload·
  App.jsx 배선 원문 대조** · 실제 홈 파일 무변경) + **E2E 17/17**(채널편집에 썸네일 칸 · 앱이 켜지며 실제로
  파일을 쓰는지 · 채널 7개 · KST 표기). 🔑 **A/B 역검증** — 화풍을 앱 값 그대로 쓰지 않고 직접 조합하게
  바꾸면 [1] 두 단언이 **실패한다**. 회귀: 파서 11 · 만들기 무음 E2E 18.
- ✅ **덤 — 스타일 서버 공유(v0.3.24)가 실전에서 확인됐다.** 로이가 메인 PC 서버를 재시작한 뒤 실측:
  `GET /styles → rev 2 · 사용자 스타일 9개`. 아내 PC 가 올린 **「연필스케치 수채화」·「따듯한 회화풍 일러스트」**
  2개가 이 PC 로 내려왔다(7→9). 첫 동기화 합치기·양방향 전파가 실제로 동작한다.
  ⚠ 이름이 같은 스타일이 둘 생겼다(양쪽 PC 가 각각 만든 「따듯한 회화풍 일러스트」 — id 는 다르다).
  헷갈리면 🎨 에서 하나를 지우면 서버에도 반영된다.
- ⏳ **아도나이로이 쪽은 그 세션이 해야 한다**(이 저장소 밖): 채널 `지침\config.md` 에 `priming_channel:`·
  `thumb_style:` 두 줄 추가 → `대시보드생성.py` 가 이 JSON 을 읽어 「썸네일 프롬프트(완성)」 복사 버튼 생성 →
  `패키징검사.py` 의 화풍 프리픽스 검사도 같은 JSON 기준으로 → config 의 하드코딩 화풍 블록 제거.
  🔴 **채널 이름 유사도 자동 매칭 금지** — 「진정한 승리」와 「조용한 승리」가 함께 있어 오결합한다.

## 🎨☁ 이미지 스타일도 **여러 PC 공용** — 참조음성과 같은 서버에 모은다 (2026-08-21, v0.3.24)
> 로이: "참조음성 목소리를 내 PC 와 아내 PC 에서 공동으로 쓸 수 있는거지? 클라우드 형태로…
>   **이미지 스타일도 같이 공유하며 활용**할 수 있도록 해줘."
- 🔑 **먼저 사실 확인 — 참조음성은 이미 공유되고 있었다**(v0.2.96~v0.3.6 에 만든 그것). 실측:
  `GET /ref-voices` → `dir D:\TTS_Model\ref-audio · voices 62개`. 채널의 목소리 값은 `srv:<이름>` 이고
  합성 때 **이름만** 보내므로 **아내 PC 엔 wav 파일이 없어도 된다.** 앱을 켜면 4초 뒤 이 PC 에만 있는
  목소리를 자동 업로드한다(`syncRefAudioToServer`). → 이번에 손댈 필요 없었다.
- 🔴 **그런데 스타일은 PC 마다 갈려 있었다**(`~/.flow-app/styles.json` · `style-order.json` = 로컬 전용).
  같은 채널·같은 대본인데 스타일 목록이 다르면 **같은 편이 다른 화풍으로 나온다.** 그래서 같은 방식으로 모았다.
- **어디에 두나 = OmniVoice 서버(9881).** 구글드라이브(G:)에 두는 쪽이 코드는 쉽지만 **바로 오늘
  G: 가 16초 사라져 큐 10개가 죽었다**(위 항목). 스타일은 몇 KB고, 이 서버는 SYSTEM 예약작업으로
  **상시 실행** + 참조음성·발음사전의 주인이므로 공유 원장으로 이미 검증된 자리다.
- **서버 `api.py` 에 `/styles` 신설**(발음사전 `/dict` 와 같은 꼴 + **rev**):
  · `GET /styles` → `{styles, order, rev, updatedAt}` · `PUT /styles` `{styles, order, baseRev}`
  · 🔑 **rev(개정번호) 낙관적 잠금** — 받아 간 rev 를 되보내고, 그 사이 다른 PC 가 저장했으면 **409 + 현재 문서**.
    발음사전처럼 Last-Write-Wins 로 두면 **한쪽 PC 가 다른 PC 의 새 스타일을 조용히 지운다**(스타일엔 삭제가 있다).
    `baseRev:-1` = 검사 없이 덮어쓰기(첫 씨딩·복구용). 저장은 tmp→`os.replace` 원자적, 필드는 id·name·prompt 만 남긴다.
  · 보관 위치 `FLOW_STYLE_PATH`(런처가 `omnivoice\data\styles.shared.json` 으로 고정 — SYSTEM 홈에 흩어지지 않게).
  · **기본 28개 스타일은 안 보낸다** — 앱 코드에 있으니까(서버엔 사용자 스타일 + 순서만).
- **앱 쪽(`core/style-store.js`) — 로컬 파일이 작업 사본이다.** `loadAll()` 은 그대로 **동기·오프라인** 동작
  (생성 경로가 네트워크를 기다리게 만들면 안 된다). 오가는 시점은 **① 앱 시작(조용히) ② 🎨 편집창 열기
  ③ 추가·수정·삭제·순서변경 직후** 뿐. `pullFromServer` / `pushToServer` / `mergeStyles` / `mergeOrder`.
  · 🔑 **첫 동기화는 합친다**(id 기준 union) — 서버로 덮으면 아내 스타일이 사라지고, 로컬로 덮으면 내 것이 사라진다.
    **그 뒤부터는 서버가 정본**(안 그러면 삭제가 영원히 전파되지 않는다). 맞춘 rev 는 `~/.flow-app/style-sync.json`.
  · 충돌(409)이면 **합쳐서 한 번만** 재시도 — 방금 이 PC 가 한 편집이 이기고, 그 사이 남이 추가한 것도 남는다
    (⚠ 대신 남이 아직 모르는 삭제는 되살아날 수 있다 — 잃는 것보다 낫다).
  · 서버가 꺼졌거나 구버전(404)이면 **조용히 이 PC 것만** 쓴다(작업이 막히지 않는다). 🎨 창을 열면 이유를 알려준다.
- **UI**: 🎨 편집창을 열 때 자동 동기화 + 「☁ 동기화」 버튼 + 상태줄(실패는 빨강). 안내문에 "여러 PC 공용" 명시.
- 검증 `npm run test:styles` = **단위 40/40**(`test/style-share.test.js` — **USERPROFILE 을 갈아끼우고 require
  캐시를 지워 "두 PC" 를 만든다** + api.py 규약을 그대로 구현한 HTTP 스텁: 첫 동기화 합치기 · 양방향 전파 ·
  삭제 전파 · **409 충돌에서 양쪽 새 스타일이 다 사는지** · 순서 공유 · 404/주소없음 폴백 · main.js 원문 배선 대조 ·
  **실제 `~/.flow-app/styles.json` 무변경**) + **앱 E2E 9/9**(`style-share.smoke.js` — 실제로 ✎ 를 눌러 렌더 35개·
  ☁ 버튼·동기화·ESC·화면오류 0 · 스타일이 사라지지 않았는지). 서버는 **FastAPI TestClient 12/12**(임시 경로라 프로덕션 무영향).
  🔑 **A/B 역검증했다** — `baseRev` 검사를 없애면 `아내가먼저` 스타일이 조용히 사라지며 테스트가 **실패한다**.
  회귀: 파서 11 · TTS 게이트 18 + fs-retry 31 · 만들기 무음 E2E 18 · 삭제 27 · comfy 41/31/14.
- 🔴 **로이가 하실 일 — 메인 PC 서버를 한 번 재시작해야 켜진다.** 실측으로 지금 서버는 `/styles` **404**
  (`/save-ref-voice` 는 200 계열 = 옛 코드가 돌고 있다). 예약작업이 **SYSTEM** 이라 비관리자로는 조회조차 안 된다
  (`Get-ScheduledTask` 실패 확인). **관리자 PowerShell** 에서 한 줄씩:
  `Get-ScheduledTask | Where-Object TaskName -like "*Omni*" | Format-Table TaskName,State` → `Stop-ScheduledTask -TaskName "OmniVoice_Backend"` → `Start-ScheduledTask -TaskName "OmniVoice_Backend"`
  (모델 로딩 ~1분 뒤 `/health` 200). 그전까지는 양쪽 PC 가 각자 스타일을 쓰고, 재시작 후 **처음 🎨 를 여는 PC 의
  스타일이 서버로 올라가고 그다음 PC 에서 합쳐진다.**
- ⚠ `D:\TTS_Model\omnivoice\api.py`·`start_server.pyw` 는 **저장소 밖이라 라이트 업데이트로 배포되지 않는다**
  (메인 PC 로컬 파일 — 백업 `api.py.bak-20260821-142505`). 앱 쪽(main·preload·App.jsx·style-store·asr-client)은 배포된다.
- ⏳ **미검증**: 서버 재시작 후의 실제 왕복(내 PC ↔ 아내 PC). 아내 PC 는 앱 재시작 + ⚙ 설정의 OmniVoice 주소가
  메인 PC 를 가리키고 있어야 한다(합성이 되는 주소면 스타일도 된다 — 같은 서버·같은 키).

## 🔴💽 G: 가 16초 사라져 **큐 10개가 전멸**했다 — 디스크 일시 장애 재시도 (2026-08-21, v0.3.23)
> 로이: "중단된 이유가 뭐야?" — 14편 큐가 4편만 끝나고 `성공 0 · 실패 10` 으로 멎었다.
> 로그: `ENOENT … tts-1\70.wav` → 남은 9개 대본이 전부 `ENOENT … mkdir …tts-1`.
- 🔑 **앱 버그가 아니라 출력 드라이브가 통째로 사라진 것이었다.** 윈도우 이벤트로그로 초 단위 확정:
  | 시각(KST) | 사건 | 출처 |
  |---|---|---|
  | 07:54:37 | **유선랜(Intel I211) 링크 끊김** | System `e1rexpress` 27 |
  | 07:54:50 | 복구 — 단 **100Mbps**(1Gbps 아님 = 케이블·포트 접촉 의심) | `e1rexpress` 33 |
  | 07:54:51 | **구글 드라이브가 G:·J:·K: 언마운트** (`Link deleted: "\DosDevices\G:"`) | `googledrivefs31931` |
  | 07:54:53 | 앱이 컷70 쓰기 실패 → 9개 대본이 **같은 초에** mkdir 실패 | `~/.shots-maker/logs/2026-08-21.log:676` |
  | 07:55:07 | G: 재마운트 (`Mounting successfully done.`) | `googledrivefs31931` |
  · 마지막 성공 파일 `69.wav` 의 mtime 이 07:54 — 언마운트 직전에 끊겼다.
  · 🔑 **다음에 또 "왜 멈췄지?" 가 나오면 여기부터 본다**: 앱 로그의 실패 시각 → `Get-WinEvent -LogName System`
    같은 구간 → `googledrivefs*` 의 `Link deleted/created` 와 `e1rexpress` 링크 이벤트. 앱 로그만 보면
    "ENOENT = 경로가 틀렸나?" 로 헛다리를 짚는다.
- 🔴 **그런데 진짜 문제는 앱이 그걸 못 견딘 것.** v0.3.22 의 문장 격리는 **`synthesize` 실패만** 감쌌다 →
  ① 컷70의 `fs.writeFileSync` 가 try 밖이라 던져서 **그 대본 전체**가 죽고(0820: 69/N 에서 중단)
  ② `fillTtsList` **첫 줄의 `fs.mkdirSync(workDir)`** 가 던져서 남은 9개는 **한 문장도 시도하지 못하고** 죽었다.
  실패 9개가 전부 같은 초에 찍힌 게 그 증거다(실제로 시도한 게 아니라 폴더 만들다 즉사).
- **수정 — `core/pipeline.js` 에 `retryFs()`/`isTransientFsError()` 신설**(export 하므로 다른 경로에서도 쓸 수 있다):
  · 대기표 **`[1s, 2s, 4s, 8s, 15s]` = 합 30초** — 실사고 공백 16초를 넉넉히 덮는다(테스트가 이 합을 단언한다).
  · **ENOENT 를 일시 장애로 본다** — 마운트가 사라지면 경로의 뿌리부터 없어져 "파일이 진짜 없는 것"과 코드가 같다.
    이 판정을 **쓰기·폴더생성 경로에서만** 쓰기 때문에 안전하다(끝내 실패하면 그대로 던진다 = 30초 늦게 드러날 뿐).
    ENOSPC(디스크 참)·EROFS(읽기전용)는 **재시도 안 함** — 기다려도 소용없고 시간만 버린다.
  · ⚠ **반드시 비동기 대기**(`setTimeout`)다. 동기 대기를 쓰면 메인 프로세스가 통째로 얼어 클릭·IPC 가 멈춘다
    (v0.3.18 「execFileSync 프리징」과 같은 계열 — 방어선을 넣다가 앱을 얼리는 게 이 프로젝트의 단골 실수다).
  · 적용 3곳: **머리의 mkdir**(끝내 실패하면 `출력 폴더를 만들 수 없습니다 — <경로> … 구글 드라이브(G:)가
    연결돼 있는지 확인하세요` 로 던진다) · **캐시 복사** · **음성 쓰기 3곳**(배속 임시 WAV·폴백 WAV·정속 WAV).
  · **쓰기 실패도 문장 단위로 격리** — `failed[]` 에 모아 그 문장만 건너뛴다(→ `missingTtsNums` 게이트가
    반쪽 .vrew 를 막는다). 연속 5문장 저장 실패면 그 대본을 멈춘다(드라이브가 영영 안 돌아올 때 헛돌기 방지,
    최악 5×30초=150초). 「🎤 TTS」를 다시 누르면 **빠진 것만** 만든다.
- 검증 `npm run test:tts` = 게이트 18/18 + **`test/fs-retry.test.js` 31/31**
  (`core/pipeline.js` **원문에서 `retryFs` 를 뽑아 실행** — 대기시간만 10ms 로 갈아끼우고 로직은 원문 그대로.
  실제 `fillTtsList` 도 돌린다: **작업폴더를 중간에 옆으로 옮겼다 되돌려** 언마운트를 재현 → 5/5 전부 생성 ·
  한 문장만 영구히 못 쓰면 4/5 성공 + `failed=[3]` · 폴더를 못 만들면 사람 말 오류).
  🔑 **A/B 역검증했다** — 같은 시나리오를 `git show HEAD:core/pipeline.js` 로 돌리면
  `💥 예외로 대본 전체 중단(ENOENT) · 음성 2/5`, 수정본은 `5/5`. (원본으로는 테스트가 함수를 못 찾아 즉시 실패도 한다.)
  회귀: 파서 11 · 만들기 무음 E2E 18/18 · 삭제 27/27 · comfy 41/31/14/69 · visual 14/10/13/7.
- ⏳ **아직 안 막은 곳(같은 장애가 다른 단계에서 나면 그 편은 여전히 실패한다)**: 이미지·비디오 저장, `.vrew`·SRT 쓰기.
  다만 그쪽은 `runMakeAllCore` 의 대본별 try/catch 가 받아 **다음 대본은 계속**되므로 이번처럼 큐가 전멸하지는 않는다.
  필요해지면 `P.retryFs` 를 그대로 갖다 쓰면 된다.
- ⚠ **근본 원인은 코드 밖에 있다**: 랜 링크가 **100Mbps 로 복구**됐다. 케이블·포트 접촉 불량의 전형적 신호이고,
  큐를 몇 시간 돌리는 작업이라 한 번만 더 끊겨도 또 온다. 랜선 재체결·교체를 권고(로이에게 전달함).

## 🎙 CosyVoice3 검토 — 도입하지 않음, OmniVoice 유지 (2026-08-16, 앱 코드 무변경)
> 로이: "TTS 를 CosyVoice 2 로 바꾸는 건 어떤가" → 불만의 정체는 **"억양이 밋밋하다"** 였다.
> **A/B 실측 후 결론: 기각.** 앱은 한 줄도 안 고쳤다.
- 🔑 **먼저 정정 — 2가 아니라 3이 최신**: CosyVoice 2 는 2024/12 판이고 **CosyVoice 3.0 이 2025/12/15
  오픈웨이트로 나왔다**(`FunAudioLLM/Fun-CosyVoice3-0.5B-2512`, Apache 2.0, 한국어 포함 9개 언어).
  3.5 는 존재하나 가중치 비공개. 그래서 비교 대상을 **OmniVoice vs CosyVoice 3.0** 으로 놓고 쟀다.
- **A/B 설계(3자 비교)** — 같은 참조음성(`02_저음 2단계`, 기본 채널 04_역사이야기) + 실제 대본
  `[역사_0820]` 에서 뽑은 **12문장**(담담한 서술·대비 강조·질문형·한자어 고유명사·숫자·어두운 정서·긴 문장).
  · **A** = OmniVoice(현행) · **B** = CosyVoice3 zero-shot · **C** = CosyVoice3 **instruct2**(문장별 감정·억양 지시)
  · 공정 조건: 양쪽 **speed 1.0 정속**(앱의 ffmpeg atempo 1.15 는 미적용 — 모델 원음을 비교해야 하므로),
    num_step 32 유지, seed 고정, 워밍업 1회 후 측정.
- 🔑 **실측 — 속도에서 이미 갈렸다** (같은 12문장, RTX 3060):
  | | 오디오 | 생성 | RTF |
  |---|---|---|---|
  | A OmniVoice | 74.8s | **34.0s** | **0.455** |
  | B CosyVoice3 zero-shot | 80.6s | 104.8s | 1.300 |
  | C CosyVoice3 instruct | 117.8s | **143.0s** | 1.214 |
  → **B 3.1배 · C 4.2배 느리다.** TTS 가 파이프라인의 70%(롱폼 5편 52.7분)라 이 격차가 총 시간에 그대로 실린다:
  **75분 작업 → 약 4시간.** 게다가 **C 는 같은 텍스트를 57% 더 길게 읽는다**(74.8s→117.8s).
  ⚠ 공정하게: `fp16`·`load_jit`·vLLM 백엔드를 **안 켠 기본값**이었다(여지는 있으나 3~4배를 뒤집을 정도는 아니라고 봤다).
- **최종 판정은 로이의 청취**(내가 소리를 못 들으므로). 블라인드 청취 페이지를 만들어 판정 →
  **"CosyVoice 는 사용 못 하겠다"** → OmniVoice 유지 확정.
- **정리 완료**: `D:\TTS_Model\cosyvoice3` **전부 삭제**(16.3GB 회수, 로이 지시). `omnivoice`·`ref-audio` 무영향
  (삭제 후 확인 — ref-audio wav 60개 그대로, 9881 `/health` 200). 검증 스크립트도 그 폴더에 있었으므로 함께 사라졌다.
- 🔑 **다음 TTS 후보를 잴 때 이 방법을 그대로 재현할 것**(위 A 수치가 기준선이라 **OmniVoice 재측정은 불필요**):
  1. 후보를 `D:\TTS_Model\<모델>\` 에 **나란히** 설치(기존 서버 무수정 — 접을 때 폴더 삭제로 끝난다).
  2. 같은 참조음성 + **실제 대본에서 뽑은 12문장**(성격 분류: 담담·대비강조·질문형·고유명사·숫자·정서·장문).
     ⚠ 성격 분류가 핵심이다 — "억양" 같은 불만은 **어떤 문장에서 갈리는지**를 봐야 판정이 된다.
  3. 공정 조건: **speed 1.0 정속**(앱 atempo 1.15 미적용) · seed 고정 · 워밍업 1회 후 RTF 측정.
  4. **블라인드 청취 페이지**(라벨 가리고 문장마다 순서 섞기 → 다 고른 뒤 정답 공개).
     🔑 바꾸고 싶다는 기대가 판단을 흔들기 때문에 **블라인드가 없으면 이 실험은 의미가 없다.**
  5. 소리 판정은 **로이가 한다**(내가 못 듣는다). 나는 속도·발음 오류 같은 잴 수 있는 것만 낸다.
- ⚠ **설치 함정 2개(윈도우, 다음 파이썬 설치 때 또 만난다)**:
  1. `D:\miniconda3\pkgs` **쓰기 권한 없음**(대청소 때 손본 옛 SID ACL 잔재가 남아 있다) → `conda create` 실패.
     **`CONDA_PKGS_DIRS` 를 프로젝트 폴더로 돌리면 관리자 권한 없이 우회**된다.
  2. `openai-whisper` 빌드가 `ModuleNotFoundError: pkg_resources` 로 실패(setuptools 81+ 에서 제거된 모듈).
     🔑 **환경에 `setuptools<81` 을 깔아도 소용없다** — pip 가 **빌드 격리 환경에 최신 setuptools 를 새로 깐다.**
     `PIP_CONSTRAINT` 도 이 케이스에선 안 먹었다. **`--no-build-isolation` 으로 그 패키지만 따로 설치**해야 풀린다.
- ⏳ **안 해본 카드(다시 볼 일이 생기면 여기부터)**: `llm.rl.pt`(가중치에 함께 온 강화학습 튜닝판 — 억양이
  더 살아 있을 가능성) · fp16/vLLM 로 속도 만회 · **하이브리드**(억양이 중요한 도입부 훅 5샷 약 40초만
  instruct 로 뽑고 본문은 OmniVoice → 편당 추가 1분 안쪽이라 속도 타격이 사실상 없다).

## ⚡ 다중작업 시간 단축 — 업스케일 폐지(1080p 직접 생성) + 비디오를 TTS 와 병렬 (2026-08-12, v0.2.99)
> 질문(로이): "다중작업을 할 때 시간을 단축할 방법". 우선순위는 **① 안정 ② 속도**(로이 명시).
- **실측 분해(서버 로그 + 출력물 mtime)** — 성경 롱폼 5편 75분:
  · TTS **52.7분(70%)** · 이미지 6.0분(TTS 와 이미 병렬) · 비디오 5.2분 · 업스케일 11.9분
  · **TTS 서버가 논 시간 21.1분 = 28%** (대본마다 TTS 끝난 뒤 비디오→업스케일→vrew 를 하느라).
  역사 4편(88분)도 같은 구조로 유휴 17.5분(20%). 대본 전환 자체는 0.2분으로 이미 매끄러웠다.
- 🔑 **핵심 측정 3개(추측 아님 — 다음 판단의 근거로 재사용할 것)**:
  1. **업스케일 ∥ TTS = TTS 가 1.8배 느려진다.** 자연 실험으로 확인 — 내 벤치 10건이 0815 업스케일
     구간(17:20:53~17:23:40)과만 겹쳤을 때 **RTF 1.26**, 업스케일이 끝난 직후 **RTF 0.70**.
     (⚠ 나는 처음에 이걸 "앱 TTS 와 간섭"으로 오진했다. 로그의 클라이언트 IP·시각을 대조해야 갈린다.)
  2. **TTS `/tts-batch` 이득은 1.20배뿐**(유휴 GPU 실측: 순차 문장당 1.89s → 배치 1.57s).
     배치 16 으로 늘려도 1.23배 = **GPU 이미 포화**. 핵심 경로를 뜯을 만한 이득이 아니라 **보류**.
  3. **comfy.org 콜드스타트는 없다** — 이미지는 첫 장부터 6초 간격 균일, 비디오 첫 배치도 29초.
     "몰아서 만들면 콜드스타트 절약" 가설은 데이터가 지지하지 않는다(이득의 정체는 **기다림 제거**).
- **① 업스케일 폐지 = i2v 를 처음부터 1080p 로 생성**(`comfy-video._videoDims` 1280x704 → **1920x1088**,
  9:16 은 1088x1920, 1:1 은 1440x1440. ⚠ LTX 는 변이 **32 의 배수**여야 해서 1080 이 아니라 1088).
  실측 대조(같은 이미지·프롬프트): **704 생성 24.5s + 업스케일 43.1s = 67.6s** vs **1080p 직접 41.3s**.
  → 더 빠르고 · **로컬 GPU 점유 0**(경합 소멸) · 화질도 직접 생성이 더 선명(업스케일본은 돌담·원경이 뭉갬).
  `maybeUpscale` 은 **해상도를 보고 자동 생략**(`getMediaInfo` 로 목표 이상이면 skip) → 옛 1280x704 영상이나
  다른 엔진 결과는 **그대로 업스케일**(하위호환). 되돌리려면 `_videoDims` 값만 옛 값으로.
- **② ComfyUI 비디오도 TTS 와 병렬**: `videoPipeline = canParallel && (grokVideoPipeline || comfyVideoPipeline)`.
  comfy 는 **클라우드**라 로컬 GPU(TTS)와 자원이 안 겹친다. videoStage 안의 호출을 `genGroupVideos` 로 바꿔
  엔진 분기(grok/grok-api/comfy)를 한 곳에 모음 — **Grok 경로는 인자·동작 그대로**. 단위검증 7케이스 통과
  (comfy·comfy::path=병렬 / grok·grok10=기존유지 / grok-api·none=순차 / canParallel=false 면 순차).
- **③ 작업 완료 시 탐색기 자동 열기 폐지**(로이 요청 — .vrew 만 열리면 충분, 큐 5편이면 창이 5개 쌓였다).
  `runMakeAllCore` 끝·`run-batch` 끝 두 곳 제거 + 죽은 `openFolder` 파라미터·주석 정리.
  **헤더 '출력폴더' 버튼·플리/출판/프리미어 XML 경로는 그대로**(별개 흐름).
- **기대**: 편당 유휴 3.8분 → 약 1.2분. 5편 75분 → **약 56분(25%)**. 로컬 GPU 를 쓰는 단계가 **TTS 하나만** 남음.
- ❌ **레인 분리(대본 간 TTS·이미지·비디오를 각각 쭉 흘리기)는 하지 않기로 결정**(로이). 이론상 최대 28%지만
  `S.parsed`/`S.outRoot`/`S.mode` 가 **전역 슬롯 1개**라 여러 대본이 동시에 살면 서로 덮어쓴다 →
  **대본이 섞이는 사고**(v0.2.76 영상 47개 오생성·v0.2.77 스타일 누락과 같은 계열). 안정 우선이라 보류.
- ⏳ **미검증**: 실제 대본 1편 전 과정. 첫 작업에서 로그에 `⬆ 업스케일 생략 — 이미 1920x1080 이상…` 과
  `🎬 … 즉시 비디오 생성(파이프라인·Comfy 클라우드)` 이 뜨는지 확인할 것.
  ⚠ 크레딧: 픽셀이 2.3배라 GPU 시간 과금이면 비디오분이 늘 수 있다(생성당 과금이면 동일) — 대시보드 확인 권장.

## 🔴 큐 7개가 전부 `imgEngine is not defined` — 같은 계열 사고 **두 번째** + 실행 E2E 신설 (2026-08-20, v0.3.22)
> 로이 로그: `✗ 실패: [승리_0821] … — imgEngine is not defined (다음 대본 계속)` × 7 → `성공 0 · 실패 7`
- **원인(내 실수)**: `runMakeAllCore` 안에서 **이미지 엔진 변수명은 `engine`**(비디오만 `videoEngine`)인데
  로컬 GPU 판정을 넣을 때 `isComfyVal(imgEngine)` 로 썼다. 그 줄은 함수 **맨 앞에서 무조건 실행**되므로
  큐의 모든 대본이 즉시 죽었다. (main.js 안에 `imgEngine` 이라는 이름이 **다른 함수 6곳**에 실제로 있어
  눈으로는 자연스러워 보인다 — 그게 함정이다.)
- 🔴 **하루에 같은 계열로 두 번 깨졌다**(앞: `onPickImgEngine` 삭제). 공통점: **미정의 식별자는 빌드가 못 잡고,
  그 코드가 실제로 실행될 때만 드러난다.** 단위테스트도 화면 E2E 도 그 함수를 **실행하지 않아** 통과했다.
  (게다가 내가 만든 pipeline-mode 테스트는 정규식으로 **버그가 있는 그 줄을 그대로 단언**하고 있었다.)
- ✅ **대책 = `test/makeall-dry.smoke.js` 신설 — 「⚡ 만들기」를 실제로 끝까지 돌린다.**
  · **무음(dry) 모드**라 TTS 서버·ComfyUI·브라우저를 전혀 부르지 않는다(무음 wav + 단계 진행만).
  · **임시 채널**을 만들어 출력 폴더를 임시 디렉터리로 돌린다 → 사용자의 G: 작업물을 건드리지 않는다.
    채널 이름에 **pid** 를 붙이고, 앞선 실행이 남긴 `__테스트채널_…` 을 먼저 지운다(이름 충돌로 흔들리지 않게).
    끝나면 채널·폴더를 삭제한다(실측: 3연속 실행 후 채널 누수 0).
  · 이미지 엔진을 `comfy::dummy.json`(=로컬 comfy)로 지정해 **이번에 깨진 `_imgLocalGpu` 줄을 반드시 지나가게** 한다.
  · 단언: 예외 없음 · 로그에 `is not defined`·`✗ 실패` 없음 · **1단계→4단계 도달 + 정상 종료 + 절전차단 해제** ·
    **이미지 게이트가 .vrew 를 막았다**(무음엔 이미지가 없으니 이게 정상 — v0.3.10 게이트가 살아 있다는 증거) ·
    음성 누락 게이트는 안 걸림 · 무음 wav 6개 생성.
  · 🔑 **역검증했다** — 버그를 일부러 되돌리면 `ERR: … ReferenceError: imgEngine is not defined` 로 **실패한다.**
    (테스트가 진짜로 그 사고를 잡는지 확인하지 않으면 통과는 의미가 없다.)
- 보조 대책: `pipeline-mode.test.js` 가 **runMakeAllCore 본문을 잘라 `imgEngine` 이 없는지** 대조한다.
  ⚠ 본문 자르기 함정 — 인자 기본값 `opts = {}` 때문에 첫 `{` 부터 세면 **시그니처만** 잘린다(`) {` 뒤부터 세야 한다).
- 🔑 **교훈**: 파이프라인처럼 **긴 함수에 한 줄을 끼워 넣을 때도** 그 함수를 한 번 실행하는 검증이 필요하다.
  `npm run test:makeall` 은 20초면 끝난다 — main.js 를 만졌으면 이걸 돌릴 것.

## 🔴🔇 음성 40개 / 누락 898개인 **반쪽 .vrew** 가 나갔다 — TTS 누락 게이트 (2026-08-20, v0.3.22)
> 로이 로그: `⚠ 컷41 TTS 실패(1/3) — 재시도: This operation was aborted` ×3 →
>   `롱폼 음성 오류: TTS 실패(컷41, 3회 시도)` → 그런데도 `📦 4단계` 가 진행돼
>   `[Vrew] TTS 누락 sentence: 41 … 938` + `✓ …vrew (clip 59)`. 로이: "지금 TTS 변환하지 않고 vrew를 만들었어."
- 🔑 **원인 두 겹**:
  ① **로컬 GPU 경합** — 그 로그는 `⚡ 1·2·3단계 파이프라인 — TTS ∥ 이미지 ∥ 비디오` 로 시작한다(=위 항목을
     고치기 **전** 버전). 로컬 ComfyUI 이미지가 3060 을 점유해 OmniVoice 가 **60초 타임아웃 3회** 났다.
     증거: 이미지 21장에 **931.5초(44초/장)** — 단독 실측 28.7초/장의 1.5배. 위 항목의 레인 분리로 해결된다.
  ② 🔴 **그러나 진짜 문제는 게이트가 없던 것.** `missingVisualGroups`(이미지) 게이트는 v0.3.10 에 넣었는데
     **음성엔 게이트가 없었다.** vrew-builder 는 `TTS 누락 sentence:` 경고만 찍고 그대로 만든다 →
     **앞부분만 소리가 나는 영상**이 조용히 나간다. TTS 가 어떤 이유로든(서버 다운·네트워크) 실패하면 재발한다.
- **수정 ① `missingTtsNums(pr)` 게이트** — 음성 파일이 **실제로 존재하지 않는** 문장이 하나라도 있으면
  그 편 .vrew 를 건너뛰고 `warnMissingTts` 팝업으로 몇 개/어느 컷인지 알린다. **💾 export-vrew · ⚡만들기 4단계
  두 경로 모두**(한쪽만 막으면 다른 쪽으로 새어 나간다 — v0.3.15 의 막다른 길 교훈과 같은 계열).
- **수정 ② 한 문장 실패가 대본 전체를 버리지 않게** — `fillTtsList` 가 3회 실패 시 `throw` 하던 것을
  **그 문장만 건너뛰고 계속**으로 바꿨다(실패 번호는 `failed[]` 에 모아 끝에 요약). 예전엔 컷41 하나가
  나머지 **897문장을 시도조차 못 하게** 만들었다. 이제 「🎤 TTS」를 다시 누르면 **빠진 것만** 새로 합성한다.
  ⚠ 단 **연속 5문장 실패면 그 대본을 멈춘다** — 서버가 죽었을 때 900문장 × 3회 × 60초를 헛돌지 않게.
- **수정 ③ 원인을 알 수 있는 메시지** — AbortError 의 기본 문구는 `This operation was aborted` 뿐이라
  무엇을 봐야 할지 알 수 없었다 → `OmniVoice 가 60초 안에 응답하지 않았습니다 — 서버가 바쁘거나(같은 GPU 에서
  이미지 생성 중) 멈춰 있습니다 (주소)`. (v0.3.7·v0.3.17 의 `fetch failed` 정체 밝히기와 같은 계열, 다섯 번째다.)
- 검증: `npm run test:tts` **18/18** — **main.js·pipeline.js 원문에서 함수를 뽑아 실행**:
  누락 판정(경로만 있고 파일이 없는 것도 잡는다) · **실사고 재현(938문장 중 898개 누락 검출)** ·
  게이트가 두 경로에 다 있고 `continue` 로 건너뛰는지 · 옛 `throw` 가 사라졌는지 · 연속 실패 상한 존재 ·
  성공 시 카운터 초기화 · 타임아웃 메시지가 사람 말인지.

## 🖥 비디오 **로컬(🖥 LTX2.5) 복구** — 아래 「클라우드 전용」을 같은 날 되돌림 (2026-08-20, v0.3.22)
> 로이: "비디오에서 로컬 LTX2.5도 추가해줘." (⚠ 바로 아래 항목에서 **그날 오전에 없앤 것**을 오후에 복구했다.
>   아래 항목은 이력으로 남겨 둔다 — 왜 없앴는지가 지금도 유효한 경고이기 때문.)
- **되살린 곳 4군데**(제거할 때의 그 4군데와 대응):
  ① `COMFY_SIDES.video` 에 `false` 복귀 → 헤더 「③ 비디오」에 **🖥 LTX2.5 · 🖥 LTX2.3** 항목이 나온다.
  ② ⚙ 설정 비디오 탭이 다시 **2분할**(🖥 로컬 주소 / ☁ 클라우드 주소+키). 🔑 **`localAllowed` 를 별도 조건으로
     두지 않고 `COMFY_SIDES` 를 읽게 바꿨다** — 예전엔 드롭다운과 주소칸이 **각자 판단**해서 한쪽만 고치면
     "드롭다운엔 있는데 주소칸이 없다" 가 됐다. 이제 **스위치가 하나**다(다음에 또 접거나 펼 때 한 줄만 고친다).
  ③ 탭 안내문에서 「클라우드 전용」 문구 제거 + **로컬 조건 명시**(필요 모델 파일 이름) · 「동시」 표시를
     `클라우드 3개 동시 · 로컬 1개씩` 으로. ④ 탭을 열 때 **양쪽 램프 실측**(`probeBoth('video')`).
- 🔑 **이게 없으면 기능이 조용히 죽는다 — `comfy-video.loadConfig()` 의 `cloud:false → true` 강제 교정 제거.**
  그 줄을 남겨 두면 헤더에서 🖥 로컬을 골라 저장해도 **다음 로드에 클라우드로 되돌아간다**(선택이 안 먹는데
  이유가 화면에 안 보인다). 대신 **주소 정합만** 지킨다(`cloud` 와 `baseUrl` 이 어긋나면 그쪽 주소로).
  E2E 로 왕복 확인: 드롭다운 ☁→🖥 전환 시 설정이 실제로 `cloud=false` 로 남고 baseUrl 이 로컬 주소가 된다.
- 🔴 **새로 막은 구멍 — 수동 「🎬 비디오」 버튼이 TTS 와 같은 3060 을 다툰다.** 로컬 i2v 는 내 PC GPU 를 쓰는데
  `video-build`·`video-group` 은 **어떤 레인도 잡지 않았다**(비디오가 클라우드 전용이던 시절엔 그럴 필요가 없었다)
  → 「🎤 TTS」 누른 뒤 「🎬 비디오」를 누르면 그대로 동시에 돈다. 이미지에서 겪은 *"수동 버튼은 여전히 겹쳤다"* 와
  같은 계열이라 처음부터 막았다: **`genGroupVideosManual()`** 이 `_vidUsesLocalGpu(engine)` 일 때만
  `_runOnLanes(['localGpu'])` 로 감싼다.
  ⚠ **make-all/run-batch 는 이 래퍼를 쓰지 않는다** — 그 경로는 이미 TTS 레인을 쥔 채 `genGroupVideos` 를 직접
  부르므로 여기서 또 레인을 잡으면 **자기 자신을 기다려 교착**된다(`enqueueImageJob` 과 같은 규칙). 테스트가
  runMakeAllCore 본문을 잘라 이 래퍼가 없는지 대조한다.
- **파이프라인은 이미 맞게 되어 있었다**(v0.2.99·오전 수정 덕): `comfyVideoPipeline` 은 **클라우드일 때만** true 라
  로컬 비디오는 자동으로 **순차 3단계**로 간다. 조합 검증 추가 — 이미지 로컬 + 비디오 로컬 = **완전 순차**.
  엔진 쪽도 무수정으로 동작한다(로컬이면 `freeMemory()` 로 이전 모델 언로드 + **동시 1개 강제**).
- 🔴 **실측 — 지금 이 PC 로 로컬 LTX2.5 는 못 돌린다(모델이 없다).** 실제로 i2v 1건을 로컬 8188 로 보내 봤다:
  업로드·그래프 주입(49노드)·프롬프트까지 정상이고 **모델 단계에서 멈춘다.** 로컬 ComfyUI(0.32.0)의 UNET 목록은
  `krea2_turbo_int8_convrot` · `z_image_turbo_bf16` **둘뿐**이라, 워크플로가 요구하는 **5개가 없다**:
  unet `ltx-2.5-22b-distilled-transformer-comfy-int8-convrot` · clip `gemma4-12b-with-proj-ltx-2.5-comfy-int8-convrot`
  · vae `ltx-2.5-video-vae-bf16` · vae `ltx-2.5-audio-vae-bf16` · upscaler `ltx-2.5-latent-spatial-upscaler-x2-bf16-1.0`.
  · 🔑 **모델 자동 대체(v0.3.22)로도 안 된다** — 그건 같은 모델의 **정밀도·양자화 판**만 바꾼다. 로컬엔 ltx 계열이
    아예 없으므로 **아무것도 안 바꾼다**(엉뚱한 모델로 조용히 그리지 않는다 = 의도된 fail-closed).
  · 대신 **오류가 사람 말로 나온다**(실측 그대로): `로컬 ComfyUI 에 모델 'ltx-2.5-22b-…' 가 없습니다 (unet_name).
    (그 밖에 4개 모델도 없음: clip_name, vae_name, vae_name, model_name) 그 서버에 있는 것: krea2…, z_image… →
    헤더 「③ 비디오」 드롭다운에서 ☁ 클라우드 로 바꾸거나, 그 파일을 로컬 ComfyUI 에 설치하세요.`
  · ⚠ **파일을 깔아도 22B 는 3060(12GB)에 안 올라간다** — 시스템 RAM 으로 넘겨 매우 느려지거나 실패한다
    (지금 RAM 여유 3GB). 로컬을 실제로 쓰려면 **더 작은 i2v 워크플로를 ＋추가**하거나, **로컬 주소를 다른 PC 의
    ComfyUI 로** 돌리는 쪽이 현실적이다(주소칸이 그 용도로 열려 있다).
- 검증: `npm run test:comfy` = 모델 41/41 · **파이프라인 31/31**(로컬 비디오 조합 + `cloud=true` 강제가 사라졌는지 ·
  래퍼 배선 · make-all 본문에 래퍼 없음) · GPU 레인 14/14 · **2분할 E2E 69/69**(비디오 탭 **칸 2개** · 「클라우드 전용」
  문구 0 · 드롭다운에 **🖥 LTX2.5 존재** · ☁↔🖥 왕복 후 설정 복구 · 램프 로컬 0.32.0 / 클라우드 v0.33.2).
  회귀: 만들기 무음 E2E 18/18 · TTS 게이트 18/18 · 삭제 27/27 · 파서 11/11 · visual 14·10·13·7.

## ☁ 비디오는 **클라우드 전용** — 로컬 항목 제거 (2026-08-20, v0.3.22) ⚠ **위 항목으로 같은 날 되돌림**
> 로이: "비디오 생성도구에서 로컬 이용 항목은 제거하자. 성능이 따라가질 못해."
- **근거**: LTX2.5·2.3 은 **22B** 라 RTX 3060(12GB)에서 못 돌아간다. 실측으로도 확인 — 로컬로 i2v 를 보내면
  `unet_name: 'ltx-2.5-22b-distilled-transformer-…' 가 없습니다` 로 즉시 실패한다(그 모델이 로컬에 아예 없다).
  **고를 수 있게 두면 고르는 순간 실패**하는 항목이라 없애는 게 맞다.
- 지운 곳 **4군데**(하나라도 남기면 어긋난다):
  ① 헤더 드롭다운 — `COMFY_SIDES = { image: [true, false], video: [true] }` 로 optgroup 생성을 제어(🖥 항목·🖥 표시 사라짐)
  ② ⚙ 설정 비디오 탭 — `ComfyTargets` 의 `localAllowed`(kind!=='video')로 **로컬 칸 자체를 안 그린다**.
     칸이 하나면 `.split2.one` 로 한 줄 전체를 쓰고, 「지금 보내는 곳」 띠에 **「비디오는 클라우드 전용」** 이유를 적는다.
  ③ 탭 안내문·하단 문구에서 로컬 언급 제거(옛 문구는 "로컬은 별도 워크플로를 ＋추가하세요" 였는데 **이제 거짓**이다),
     「동시」 표시도 `3개 — 앱 고정`(로컬 1개씩 문구 삭제)
  ④ 탭을 열 때 **로컬 램프를 재지 않는다**(`probeBoth('video')` → `probeComfyTarget('video','cloud')`) — 헛 요청 제거
- 🔑 **옛 설정도 교정한다**: `comfy-video.loadConfig()` 가 `cloud === false` 면 **true 로 올리고** baseUrl 이
  로컬 주소면 클라우드 주소로 바꾼다. 안 하면 드롭다운 값과 일치하는 option 이 없어 **빈칸**으로 보이고,
  생성은 로컬로 나가 실패한다(v0.3.14 의 `concurrency` 상한 클램프와 같은 정책).
  ⚠ **되돌리는 법**: `COMFY_SIDES.video` 에 `false` 추가 + `localAllowed` true + loadConfig 의 두 줄 삭제.
- ⚠ **이미지는 그대로 로컬·클라우드 둘 다** 제공한다(로컬 Krea2 int8 은 3060 에서 실측 28.7초/장으로 돌아간다).
- 검증: 2분할 E2E **61/61** — 비디오 탭 **칸 1개**·로컬 칸 없음·「클라우드 전용」 안내 · **비디오 드롭다운에
  `::local::`·🖥 0개**·클라우드 항목은 존재 · **이미지 드롭다운은 로컬·클라우드 둘 다 유지**.

## 🖥+☁ 이미지=로컬 · 비디오=클라우드 조합 — 파이프라인이 **로컬 이미지를 TTS 와 겹치던 것** (2026-08-20, v0.3.22)
> 로이: "이미지는 로컬에서 만들고 비디오는 클라우드에서 만들어야 해. 이미지까지는 어떻게 되는데 비디오는 내 PC 에서 불가능하네"
- **조합 자체는 원래 지원된다** — 이미지·비디오는 **설정 파일이 따로**(`comfy-image-config.json` /
  `comfy-video-config.json`)이고 각자 `cloud` 플래그와 헤더 드롭다운을 가진다. 헤더에서 ② 이미지 `🖥 Krea2`,
  ③ 비디오 `☁ LTX2.5` 를 고르면 그대로 동작한다(로컬 비디오는 LTX2.5 가 **22B** 라 3060 에서 불가능 — 정상).
- 🔴 **그런데 「⚡ 만들기」 파이프라인이 깨져 있었다.** `const noLocalGpuImg = true` 가 **하드코딩**이었다
  (주석: "이미지 = Flow+Genspark 순환(브라우저) — 로컬 GPU 미사용"). ComfyUI 로컬 이미지를 고르면 그 가정이
  거짓인데 코드는 계속 **TTS ∥ 이미지** 로 돌렸다 → **같은 3060 을 다툰다.**
  (실측 계열 근거: v0.2.99 에서 **로컬 업스케일 ∥ TTS = TTS 가 1.8배 느려짐**(RTF 0.70→1.26)을 확인했다.)
- **수정**: `_imgLocalGpu = isComfyVal(imgEngine) && !comfy-image.loadConfig().cloud` 로 **실제 설정을 읽어** 판정.
  · 🔑 **그렇다고 비디오 병렬까지 버리지 않는다** — 비디오가 클라우드면 로컬 GPU 와 겹치지 않으므로,
    **`(TTS → 이미지)` 를 순차로 묶고 그 옆에서 클라우드 비디오만 병렬**로 돌리는 분기를 새로 뒀다.
    로그: `⚡ 파이프라인 — (TTS → 이미지: 둘 다 내 PC GPU 라 순차) ∥ 비디오(클라우드)`.
  · `comfyVideoPipeline` 도 **클라우드일 때만** true 로 좁혔다(로컬 comfy 비디오를 TTS 와 겹치지 않게).
- **조합별 결과**(단위검증으로 고정): 이미지로컬+비디오클라우드 = `tts→img ∥ video` / 둘 다 클라우드 =
  `tts ∥ img ∥ video`(기존 유지) / 순환·나노바나나 이미지 = 기존 유지 / 이미지로컬+Grok = 완전 순차 /
  비디오 로컬 = 비디오만 순차 3단계 / dry·그룹재구성 = 순차(기존 규칙).
- 🔴 **두 번째 구멍 — 수동 버튼은 여전히 겹쳤다**(로이 재확인 질문 "그 장치가 준비되어 있는거지?").
  `ttsJobChain` 과 `imageJobChain` 이 **별개 큐**라 「🎤 TTS」 누른 뒤 「🖼 이미지」를 누르면 **동시에 돌았다**
  (make-all 진행 중에 🔄 단건 재생성을 눌러도 같다). 파이프라인만 고치면 절반만 막힌 것.
  → **작업 레인 3개**(`_LANES = {tts, image, localGpu}`)로 재구성하고 한 작업이 필요한 레인을 **함께 잡는다**:
  · TTS = `['tts','localGpu']` (OmniVoice 는 항상 내 PC GPU)
  · 이미지 = 로컬 ComfyUI 면 `['image','localGpu']` · 순환/API/클라우드면 `['image']` (**클라우드는 예전처럼 병렬**)
  · `Promise.allSettled` 로 잡으므로 앞 작업이 실패해도 다음이 실행된다. 대기 시 로그로 이유를 남긴다
    (`⏳ … 내 PC GPU 작업이 끝난 뒤 시작합니다`).
  ⚠ **교착 없음**: make-all/run-batch 는 TTS 레인을 쥔 채 내부에서 이미지를 만들지만 `enqueueImageJob` 을
    다시 부르지 않는다(runRotatingImages/runComfyImages 직접 호출) — 단계 순서는 runMakeAllCore 가 정한다.
  ⚠ `image-build`·`regen-group` 이 **engine 을 세 번째 인자로 넘겨야** 레인을 잡는다(안 넘기면 조용히 병렬로 돈다).
- 검증: `npm run test:comfy` 에 두 테스트 추가 —
  · `test/pipeline-mode.test.js` **20/20**: main.js 원문에서 판정 줄을 정규식으로 대조(옛 하드코딩이 없는지 ·
    설정의 cloud 를 실제로 읽는지 · 새 분기가 TTS→이미지를 순차로 묶는지) + 조합 9가지.
  · `test/gpu-lanes.test.js` **14/14**: **main.js 원문에서 `_runOnLanes` 를 뽑아 실행** — TTS ∥ 로컬이미지를
    동시에 요청해도 **최대 동시 1**(순서까지 확인) · TTS ∥ 클라우드이미지는 **최대 동시 2**(병렬 유지) ·
    로컬이미지끼리도 순차 · 앞 작업 실패에도 다음 실행 · 대기 로그 · 레인 누수 없음.
- ⚠ **속도는 사용자의 선택**: 로컬 이미지는 실측 **28.7초/장**(클라우드 12~18초)이고 이제 TTS 와 겹치지도 않으니
  총 시간이 늘어난다. 큐를 여러 개 돌릴 땐 ☁ 클라우드가 유리하고, 크레딧을 아낄 땐 🖥 로컬이 유리하다.

## 🔁 모델 파일명이 서버마다 달라 **로컬에서만 생성 실패**하던 것 — 자동 대체 (2026-08-20, v0.3.22)
> 로이 로그: `✗ G1 실패: 워크플로 노드 오류: … unet_name: 'krea2_turbo_fp8_scaled.safetensors' not in
>   ['krea2_turbo_int8_convrot.safetensors', 'z_image_turbo_bf16.safetensors']`
- 🔑 **원인 = 같은 모델의 양자화 판이 서버마다 다르다.** 번들 워크플로 `image_krea2_turbo_t2i` 는
  `krea2_turbo_**fp8_scaled**` 를 요구하는데 **클라우드엔 있고**(실측: krea2_turbo 판 5개 — fp8_scaled·bf16·
  int8_convrot·mxfp8·nvfp4) **로컬엔 `int8_convrot` 뿐**이었다. CLIP(`qwen3vl_4b_fp8_scaled`)·VAE(`qwen_image_vae`)·
  LoRA(`krea2_darkbrush`)는 **양쪽 다 있었다** → **UNET 파일명 하나 때문에** 로컬 생성이 통째로 실패.
  즉 "한 워크플로로 로컬·클라우드 왕복"(2분할의 목적)을 하려면 앱이 이 차이를 흡수해야 한다.
- 🔑 **미리 물어보지 않는다 — 실패 응답이 목록을 담아 준다.** ComfyUI `/prompt` 400 의 `node_errors` 에
  `value_not_in_list` 와 함께 **그 서버가 가진 파일 목록**이 들어 있다(`extra_info.input_config[0]`, 없으면
  `details` 문자열). → **정상일 때 추가 요청 0회**, 실패했을 때만 그 목록으로 고쳐 **1회 재제출**.
  ⚠ 매 장 `/object_info` 를 받는 방식은 안 썼다 — **클라우드 응답이 수 MB**(UNET 항목만 376개)라 낭비다.
  고친 내용은 `_modelFixes` 에 기억해 **두 번째 장부터는 헛왕복도 없다**(`applyRemembered`).
- 🔑 **대체 규칙 — 정밀도·양자화 토큰만 떼고 나머지가 같을 때만 바꾼다**(`core/comfy-models.js`).
  `krea2_turbo_fp8_scaled` ↔ `krea2_turbo_int8_convrot` → 둘 다 `krea2 turbo` = OK.
  🔴 **`z_image_turbo_bf16` → `z_image_bf16` 은 안 바꾼다** — turbo·distilled·dev 는 **모델 정체성**이라 떼지 않는다.
  (여기서 느슨하게 매칭하면 "엉뚱한 모델로 조용히 그려진다" = v0.3.20 의 '남의 영상' 계열 사고가 된다.)
  후보가 여럿이면 원래 정밀도 토큰이 겹치는 것 → 이름 짧은 것 순. **같은 모델이 없으면 아무것도 안 바꾼다.**
- **오류 메시지를 사람 말로**: 예전엔 raw JSON 이 300자에서 잘려 읽을 수가 없었다 → `explain()` 이
  「로컬 ComfyUI 에 모델 'X' 가 없습니다 (unet_name). 그 서버에 있는 것: A, B → 헤더 「② 이미지」에서
  ☁ 클라우드로 바꾸거나 그 파일을 설치하세요」로 바꾼다.
- **실측 검증(이게 결정적)**: 로컬 ComfyUI(8188)로 **실제 1장 생성 성공** — 로그에
  `🔁 모델 자동 대체: krea2_turbo_fp8_scaled → krea2_turbo_int8_convrot`, **28.7초 · 1344x768 · 1186KB 정상 그림**.
  → RTX 3060(12GB)에서도 Krea2 Turbo **int8 판은 돌아간다**(참고: 클라우드는 장당 12~18초라 여전히 더 빠르다).
- 검증: `npm run test:comfy` = 단위 **41/41**(위 로그 원문에서 목록 파싱 · turbo→비turbo 거부 · distilled→dev 거부 ·
  대체본 없으면 그래프 불변 · **엔진 원문에 `_queueFixing` 배선이 남아 있는지 대조**) + 2분할 E2E 62/62.
- ⏳ 남은 것: 비디오(LTX)는 같은 코드를 타지만 **실사용 미검증**(로컬 LTX2.5 는 22B 라 3060 에서 못 돈다).

## 🖥☁ ⚙ 설정의 ComfyUI 탭 = 로컬/클라우드 **2분할** + 이중 진입점 제거 (2026-08-20, v0.3.22)
> 로이: "서버는 뭐고 주소는 뭐지? 그 아래 클라우드 체크표시는 뭐고? 워크플로우는 뭐야? 타임아웃은?
>   컴피유아이 로컬과 클라우드를 화면 2분할 하여 만들 수는 없는 건가."
- 🔴 **질문 자체가 버그 신고였다.** 로이가 보낸 스크린샷에 **서버=「[클라우드] comfy.org」인데 주소는
  `http://127.0.0.1:8188`·클라우드 체크는 꺼짐** 인 어긋난 상태가 찍혀 있었다(설정파일 실측으로 확인).
  원인 2개: ① `activeServer`(서버 프로필 이름)는 **표시용 라벨일 뿐** 실제 값과 연동되지 않는다
  ② 「클라우드」 체크박스가 **헤더 드롭다운(☁/🖥 × 모델)과 같은 값**을 만지는 두 번째 진입점이었다
  (`pickComfy` 가 cloud+baseUrl+workflowPath 를 함께 저장한다). v0.3.2 의 「워크플로 select 이중화」와 같은 계열.
- **바꾼 것 — 팝업은 「어디로 보낼까」를 정하지 않는다. 주소·키만 관리한다.**
  · 「서버 프로필」 행(select+💾+🗑)·「주소」 단일 행·「클라우드」 체크박스 **전부 제거** →
    **`ComfyTargets` 2분할 패널**(이미지·비디오 **공용 1벌** — 두 벌 복사하면 다음 사람이 엉뚱한 쪽을 고친다).
    왼쪽 **🖥 로컬(내 PC)** = 주소 / 오른쪽 **☁ 클라우드(comfy.org)** = 주소 + API키. 각 칸에 **🔌 테스트 + 램프**.
  · 맨 위에 **「지금 보내는 곳 ☁/🖥 · 모델 이름」** 띠 — **표시 전용**이고, 바꾸는 곳은 헤더 드롭다운뿐이라고 적어 둔다.
    지금 쓰는 칸은 주황 테두리 + 「지금 사용」 배지로 눈에 보이게 → 스크린샷 같은 어긋남이 **한눈에** 드러난다.
  · 관련 함수 6개(`pickComfyServer`/`saveComfyServer`/`removeComfyServer` × 이미지·비디오)와 하단의 중복
    「🔌 연결 테스트」 버튼 2개·죽은 `testComfy` 제거. ⚠ 설정파일의 `servers`·`activeServer` 필드는 **코드가
    더는 읽지 않으므로 방치해도 무해**(되돌릴 여지로 남김).
- 🔑 **램프는 진짜로 찔러 본다 — 그런데 엔진 `health()` 를 쓰지 않는다.** `health()` 는 클라우드일 때
  **API 키 유무만** 본다(생성 도중 일시적 네트워크 오류로 작업이 통째로 죽지 않게 하려는 의도 — v0.3.17 계열).
  그래서 main 에 **`probeComfy(kind, cfg, side)`** 를 따로 뒀다: **저장된 설정을 바꾸지 않고** 엔진 인스턴스만
  그 쪽 값으로 만들어(주소 정규화·`/api` 접두·`X-API-Key` 를 **생성 경로와 같은 코드**로 얻는다) `/system_stats` 만 호출.
  · 실측: **로컬 200(ComfyUI 0.32.0) · 클라우드도 200**(`/api/system_stats`, 258바이트·약 0.4초 → 버전 v0.33.2).
    클라우드가 이 경로를 준다는 걸 확인해서 램프가 "키가 있다"가 아니라 **정말 닿는다**를 뜻하게 됐다.
  · **⚙ 를 열거나 탭을 누르면 양쪽을 자동 실측**한다 → "로컬이 꺼져 있는데 로컬로 보내고 있었다"를 만들기 전에 안다.
  · ⚠ 🔌 테스트는 **화면의 입력값을 함께 보낸다**(`{side, baseUrl, apiKey}`) — 버튼 클릭과 `onBlur` 저장이
    경합해서 **방금 고친 주소가 아니라 옛 주소를 재는** 일을 막는다.
- **화면에서 답한 것**(로이 질문 그대로): 주소=지금 요청을 보내는 곳 · 클라우드체크=통신 규약(`/api`+키) 전환 ·
  워크플로=ComfyUI 「저장(API 포맷)」 JSON(설계도, 등록·삭제 전용) · 타임아웃=결과를 기다리는 최대 시간
  (넘으면 그 건만 실패 처리, 이미지 300·비디오 600) · 동시생성=앱 고정값(입력칸 제거).
- 🔴 **발행 직후 사고 — 서버 프로필 함수를 지우면서 그 아래 붙어 있던 `onPickImgEngine`·`pickComfy` 까지 잘라냈다.**
  로이가 로그에서 잡았다(`🐞 화면 오류: onPickImgEngine is not defined`). **이미지·비디오 드롭다운이 둘 다** 죽는다
  (`onPickVideoEngine` 도 `pickComfy` 를 부른다). 🔑 **JSX 핸들러 안의 참조는 빌드가 못 잡는다** — esbuild 는
  미정의 식별자를 오류로 내지 않고, 클릭할 때 런타임 ReferenceError 가 난다. E2E 도 드롭다운을 안 눌러 통과했다.
  · 되살릴 때 **직전 커밋에서 원문을 꺼내 붙였다**(`git show d444083:renderer/src/App.jsx`) — 손으로 다시 쓰면 또 다르다.
  · **테스트를 보강했다**: ① App.jsx 원문에 `onPickImgEngine`·`pickComfy`·`onPickVideoEngine`·`parseComfyVal`·
    `comfySelectValue`·`mkComfyVal` 정의 존재 단언 ② **헤더 드롭다운 ☁↔🖥 왕복**(설정 cloud·baseUrl 이 따라 바뀌고
    팝업 배지도 따라오는지 → 끝에 원래대로 복구) ③ **`pageerror`·console error 를 수집해 0건 단언**.
  · 🔑 **함수 블록을 지울 때는 "그 아래 어디까지 지워지는지" 를 눈으로 확인할 것.** 이번엔 끝 앵커를
    `async function testComfy()` 로 잡았는데 그 사이에 남의 함수 2개가 끼어 있었다.
- 검증: `npm run test:comfy` **62/62** — 두 탭 각각 칸 2개·왼쪽 로컬/오른쪽 클라우드·API키는 클라우드에만 ·
  **「지금 사용」 배지가 설정 `cloud` 와 일치** · 주소가 설정값과 일치 · 옛 select·체크박스·하단 테스트 버튼 0개 ·
  **램프 자동 실측 통과(로컬 0.32.0 / 클라우드 v0.33.2)** · 팝업이 화면 안(564px/821px, 스크롤 0) ·
  **설정파일 8개 필드가 화면을 열고 닫아도 그대로**(읽기 전용 화면 확인). 회귀: 파서 11 · 삭제 27 · grok 81 ·
  이미지/로그/프롬프트/캐시 · 타임스탬프 54+E2E.
## 🗑 이미지·비디오 일괄 삭제 버튼 + ComfyUI 「동시 생성」 입력칸 제거 (2026-08-20, v0.3.22)
> 로이: "TTS는 만들어 놓은 파일을 삭제하는 버튼이 있잖아, 이런 것처럼 이미지와 비디오도 삭제할 수 있는 버튼을 만들어줘."
>   + "동시생성은 사용자가 입력하는 방식을 제거하고 설정된 값으로만 생성하게 하자."
- **위치**: 헤더 제작 파이프라인 행의 **② 이미지 · ③ 비디오 그룹에 각각 「🗑 삭제」**(① 음성의 🗑 삭제와 같은 자리·같은 방식).
  대상은 **지금 열려 있는 대본 전체**(TTS 삭제와 동일 범위). 확인 팝업 → 파일 삭제 → 화면 즉시 갱신.
  ⚠ 비디오 삭제 버튼은 **엔진이 「없음(이미지만)」이어도 보인다** — 옛 영상을 지우려면 엔진을 바꿔야 하는 건 말이 안 되므로 분기 밖에 뒀다.
- 🔑 **파일만 지우면 안 된다 — 재활용 캐시가 되살린다.** v0.3.17 의 「캐시가 노이즈 이미지를 되살리고 있었다」와 같은 계열이라 처음부터 막았다:
  · 이미지 = `MC.del`(이번 실행 키 `_imgCacheKey` + **현재 스타일·엔진으로 다시 계산한 키** — 재시작 후엔 `_imgCacheKey` 가 없다)
    + `imageCleared = true`(스냅샷에 저장되므로 앱을 껐다 켜도 유지 → prefill 이 캐시를 안 본다).
  · 비디오 = `MC.videoKey(영상프롬프트, **원본 이미지 내용해시**, 비율, grok)`. 🔑 **이미지가 아직 있는 지금 계산해야** 지울 수 있다
    (이미지를 먼저 지우면 그 키를 두 번 다시 만들 수 없다). ComfyUI i2v 는 캐시를 안 쓰므로 이 키만 지우면 끝.
- 🔴 **가장 위험했던 것 — 참조만 보고 지우면 사용자 원본이 사라진다.** 일괄첨부(bulk-attach)는 `g.videoPath` 에
  **사용자가 고른 원본 경로를 그대로** 넣는다(미디어폴더로 복사하지 않는다). → `_inDir(file, mediaDir)` 로
  **출력폴더(media-N) 안의 파일만 unlink** 하고, 폴더 밖이면 **참조만 끊고 파일은 남긴다**(로그에 "외부 첨부 N개는 참조만 해제").
  · 덤으로 `_wipeByExt(mediaDir, /확장자/)` 로 **참조가 끊긴 고아 파일·업스케일본(`NN_1080.mp4`)까지** 정리한다.
    ⚠ BGM(`bgm_*.wav/mp3`)·자막(`.srt`)은 확장자가 달라 걸리지 않는다(테스트로 고정).
- **분업**: 이미지 삭제는 **비디오를 건드리지 않고**, 비디오 삭제는 **이미지를 남긴다**(→ 켄번스로 그대로 진행 가능).
  단건 ✕(`clear-asset`)의 "영상 먼저, 그다음 이미지" 단계 삭제와 달리 **버튼이 대상을 명시**하므로 헷갈리지 않는다.
- **⚙ 설정의 「동시 생성」 입력칸 제거**(이미지·비디오 양쪽) → **고정값 표시만**. 값은 코드가 정한다:
  이미지 **클라우드 2 · 로컬 1**(3 이상은 노이즈·검정을 유발해 `loadConfig` 가 읽을 때마다 상한 2 로 클램프 — v0.3.14),
  비디오 **클라우드 3 · 로컬 1**. 설정파일(`concurrency`)은 그대로 쓰이므로 되돌릴 여지는 남아 있다.
- 검증: `npm run test:delmedia` **27/27**(main.js **원문에서 `_inDir`·`_wipeByExt` 를 뽑아 실행** — 복사해 두면 앱과 갈라져도
  통과한다. sweep 정규식이 main.js 의 것과 문자열까지 일치하는지도 대조) + **앱 E2E**(부팅 → ①②③ 그룹에 🗑 삭제 각 1개 ·
  대본 없으면 비활성 · ⚙ 설정 두 탭에 동시 입력칸 없고 고정 문구 표시). 회귀: 파서 11/11.
## ⏱ 유튜브 설명글 타임스탬프(챕터) 복사 (2026-08-20, v0.3.22)
> 로이: "TTS변환을 하고 유튜브 설명글에 작성할 타임스탬프를 복사할수 있는 기능을 추가해줘. 버튼은 이미지의 앞부분에."
- **위치**: 작업바(capbar) **맨 앞**(✂ 분할 바로 앞)에 「⏱ 타임스탬프」 버튼. TTS 가 하나도 없으면 비활성.
  누르면 창이 뜨고(편집 가능한 textarea + 📋 복사), ESC/닫기로 종료. 대본은 바뀌지 않는다(표시 전용).
- 🔑 **시작시각의 근거**: `.vrew` 타임라인은 문장 TTS 를 **빈틈 없이 이어 붙인 것**이므로
  (vrew-builder 의 clipDurations 누적 = 총길이) **챕터 시작 = 앞 그룹들의 TTS 길이 합**. 무음·간격이 없어 값이 정확하다.
  초는 **내림**한다(올림하면 챕터가 내용보다 뒤에서 시작한다). 1시간 넘으면 `h:mm:ss`.
- 🔑 **챕터 단위 = H3 가 아니라 상위 H2** — 이게 핵심이었다. 기본 분할이 `h3` 라 그룹이 H3 섹션마다 쪼개져
  (실측 대본 40그룹) 그대로 쓰면 챕터가 40개가 된다. 대본의 H2(`## 강화도의 열아홉 살`)가 사람이 생각하는 챕터다.
  → 그룹에 **`h2Title`** 을 새로 보관(`group-builder` 의 startNewGroup)하고 DTO 에 `cut.h2` 로 노출,
  렌더러가 **연속으로 같은 H2 인 그룹을 한 챕터로** 묶는다. 실측: 철종 대본 40그룹 → **챕터 10개**(0:00 도입부 …).
  H2 가 없는 대본은 그룹 섹션명(phase)으로 폴백, 제목 없는 그룹은 **앞 챕터에 흡수**(맨 앞이면 「시작」).
- ⚠ **h2Title 을 잃는 자리를 전부 막았다**(안 막으면 조용히 H3 챕터로 되돌아간다):
  ① 도입부 TTS 재배치(`regroupIntroByTtsDuration`)는 **새 그룹을 만들므로** 원래 도입부 그룹의 제목을 물려받게 했다
  ② 그룹 분할(✂·`splitGroupAt`)·쇼츠 8초 재구성(`mergeGroupsByTts`)도 h2Title 승계 ③ 스냅샷(.smproj) 저장·복원에 포함.
- 🔑 **옛 작업본(.smproj)에는 h2Title 이 없다** → 그대로면 H3 제작메모(`〔장면·대화형 후킹 · ~48자·8s〕`)가 챕터명이 된다.
  → 복원 시 **원본 .md 를 훑어 「H3 섹션명 → 상위 H2」 맵**을 만들어 보충(`h2MapFromScript`). **순서가 아니라 이름으로**
  잇기 때문에 그 뒤 그룹을 쪼개거나 합쳤어도 맞는다. 실측: 제갈량 대본 40그룹 → H2 40개 복구 → 챕터 5개.
  대본 파일이 없어졌으면 그냥 폴백(앱 안 죽음).
- 제목 꼬리의 제작 표기 제거: `— 0:00~0:30 · I2V 5샷` / `(0:30~3:50)` / ` ★` / 꼬리 `〔… 5샷 · I2V〕`.
  ⚠ 괄호 제거는 **숫자+샷/초/자 또는 I2V·콜드오픈·후킹이 들어있을 때만** — 안 그러면 `명량 (전자책)`·`이순신(1545)` 같은
  멀쩡한 제목이 잘린다(실측 확인).
- **유튜브 규칙을 창에서 알려 준다**(고쳐 주지 않고 경고만 — 임의로 합치면 라벨이 어긋난다):
  첫 줄이 `0:00` 이어야 인식 / 챕터 3개 이상 / 각 10초 이상. TTS 가 빠진 문장이 있으면 "시간이 실제보다 짧다" 경고.
- 검증: `npm run test:timestamps` — 단위 **54 단언**(App.jsx·main.js **원문에서 함수를 뽑아 실행** — 복사해 두면 앱과
  갈라져도 통과한다) + **앱 E2E**(실제 앱 부팅 → 작업본 열기 → 버튼이 분할바 앞에 있는지 → 창 텍스트 형식 → 복사 → ESC).
  회귀: 파서 11/11 · 이미지·로그·프롬프트·캐시 14/10/13/7 · grok 81/0.
- ⚠ **작업 중 발견한 함정(다음 사람도 밟는다)**: 이 환경의 bash heredoc + python 편집은 `\n` 을 **진짜 줄바꿈으로**
  넣어 버린다(문자열·정규식이 깨진다). 편집 후 `node -e "new vm.Script(...)"` 로 문법을 확인할 것.
  또 python 으로 저장하면 **파일 전체가 CRLF 로 바뀌어** `main.js` 를 LF 로 읽는 테스트(visual-defect)가 깨진다 →
  편집 뒤 LF 로 되돌릴 것(git 은 정규화하므로 커밋 diff 로는 안 보인다).

## 👤 계정 관리를 ⚙ 설정 한 곳으로 통합 + 자격증명 자동입력(반자동) + 15초 영상 (2026-08-19, v0.3.21)
> 로이: "설정의 API키 섹션에 그록 아이디·비번을 입력해 로그인하게 하자. 젠스파크·구글플로우 아이디 입력하는
>   부분도 여기로 통합해서 한번에 관리하자." → **통합은 전적으로 동의. 자동 로그인은 이견을 냈고, 반자동으로 합의.**
- 🔑 **먼저 정정한 사실**: 「Genspark 계정」·「Flow 계정」 버튼은 **아이디 입력칸이 아니었다.** 저장되는 건
  `{id,label}` 뿐이고(`core/accounts.js`) 실체는 **브라우저 프로필 1개**다. 각 줄의 **🔑 로그인**으로 한 번
  로그인하면 그 프로필에 쿠키가 남아 계속 쓰인다. **이 앱은 지금까지 비밀번호를 저장한 적이 없다.**
- **내가 반대한 이유(로이에게 근거를 대고 설득 → 반자동으로 합의)**: ① **구글은 자동화 브라우저의 비밀번호
  로그인을 차단**한다("이 브라우저 또는 앱은 안전하지 않을 수 있습니다") ② **X 는 arkose CAPTCHA + 2FA** 를
  요구한다 — 앱도 나도 못 넘는다 ③ 지금 방식은 로그인 한 번이 **몇 달** 간다(실측: 로이가 예전에 로그인해 둔
  `grok-profiles/default` 가 오늘도 살아 있었다). 즉 완전 자동화로 얻는 건 "타이핑 10초"뿐인데 평문 비밀번호
  위험을 진다. → **결론: 자격증명은 저장하되 "자동 입력 + 막히면 사람이 마무리"**.
- 🔒 **`core/account-creds.js` 신설 — 평문 저장 금지**: `tts/secret-store.js` 는 **평문 JSON** 이라(주석에도
  "1차: 평문 저장") 구글·X 비밀번호를 넣을 곳이 아니다(구글 비번 하나면 지메일·드라이브 G:·유튜브가 함께 넘어간다).
  → **Electron `safeStorage`(윈도우 DPAPI)** 로 암호화해 `~/.flow-app/account-creds.json` 에 저장.
  safeStorage 를 못 쓰는 환경이면 **저장을 거부**한다(평문으로 몰래 떨어뜨리지 않는다).
  🔑 **비밀번호는 렌더러로 되돌려 주지 않는다** — UI 는 쓰기만 하고 읽을 때는 `hasPassword` 플래그만 받는다.
  복호화 값은 메인 프로세스의 자동입력에만 쓰이고 **로그에도 남기지 않는다**(아이디는 `ab***@…` 로 마스킹).
- **`core/login-autofill.js` 신설**: 아이디→(다음)→비밀번호→(제출) 을 1회 시도하고, **CAPTCHA·2FA·구글 차단·
  계정보호 화면을 감지하면 그 자리에서 멈추고 사람에게 넘긴다**(창은 열려 있다). ⚠ **재시도하지 않는다** —
  반복 실패가 계정 잠금으로 이어지기 때문. 훅 지점: grok/genspark 은 `login(onConfirm)` 안에서 다이얼로그
  **전에**, Flow 는 `login(onReady)` 를 새로 받아 로그인 화면 진입 직후.
- **⚙ 설정 → 「👤 계정」 탭 신설**: Genspark·Flow·Grok 계정을 한 화면에서 관리(이름·일일한도·🔑 로그인·
  아이디/비번 저장·삭제·추가). 흩어져 있던 **단독 모달 3개는 제거**하고 옛 진입점(헤더 ⚙계정, 이미지순환의
  🔑 계정 버튼)은 이 탭으로 재배선했다 — **UI 가 두 곳이면 다음 사람이 엉뚱한 쪽을 고친다**(v0.3.2 교훈).
- 🔎 **로그인 상태 표시(신설)** — 브라우저를 띄우지 않고 **프로필의 쿠키 파일 mtime** 만 본다(쿠키 DB 를
  파싱하지 않는다). `오늘 사용` / `N일 전 사용` / `N일 전(만료됐을 수 있음)` / `로그인 안 함`.
  ⚠ 경로는 요즘 크롬 기준 **`Default/Network/Cookies`**(옛 `Default/Cookies` 는 폴백). 실측 표시 예:
  Genspark 기본계정 4일 전 · Flow 계정1/2/3 각각 27·21·13일 전 → **오늘 같은 "로그아웃돼 있었다" 를 미리 안다.**
- **⏱ 15초 영상**: `pipeline.js` 자동 길이가 **TTS 13초 초과 → 15s** · 6초 초과 → 10s · 그 외 6s (로이 지정).
  경계 검증: 13.0s→10s / 13.01s→15s. 해상도는 **720p 유지**(1080p 는 열지 않았다 — 한도 소모).
- 🧹 **genspark-engine 의 죽은 프로필 복사도 은퇴**(v0.3.20 의 grok 과 같은 버그) + 한 번도 쓰이지 않은
  `userchrome` 폴더 **1.8GB 삭제**(grok 1.1GB + genspark 747MB. ⚠ `default` 는 실제 로그인 프로필이라 보존).
- 검증: **앱 E2E 16/16**(자격증명 왕복 — 저장 성공 · 아이디는 UI 로 돌아오고 · **비밀번호는 안 돌아오고** ·
  **파일에 평문 없음** · hasPassword=true(=복호화 성공 증거) · 삭제 확인 / 계정 탭에 3서비스·🔑로그인·일일한도
  렌더 · 저장된 비번은 placeholder 「저장됨」 으로만 표시 · 화면 어디에도 비번 값 없음) + `npm run test:grok` 81/0
  + 파서 11/11 + 이미지·로그·프롬프트·캐시 회귀 14/10/13/7. deps 무변경 → 라이트 업데이트로 배포된다.
- ⏳ **실사용 미검증**: 자격증명 자동입력의 실제 동작(로이가 아이디·비번을 넣고 🔑 로그인을 눌러야 확인된다).
  구글 쪽은 차단이 예상되므로 **막히면 그 화면에서 직접 로그인**하면 된다(기존과 동일하게 동작).

## 🎬 Grok(브라우저) 영상 = grok.com UI 개편으로 통째로 깨졌던 것 재설정 + **엉뚱한 영상** 차단 (2026-08-19, v0.3.20)
> 로이: "비디오를 만드는 사이트의 페이지 구성이 바뀌었어. 그래서 다시 셋팅을 해야해."
- 🔴 **원인 = 셀렉터의 뿌리가 사라졌다.** `CHIPS_CONTAINER = 'form div.flex.flex-wrap.items-center'` 가
  **매치 0개**(실측). 여기 매달린 셀렉터 7개(비디오/이미지 칩·480p·720p·6s·10s·비율 트리거)가 한꺼번에 죽어
  `"비디오" 칩 못 찾음` 으로 영상 생성이 통째로 실패했다. 게다가 칩이 전부 **`role="radio"`** 가 되면서
  비활성 칩은 **텍스트조차 없어져**(`has-text("비디오")` 도 실패) 컨테이너만 고쳐도 안 됐다.
- **새 UI 실측(전부 aria-label 앵커로 교체)**: `생성 모드`(이미지·비디오·**에이전트**) · `비디오 해상도`
  (480p·720p·**1080p**) · `동영상 길이`(6s·10s·**15s**) · `종횡비` · **`Video audio`(신설)** · `업로드` · `제출`.
  프롬프트는 **textarea 가 아니라 `div[role=textbox]`(contenteditable)**. 결과 URL 은 `/imagine/post/<UUID>?conversation=…`.
  ⚠ **1080p·15s 는 로그인 상태에서만 보인다**(로그아웃 화면으로 조사하면 6s·720p 까지만 보여 오판한다 — 실제로 한 번 오판했다).
- 🔴🔴 **가장 위험했던 것 — 셀렉터만 넓히면 "남의 영상"이 .vrew 에 실린다.** 새 UI 는 **좌측 히스토리
  썸네일마다 완성된 `<video>`** 를 깔아 둔다(50x50, `button.aspect-square > video`). 옛 완성 판정
  (`https src + readyState≥2 + dur>1` 2회 안정)이 그중 하나를 집는다. **실측으로 재현했다** — 화면은
  `생성 중 13%` 인데 **10.04초·736x400·오디오 있는 예전 영상**을 내려받았다. 타임아웃 실패보다 나쁘다(조용히 틀린다).
  · ✅ **해법 = `_findMainVideo()` 의 3조건 AND**: ① 페이지 URL 의 **post UUID 가 video src 경로에 있다**
    (`assets.grok.com/users/<uid>/generated/<postUUID>/generated_video.mp4?cache=1`) ② `closest('button')` 이
    없다(=사이드바 아님) ③ `readyState≥2 && duration>1`. **post UUID 를 못 읽으면 아무것도 완성으로 보지 않는다(fail-closed).**
  · 옛 `main article video` 는 **`main` 조상이 없어져** 0개였다(그래서 5분 타임아웃 → 재시도 → 크레딧 이중 소모).
- ⚠ **비율 메뉴 첫 항목에 「Auto」가 생겨 6개**가 됐다 → 옛 `nth-child(4)`=9:16 / `(5)`=16:9 가 **한 칸씩 밀려**
  9:16 을 요청하면 1:1 이, 16:9 를 요청하면 9:16 이 선택됐다(또 하나의 조용한 오작동). 라벨도 한국어
  (`9:16 수직`·`16:9 와이드스크린`)라 영어 폴백(`Vertical`·`Widescreen`)도 죽었다 → **숫자 텍스트로 매칭**한다.
- 🔇 **영상 오디오를 끈다(신설 토글, 기본 `aria-pressed="true"`)** — 그냥 두면 Grok 이 만든 소리가 mp4 에
  구워져 우리 TTS 와 겹친다. 끄면 실제로 **오디오 트랙 없는 mp4** 가 온다(실측 확인).
- 🧹 **`_ensureUserChromeProfileCopy` 은퇴(no-op)** — 완료 판정을 `Default/Cookies` 로 했는데 요즘 크롬은
  **`Default/Network/Cookies`** 라 그 파일이 생길 수 없다 → **한 번도 작동한 적 없이** 매 실행 10~30초를
  헛돌리고 "복사 실패" 를 찍었다. 🔑 **경로만 고치면 오히려 깨진다** — 실제로 로그인돼 있는 건 격리 프로필
  (`grok-profiles/default`)이고, 복사가 성공하면 평소 크롬 Default 사본을 쓰게 되어 **잘 되던 로그인이 사라진다**
  (이 PC 크롬 프로필이 24개인데 grok 로그인은 Default 에 없다). "계정=프로필, 🔑 로그인 한 번" 설계와도 어긋난다.
  ✅ genspark-engine.js 의 같은 죽은 코드도 v0.3.21 에서 은퇴시켰다.
- 🔎 **다음에 또 깨지면 이렇게 한다(방법 자체가 자산)**:
  1. 앱이 쓰는 프로필(`~/.flow-app/grok-profiles/default`)로 Playwright 를 띄운다 = **로그인 세션 그대로**.
     ⚠ 로그아웃 화면으로 조사하면 옵션이 덜 보여 오판한다. 비밀번호는 입력하지 않는다(계정당 🔑 로그인 1회).
  2. 라디오그룹·버튼의 **aria-label·role·aria-checked** 를 덤프한다 → 그게 새 셀렉터다.
     앱에도 같은 덤프를 넣어 뒀다: 칩을 못 찾으면 로그에 **`[DUMP 칩바]`** 한 줄이 남는다(`_dumpChipBar`).
  3. 결과 화면은 **실제로 1개 생성해서** 관찰한다(480p·6s = 가장 싼 조건). ⚠ 이때 **사이드바 완성 영상**과
     본편을 반드시 구분할 것 — 안 하면 위 사고가 그대로 재발한다.
  4. 받은 mp4 를 **ffmpeg 로 검사**한다(길이·해상도·오디오 유무). 요청값과 다르면 엉뚱한 영상을 집은 것이다.
- 검증: 단위 `test/grok-selectors.test.js` **81/0**(`npm run test:grok` — 금지 앵커 검사 + 옛 키 잔존 0 +
  **원문에서 뽑은 `_findMainVideo` 콜백**을 가짜 DOM 으로 실행: 생성 중 썸네일만 있을 때 완성으로 안 봄 ·
  UUID 불일치 거름 · post 아니면 fail-closed) + **실제 앱 엔진 E2E**: 42초 만에 완주,
  `6.04초 · 1264x720 · 오디오 없음`(요청 6s·720p·16:9 와 정확히 일치), 로그에 `페이지 video 10개 중 본편만 채택`.
- ✅ 로이 결정(v0.3.21 에 반영): ① 자동 길이 = **TTS 13초 초과 → 15s** · 6초 초과 → 10s · 그 외 6s
  ② 해상도는 **720p 유지**(1080p 는 열지 않는다 — 주간 한도 소모).

## 🎨 세 번째 실패 유형 — **색이 깨진 이미지**(얼굴 타일 + 형광색) + 🔄 가 같은 그림을 다시 만들던 것 (2026-08-19, v0.3.19)
> 로이: "이미지가 이상하게 생성되었어. **재생성 버튼을 클릭하였으나 동일한 이미지가 그대로 다시 들어와.**"
>   ([고전_0826] 홍길동전 G27 — 강변의 배 대신 웃는 사람 얼굴이 타일처럼 반복되고 색이 형광으로 튄 그림)
- 🔑 **노이즈도 검정도 아닌 세 번째 유형이다.** 같은 CLIP 조건 깨짐인데 **구조는 남아 있어서**(8x8 표준편차 26.2)
  v0.3.14 의 노이즈 규칙(구조 ≤12)으로는 **원리적으로 못 잡는다.** 실제로 .vrew 까지 실릴 뻔했다.
- **가른 지표 2개**(실측 262장 대조):
  · **형광 비율** = 채널차 200 넘는 화소 % → 문제 **6.07%** / 정상 최대 1.11%
  · **색 거칠기** = 이웃 화소 색차(U·V) 평균 → 문제 **7.97** / 정상 최대 4.19
  🔑 **둘 다 걸릴 때만** 폐기(AND). 형광만 보면 **창밖 파랑 + 램프 주황**처럼 대비가 센 정상 그림(1.11%)이,
  색 거칠기만 보면 붓질 거친 그림(4.19)이 지워진다. 실측 **262장 중 그 1장만 적중 · 오탐 0 · 영상 31개 오탐 0**.
  ⚠ `visualStats` 를 gray → **rgb24** 로 바꿨다(같은 ffmpeg 호출 1회, 버퍼 128KB→393KB, 장당 58→94ms).
- 🔴 **🔄 재생성이 같은 그림을 다시 만들던 이유**: 씨앗(seed)만 새로 뽑고 **프롬프트는 같은 글자를 다시 보냈다.**
  이 버그는 **프롬프트 텍스트에 결정적**이라(v0.3.16) 몇 번을 눌러도 똑같이 깨진다.
  → 🔄 를 누를 때마다 `g._regenN` 을 세어 **1단계↔2단계 프롬프트 변형을 번갈아** 적용
  (`runRotatingImages(..., retryLevel)` → `runComfyImages(..., baseRetryLevel)`).
  버리는 건 맨 끝 부정 절이라 그림 손실이 사실상 없다.
- ⚠ **테스트 함정**: 정상 영상 대역으로 `testsrc2`(컬러바)를 쓰면 **규칙 ③이 겨냥하는 특징을 그대로 가져** 걸린다
  (순수 원색 패턴). 실제 생성물엔 그런 그림이 없으므로(형광 0.00%) **매끄러운 그라데이션**으로 바꿨다.
- 검증: 합성 14/14(색 깨짐 폐기 · **색만 거친 그림은 유지** = AND 조건 확인) · 실제 262장 적중 1/1 · 오탐 0.

## 🧊 「만들기」 누르면 앱이 장시간 얼어붙던 것 — **내가 넣은 검사가 메인 프로세스를 막고 있었다** (2026-08-19, v0.3.18)
> 로이: "만들기 버튼을 클릭하니 장시간 프리징… 다른 단계에서도 반복되고 있네"
- 🔴 **원인은 v0.3.14~17 에서 내가 넣은 이상 이미지 검사다.** `execFileSync` 로 ffmpeg 를 돌렸다 —
  **메인 프로세스에서 동기 자식프로세스를 부르면 IPC 도 렌더링도 같이 멈춘다.** 이 앱의 금기다.
  실측: **이미지 1장 263ms · 영상 1개 1412ms** → 42장+영상5개 한 번 훑는 데 **18.1초 정지**,
  생성 직후·캐시 프리필·.vrew 직전 sweep 까지 **세 번**이면 **최대 54초**. 증상과 정확히 일치한다.
  ⚠ 옛 `looksBlankImage` 는 `size > 120KB → 즉시 정상` 이라 ffmpeg 를 **거의 안 돌렸다.** 그 빠른 길을
    없애면서(노이즈는 파일이 더 크다) 비용이 드러난 것 — **판정 정확도를 올릴 때 비용을 같이 안 봤다.**
- **수정 세 가지**:
  ① **비동기화** — `execFileSync` → `execFile`(Promise). `visualStats`·`looksBadImage`·`looksBadVideo`·
     `sweepBadVisuals`·`prefillImageCache` 전부 async. ⚠ **`await` 를 빠뜨리면 Promise 는 항상 truthy** 라
     **멀쩡한 자산을 전부 '이상' 으로 판정해 지운다** — 실제로 한 번 그렇게 됐다. 고칠 때 호출부를 전수 확인할 것.
  ② **ffmpeg 호출을 절반으로** — 두 지표를 한 번에 받는다. 한 장의 세로 결합 그림을 받아
     `[0:v]crop=256:256[a];[0:v]scale=8:8,scale=256:256:flags=neighbor[b];[a][b]vstack` →
     위 256x256 = 원본 화소(거칠기) · 아래 = 8x8 을 최근접 확대(구조, 32x32 블록 중앙 한 점만 읽으면 원값과 같다).
     전송량도 1MB → 128KB. ⚠ 거칠기는 **축소하면 사라지므로** 반드시 원본 화소 crop 이어야 한다.
  ③ **결과 기억 + 동시 실행 제한** — 같은 파일을 단계마다 다시 재지 않도록 `경로|수정시각|크기` 로 기억(`_visMemo`),
     검사는 `_mapLimit(…, 4, …)` 로 **동시 4개까지만**(Promise.all 로 40개를 한꺼번에 띄우면 ffmpeg 가 수십 개 떠 CPU 가 튄다).
- **결과(실측)**: 42장+영상5개 **18.1초 → 2.9초(6.2배)** · 두 번째 훑기는 메모로 203장 **232ms** ·
  **이벤트 루프 최대 정지 192ms**(전에는 수 초씩 연속 정지) = 체감 프리징 없음.
- **정확도는 그대로**: 실제 클라우드가 만든 노이즈 원본 **7/7 적중** · 현재 대본 이미지 203장에서 **옛 방식과 판정 100% 일치**
  (불일치 0) · 합성 12/12 · 캐시 방어 7/7.
- ✅ **v0.3.17 이 실전에서 작동한 증거**(로그 파일 덕에 확인): `11:09:40 ⬛ G8 캐시에 있던 이미지가 이상 — 캐시에서
  삭제, 새로 생성합니다` (G8·G11·G14·G34) → `11:10:32 ⚡⚡ 큐 제작 종료 — 성공 2 · 실패 0`.
- 🔑 **교훈**: 메인 프로세스에서 **동기 외부 프로세스 호출 금지.** 검사·검증을 추가할 땐 **정확도와 함께 '한 번 훑는 데
  몇 초 걸리는지'를 반드시 재고**, 반복 호출되는 자리라면 결과를 기억시킬 것.

## 🔴♻ 캐시가 노이즈 이미지를 **되살리고 있었다** + 네트워크 오류 재시도 (2026-08-19, v0.3.17)
> 로이: "이제는 로그 볼수있지? 아직도 안되는데?" → **로그 파일 덕에 30초 만에 갈렸다**(v0.3.15 의 첫 성과).
- 로그가 보여 준 것(그대로 옮김):
  `[10:58:24] ♻ 이미지 3개 재활용(캐시)` → `♻ 롱폼 전부 캐시 재활용 — 생성 생략`
  `[10:59:13] ✗ G10 실패: fetch failed`
- 🔴 **①(중대) 미디어 캐시가 노이즈를 되살렸다.** 노이즈를 감지해 지웠는데 **다음 실행에서 캐시가 그대로
  복사해 넣었다**(그 3개 = G10·G19·G33). 즉 **v0.3.14 방어선이 실행을 넘기면 무력화**되고 있었다.
  · 범인은 `prefillImageCache` 가 `g.imageCleared` 플래그만 보고 건너뛰는 구조인데,
    **그 플래그가 스냅샷(`buildSnapshot`)에 저장되지 않는다** → 앱을 껐다 켜면 사라진다.
    (로그의 `♻ 작업본 이어받기` 가 바로 그 상황이었다.)
  · **수정: 플래그에 기대지 않는다 — 캐시에서 꺼낸 파일도 `looksBadImage` 로 검사**하고, 이상이면
    파일과 **캐시 항목(`MC.del`)까지 지운다.** 여기가 캐시가 나가는 **유일한 문**이라 모든 경로가 한 번에 막힌다.
  · 보조로 폐기 시점(생성 중·sweep)에도 캐시 항목을 지우고 `imageCleared` 를 세우며,
    **스냅샷에 `imageCleared` 를 저장·복원**하도록 했다(재시작해도 유지).
  🔑 **교훈**: "지웠다"로 끝내면 안 된다 — **그 물건의 사본이 어디에 남는지**(캐시·스냅샷)를 같이 봐야 한다.
- **② `fetch failed` 재시도**: 같은 시각 comfy.org 는 **정상(200, 0.6초)** 이었다 → 일시적 네트워크였는데
  한 장이 통째로 실패했다. `textToImage` 가 **네트워크 오류일 때만 5초 뒤 1회 재시도**한다(워크플로·키·모더레이션
  오류는 재시도하지 않는다 — 시간만 버린다). 제출 전 끊김이 대부분이라 크레딧 이중 청구 위험도 낮다.
  · 🔑 **`fetch failed` 한 줄로는 아무것도 알 수 없다** — undici 가 진짜 이유를 `e.cause` 에 숨긴다.
    `_netMsg()` 로 **원인 코드 + 사람 말**을 붙였다(`fetch failed [UND_ERR_CONNECT_TIMEOUT] — 그 주소에
    아무도 응답하지 않습니다`). v0.2.51·v0.2.84·v0.3.7 에 이어 **네 번째** 같은 계열 사고다.
- **③ 로그의 버전이 거짓말했다**: `[10:57:52] Priming v0.3.14 시작` 인데 실제로 돈 건 v0.3.15 코드였다.
  `app.getVersion()` 은 **업데이트가 적용되기 전** 값을 준다(Electron 이 실행 시점 package.json 을 읽어 두고,
  라이트 업데이트는 그 뒤에 파일을 갈아끼운다). → **디스크의 package.json 을 다시 읽어** 찍는다.
- 검증: `test/cache-guard.test.js` **7/7**(노이즈 캐시는 거르고 **캐시 항목까지 삭제** · 정상 캐시는 그대로 재활용) ·
  네트워크 판별 6/6(fetch failed 계열은 재시도, 워크플로·모더레이션 오류는 재시도 안 함) · 기존 회귀 12/12·10/10·13/13.

## 🔴🌈 노이즈 이미지의 **진짜 원인** — comfy.org Krea2 CLIP 의 토큰 경계 버그 (2026-08-19, v0.3.16)
> 로이: "이미지가 안 만들어지는게 아니라 **노이즈 이미지를 계속 만들고 있는데**" (comfy.org 큐 전체가 노이즈, 3~4초)
- 🔴 **정정 — v0.3.14 의 "동시 생성 4가 원인" 진단은 틀렸다.** 동시 4 와 겹친 건 우연이었다. 재생성이
  **순차(동시 1)에서도 100% 노이즈**로 나오는 걸 보고 뒤집혔다. (검정 이미지가 동시 생성 탓이라는
  v0.2.80 실측은 별개로 여전히 유효 — 그래서 동시 상한 2 는 그대로 둔다.)
- 🔑 **추적 방법(다음에도 이렇게 한다)**: comfy.org `/api/jobs?limit=N` → `/api/jobs/{id}` 로 **실제 실행된
  그래프를 회수**했다. 결과: 우리가 보낸 값은 **전부 정상**(steps 8 · cfg 1 · denoise 1 · 1344x768 ·
  KSampler→VAEDecode 배선 정상). 즉 **앱이 잘못 보낸 게 아니라 서버가 노이즈를 돌려주고 있었다.**
- 🔑 **가른 실험(이게 결정적이었다)** — 같은 서버·같은 워크플로에 프롬프트만 바꿔 가며 실측:
  · 내 짧은 프롬프트 → 정상 · **앱이 쓴 그 프롬프트(989자) → 노이즈**
  · 앞 985자 → **정상** / 989자(원본) → **노이즈 4회 연속(결정적)** / **989자 + 마침표 하나 → 정상**
  · 내용이 다른 1300자 프롬프트 → 정상 → **길이 문제가 아니다. 특정 문구도 아니다.**
  → **토큰 수가 어떤 경계에 딱 떨어질 때 CLIP(qwen3vl_4b, type=krea2)의 조건이 깨진다.**
  이 워크플로는 **cfg=1 + ConditioningZeroOut** 이라 조건이 깨지면 붙잡아 줄 것이 전혀 없다 →
  디노이즈가 사실상 0 → **디노이즈 안 된 latent 가 그대로 디코드 = 컬러 모래알.**
- 🔑 **왜 "계속" 노이즈였나**: 실패가 **프롬프트 텍스트에 결정적**이다. 재생성은 씨앗(seed)만 새로 뽑고
  **같은 글자를 다시 보냈다** → 몇 번을 해도 똑같이 노이즈. 복구 패스가 오히려 헛돌았다.
- **수정 두 겹**:
  ① `buildImagePrompt` 가 **항상 마침표로 끝낸다** — 장식이 아니라 토큰 경계를 미는 회피책이다.
  ② `nudgePromptForRetry(prompt, level)` 신설 + `runComfyImages` 의 `genOne(g, retryLevel)` —
     재시도 때 **프롬프트 자체를 바꾼다**. 버리는 건 **맨 끝 부정 절**(1단계=하나, 2단계=전부):
     cfg=1 + 네거티브 zero-out 이라 **부정문은 어차피 거의 작동하지 않으므로**(v0.2.83) 그림 손실이 사실상 없다.
  ⚠ `` 없이 `/^no/` 로 쓰면 **`northern light` 를 부정으로 오인해** 멀쩡한 묘사를 지운다(실제로 한 번 그렇게 깨졌다 —
    파이썬 heredoc 이 `` 를 **백스페이스 문자(0x08)로** 넣어 정규식이 죽었다. 편집 후엔 제어문자 검사를 할 것).
- **검증(실측)**: 그 프롬프트로 ① 원본 → 노이즈 · ② 마침표 정규화 → **정상** · ③ 재시도 1단계 → **정상** ·
  ④ 재시도 2단계 → **정상**. 단위 13/13(`test/prompt-guard.test.js`, `npm run test:visual` 에 포함).
- ⏳ **남은 것**: 비디오(LTX)는 CLIP 이 달라 같은 증상이 확인되지 않았다 — 근거 없이 손대지 않았다.
  ⚠ 이 회피책은 **경계를 피하는 것**이지 버그를 고치는 게 아니다. 다른 프롬프트가 또 다른 경계에 걸릴 수 있으나,
  그때는 노이즈 감지(v0.3.14)가 잡고 프롬프트를 바꿔 재시도하므로 **스스로 빠져나온다.**

## 🗒 로그를 파일로 남긴다(7일 보관) + 💾 .vrew 경로의 **막다른 길** 제거 (2026-08-19, v0.3.15)
> 로이: "노이즈 이미지는 제거는 했는데 새로운 이미지를 만들지는 못하네" ([고전_0821] G10·G19·G33)
>   + "지금 로그 볼수 있지?" → **볼 수 없었다.** 로그가 화면 로그창에만 있어 앱을 닫으면 사라졌다.
- 🔑 **감지는 정상이었다** — v0.3.14 방어선이 그 3장을 정확히 잡아 지웠다(파일 mtime 으로 확인: 3장 삭제됨,
  새 파일 0개). 문제는 **그 다음**이었다.
- 🔑 **범인 = 💾 .vrew(export-vrew) 경로는 "검사만 하고 재생성은 안 한다"** 는 v0.3.10 의 설계 결정.
  ⚡만들기 4단계에는 순차 재생성이 있는데 수동 경로엔 없었다 → **파일은 지워졌는데 아무것도 안 만들어지고
  게이트에 막혀 .vrew 도 안 나오는 막다른 길**이 된다. "🔄 로 다시 만든 뒤 저장하세요" 라고 안내만 했다.
  → export-vrew 도 **그 자리에서 순차 재생성**하도록 통일(⚡만들기 4단계와 같은 `runRotatingImages(..., bad)`).
  이를 위해 렌더러가 `exportVrew` 에 **styleId·imgEngine 을 함께 보낸다**(예전엔 안 보내서 재생성을 할 수가 없었다).
- **로그 파일**: `~/.shots-maker/logs/YYYY-MM-DD.log` — 하루 1개, **7일 보관**(앱 시작 때 + 날짜가 바뀔 때 정리).
  · `log()` 가 화면과 파일에 함께 쓴다 + **화면(렌더러)에서 난 줄도** IPC `append-log` 로 파일에 남긴다
    (`[화면]` 접두 — 🐞 화면 오류처럼 main 을 안 거치는 것들). ⚠ main 이 보낸 줄은 렌더러가 되보내지 않는다(중복 방지).
  · 파일마다 `──── Priming v0.3.15 시작 ────` 세션 구분선. 시각은 **KST**(⚠ `toISOString()` 은 UTC 라 밤에 하루 밀린다).
  · 로그창에 **「📁 파일」 버튼**(IPC `open-logs`) — 문제가 나면 그날 파일을 그대로 보내면 된다.
  · 로그가 실패해도 앱은 그대로 간다(스트림 오류 시 조용히 포기) — 로그 때문에 작업이 멈추면 본말전도.
- 검증: 단위 10/10(`test/log-file.test.js` — KST 환산·기록·**보관 경계 7일 보존/8일 삭제**·로그 아닌 파일 불변) +
  **앱 E2E**(실제로 띄워 파일 생성·화면 줄 기록·세션 구분선·📁 버튼 확인). `npm run test:visual` 에 함께 묶임.

## 🌈 이미지가 **컬러 노이즈**로 나오던 것 — 검정 감지가 원리적으로 못 잡던 두 번째 실패 (2026-08-19, v0.3.14)
> 로이: "왜 이런 이미지가 생성되는거지?" (G8 썸네일이 컬러 모래알) + "이 대본에서만 4개가 이런 이미지야"
- 🔑 **정체 = 디노이즈가 한 번도 안 된 latent 가 그대로 VAE 디코드된 것.** 그림이 아니라 **그림 그리기 전의 난수**다.
  `[고전_0823]` 42장 중 **08·11·14·34** 4장. 넷 다 평균 RGB 가 **115,119,96 으로 동일** = 서로 다른 그림이 아니라
  **같은 실패의 복사본**. 전 대본을 훑으니 **`[고전_0821]` 에도 3장**(10·19·33)이 더 있었다 — 로이는 모르고 있었다.
  ⚠ 0821 은 **이미 .vrew 까지 나갔다**(그 3장이 영상에 실렸다).
- 🔑 **v0.3.10 의 검정 방어선이 여기엔 원리적으로 무력했다** — 이게 진짜 문제였다:
  ① `looksBlankImage` 첫 줄이 `size > 120KB → 정상`. 그런데 **노이즈는 정상보다 크다**(2.0MB vs 1.0~1.3MB —
     난수는 압축이 안 된다) → **검사에 들어가지도 못했다.** ② 들어가도 기준이 `평균 RGB 합 < 30` 인데 노이즈는 **330**.
  `sweepBlankVisuals`(.vrew 직전 재검사)도 같은 함수를 쓰므로 **게이트도 안 걸리고 그대로 영상에 실렸다.**
- **수정 — "빈 화면인가"가 아니라 "그림의 구조가 있는가"를 본다.** 지표 2개(`visualStats`):
  · **거칠기** = 이웃 픽셀 밝기차 평균 → 노이즈는 크다 (실측 **노이즈 13.4~13.6 / 정상 최대 4.8**)
  · **구조**   = 8x8 로 줄였을 때 표준편차 → 노이즈는 줄이면 평평해진다 (**5.4~7.5 / 정상 최소 14.0**)
  🔑 **둘 다 걸릴 때만** 폐기한다(AND). 하나만 쓰면 **어두운 그림**(구조 14.0)이나 **결이 거친 그림**(거칠기 4.8)을
  멀쩡한데 지운다 — 실제 `[고전_0821] 26.png` 가 구조 14.0 이지만 거칠기 1.2 인 정상 야경이었다.
  검정은 같은 통계의 `평균 밝기 < 10` 으로 함께 잡는다(옛 'RGB 합 30' 과 동일 기준). `looksBlankImage/Video` →
  **`looksBadImage/Video`**, `sweepBlankVisuals` → `sweepBadVisuals` 로 개명(이름이 검정만 뜻해 다음 사람을 속인다).
- ⚠ **crop 을 쓰지 않는다** — 좌표가 9:16·1:1 에서 범위 밖이 될 수 있다. 전체 프레임이 분리도 더 좋았다(정상 최대 5.5→4.8).
- 🔑 **원인 = 동시 생성 4.** 설정파일에 `concurrency:4` 가 남아 있었다(코드 기본은 2인데 **저장값이 이긴다**).
  이 값은 v0.2.80 에서 **검정 12.5% 를 유발한다고 실측돼 2 로 낮췄던 바로 그 설정**이고, 이번 발생률은 **7/203 = 3.4%**
  (0823 만 보면 9.5%)로 같은 계열이다. 검정이냐 노이즈냐만 달랐다. 게다가 타임스탬프상 **비디오(동시 3)가 겹쳐**
  같은 comfy.org 에 올라갔다 → 순간 최대 7건.
  → **사용자가 설정을 만질 필요가 없도록 코드가 깎는다**: `comfy-image.loadConfig` 가 **읽을 때마다 상한 2 로 클램프**
  (옛 설정의 4 도 자동 교정) + `runComfyImages` 에서도 2 로 클램프(이중) + ⚙ 설정 입력 `max=2`.
  ⚠ 비디오 동시 3 은 **그대로 뒀다** — 영상 25개를 다 재 봤지만 이상이 하나도 없었다(거칠기 최대 1.8·구조 최소 22.6).
  근거 없이 낮추면 벽시계 시간만 손해다. 대신 **영상에도 같은 노이즈 판정을 넣었다**(전엔 검정만 봤다).
- 검증: **실제 파일 203장 + 영상 25개 — 적중 7/7 · 오탐 0 · 미탐 0** · 합성 12케이스 12/12
  (`test/visual-defect.test.js`, `npm run test:visual`). 🔑 테스트는 **main.js 원문에서 판정 블록을 뽑아 실행**한다 —
  로직을 복사해 두면 앱과 갈라져도 통과해 아무것도 못 지킨다.
- ⚠ **로이가 하실 일**: `[고전_0821]`·`[고전_0823]` 의 그 7장은 **이미 만들어진 파일**이라 앱이 소급 삭제하지 않는다.
  두 대본을 다시 「만들기」하면(또는 ✕ 로 지우고 🔄) 이번 방어선이 잡아 순차로 다시 만든다.

## 🖼 출력폴더 이름에 `#` 이 있으면 **모든 썸네일·미리보기가 깨졌다** (2026-08-15, v0.3.13)
> 로이: "이미지 삽입이 깨지네" (06_고전서재 채널, 검은 썸네일 + 깨진 이미지 아이콘)
- 🔑 **파일은 멀쩡했다** — `media-1/01.png` 866KB 정상(검정 이미지 건과 무관). **화면이 못 읽은 것**뿐이고,
  `.vrew` 산출물은 파일 경로를 직접 쓰므로 **영향 없다**(표시 전용 버그).
- 🔑 **범인 = `main.js` media 프로토콜 핸들러가 「디코드 → 자르기」 순서**였던 것:
  `decodeURIComponent(url).split(/[?#]/)[0]`. 렌더러는 `encodeURIComponent` 로 `#` 을 **`%23`** 으로 잘 보냈는데,
  핸들러가 그걸 `#` 으로 되돌린 **뒤에** 잘라서 경로가 그 지점에서 끊겼다.
  · 이 채널 출력 경로가 `G:\내 드라이브\## 유튜브채널\…` → 실제로 연 경로는 **`G:\내 드라이브\`(폴더)**
    → statSync 는 성공하는데 파일이 아니라 폴더라 스트림이 실패 → 브라우저가 깨진 이미지로 표시.
  · **`#` 이 없는 채널은 멀쩡**했다(역사이야기 등) → "이 채널만 깨진다"는 증상과 정확히 일치.
- **수정**: **자르기를 디코드보다 먼저** — `decodeURIComponent(url.split(/[?#]/)[0])`.
  날것 URL 에선 경로 속 `#` 이 `%23` 이라 안 잘리고, 진짜 캐시버스터(`?t=…`)만 제거된다.
- 검증: 실경로 단위대조(현재=`G:\내 드라이브\` / 수정=실제 파일) · 회귀(`?t=` 제거 · `#` 없는 경로 불변) ·
  **앱 E2E** 로 그 png 가 `1344x768` 로 로드됨 확인.
- ⚠ 같은 계열 점검: `media://` 를 만드는 곳은 전부 `encodeURIComponent`(App.jsx·main 3곳) 로 정상,
  디코드하는 곳은 이 핸들러 하나뿐. 출판 미리보기(조판 HTML)도 같은 경로라 함께 고쳐졌다.

## 🔴 채널 순서가 항상 되돌아가던 이유 — `loadAll()` 이 읽을 때마다 기본채널을 맨 위로 (2026-08-14, v0.3.12)
> 로이: "순서를 변경했는데 왜 역사이야기가 항상 위에 놓이게 되는거지?"
- **실측 대조로 즉시 확정**: 파일(`~/.flow-app/tts-presets.json`)에는 사용자가 ↕ 로 정한 순서가 **정확히 저장돼
  있었다**(01_고전 → 02_역사 → 04 → 05 → 06 → 플리 → 출판). 그런데 `P.listPresets()` 는 **02_역사이야기를
  맨 앞**으로 돌려줬다. → 저장이 아니라 **읽기**가 범인.
- 🔑 **범인 = `preset-store.loadAll()` 끝의 `_sortByDefault(data)`** — `isDefault:true` 인 채널을 항상 맨 위로
  끌어올린다(정렬이 stable 이라 나머지는 파일 순서 유지 → "하나만 위로 튄다"는 증상과 정확히 일치).
  주석에 남은 원래 의도는 「좌측 카드 첫 번째 표시」인데 **그 UI 는 오래전에 사라졌다.**
- 🔑 **더 나빴던 2차 피해**: `add`/`update`/`remove` 가 **`loadAll()` 결과를 그대로 `saveAll()`** 한다 →
  채널을 한 번이라도 편집하면 **뒤바뀐 순서가 파일에 영구히 박힌다.** ↕ 로 고쳐도 다음 편집에 또 무너지는 이유.
- **수정**: `_sortByDefault` **제거** — 파일 순서가 유일한 진실. 기본 채널은 `getDefault()` 가 `isDefault` 로
  직접 찾으므로 무영향. 순서 첫 항목에 의존하던 렌더러 2곳(`loadPresets`·`deleteChannel`)은
  **`ps.find(p => p.isDefault) || ps[0]`** 로 바꿔 "처음 고를 채널"을 순서가 아니라 플래그로 정하게 했다.
- 검증: 파일 순서 = `listPresets()` 순서 일치 · `getDefault()` 여전히 02_역사이야기 · **앱 E2E** 로 드롭다운
  표시 순서까지 파일과 일치 확인.

## 🔒 입력 잠김(클릭·키·ESC 가 전부 안 먹음) — 숨은 모달 대화상자 차단 + 자동복구 (2026-08-14, v0.3.12)
> 로이: "텍스트 입력이 어느 때는 되고 어느 때는 안 된다. 버그인가, 언어의 한계인가?" (v0.3.9 의 미해결 건과 같은 계열)
- **가른 질문 2개가 결정적이었다**: ① **영문도 안 쳐진다**(→ 한글 IME 문제 아님) ② **버튼도 ESC 도 안 먹는다.**
  🔑 화면 안쪽(오버레이·포커스) 문제라면 **ESC 는 먹어야 한다** — ESC 는 window keydown 리스너가 받으니까.
  ESC 조차 안 먹는다 = **OS 가 창 자체를 잠근 상태**(`EnableWindow(false)`).
- **배제한 것(근거)**: 렌더러를 막는 동기 IPC `sendSync` **없음**(전부 async invoke) · 키를 삼키는 전역 핸들러는
  ESC·Ctrl+F 뿐 · 증상 시각 프로세스 **CPU ≈0, Responding=True**(계산에 묶인 멈춤 아님) ·
  Win32 `EnumWindows` 로 확인 시점엔 창이 `enabled=True`(그 순간엔 이미 풀린 뒤).
- **남은 원인 계열 = 모달 대화상자가 열린 채 다른 창 뒤에 숨은 것.** 통로는 `dialog.show*(win, …)` **21곳**
  (전부 `win` 을 부모로 → 열려 있는 동안 창이 잠김) + 렌더러 `alert/confirm` **10곳**.
  ⚠ 특히 작업이 끝나면 `.vrew` 를 `shell.openPath` 로 자동으로 여는데(=**Vrew 가 앞으로 튀어나옴**)
  그 직후 경고 팝업(v0.3.10 의 검정이미지·미생성 경고 등)이 뜨면 팝업이 그 뒤로 숨어 정확히 이 상태가 된다.
- **대책 ①(예방)**: `dialog.showOpenDialog/showSaveDialog/showMessageBox` 를 **한 곳에서 감싸** 열기 직전에
  부모 창을 앞으로 끌어온다(`_bringFront`). 21곳을 각각 고치지 않아도 전부 적용된다.
  렌더러 `alert/confirm` 은 `uiAlert`/`uiConfirm` 로 바꿔 **IPC `focus-window` 로 창을 먼저 앞으로** 보낸다.
- **대책 ②(자동복구+증거)**: 열린 대화상자 수(`DLG.n`)를 세고, **하나도 없는데 창이 10초 넘게 잠겨 있으면**
  `win.setEnabled(true)` 로 풀고 **로그창에 `🩹 입력 잠김을 풀었습니다`** 를 남긴다.
  ⚠ 포커스는 뺏지 않는다 — 렌더러 alert/confirm 이 떠 있으면 그 위를 덮어버리기 때문.
- 🔑 **이 로그가 원인 확정의 증거다**: 다음에 멈췄을 때 그 줄이 **찍히면** 위 가설이 맞고(그리고 이미 스스로 풀린다),
  **안 찍히는데도 멈추면** 창 잠김이 아니라 렌더러가 멈춘 것이므로 그때 다른 곳을 봐야 한다.
- 검증(앱 E2E): 창을 강제로 `setEnabled(false)` → **8~13초 사이 자동 해제 + 로그 1줄** 확인 /
  회귀 — **진짜 폴더 선택 창을 띄운 채 14초**를 기다려도 `enabled=false` 유지(정상 대화상자를 잘못 풀지 않음).
- ⏳ **아직 "고쳤다"고 단정하지 않는다** — 재현을 못 잡았고, 위는 계열 차단·자동복구·계측이다.

## 🗂 채널편집 팝업 = 세로 스크롤 폐지, 상단 탭 5개 (2026-08-14, v0.3.11)
> 로이: "아랫방향으로 스크롤을 만들지 말고, 상단에 섹션별로 탭을 두어서 관리하자."
- 섹션이 계속 늘어(이름·그룹·시작화면 / 목소리 / 자막·분할 / 배속·스타일 / 이미지·비디오 도구 / 폴더·AI고지)
  `.modal-card.wide` 의 `max-height:88vh; overflow-y:auto` 안에서 **세로로 쌓여 스크롤**해야 했다.
  → **⚙ 설정 팝업과 같은 탭 패턴**(버튼 줄 + 조건부 렌더)으로 통일: `🏠 기본 / 🎙 음성 / 📝 자막·분할 /
  🖼 제작 도구 / 📁 폴더`. 상태 `chTab`, 열 때마다 `basic` 으로 초기화.
- 🔑 **섹션을 탭에 그대로 옮기지 않고 의미 단위로 재배치**했다 — 옛 「🔊 음성 배속 · 🎨 이미지 스타일」 은
  한 줄에 성격이 다른 둘을 묶은 것이라, **배속은 음성 탭 · 스타일은 제작 도구 탭**으로 갈랐다.
  AI 고지는 「폴더·기타」의 기타였는데 폴더와 무관하므로 **기본 탭**으로. 롱폼/쇼츠 2열 구조는 유지.
- CSS `.modal-card.tabbed` 신설(styles.css): `overflow:visible`(스크롤 없음) + `.tabbody{min-height:246px}`
  로 **탭을 바꿔도 창 높이가 튀지 않게** 고정 + 탭 첫 `.subhead` 의 윗줄/여백 제거.
- 검증(앱 E2E, Playwright `_electron`): 5개 탭 전부 `scrollHeight - clientHeight = 0`(스크롤 0) ·
  카드 높이 417~445px(뷰포트 821px) · 각 탭 스크린샷으로 항목 누락 없음 확인.

## ⬛ 검정 이미지 최종 방어선 — .vrew 직전 재검사 + 자동 재생성 (2026-08-14, v0.3.10)
> 로이: "첨부 이미지처럼 검정 화면이 종종 나오는데, 기술적으로 막거나 반드시 이미지가 들어가게 강제할 수 있지?"
- **실측 확인**: `[승리_0816]` media-1 의 26개 중 **`09.png` 만 11KB·RGB합 9 = 진짜 검정**(나머지 25개 정상).
  v0.2.80 의 검정 감지가 있는데도 살아남았다.
- 🔑 **구멍 = fail-open**: `looksBlankImage` 는 ffmpeg 판정이 실패하면 `catch { return false }` 로 **정상 취급**했다.
  출력이 **G: (구글드라이브 스트리밍)** 이라 ffmpeg 읽기가 실패·지연되면 검정이 그대로 통과한다.
  → **작은 파일은 검정으로 본다(fail-closed)**: 1344x768 정상 이미지는 40KB 미만이 될 수 없다.
- 🔑 **생성 시점 검사만으로는 부족**하다(다른 엔진 경로·나중에 덮어쓰기 등). → **`sweepBlankVisuals()` 신설**:
  **.vrew 를 만들기 직전에 실제 파일을 다시 훑어** 검정이면 비운다.
  · `runMakeAllCore` 4단계: 비운 그룹을 **그 자리에서 순차 재생성**(`runRotatingImages(..., bad)`) →
    그래도 검정이면 `missingVisualGroups` 게이트가 **.vrew 를 막고 어느 그룹인지 팝업**으로 알린다.
  · `export-vrew`(💾) 경로: 검사만 하고 게이트로 알린다(수동 경로라 자동 재생성은 안 함).
- 이로써 **검정 이미지가 .vrew 에 실려 영상으로 나가는 일은 구조적으로 차단**된다(막히거나, 다시 만들거나 둘 중 하나).
- 검증: 실제 `09.png`/`10.png` 로 6/6 — 검정만 판정·비움·삭제, 정상은 그대로.

## ✏ 대본수정·검색 입력 비제어 전환 + 화면 오류 안전망 (2026-08-14, v0.3.9)
> 로이: "대본수정 화면에서 클릭도 수정도 안 되고, Ctrl+F 검색창에도 입력이 안 된다."
- 🔴 **내 첫 진단은 틀렸다(정정).** "긴 텍스트를 제어 컴포넌트로 두어 글자마다 전 화면이 재렌더된다"고
  판단했지만, **고치기 전 코드로도 타이핑이 된다**(실측: 78KB 대본 로드 후 글자당 14ms → 고친 뒤 8ms).
  재렌더 부하는 체감 원인이 아니었다. 재현 조건을 아직 못 잡았다.
  · 재현 방법은 확보했다 — **Playwright `app.evaluate` 로 메인 프로세스의 `dialog.showOpenDialog` 를 스텁**하면
    실제 대본을 띄운 상태로 UI 를 자동 검증할 수 있다(앞으로 이 계열 버그에 재사용할 것).
- 그래도 남긴 개선: **대본수정·붙여넣기·검색 입력을 비제어(uncontrolled)로** 전환(ref 로 [적용] 때 한 번만 읽음).
  타이핑당 App 재렌더가 사라져 14ms → 8ms. `findText` state 도 제거(ref 로 이동).
- 🔑 **진짜 대비책 — 조용히 죽지 않게 한다**: 렌더 중 예외가 나면 React 는 트리를 버려서 **화면이 멈춘 것처럼
  보이고 클릭·입력이 전부 안 먹는다.** 지금까지는 아무 흔적도 안 남았다. →
  ① `ErrorBoundary` 로 카드 영역을 감싸 **오류 메시지 + 「다시 시도」** 를 그 자리에 표시(헤더·모달은 살아 있음)
  ② `window.onerror`·`unhandledrejection` 을 **로그창에 기록**(`🐞 화면 오류: …`).
  다음에 같은 일이 나면 원인이 로그에 남는다.
- ⚠ 아직 미해결: 로이가 겪은 그 상황의 실제 원인. 재발 시 **로그창의 `🐞 화면 오류` 줄**을 보내 주면 즉시 잡힌다.

## 🔴🔴 채널 참조음성이 **통째로 지워져** 엉뚱한 목소리로 TTS 가 나감 (2026-08-14, v0.3.8)
> 로이: "조용한승리 채널에서 음성선택을 했으나 전혀 상관없는 음성으로 TTS 가 만들어지고 있네."
- **범인: `tts/preset-store.js` 의 옛 정규화 한 줄.**
  `if (engine==='omnivoice' && refAudio && !/[/\]/.test(refAudio)) refAudio = ''`
  — 원래는 인덱스 문자열("0","1") 같은 쓰레기값을 지우려던 코드인데, **`srv:<이름>` 은 경로가 아니라
  이름이라 슬래시가 없다** → 서버 공용 목소리를 고른 채널은 **로드할 때마다 값이 통째로 사라졌다.**
  · 파일에는 `"srv:1nd_고전_남성"` 이 멀쩡히 있는데 `loadAll()` 은 `""` 를 돌려줬다(실측으로 확인).
  · 그 결과 pipeline 이 refName·refAudioPath 를 **둘 다 못 받아 참조음성 없이 합성** → OmniVoice 기본(Auto)
    목소리가 나갔다. "전혀 상관없는 음성"의 정체가 이것.
  → `!/^srv:/` 예외 추가. 회귀 7/7('0'·'1'·'abc' 는 계속 제거 / `srv:`·경로는 보존).
- 🔑 **2차 위험도 함께 막았다**: 값이 비면 `<select>` 는 **첫 항목을 조용히 가리킨다.** 그 상태로 저장하면
  사용자가 고르지도 않은 목소리(목록 첫 번째)가 채널에 박힌다. → **`— 선택 안 됨 —` placeholder** 를 넣어
  비어 있음이 눈에 보이게 했다. (실제로 이 채널은 편집창에서 `02_저음 2단계`(첫 항목)를 가리키고 있었다.)
- **채널편집 화면의 「이미지 도구·비디오 도구」도 헤더와 같은 구조로 교체**(로이 요청) —
  `ComfyUI (Krea2·z-image 등)` 같은 뭉뚱그린 항목 대신 **☁/🖥 × 모델**을 여기서 바로 고른다.
  저장값은 헤더와 동일한 `comfy::<경로>`. 안내 문구의 옛 RunPod 언급도 제거.
- 검증: loadAll 실측(13채널 전부 값 유지) · 정규화 7/7 · 드롭다운 15/15 · **앱 E2E** 로 편집창이
  `☁ 1nd_고전_남성`(인덱스 2)을 정확히 가리키는 것 확인(고치기 전엔 인덱스 0).
- ⚠ **로이가 하실 일**: 이 채널의 목소리를 **의도한 것으로 다시 고르고 저장**하세요. 지금 저장된
  `1nd_고전_남성` 은 값이 지워진 상태에서 잘못 박힌 것일 수 있습니다(참조텍스트도 「테스트」의 문장입니다).

## 🔴 아내 PC 가 서버에 못 닿던 진짜 이유 — **메인 PC 의 LAN IP 가 바뀌었다** (2026-08-14, v0.3.7)
> 아내 PC: `⚠ 업로드 실패 — fetch failed`. 주소를 9893→9881 로 옮겼는데도 실패.
- 🔑 **원인: 메인 PC IP 가 `192.168.219.157` → `192.168.219.145` 로 바뀌었다**(이더넷·DHCP).
  아내 PC 설정은 옛 IP 그대로 → 닿을 곳이 없다. **메인 PC 에서 자기 옛 LAN IP 로 접속해 보고 확인**했다.
  · 이러면 `listServerVoices()` 도 실패 → 빈 배열 → 목록이 **로컬 파일로 폴백**된다.
    아내가 "참조음성 선택항목이 그대로"라고 한 게 이것이었다(☁ 목록을 못 받아 옛 화면처럼 보인 것).
- 🔑 **`fetch failed` 가 아무것도 안 알려준 게 진짜 문제였다.** undici 는 실제 이유를 `e.cause` 에 숨긴다.
  → `_netErr()` 신설: **원인 코드 + 사람 말 + 시도한 주소**를 함께 찍는다.
  실측 결과 `fetch failed [UND_ERR_CONNECT_TIMEOUT] — 그 주소에 아무도 없습니다… (http://192.168.219.157:9881)`.
  목록 폴백 로그에도 **어느 주소로 시도했는지** 붙였다. (같은 계열 사고가 v0.2.51·v0.2.84 에 이어 세 번째다.)
- ✅ **DHCP 로 또 바뀌지 않게 — 주소를 IP 대신 컴퓨터 이름으로.** 실측: `http://DESKTOP-CBQLOLJ:9881` ·
  `http://DESKTOP-CBQLOLJ.local:9881` 둘 다 200. IP 가 바뀌어도 이름은 그대로다.
- ⏳ 9881 은 **아직 옛 코드**(`/save-ref-voice` → 404 확인). 관리자 권한 재시작 필요.

## ☁ 참조음성 업로드를 **OmniVoice(9881)로 이관** — 9893 은 대부분 꺼져 있다 (2026-08-14, v0.3.6)
> 아내 PC 로그: `⚠ 업로드 실패 — connect ETIMEDOUT 100.112.7.63:9893`.
> 로이 지적: "꼭 9893 을 써야 해? 옴니보이스는 항상 켜져 있으니 그 포트를 쓰면 안 되나?" — **맞는 지적이었다.**
- **원인 2겹**: ① 9893 은 온디맨드라 대부분 꺼져 있다(v0.3.5 에서 안 죽이게 고쳤지만, 부팅 전엔 여전히 없음)
  ② 아내 PC 의 보이스디자인 주소가 **Tailscale IP(100.112.7.63)** 인데 **그 PC 엔 Tailscale 이 없다**
  → 애초에 닿을 수 없는 주소였다(OmniVoice 는 LAN 주소라 잘 되고 있었다). 그래서 timeout.
- 🔑 **구조적으로 OmniVoice 가 맞다** — `REF_LIB`(`D:\TTS_Model
ef-audio`)의 **주인이 이미 OmniVoice** 이고
  (`/ref-voices` 가 거기서 읽는다), 이 서버는 **SYSTEM 예약작업으로 상시 실행**이다. 업로드만 남의 서버에
  맡기고 있었던 게 이상했다. → `api.py` 에 **`POST /save-ref-voice`** 신설(9893 의 `/save-voice` 와 같은 규약:
  `_safe_voice_name` 경로탈출 차단 · 같은 이름은 `_2`,`_3` 으로 회피(덮어쓰기 금지) · RIFF 헤더 검사).
- **앱**: `asr-client.saveServerVoice()` 신설 → `uploadRefVoice()` 가 **9881 우선, 404(구버전)면 9893 폴백**.
  동기화·보이스디자인 저장 둘 다 이 경로를 쓴다. 실패 안내도 "OmniVoice 주소를 확인하라"로 교체
  (**합성이 되는 주소면 업로드도 된다** — 사용자가 확인할 게 하나로 줄었다).
- 검증(TestClient, 임시 REF_LIB 로 프로덕션 무영향): 정상 200 · 같은 이름 → `_2` · `../../탈출` → 라이브러리
  안 `탈출.wav` · 비-wav 400 · 빈 이름 400 · `/ref-voices` 에 3개 반영.
- ⚠ **`D:\TTS_Model\omnivoicepi.py` 는 저장소 밖이라 라이트 업데이트로 배포되지 않는다**(메인 PC 로컬).
  9881 은 SYSTEM 예약작업이라 **관리자 권한으로 재시작해야** 새 엔드포인트가 뜬다. 그전까지는 404 → 9893 폴백.

## 🔴 참조음성 자동 업로드가 안 돌던 이유 2개 — 서버가 꺼져 있었고, 동기화 시점도 틀렸다 (2026-08-14, v0.3.5)
> 아내 PC 를 v0.3.4 로 올리고 앱을 켰는데 **참조음성 목록이 그대로**였다. 원인은 둘 다 내 쪽이었다.
- 🔴 **① 업로드 대상 서버(9893)가 꺼져 있었다.** 업로드는 보이스디자인 서버의 `/save-voice` 를 쓰는데,
  `QD.stop()` 이 **우리가 spawn 한 서버는 `/shutdown` 으로 완전히 종료**했다(v0.2.91 정책의 구멍 —
  "우리가 안 띄운 서버는 안 죽인다"였지만, 부팅 인스턴스가 한 번 죽은 뒤엔 앱이 매번 자기 것을 띄우고 죽였다).
  결과: **보이스디자인 창을 닫는 순간 9893 이 내려가** 참조음성 라이브러리도 함께 막혔다.
  로그가 그대로 보여준다 — `12:33:48 server up` → `12:36:32 server stopped`.
  → **어떤 경우에도 죽이지 않고 `/release` 만** 한다. 서버는 lazy-load + 유휴 자동해제라 **떠 있어도 VRAM 0**
    이므로 살려 두는 게 v0.2.91 의 원래 설계 의도("상시 대기하되 VRAM 0")에 맞다.
- 🔴 **② 동기화가 채널편집(⚙)을 열 때만 돌았다.** `list-ref-audio` 안에만 있었으므로 **앱만 켠 PC 에서는
  아무 일도 일어나지 않는다.** → `syncRefAudioToServer()` 로 분리해 **앱 시작 4초 뒤 배경에서도 1회 실행**.
  창을 막지 않고, 실패해도 조용히 넘어간다(오프라인·서버 꺼짐).
- 실패 시 로그에 **무엇을 해야 하는지** 남긴다("메인 PC 시작프로그램의 「보이스디자인서버」를 실행하세요").
- 검증: 임시 목소리 1개를 로컬에만 두고 동기화 → 서버 목록 12→13 개로 실제 반영, 정리 후 12 개 복귀.
  9893 재기동 후 `/health` 200 · `loaded:false`(VRAM 0) · **Tailscale 100.112.7.63:9893 도 200**(아내 PC 경로 정상).
- ⚠ 전제: 아내 PC 의 ⚙ 설정 → 🖧 TTS 서버 → **「보이스디자인」 칸에 메인 PC 주소**가 들어 있어야 한다
  (비어 있으면 자기 localhost 로 올리려 해서 실패). 보이스디자인을 그 PC 에서 써 왔다면 이미 설정돼 있다.

## 🔴 `✂ ≈5초`가 정반대로 동작했음 + 참조음성 목록을 서버(☁) 하나로 통일 (2026-08-14, v0.3.4)
> 로이가 첫 실사용으로 저장한 `테스트.wav`(5.12초)를 열어보니 **끝이 여전히 0.5%** — 잘랐는데도 무음으로 끝났다.
- 🔴 **내 설계 오류(정정)**: v0.3.3 의 `✂ ≈5초` 는 "단어 중간이 잘리지 않게 **가장 조용한 지점**에서 끊는다"였다.
  그런데 **그 조용한 지점이 바로 없애려던 문장 끝 감쇠·무음**이다. 정책 실측 비교:
  · 가장 조용한 지점 = 끝 **3.0% / 0.1%** (사실상 무의미) · 에너지 임계 기준 = **20~38%**.
  → **"소리가 아직 살아 있는 마지막 순간"에서 끊도록 뒤집었다.** 단어가 잘릴 수 있지만 그건 참조텍스트를
    고쳐 해결할 수 있고, 무음으로 끝나는 참조음성은 고칠 방법이 없다(그게 TTS 로 그대로 옮겨간다).
- 🔑 **임계는 최대 진폭이 아니라 "말하는 구간의 대표 음량"(loud 프레임 중앙값) 기준**. peak×0.4 로 했더니
  순간적으로 튄 한 지점이 기준을 끌어올려 통과 지점이 900칸 중 53칸뿐이었고, 5초를 요청해도 **3.50초**가
  나왔다. **대표음량×0.8** 로 바꾸니 **4.93초·끝 27.5%** 로 균형이 맞았다(±1.5초 안에서 목표에 가장 가까운 지점).
- `suggestRange` 기본 임계도 0.2/pad0.08 → **0.35/pad0.04**(끝 20.3% → 26.2%).
- **참조음성 목록 = 서버 공용 라이브러리(☁)만** — 예전엔 서버 목록 + 로컬 파일을 둘 다 나열해 같은 목소리가
  두 줄씩 보였다. 🔑 **그냥 로컬을 숨기면 아직 안 올라간 목소리가 사라진다** → `list-ref-audio` 가 **먼저
  로컬에만 있는 wav 를 서버로 올리고(동기화)** 서버 목록만 반환한다. 서버를 못 쓰면 로컬로 폴백(앱 안 멈춤).
  옛 로컬 경로로 저장된 채널 값은 같은 이름의 `srv:` 로 자동 정규화(저장 눌러야 반영). `resolveRefPath` 는
  서버 폴더 → 이 PC ref-audio 순으로 찾아 **아내 PC 에서도 미리듣기**가 되게 했다.
- 🔎 **아내 PC 목소리가 왜 안 보였나**: 서버 라이브러리(`D:\TTS_Model\ref-audio`)에 **아내가 만든 게 하나도
  없었다** — 전부 메인 PC 산. 서버는 정상(9893 `/save-voice` 존재·9881 `/ref-voices` 200)이므로 원인은
  **아내 PC 앱이 v0.2.96 이전이거나 업로드가 실패(경고만 남기고 성공 반환하는 설계)** 였을 가능성.
  → 이제 그 PC 에서 **참조음성 목록을 한 번 열기만 하면** 로컬 목소리가 서버로 올라가 메인 PC 에도 보인다.
- 검증: 잘라내기 정책 4/4(끝 27.5%·36.9%) · wav-slice 12/12 · 저장 8/8 · 실제 서버로 목록 조회 12개 전부 ☁.

## ✂ 보이스디자인 **슬라이스** — 끝 감쇠를 잘라내 TTS 문장 끝 끊김 해결 (2026-08-14, v0.3.3)
> 증상(로이): "보이스디자인으로 만든 음성은 **문장 끝이 자꾸 작아지는 페이드**가 걸린다. 그래서 그걸 참조음성으로
>   쓴 TTS 결과도 문장 끝이 계속 끊기는 느낌이다. 10초쯤 만들어 끝을 잘라 쓰면 좋아지지 않을까."
- **실측 확인(진단)**: 생성물의 100ms RMS 포락선을 보면 **마지막 0.7~0.9초가 최대 대비 40%→0% 로 단조 감쇠**하고
  정확히 디지털 무음으로 끝난다(preview.wav·실제 저장된 참조음성 둘 다 동일). `qwen_design_server.py` 에는
  fade/trim 코드가 **없다** → 우리가 거는 게 아니라 **모델 출력 자체의 특성**. 로이의 진단이 맞았다.
  참조음성이 "잦아들며 끝나는" 소리를 담고 있으니 OmniVoice 가 그 끝맺음까지 복제한 것.
- **`core/wav-slice.js` 신설** — ffmpeg 없이 **직접 자른다**(PCM 은 프레임 경계로 자르면 되고 재인코딩이 없어 무손실).
  `parseWav`(청크 순회 · data 크기가 실제보다 크게 적힌 파일도 파일 끝으로 보정) · `sliceWav` ·
  `envelope`(8/16/24/32bit·float32·다채널) · **`suggestRange`**(앞 무음 + **끝 감쇠** 제거, 임계 20%·pad 80ms).
  단위검증 12/12 — 길이 정합·**샘플 바이트 일치(무손실)**·포맷 보존·RIFF 크기·클램프·끝<시작 에러·스테레오 48k.
- **UI(보이스디자인 모달)**: 생성하면 **파형 캔버스**가 뜨고 드래그로 구간 선택(손잡이 주황=시작·빨강=끝),
  숫자 입력 미세조정, **▶ 구간 듣기**(저장될 소리 그대로), **✂ ≈5초**, **↺ 전체**.
  파형은 Web Audio `decodeAudioData` 로 그린다 — **의존성 추가 없음**(deps 가 바뀌면 라이트 업데이트가 막힌다).
- 🔑 **`✂ ≈5초` 는 5초에서 그냥 자르지 않는다** — ±1.2초 안에서 **가장 조용한 지점(말이 쉬는 곳)** 을 찾아 끊는다.
  단어 중간에서 자르면 참조텍스트를 맞추기가 곤란하기 때문.
- 🔑 **참조텍스트 편집칸을 함께 넣은 게 핵심** — 오디오를 자르면 **실제로 들리는 말이 달라진다.** `.wav` 와 `.txt` 가
  어긋나면 음성 복제가 흐트러지므로, 저장 시 UI 의 텍스트를 쓰고(`args.text`) **서버 공용 라이브러리 업로드에도
  같은 텍스트**를 보낸다(예전엔 `S.vdLastText` 원문을 올려 잘라도 원문이 남았을 것). 경고 문구도 UI 에 명시.
- 하위호환: 구간을 안 보내면(구버전 렌더러) 원본 그대로 + 옛 참조텍스트 폴백. 전체 범위면 **재작성조차 안 함**.
- 기본 「미리들을 문장」을 **길게**(약 2문장) 바꿨다 — 끝을 잘라내고도 쓸 5초가 남도록. 실측 생성 길이 16초.
- 검증: wav-slice **12/12** · 저장 경로(자르기+텍스트) **8/8**(잘라낸 파일의 끝 RMS 0.0% → 61.0% 로 회복) ·
  **실제 앱 E2E**(채널⚙ → 🎨 디자인 → 생성 52초 → 파형·구간 1.00~14.96s·참조텍스트·저장 UI 확인).
- ⏳ **미검증**: 잘라낸 참조음성으로 실제 TTS 를 돌려 문장 끝 끊김이 사라졌는지(로이가 확인할 것).

## 🧹 Wan2.2 제거 + ⚙ 팝업의 워크플로 행을 **관리 전용**으로 (2026-08-14, v0.3.2)
> 로이: "wan 2.2 는 제거하자. 첨부한 이미지의 화면구성도 변경되어야 하지 않아?" — 맞는 지적이었다.
>   모델 선택이 헤더로 옮겨갔는데 팝업에 **선택용 select** 가 그대로 남아 "어느 쪽이 진짜냐"가 모호했다.
- **Wan2.2 제거**: `comfy/video_wan2_2_5B_ti2v.json` 삭제 + `BUNDLED` 에서 제외.
  🔑 **그것만으론 부족하다** — `_ensureBundled` 는 "번들 파일명과 안 겹치는 항목 = 사용자 커스텀"으로 보존하므로,
  기존 PC 설정에 남은 Wan 항목이 **커스텀으로 승격돼 드롭다운에 계속 남는다**(파일은 없는데 목록엔 있는 상태).
  → **`RETIRED` 목록 신설**로 명시적으로 걸러내고, Wan 이 활성이었으면 활성값도 비워 기본(LTX2.5)으로 복구.
  ⚠ **로컬(🖥) 그룹은 남겼지만 실질 선택지가 없다** — LTX2.5·2.3 은 22B 라 RTX 3060(12GB)에선 못 돈다.
  로컬을 쓰려면 별도 워크플로를 ＋추가해야 한다(팝업 안내 문구에 명시).
- **팝업 화면 구성 변경**: 「워크플로」 행의 select 를 **칩 목록 + 🗑 + ＋추가**(`WorkflowManageRow`)로 교체 —
  **선택이 아니라 등록·삭제만** 한다. 이미지·비디오 탭 공용. 안내 문구도 "여기선 주소·키·등록만 정하고,
  **어느 모델로 만들지는 헤더 드롭다운에서**"로 교체. (선택 select 를 남기고 양방향 동기화하던 `pickWfHere` 는
  더 필요 없어졌지만, 함수는 남겨두지 않고 제거 — 진입점이 하나여야 헷갈리지 않는다.)
- `removeWf(path, cfg, save)` 공용화 — 예전엔 "**활성** 워크플로를 지운다"였는데 칩마다 지우려면 경로 인자가 필요.
  지운 게 활성이었을 때만 활성을 옮긴다(엉뚱한 활성 이동 방지).
- 검증: `_ensureBundled` **단위 6/6**(Wan 활성/목록만/이관 후 2.3 유지/새 설치/실재 커스텀 유지/없는 커스텀 복구) +
  드롭다운 값 15/15 + **앱 부팅 E2E** 로 Wan 잔존 0 확인 및 팝업 스크린샷 확인.

## 🎛 이미지·비디오 드롭다운에서 **모델(워크플로)까지 직접 선택** (2026-08-14, v0.3.1)
> 요청(로이): "비디오 항목을 LTX2.3·LTX2.5 항목으로 만들어 원하는 방식으로 만들자. **별도 팝업창에서 모델을
>   선택하는 게 아니라 선택항목 자체에서** 고르게. 비디오도 이미지도 마찬가지."
- **되돌린 결정**: v0.2.75 에서 "드롭다운이 복잡해진다"며 항목을 「ComfyUI 로컬/클라우드」 2개로 줄이고 모델은
  ⚙ 팝업으로 뺐는데, 매번 팝업을 열어야 해서 더 번거로웠다. → **로컬/클라우드는 `optgroup` 으로 묶고 그 아래에
  모델을 나열**한다(비디오 Wan2.2 5B·LTX2.3·LTX2.5 / 이미지 Z-Image·Krea2). 그룹 라벨은 접히면 안 보이므로
  항목 텍스트에 **☁(클라우드)·🖥(로컬)** 을 붙여 접힌 상태에서도 구분된다(`☁ LTX2.5`).
- 🔑 **값 설계 — 모드를 값에 넣지 않았다**: select 값은 표시용 `comfy::<local|cloud>::<경로>` 지만,
  **저장되는 엔진 값은 `comfy::<경로>`** 뿐이다. 로컬/클라우드(주소·API키)는 **설정파일이 단일 진실**이고
  드롭다운이 고를 때 함께 저장한다. 모드를 두 곳에 두면 어긋난다(v0.2.51 의 "클라우드 체크 + LAN IP 혼합" 계열 사고).
- **main.js**: `isComfyVal`/`comfyWfOf` 공용 헬퍼 신설 — 흩어져 있던 `=== 'comfy' || indexOf('comfy::')===0`
  세 곳을 통일. **이미지도 워크플로 오버라이드를 받는다**(`runComfyImages(..., workflowPath)` — 비디오와 동일 구조).
  이전엔 이미지는 `'comfy'` 단독만 인식해 항목별 모델 지정이 불가능했다.
- **팝업의 「워크플로」 select 는 남긴다**(추가·삭제용). 단 거기서 바꾸면 **엔진 값도 함께 갱신**(`pickWfHere`) —
  안 그러면 팝업은 Krea2 인데 실제 생성은 옛 경로로 나가는 어긋남이 생긴다(엔진 값이 생성 때 이긴다).
  현재 엔진이 comfy 가 아니면(순환·Grok) 설정만 저장하고 도구를 몰래 바꾸지 않는다.
- **목록에 없는 활성 워크플로도 option 으로 보충**(`comfyWorkflows`) — 안 그러면 select 값과 일치하는 option 이
  없어 **드롭다운이 빈칸**으로 보인다.
- 검증: 값 인코딩/파싱 **단위 15/15**(App.jsx·main.js 원문 헬퍼를 그대로 뽑아 실행 — 두 파서가 어긋나면 실패) +
  **실제 앱 부팅 E2E** 로 두 드롭다운의 항목·선택 상태 확인(이미지 ☁ Krea2 Turbo / 비디오 ☁ LTX2.5).

## 🎬 비디오 엔진 = LTX 2.5 i2v 로 교체 (2026-08-14, v0.3.0)
> 로이: "비디오 생성도구를 클라우드 컴피유아이의 ltx2.5 i2v로 변경하고 싶어." → **조사 결과 이번엔 조건이 갖춰졌다.**
>   아래 8/12 항목의 "대기 조건"이 충족됐고, **그때 내 조회 방법이 틀렸다는 것도 함께 확인**했다.
- 🔴 **8/12 내 진단 정정**: "comfy.org 에 LTX 2.5 가중치 없음(파트너 API 노드뿐)"은 **틀렸다.**
  나는 `CheckpointLoaderSimple.ckpt_name` **하나만** 뒤졌는데, LTX 2.5 는 체크포인트가 아니라
  **`UNETLoader.unet_name`**(디퓨전 모델) 계열로 올라온다. 지금 실조회하면 다 있다:
  `ltx-2.5-22b-distilled-transformer-{bf16,comfy-int8-convrot,nvfp4}` · `ltx-2.5-22b-dev-transformer-bf16` ·
  VAE `ltx-2.5-video-vae-bf16`/`ltx-2.5-audio-vae-bf16` · CLIP `gemma4-12b-with-proj-ltx-2.5-*` ·
  LoRA `ltx-2.5-22b-distilled-lora-450-bf16` · 업스케일러 `ltx-2.5-latent-spatial-upscaler-x2-bf16-1.0`.
  → **가중치 유무는 한 로더만 보지 말고 전 로더의 옵션 배열을 훑어야 한다**(교훈).
- **그래서 파트너 API 노드를 안 쓴다 = 과금 구조가 그대로다.** 로컬노드 워크플로라 **우리가 쓴 GPU 시간**만 나간다.
  (참고로 파트너 노드 `LtxApi25ImageToVideo` 의 실제 단가는 `price_badge` jsonata 에 박혀 있다 —
  **Fast 1080p $0.1859/초 · Pro 1080p $0.2431/초**. 5초 영상 1개가 $0.93 → 편당 5개면 $4.65. 안 쓴다.)
- **실측 대조(같은 소스 이미지·같은 프롬프트·1920x1088·5초, comfy.org 서버 실행시간 기준)**:
  **2.3 = 31.9초 / 2.5 = 44.6초 (+40%)**. 왕복 포함 벽시계는 42.8s → 53.9s. 편당 영상 5개면 GPU 시간이
  약 +64초 늘 뿐이라 비용 영향은 미미하다(0.266 크레딧/초 기준 편당 42→59 크레딧).
  화질은 **2.5 가 원본 구도·돌담 질감을 더 정확히 보존**(2.3 은 더 밀리고 뭉갠다). 양쪽 다 실사화 이탈 없음.
- **워크플로 확보 방법 — 웹 UI "API 포맷 저장"을 안 거쳤다.** comfy.org 가 서빙하는 템플릿
  `/templates/index.json` → `/templates/video_ltx2_5_i2v.json` 은 **UI 포맷 + 서브그래프**라 엔진이 못 읽는다.
  → **`scripts/comfy-ui2api.js` 신설**(UI→API 변환기, 서브그래프 평탄화). 프론트엔드 graphToPrompt 규칙 재현:
  내부 노드 id `<인스턴스id>:<내부id>` · widgets_values 를 object_info `input_order` 순으로 소비
  (`control_after_generate` 는 1칸 더 먹음 · `COMFY_DYNAMICCOMBO_V3` 는 `name.sub` 로 전개 —
  **required 뿐 아니라 optional 까지** · `COMFY_AUTOGROW_V3` 는 위젯값을 안 먹고 `values.a` 링크로 들어옴 ·
  `"FLOAT,INT"` 같은 **복합 타입도 위젯**) · `Reroute` 는 접어 없앰 · 서브그래프 경계는 `origin_id -10`/`target -20`.
  🔑 **검증법이 핵심이었다** — 2.3 은 우리에게 **정답지(기존 API 파일)가 있으므로** 같은 변환기로 2.3 템플릿을
  돌려 대조했다: **노드 51/51 일치, 배선 전 필드 일치**. 남은 차이 2건은 컨버터 버그가 아니라 **템플릿 개정 차이**
  (현 템플릿은 프롬프트 증강 기본 OFF + 이미지-인식 프롬프트 링크 추가). 이 대조 없이는 신뢰할 수 없었다.
- **`--unlink=width,height` 옵션**: 2.5 템플릿은 해상도를 `ResolutionSelector` 노드에서 **링크로** 받는다.
  그대로 두면 우리 엔진의 해상도 주입(`_isPrimNum` 리터럴 탐색)이 안 먹는다 → 승격 위젯 리터럴로 바꾸고
  ResolutionSelector 를 제거. 그 결과 **엔진 코드는 한 줄도 안 고쳤다** — 2.5 내부 노드 제목이 이미
  `Prompt`/`Width`/`Height`/`Duration`/`Frame Rate`/`Boolean (Enable Prompt Enhance)` 라 기존 주입 로직이 그대로 맞는다.
- **등록**: `comfy/video_ltx2_5_i2v.json`(49노드) + `BUNDLED` 에 `LTX2.5` 추가 + `DEFAULT_ACTIVE_FILE` 을 2.5 로.
  **1회성 이관 `migratedLtx25`** — 활성이 2.3 이면 2.5 로 한 번 바꾸고 플래그를 세운다. 사용자가 드롭다운에서
  2.3 으로 되돌리면 saveConfig 가 플래그를 영속시켜 **그 선택이 유지**된다(Wan 사용자는 무영향). 단위검증 4/4 통과.
- ⚠ 남은 차이: 2.5 는 **24fps**(2.3 은 25fps) — 설정의 `fps:25` 는 Duration 프리미티브 경로를 쓸 땐 안 쓰이므로 무해.
  ⚠ `comfy/` 는 매니페스트 포함이라 워크플로 파일은 라이트 업데이트로 아내 PC 에도 내려간다. `scripts/` 는 제외(개발용).
- ⏳ **미검증**: 실제 대본 1편 전 과정(영상 5개). 첫 작업에서 로그의 `노드수 49` 와 업스케일 생략 문구를 확인할 것.

## 🎬 LTX 2.5 검토 — 지금은 도입하지 않음(기존 2.3 i2v 유지) (2026-08-12) ⚠ **위 8/14 항목으로 뒤집힘**
> 로이: "오늘 LTX 2.5 가 공개됐다. 비디오는 LTX_2.5_t2v 로 바꾸고 싶다. 이미지→비디오라 크레딧이 2중으로
>   나갔으니 비디오를 바로 만들면 절약될 듯." → **조사 결과 반대였다. 기존 방식 유지로 결론.**
- **클라우드 현황(`/api/object_info` 실조회)**: comfy.org 체크포인트에 **LTX 2.5 가중치 없음**(2.3 까지만 —
  `ltx-2.3-22b-dev/distilled`). 2.5 는 **파트너 API 노드**로만 존재: `LtxApi25TextToVideo`·`LtxApi25ImageToVideo`
  (category `partner/video/LTXV`). 오픈웨이트는 나왔지만 **22B** 라 RTX 3060 12GB 로컬 구동은 불가.
- 🔑 **과금 기준이 다르다(이게 핵심)**: 우리 워크플로(이미지 Krea2·비디오 LTX2.3)는 **파트너 노드 0개** =
  **우리가 쓴 GPU 시간** 과금(0.266 크레딧/초). 파트너 API 는 **만들어진 영상 길이** 과금
  (LTX-2.5 Fast 1080p **39.22 크레딧/초**) → **초당 단가 147배**.
  · 영상 1개(5초): 지금 = 이미지 1 + i2v 11 = **12 크레딧** / 2.5 t2v = **196 크레딧**
  · 편당(이미지 22장+영상 5개): **77 → 997 크레딧**(13배). 아끼는 이미지 5장은 **5 크레딧**뿐.
  → **"이미지를 건너뛰어 절약"은 성립하지 않는다** — 아끼는 쪽이 이미 공짜에 가깝다.
- 🔑 **대본이 t2v 와 호환되지 않는다(비용보다 근본적)**: 대본의 `🎬 영상:` 은 **모션만** 서술한다
  (예 "The morning light strengthens slowly across the terrace stones"). 인물·배경·구도·화풍은 전부
  `🖼️ 이미지:` 쪽에 있다 → t2v 에 그대로 넣으면 **솔로몬도 왕궁도 없는 엉뚱한 영상**이 나온다.
  t2v 로 가려면 **대본 영상 프롬프트를 전면 재작성**해야 한다(장면+인물+화풍+모션을 한 문장에).
- 화풍 일관성: 롱폼 1편에서 영상은 5개뿐이고 나머지는 이미지+켄번스 → t2v 영상만 화풍이 튄다.
  (v0.2.63 에서 **i2v 인데도** 실사화가 나 Prompt Enhance 를 껐던 전례 — 앵커가 없으면 더 심하다.)
- ✅ **결론/대기 조건**: comfy.org 에 **`ltx-2.5-*` 체크포인트가 올라오면** 그때 전환한다. 그러면 지금과 같은
  **GPU 시간 과금**이라 비용이 그대로면서 2.5 품질을 얻고, **i2v 노드를 쓰면 대본·화풍도 손댈 필요 없다**.
  확인법: `/api/object_info` → `CheckpointLoaderSimple.input.required.ckpt_name[0]` 에 `ltx-2.5` 검색.

## 🔴⏹ 중단을 눌러도 큐가 다음 대본을 계속 시작하던 것 (2026-08-21, v0.3.0)
> 로그(로이): `[0826] ⏹ 중단 요청 → ✗ G1 실패: 중단됨 → ✓ 롱폼 이미지 완료` 인데 곧바로
>   `[0827] 🖼 롱폼 이미지 생성 … 39장` 이 시작되고, 또 중단하면 `[0828] … 40장` 이 시작됐다.
- 🔑 **원인 = 렌더러의 큐 루프가 중단을 알 수 없다.** 중단 플래그 `S.abort` 는 **main 프로세스**에 있고,
  `runStageQueue`(상단 🎤/🖼/🎬 버튼 = 큐 전체)는 **렌더러**의 for 루프다. 그래서 중단은 **그 순간 돌던
  대본의 생성만** 멈추고, 루프는 아무것도 모른 채 **다음 항목의 IPC 를 새로 호출**했다.
  · `✓ 롱폼 이미지 완료` 가 함께 찍히는 것도 오해를 키운다(실제 생성 0장 — main 의 단계 종료 로그다).
- **수정(App.jsx)**: `queueAbortRef`(state 아님 — **setState 는 비동기라 실행 중 루프에 즉시 안 보인다**) 신설.
  `abort()` 가 이 플래그를 세우고, ① 큐 시작 시 초기화 ② **phase 루프**(imgvid 의 이미지→비디오 2패스)
  ③ **항목 루프**에서 매번 검사해 남은 편수를 로그로 알리며 빠져나온다(`⏹ 큐 이미지 중단 — 남은 2편은 건너뜁니다`).
  끝 상태 문구도 `⏹ 큐 … 중단됨` 으로 구분한다.
- 검증: App.jsx **원문에서 runStageQueue 본문을 뽑아** 5개 단언(초기화·phase 검사·항목 검사·남은편수 로그·
  완료문구 구분) + 루프 시뮬레이션(3번 처리 중 중단 → **4·5는 시작조차 안 함**).
- ⚠ 「⚡ 만들기」 큐(run-batch)는 main 안에서 도는 루프라 이 문제와 별개다(그쪽은 S.abort 를 직접 본다).
- ✅ 같은 로그에서 확인된 정상 동작: 재설치한 **로컬 ComfyUI(Krea2 Turbo)** 가 13·14·15·34·34장을 전부 성공,
  **모델 자동 대체**(`krea2_turbo_fp8_scaled → int8_convrot`, v0.3.22)도 그대로 작동.

## 🔴⬆ LTX 1080p 영상까지 업스케일하던 것 — 해상도 판정이 fail-open 이었다 (2026-08-21, v0.2.99b)
> 로이: "그록은 업스케일이 필요하지만 LTX 는 필요 없는 거지? 그런데 **아내 PC 에서 LTX 로 만든 비디오를
>   업스케일한다고 오랫동안 진행**하고 있어."
- 🔑 **원인 = 해상도를 못 재면 업스케일하는 구조(fail-open)**. `getMediaInfo` 는 실패 시 예외가 아니라
  **`{width:null}`** 을 돌려주는데, 판정이 `if (info && info.width >= W ...)` 라 `null >= 1920` = false
  → **`targets.push(g)`** → 업스케일 실행. 즉 **ffmpeg 측정이 실패하는 PC 에서는 LTX 1080p 영상도 전부**
  로컬 GPU 로 수십 분씩 업스케일한다(아내 PC 실사고).
  · 측정 실패 원인 후보: 구글드라이브(G:) 지연·파일 잠김·ffmpeg 실행 실패.
  · ⚠ 내 PC 에서 재현되지 않은 이유 = PATH 에 `D:fmpegin` 이 있어 측정이 항상 성공했다.
- **수정(main.js `_maybeUpscaleCore`)**: ① 측정 **1회 재시도**(1.2초 뒤 — G: 일시 지연 대비)
  ② 그래도 못 재면 **건너뛴다(fail-closed)** + 파일 경로와 함께 경고 ③ **업스케일 대상은 실측값을 로그로**
  남긴다(`⬆ G1 1264x720 → 1920x1080 업스케일 대상`) — 다음에 "왜 업스케일하지?" 가 로그 한 줄로 갈린다.
  🔑 "못 재는 것"을 근거로 긴 GPU 작업을 하는 건 잘못된 기본값이다. 재려면 ffmpeg 를 고쳐야 한다.
- **엔진별 목표 대비 실제 해상도(확정)**: ComfyUI 클라우드 LTX2.5 = 16:9 **1920x1088** · 1:1 1440x1440 ·
  9:16 1088x1920 → **전부 목표(1920x1080/1080x1080/1080x1920) 초과 = 업스케일 불필요**.
  **Grok = 720p**(설정 실측 `videoResolution:"720p"`, 결과 1264x720) → **업스케일 필요**.
  그래서 업스케일 기능 자체는 **제거하지 않는다**(비디오 엔진 기본값이 아직 `grok` 이다).
- 검증: main.js **원문에서 판정 블록을 뽑아 실행** — LTX 1920x1088 생략 ✅ · Grok 1264x720 대상 ✅ ·
  측정 실패 시 건너뜀 + 경고 2건 ✅ · 1차 실패→재시도 성공 시 생략 ✅.
  🔑 **A/B 역검증**: 같은 시나리오(측정 실패 5개)를 `git show HEAD:main.js` 로 돌리면 **업스케일 대상 5개**,
  수정본은 **0개**.

## 🎬 출판 탭에 「영상 대본 모드」 — 대본을 깔끔히 정독 (2026-08-08, v0.2.98)
> 요청(로이): 출판 탭의 1순위는 전자책·POD, **2순위는 영상 대본을 전체적으로 읽어보는 것**.
>   ✏대본수정 창은 전문이 보이지만 이미지 프롬프트까지 섞여 읽기가 어렵다.
- **증상**: 영상 대본(.md)을 출판 탭에서 열면 9~11쪽이 제작 메모로 가득 찬다.
- **실측 진단(파서 확인)**: 앱 경로 `parseBookFiles` 는 `normalizeChapterFile` 로 헤딩을 **한 단계 밀어**
  원 `# 제목`이 장, `## 도입부/장N`이 h3, `### 샷N`이 h4 가 된다. 그래서 **`##` 앞의 인용 3개**
  (🎯 단일 아크·📝 주석·안전필터·🎨 일관성 앵커)가 **본문 첫 장에 들어간다**.
  · 인용 9개 = **4,585자** 중 **제작메모는 1개뿐**이고 나머지 8개는 **성경 낭독**(본문에 있어야 할 것).
  · `> 🖼️ 이미지:`·`> 🎬 영상:` 프롬프트는 **파서가 이미 걸러내고 있었다**(0개) — 오해 없도록 기록.
- **선택: 대본이 아니라 프로그램을 고쳤다.** 대본은 하루 1편씩 생기는 영상 파이프라인의 입력이라
  사람이 매번 위치를 옮기는 건 지속 불가능하고, 롱폼 파서가 `## [예약]` 을 건너뛰는 것과 **대칭**이다.
- **구현(html-builder, 조판 시점 필터 — 원고 파일 불변)**: `scriptFilter()` 신설 + `blocksHtml`·
  `chapterBlocksHtml` 진입점에 적용. ① 제작용 이모지(🎯📝🎨🖼🎬🎞)로 **시작하는 인용만** 제거
  → **본문 인용은 마커가 없으므로 안전** ② `hr`(`---`) 제거 ③ 제목 꼬리 `— 0:00~0:30 · I2V 5샷`·
  `(0:30~3:50)`·` ★` 제거 ④ `scriptHideShots` 면 `샷N` 제목까지 숨겨 줄글로.
- **UI**: 우측 조판 패널에 **「🎬 영상 대본 모드」** + (켜면) **「샷 제목도 숨기기」**. `layout` 에 저장되므로
  항목별로 유지되고, **책 원고는 기본 OFF 라 영향 0**. 미리보기·PDF·ePub 공통 적용.
- **검증(실제 0818 대본)**: 본문 23,970 → **19,748자**(-4,222) · 제작메모 흔적 **0** · 영어 프롬프트 **0** ·
  타임코드 제목 **0** · **성경 낭독 인용 8개 그대로 보존**(blockquote 9→8).
- 🔴 **내 조회 실수 정정 2건**(같은 함정을 다시 밟지 않도록): ① `parseBookFiles(files)` 는
  **`[{path,text}]`** 를 받는다(경로 문자열 배열이 아니다) ② 책 모델은 `chapters` 가 아니라
  **`parts[].chapters[]`** 다. 둘 때문에 "장 인식 0개"라고 두 번 잘못 진단했다.

## ☁ 참조음성을 이름만으로 — 서버 라이브러리 합성 (B안 2단계 완결) (2026-08-08, v0.2.97)
> 1단계로 목소리가 서버에 **모이게** 했고, 2단계는 그걸 **파일 없이 쓰게** 한다.
> 목표: 아내 PC 에 wav 가 아예 없어도 드롭다운에서 목소리를 골라 합성.
- **OmniVoice 서버(`D:\TTS_Model\omnivoice\api.py`)**:
  · `GET /ref-voices` — 라이브러리 목록(name·참조텍스트·bytes). `REF_LIB` = 환경변수 `FLOW_REF_LIB`
    또는 기본 `D:\TTS_Model\ref-audio`(보이스디자인 `/save-voice` 가 채우는 그 폴더).
  · `/tts`·`/tts-batch`·`/tts-batch-save` 에 **`ref_name`** 추가. 우선순위 **ref_name → ref_token → ref_audio**.
    참조텍스트는 **서버가 `<이름>.txt` 에서 읽으므로 클라이언트가 안 보내도 된다**.
  · 🔴 **없는 이름이면 400 으로 실패시킨다.** 처음엔 조용히 참조음성 없이(Auto) 합성돼 **엉뚱한 목소리가
    성공처럼 나갔다**(실측으로 발견 — 영상에 그대로 실릴 뻔). 경로 탈출 시도도 여기서 함께 걸린다.
- **앱**: `tts/asr-client.js` 에 `listServerVoices()`(404=구버전 → 빈 배열) · provider 에 `opts.refName`
  분기(있으면 업로드 생략) · `list-ref-audio` 가 **서버 목록(☁) + 로컬 파일**을 합쳐 반환.
- 🔑 **설계 선택 — 프리셋 필드를 늘리지 않았다**: 서버 목소리는 `voiceCloneRefAudio` 에 **`srv:<이름>`** 으로
  저장한다. pipeline 이 이 접두만 보고 `refName` 으로 보낸다(참조텍스트는 서버 것을 쓰도록 undefined).
  옛 프리셋의 일반 경로는 그대로 동작 = **하위호환**. `resolveRefPath()` 가 `srv:` 를 로컬 파일로 되짚어
  미리듣기·폴더열기도 메인 PC 에선 그대로 된다(파일이 없는 PC 는 미리듣기만 안 되고 합성은 정상).
- **검증(실측)**: `ref_name` 합성 결과가 업로드 방식과 **바이트 단위 완전 일치** · 없는 이름/경로탈출 400 ·
  옛 업로드 방식 하위호환 정상 · `/ref-voices` 가 목소리 10개 반환.
- ⚠ **적용 조건**: 9881 서버는 **SYSTEM 예약작업**이라 관리자 권한으로 재시작해야 새 api.py 가 로드된다.
  그 전까지 `/ref-voices` 는 404 → 드롭다운에 ☁ 항목이 안 보이고 기존(업로드) 방식으로 정상 동작한다.
  `D:\TTS_Model\omnivoice` 는 저장소 밖이라 **api.py 는 라이트 업데이트로 배포되지 않는다**(메인 PC 로컬).

## ☁ 보이스디자인 목소리를 서버 공용 라이브러리에 모음 (B안 1단계) (2026-08-08, v0.2.96)
> 문제(로이): "아내도 나도 보이스디자인으로 음성을 만들어 쓰는데 **그 파일을 서로 공유하지 않는다.**"
- **원인(구조)**: 목소리는 **메인 PC 의 9893 서버**가 만드는데, 결과 wav 는 **요청한 PC 의**
  `~/.flow-app/ref-audio` 에만 저장됐다 → 서로 못 쓰고, 그 PC 를 초기화하면 사라진다.
- **선택지 비교 후 B안 채택**: (A) 구글드라이브 공유 폴더 — 코드는 쉽지만 **G: 가 멈추면 참조음성 읽기가 막혀
  TTS 가 통째로 실패**(2026-08-07 실제로 G: 쓰기 불능 사고를 겪음) + 동기화 지연. **(B) 서버 보관** — 참조음성은
  어차피 매번 서버로 업로드되는 구조이니 서버에 두는 게 자연스럽다. G: 의존을 늘리지 않는다.
- **1단계 구현(이번)**: `qwen_design_server.py` 에 **`POST /save-voice`**(name·text·instruct·wav_b64) +
  **`GET /voices`**(목록) 신설. 저장 위치 `VOICE_LIB` = `--voice-lib` 인자 · 기본 **`D:\TTS_Model\ref-audio`**
  (없으면 스크립트 옆 `ref-audio`). `/health` 에 `voiceLib` 추가. 같은 이름은 **`_2`,`_3` 으로 피하고 덮어쓰지 않음**,
  `_safe_name()` 이 `..`·슬래시·금지문자 제거(경로 탈출 차단), `RIFF` 헤더 검사로 비-wav 거부.
  `<name>.txt`(참조텍스트) + `<name>.instruct.txt`(만든 설명 — 재현·수정용)도 함께 저장.
- **앱**: `core/qwen-design.js` 에 `saveVoice()`·`listVoices()` 추가(원격/로컬 공용 `_target()` 사용).
  `qwen-design-save` 가 **로컬 저장 후** 서버에도 등록 — ⚠ **실패해도 경고만** 하고 성공 반환(로컬 저장은 이미 끝났고,
  구버전 서버는 404 → `unsupported` 로 구분해 안내). generate 시 `S.vdLastInstruct` 보관.
- **검증(실측 4케이스)**: 정상 저장 · 같은 이름 → `_2` · `../../탈출시도` → 라이브러리 안에 `탈출시도.wav`(탈출 차단) ·
  비-wav 거부. `/voices` 가 기존 목소리 10개를 그대로 목록화. 검증 파일은 삭제함.
- ⏳ **2단계(미구현)**: OmniVoice 서버에 `/ref-voices` + `ref_name` 지원 → 클라이언트가 **이름만 골라** 합성.
  그러면 **아내 PC 에 wav 파일이 아예 불필요**해진다. 현재는 합성 시 여전히 로컬 wav 를 업로드하는 구조라
  (`_ensureToken` 이 `fs.readFileSync`), 각 PC 에 파일이 있어야 한다. [[tts-standard-contract]]
- ⚠ `qwen-design/` 는 매니페스트 제외라 **서버 파일은 라이트 업데이트로 배포되지 않는다**(메인 PC 로컬).
  앱 쪽(main.js·core/qwen-design.js)은 배포된다 → 아내 PC 는 앱만 최신이면 이 기능이 동작한다.

## 🧹 ACE-Step(BGM·플리 음악) 제거 — 13.1GB (2026-08-08)
- 판단 근거(실측): **BGM 기본값 OFF**(App.jsx:135) · 플리 출력 폴더에 파일럿 1개뿐 **음악 파일 0개**(7/12) ·
  ace-step 설정 파일 없음 → 실질 미사용 확인.
- **삭제**: `D:\Priming\ace-step\venv` 5.35GB + `C:\Users\…\.cache\ace-step` 7.71GB.
- **남긴 것**(총 13KB): `1_최초설치.bat`·`ace_step_server.py` 등 스크립트 6개 + `core/ace-step.js`(매니페스트 포함).
  → 나중에 필요하면 **`1_최초설치.bat` 한 번으로 복원**된다. BGM 을 켜도 서버가 없으면 "BGM 없이 진행"으로
  graceful 처리되므로 작업이 깨지지 않는다.
- ⚠ 참고: `C:\Users\…\.cache\huggingface\hub` 에 **61.5GB** 가 남아 있다(옛 실험 모델들). C: 정리 시 최대 후보.

## 🎙 OmniVoice 를 `D:\TTS_Model\omnivoice` 로 이관 — TTS 를 앱에서 독립 (2026-08-08, 앱 코드 무변경)
> 계기(로이): "TTS 모델을 Priming 에 종속시키지 말고 별도로 두고 Priming 이 활용하게. 그러면 TTS 구축 따로,
>   Priming 수정 따로 할 수 있다." + "**기존처럼 PrimingFlow 하위에 TTS 모델이 있어서 안 쓰는 PrimingFlow 를
>   삭제하지 못하는 상황**이 생기지 않을 것 같은데" → **정확한 진단이었다**(아래 정정 참조).
- **새 구조 — 폴더 하나가 곧 그 모델의 전부(자족형)**:
  ```
  D:\TTS_Model\
      omnivoice\   env\(전용 파이썬 7.6GB) hf\(가중치 4.6GB) data\(발음사전)
                   api.py  apikey.txt  start_server.pyw  server.log  logs\
      ref-audio\   참조음성 wav+txt (공용 — 모델이 늘어도 여기 하나)
      goodvoice\   ← 새 모델은 이렇게 나란히 추가. 포트만 다르게(omnivoice 9881·보이스디자인 9893)
  ```
  Priming 은 `~/.flow-app/tts-config.json` 의 **주소만** 알면 된다 → 모델 교체 시 앱 수정 0.
- **이관 방법**: `conda create --prefix … --clone D:\miniconda3\envs\OmniVoice` (**다운로드 없이 정확 복제** —
  버전 어긋남 위험 0). 모델 캐시는 **`mv` 로 이동**(같은 드라이브라 즉시 + 심볼릭 링크 50개 보존).
  ⚠ `cp -r` 은 링크를 실제 파일로 복제해 **4.6GB → 16GB 로 부푼다**(실측, 되돌렸음).
- **`start_server.pyw` 가 하는 일**(런처를 따로 둔 이유): ① `HF_HOME`·`FLOW_DATA_DIR`·`FLOW_DICT_PATH` 를
  **이 폴더 안으로 고정**(환경변수를 셸에서 넣으면 자동시작마다 빠뜨린다) ② `FLOW_API_KEY` 를 `apikey.txt`
  에서 읽음(코드에 키를 안 박는다) ③ **stdout/stderr 를 server.log 파일로 돌린다**.
- 🔑 **함정 3개(전부 실측으로 겪음)**:
  1. **pythonw 로 띄우면 포트조차 안 열리고 조용히 죽는다** — 콘솔이 없어 `sys.stdout` 이 무효인데 tqdm 진행바·
     logging 이 출력하다 예외. → 런처가 **api.py 를 import 하기 전에** 파일 핸들로 교체. (python.exe 로는 정상 기동
     → 이 차이 때문에 원인을 놓치기 쉽다)
  2. 인자는 `--host` 가 아니라 **`--address`** (api.py:618). 틀리면 즉시 종료.
  3. 🔴 **`-s` 없이 실행하면 TTS 가 500** — `'numpy.ndarray' object has no attribute 'dim'`.
     `C:\Users\Pink-Desktop\AppData\Roaming\Python\Python311\site-packages` 에 **omnivoice 0.1.5** 가 따로 깔려
     있어 env 안의 **0.1.2** 를 가린다. 0.1.5 의 `generate()` 는 numpy 를 돌려주는데 api.py 는 tensor 를 기대한다.
     **옛 서버가 멀쩡했던 건 SYSTEM 계정이라 그 사용자 폴더가 애초에 안 보였기 때문일 뿐**이다(내 계정으로 켰다면
     똑같이 깨졌다). → 실행은 반드시 **`pythonw.exe -s start_server.pyw`**.
- **자동시작**: 예약작업 **`OmniVoice_Backend`**(SYSTEM·부팅 시)의 대상을 새 경로로 변경.
  ⚠ 이 작업은 **관리자 권한 필수**(비관리자는 `Get-ScheduledTask` 로 조회조차 안 된다).
- **검증(실측)**: 앱의 실제 provider 코드로 호출 → **옛 서버와 동일 출력(177KB · 3.78초 음성)** ·
  재부팅 후 **부팅 1분 만에 새 폴더에서 자동 기동**(20:16:46 부팅 → 20:17:47 모델 로드 완료) ·
  로컬·**Tailscale 100.112.7.63:9881 둘 다 200** · **검은 콘솔 창 0개** · 유휴 GPU 점유 4,400→2,939MB.
- 📌 **참조음성은 각 PC 에 로컬 파일이 있어야 한다**: provider `_ensureToken()` 이 `fs.readFileSync` 로 읽어
  `/upload-ref-audio` 로 **파일 바이트를 올리고** 토큰을 받는 구조 — 서버는 클라이언트 경로를 모른다.
  따라서 `D:\TTS_Model\ref-audio` 는 **메인 PC 용**이고 아내 PC 는 자기 `~/.flow-app/ref-audio` 를 쓴다.
  ⏳ 미적용: main.js 3곳(`list-ref-audio`·폴더열기·보이스디자인 저장)이 `~/.flow-app/ref-audio` **하드코딩** →
  `D:\TTS_Model\ref-audio` 우선 + 없으면 홈 폴더 폴백으로 바꿔야 정본이 된다(아내 PC 때문에 폴백 필수).
  🔜 더 나은 해법: 서버가 참조음성 라이브러리를 갖고 클라이언트는 이름만 고르게(`/voices` + `ref_name`)
      → 아내 PC 에 wav 가 아예 불필요해진다. [[tts-standard-contract]]
- 🔴 **정정 2건(내 앞선 진단이 틀렸다)**:
  1. **실제 돌던 OmniVoice 는 `D:\PrimingFlow\rebuild\tts\omnivoice-backend\api.py` 였다.** 내가 "PrimingFlow 가
     아니라 D:\Work\TTS_Engine 에 있다"고 한 건 오진. 구분법 = **인증(X-API-Key)·`/upload-ref-audio`·`/asr-upload`
     가 있는 쪽이 진짜**(`06_OmniVoice` 판에는 셋 다 없고 `/gpu` 만 있다).
  2. **2026-07-22 GPU 온도 로거는 한 번도 동작한 적이 없다** — 돌지 않는 `06_OmniVoice\api.py` 에 넣었다
     (`logs` 폴더조차 안 생겼다). 위 CLAUDE.md 항목의 "서버 재시작 필요" 기록도 틀렸다. GPU 온도는 미기록 상태.

## 🧹 D: 대청소 — 236GB → 559GB (2026-08-08)
- **삭제**: 휴지통 33,155개 48.3GB(대부분 옛 TTS 출력 wav) · **`D:\ComfyUI` 215.7GB**(로컬 GPU 이미지가 클라우드보다
  12배 느려 버린 경로. 8188 미가동·마지막 로그 7/31. 앱 워크플로 4개는 전부 `D:\Priming\comfy\` 안이고 클라우드
  모드라 무영향 확인 후 삭제) · **conda env 9개 46.7GB**(GPTSoVits·audiobook·CosyVoice·Qwen3TTS·CohereTranscribe·
  VoxCPM2·OuteTTS·Zonos·Kokoro) · `D:\PrimingFlow` 14.6GB · `D:\Shots-maker` 3.1GB · `D:\PrimingBook` 1.3GB.
- 🚫 **삭제 금지(확인된 의존)**: **`D:\miniconda3\envs\bgm`** — `D:\Priming\ace-step\venv` 가 **stdlib 을 여기서
  읽는다**(venv 는 자기 stdlib 이 없다. 실측: `os.__file__` = `envs\bgm\Lib\os.py`) · **`D:\miniconda3` 본체** —
  `qwen-design\venv` 가 의존 · `envs\OmniVoice` — 이관 원본이라 당분간 백업으로 남김.
- 🔑 **권한 함정**: `D:\miniconda3` 를 지울 수 없었던 이유 = **윈도우 재설치 전 계정의 SID** 가 소유자로 남아 있고
  현재 계정은 `Users(읽기·실행)` 뿐이었다(D: 는 포맷을 안 하니 옛 ACL 이 살아남는다). **혼자 쓰는 PC 라도 권한이
  없을 수 있다.** → 관리자 창에서 `takeown /f … /r /d y` + `icacls … /grant "<계정>:(F)" /t` 로 해결(완료).
  파일 20만 개면 10~20분 걸리는데 **진행 표시가 없어 멈춘 것처럼 보인다**(icacls 프로세스 존재로 확인 가능).
- ⚠ 슈퍼토닉(9882)은 자동시작이 `PrimingFlow\...\supertonic-backend\start-autorun.bat` 을 가리켰기 때문에
  **폴더 삭제로 함께 사라졌다**(재부팅 후 9882 닫힘 확인). 예약작업 검색으로는 못 찾았던 등록의 정체가 이것.
- ⚠ Ollama: 설치돼 있고 모델 26GB(C:)가 있으나 **자동시작 등록이 없어 평소엔 꺼져 있다.** 게다가 사용자 환경변수
  `OLLAMA_MODELS=D:\OllamaModels` 가 **빈 폴더**를 가리켜 모델 0개로 보인다(모델 실체는 `C:\Users\...\.ollama`).
  프롬프트 자동작성·BGM 무드는 Ollama 실패 시 Gemini/기본값으로 폴백하므로 작업은 막히지 않는다.

## 🖥 부팅 때 뜨던 검은 「보이스디자인서버」 창 제거 — 파이썬 자체 로깅 + 바로가기 직접 pythonw (2026-08-05, v0.2.95)
> 증상: 부팅하면 검은 콘솔 창(제목 `보이스디자인서버`)이 계속 떠 있음.
- **원인 = 내가 v0.2.94 에서 잘못 판단한 것**. 그때 주석에 "pythonw.exe 는 GUI 서브시스템이라 cmd 가 대기하지 않는다"
  고 적었지만 **틀렸다 — cmd.exe 는 서브시스템과 무관하게 자식이 끝날 때까지 기다린다.** 그래서 시작프로그램
  바로가기가 `4_서버_상시실행.bat` 을 가리키는 동안엔 **서버가 사는 내내 cmd 창이 화면에 남는다**(최소화 스타일로
  등록돼 있어도 창 자체는 존재). 예전 3분 타임아웃 테스트도 같은 이유였는데 그때 해석을 못 했다.
- **왜 리다이렉션을 못 지웠나 → 지웠다**: `>> server.log` 는 "pythonw 는 콘솔이 없어 print 가 실패할 수 있다"는
  이유로 남겨뒀던 것 → **서버가 스스로 로그를 쓰게 바꿔서 그 이유를 없앴다.** `qwen_design_server.py` 에
  `_log(msg)` 신설(타임스탬프 + `server.log` 에 UTF-8 append, `sys.stdout` 이 있을 때만 화면에도 print) →
  기존 `print(..., flush=True)` **7곳 전부 `_log()` 로 교체**. 이제 셸을 거치지 않아도 로그가 남는다.
- **조치**: ① 시작프로그램 바로가기 `보이스디자인서버.lnk` 의 대상을 **`venv\Scripts\pythonw.exe` 로 직접** 변경
  (인자 `qwen_design_server.py --host 0.0.0.0 --port 9893 --idle-timeout 600`, 시작 위치 `D:\Priming\qwen-design`).
  **pythonw.exe 는 콘솔이 아예 없으므로 cmd 도 콘솔창도 생기지 않는다.** ② bat 은 **수동 실행용으로만** 남기고
  리다이렉션 제거 + 이 함정을 주석에 기록(ASCII 유지, `start` 금지도 그대로).
- **검증(실측)**: 바로가기로 기동 → **검은 창 없음** · 9893 리스너 정확히 1개 · 로컬 `/health` 200 ·
  **Tailscale `100.112.7.63:9893` 200** · `server.log` 에 `[2026-08-05 08:47:58] server up …`(자체 로깅, `·` 깨짐 없음) ·
  `/prepare` → 70초 후 loaded(VRAM 7,797MB) → `/release` → 3,685MB(약 4.1GB 반납). 요청이 두 프로세스로 갈리지 않음
  (venv 의 pythonw 는 부모=런처·자식=실서버 쌍으로 뜨지만 **바인딩은 자식 하나뿐**).
- ⚠ 옛 부팅 인스턴스(cmd + python)는 이번에 종료했다. `qwen-design/` 은 매니페스트 제외라 **이 서버 파일은 라이트
  업데이트로 배포되지 않는다**(메인 PC 로컬). 아내 PC 는 서버가 필요 없으므로 무관.
- 🔑 **교훈**: 콘솔 없이 백그라운드 상시 실행이 필요하면 **bat 을 거치지 말고 `pythonw.exe` 를 바로가기 대상으로**.
  로그는 프로그램이 직접 파일에 쓴다(셸 리다이렉션에 의존하지 않는다). 1번회사 `웹관리자_자동시작.vbs` 가 이미
  이 방식(`sh.Run "pythonw … ", 0, False`)이라 그 창은 애초에 안 떴다.

## 🐞 ".log 파일을 열 앱 선택" 창 — 배치파일 인코딩 + `start` 오용 (2026-07-31, v0.2.94)
> 증상: `4_서버_상시실행.bat` 실행 시 Windows 가 **"이 .log 파일을 열 앱 선택"** 창을 띄움(메모장/Antigravity).
- **원인 2개(둘 다 내 실수)**:
  ① **인코딩** — Write 툴이 .bat 를 **UTF-8** 로 저장했는데 **cmd.exe 는 .bat 를 시스템 ANSI 코드페이지(CP949)로 읽는다.**
     한글 주석·echo 가 깨지며 줄이 쪼개져, `rver.log` 같은 조각이 **명령으로 실행** → 셸이 .log 연결앱을 물음.
     실측: 실행 시 `'작'`·`'rver.log'` 를 명령으로 인식하는 에러가 그대로 찍혔음.
  ② **`start` 오용** — `start "" /b prog ... >> "server.log" 2>&1` 형태는 cmd 가 server.log 를 **실행 대상**으로
     넘길 수 있어 같은 창을 띄운다. 애초에 **pythonw.exe 는 GUI 서브시스템이라 cmd 가 대기하지 않으므로 `start` 불필요.**
- **수정**: 배치파일을 **전부 ASCII(영문)로 재작성** + `start` 제거하고 pythonw 직접 호출.
  ⚠ 리다이렉션(`>> server.log 2>&1`)은 **유지** — pythonw 는 콘솔이 없어 stdout 이 무효라, 우리 서버의 `print` 가
  실패할 수 있다. 파일 핸들로 돌려두면 안전하고 로그도 남는다. 파일 안에 이 함정 2개를 주석으로 박아둠.
- **검증**: 비ASCII 바이트 **0개** · 배치 실행 후 `/health` 200(`lazy:true`) · `server.log` 에 정상 기동 로그 ·
  **Tailscale 주소(100.112.7.63:9893)로도 200**(아내 PC 경로 정상).
- 🔑 **교훈(재발 방지)**: 앞으로 **.bat/.cmd 는 ASCII 로만** 작성한다. 한글 안내가 필요하면 README·CLAUDE.md 에 쓴다.

## 🔌 작업 중 절전 차단 — 화면 꺼짐으로 인한 흔들림 방지 (2026-07-31, v0.2.93)
> 질문(로이): "윈도우를 안 쓰면 화면이 커지는 현상이 있을 때 TTS·이미지·비디오도 같이 멈추는 건가?"
- **실측 진단**: 이 PC 전원 설정은 **시스템 절전 0초(=안 함)** · **화면 끄기 180초(3분)**. 앱은 절전 차단을 안 하고 있었음.
  → **작업이 멈추는 건 아니다**(TTS 는 OmniVoice 서버, 이미지·영상은 comfy.org, 업스케일은 자식 프로세스에서 돌고
  진행·폴링은 메인 프로세스(Node)라 화면 상태와 무관). "화면이 커지는 현상" = **모니터 절전 복귀 시 해상도/DPI 가
  기본값으로 떨어지는 것**. 다만 그 순간 **브라우저 자동화(Genspark/Flow/Grok)는 좌표·타이머가 흔들릴 수 있다**
  (Chromium 은 가려진/최소화 창을 강하게 throttle). 즉 "멈춤"보다 **자동화 불안정 + 창 배치 흐트러짐**이 실체.
- **수정**: `powerSaveBlocker('prevent-display-sleep')` 을 **참조 카운트**로 감싸는 `withAwake(label, fn)` 신설.
  적용 지점(핸들러 본문은 안 건드리고 funnel 지점만): **`enqueueTtsJob`·`enqueueImageJob`**(→ tts-build·tts-group·
  make-all·run-batch·image-build·regen-group 전부 커버) + **`genGroupVideos`**(→ 큐를 안 타는 video-build·video-group
  까지 커버) + **`maybeUpscale`**. 중첩·병렬이어도 **시작 1회 / 마지막 작업 끝날 때만 해제**.
- `before-quit` 에서 카운터 무시하고 강제 해제 — 작업 중 강제 종료 시 절전 차단이 남는 것 방지.
- 로그로 가시화: `🔌 절전 차단 — 작업 중 화면이 꺼지지 않습니다 (라벨)` / `🔌 절전 차단 해제`.
- 단위검증 5케이스 통과(단일·중첩 1회만 시작·병렬·예외에도 해제·카운터 0 복귀).
- ⚠ 미적용(의도): 플리(make-playlist*)·STT·mp3 추출은 사용자가 앞에 있는 짧은 작업이라 제외. 필요하면 추가.
- ⚠ 전원 설정(화면 끄기 3분)은 **코드로 바꾸지 않음** — 늘리고 싶으면 사용자가 Windows 설정에서 직접.

## 🔌 보이스디자인 서버 기동 + 안내 문구 파일명 정정 (2026-07-31, v0.2.92)
> 증상: 아내 PC(v0.2.91)에서 `❌ 원격 서버에 연결할 수 없습니다 — 100.112.7.63:9893`.
- **원인 = 메인 PC 에 서버가 안 떠 있었음** (v0.2.91 검증 끝에 내가 `/shutdown` 으로 테스트 서버를 내렸음).
  아내 PC 설정은 정상이었다(원격 모드·대상 주소 올바름) — 앱 문제 아님.
- **조치**: 메인 PC 에서 `pythonw qwen_design_server.py --host 0.0.0.0 --port 9893 --idle-timeout 600` 기동.
  검증: `netstat` **0.0.0.0:9893 LISTENING** · 로컬 `/health` 200 · **Tailscale 주소 `100.112.7.63:9893` 로도 200**
  (= 바인딩·방화벽 규칙 모두 정상 동작 확인).
- **문구 정정**: 안내가 옛 파일명 `2_서버_수동테스트.bat` 을 가리키고 있었음 → **`4_서버_상시실행.bat`** 으로 교체
  (core/qwen-design.js 원격 오류 메시지 · App.jsx 연결테스트 메시지 2곳).
- ⚠ 남은 것(사용자): `shell:startup` 에 `4_서버_상시실행.bat` 바로가기 등록 — 그러면 재부팅 후에도 자동 대기.

## 🎨 보이스디자인 지연 로딩 + 유휴 자동 해제 (C안 — 상시 대기하되 VRAM 0) (2026-07-31, v0.2.91)
> 질문(로이): "서버를 메인 PC 에서 매번 켜줘야 하는거야?" → 상시 실행하면 VRAM 점유, 매번 켜면 번거로움.
>   **C안 채택**: 서버는 상시 대기(모델 없이 포트만) · 요청 시 로드 · 유휴 시 자동 해제.
- **실측 근거**: 모델 VRAM **약 4.2GB**(로드 전 5,201MB → 로드 후 9,417MB). RTX 3060 **12GB** 에서 OmniVoice·
  업스케일과 함께 쓰기엔 상시 점유가 부담 → 지연 로딩이 정답.
- **`qwen_design_server.py` 변경**: 시작 시 로드 폐지(기본 **지연 로딩**, `--preload` 로 옛 동작 가능) ·
  **`POST /prepare`**(모델 로드 요청, 멱등) · **`POST /release`**(모델만 내려 VRAM 반납, 서버 유지) ·
  `/design` 은 미로드면 **그 자리에서 로드하고 대기**(prepare 안 거쳐도 동작) · `_idle_watchdog` 스레드가
  `--idle-timeout`(기본 600초) 유휴 시 자동 해제(gc + `torch.cuda.empty_cache/ipc_collect`) ·
  `/health` 에 `lazy`·`idleTimeout`·`idleSec` 추가 · `_LOAD_LOCK` 으로 동시 요청의 이중 로드 방지.
- **`core/qwen-design.js` 변경**: `start()` 가 서버가 떠 있고 미로드면 **`/prepare` 호출 후 loaded 폴링**(원격도 동일).
  **`stop()` 정책 변경 — 우리가 spawn 한 서버가 아니면(`!S.child`) 또는 원격이면 죽이지 않고 `/release` 만**
  호출한다. 상시 실행 서버를 앱이 내려버려 다른 PC 가 못 쓰게 되는 것을 막는다(창 닫으면 VRAM 은 즉시 반납).
- **`4_서버_상시실행.bat` 신설**: `pythonw` 로 콘솔 없이 백그라운드 실행(`--host 0.0.0.0 --idle-timeout 600`),
  로그는 `server.log`. **자동 시작 = Win+R → `shell:startup` 에 이 파일 바로가기 넣기**(관리자 권한 불필요).
- **검증(실측 전 과정)**: 시작 직후 `loaded:false` VRAM 기여 0 → `/prepare` 로 ~20초 만에 로드(9,417MB) →
  `/release` 로 5,309MB 반납·서버 200 유지 → **유휴 60초 후 자동 해제 확인**(9,422→5,309MB).
- ⚠ 방화벽 9893 인바운드 Allow(Private) 규칙 추가 완료(사용자가 관리자 PowerShell 로 직접 실행).
- ⚠ `qwen-design/` 는 매니페스트 제외 폴더라 **이 서버 파일은 라이트 업데이트로 배포되지 않는다**(메인 PC 로컬 파일).
  git 에는 추적됨. 아내 PC 는 서버가 필요 없으므로 무관.

## 🎨 보이스디자인 원격 사용 지원 — 아내 PC 에서도 동작 (2026-07-31, v0.2.90)
> 요청(로이): 스크린샷은 아내 PC 였고 **아내 PC 에서도 보이스디자인이 되어야 한다.**
- **구조 = OmniVoice 원격 모드와 동일 정책**: 설정에 주소가 있으면 **spawn 하지 않고 그 서버에 붙는다**.
  `~/.shots-maker/qwen-design-config.json` 에 `baseUrl` 추가(기존 `dir` 와 공존).
  `remoteTarget()` 파싱은 `host` / `host:port` / `http://host:port` / 꼬리슬래시 모두 허용, **127.0.0.1·localhost 는
  로컬 모드로 간주**(단위검증 7케이스 통과). `_get/_postJson` 이 `_target()`(원격 or 로컬)으로 전송.
- **원격 모드 동작**: `start()` 는 **health 폴링만**(spawn 없음) · 서버가 없으면 "메인 PC 에서 2_서버_수동테스트.bat 실행"
  안내 · `stop()` 은 **아무것도 안 함**(남이 쓰는 서버를 내려버리면 안 됨) · `status()` 는 `installed:true` 로 보고해
  UI 가 '설치 안 됨' 으로 막지 않게 함 + `remote`·`target` 반환. 음악 생성 중 GPU 뮤텍스도 원격이면 통과(로컬 GPU 무관).
- **메인 PC(서버 쪽) 변경**: spawn 인자에 **`--host 0.0.0.0`** 추가(기본 127.0.0.1 이라 원격 접속 불가였음).
  ⚠ **방화벽 9893 인바운드 허용은 사용자가 직접**(코드로 안 바꿈). ⚠ 서버는 창을 닫을 때 꺼지므로, 아내 PC 가 쓸 때는
  메인 PC 에서 **`2_서버_수동테스트.bat` 로 켜 두어야** 한다(상시 실행 전환은 미구현).
- **UI**: ⚙ 설정 → 🖧 TTS 서버 탭에 **「보이스디자인」 주소 칸 + 연결테스트** 추가(비우면 이 PC 로컬 실행).
  연결테스트는 원격/로컬·로딩·미기동을 구분해 팝업 안에 표시. 보이스디자인 모달의 '설치 안 됨' 문구도 **원격 설정 안내**로 교체.
- ⚠ 아내 PC 는 앱 재시작 후 ⚙ 설정에서 주소 입력 필요. deps 무변경.

## 🎨 보이스디자인 "서버 미기동" 오류 — 준비 전 생성 버튼 잠금 + 문구 개선 (2026-07-31, v0.2.89)
> 증상: 보이스디자인에서 `⚠ 생성 실패: 서버 미기동 — 먼저 start() 필요`.
- **원인 = 준비 안 됐는데 「목소리 생성」이 눌리는 것**. 버튼 조건이 `disabled={vdBusy}` 뿐이라, ① 설치 안 된 PC
  (`⚠ 설치 안 됨`) ② start 실패 — 두 경우 모두 vdBusy=false 가 되어 **버튼이 살아 있고**, 누르면 generate 가
  `health()` 실패로 이 메시지를 띄우며 **진짜 원인이 덮인다.**
- **실측(이 PC)**: `resolveDir()`=`D:\Priming\qwen-design` · venv python 있음 · `/health` **200** · torch 2.12.1+cu126
  CUDA True → **메인 PC 는 정상**. 즉 스크린샷은 설치 안 된 PC 이거나 start 실패 직후 클릭한 경우.
- **수정**: `vdReady` 상태 신설 — start 성공 시에만 true, 생성 버튼 `disabled={vdBusy || !vdReady}`.
  준비 로직을 `vdPrepare()` 로 분리하고 실패 시 **「🔄 서버 다시 준비」** 버튼 노출(창 닫고 다시 열 필요 없음).
  닫을 때 vdReady 해제. 설치 안 됨 문구에 **"메인 GPU PC 에서만 동작"** 명시.
- **백엔드 문구 개선**(사용자 노출): `서버 미기동 — 먼저 start() 필요`(개발자용) → **무엇을 해야 하는지** 안내로 교체.
  `모델 로딩 중` 도 "준비 완료 메시지 뒤 다시" 로 구체화.
- ⚠ **보이스디자인은 로컬 spawn 전용** — `qwen-design` 폴더(venv 포함)는 git·매니페스트 제외라 **아내 PC 엔 없음**.
  원격 사용은 미구현(추후 코드 필요).

## 🧹 옛 저장소 이름 폴백 제거 — 쓸모없는 코드였음 (2026-07-31, v0.2.88)
> 지적(로이): 실질 사용자가 나·아내 2명뿐인데 옛 이름을 굳이 남겨야 하나?
- **내 앞선 판단이 틀렸다(정정)**. v0.2.87 에서 "구버전 코드 PC 를 위한 안전망"이라며 남겼으나, **구버전 코드는 URL 이
  하드코딩돼 있어 `REPOS` 목록을 아예 읽지 않는다**(그 PC 는 GitHub 리다이렉트로 동작). 신버전 코드는 `Priming` 이
  성공하므로 **폴백까지 도달할 일이 없다.** → 폴백은 사실상 **죽은 코드 + 앱 시작마다 404 요청 1회**.
- **수정**: `REPOS = ['Priming']` 단일화. 배열 구조는 유지(나중에 또 옮길 때 재사용 가능). 로직 검증 완료
  (매니페스트 조회 성공 · v0.2.87 · 104개 파일).
- **참고 — 배포 구조 정리(질문 답)**: 저장소 자체가 배포 채널이다. `update:publish` 로 변경파일 + `update-manifest.json`
  (파일별 sha1)을 push → 각 PC 가 앱 시작 시(main.js 로드 **전**) raw.githubusercontent.com 에서 매니페스트를 받아
  설치폴더 파일 sha1 과 비교 → **다른 파일만** 받아 원자적 교체 → 그 실행이 곧 최신. **리다이렉트는 설계가 아니라**
  이름 변경 후 GitHub 가 제공하는 부가 동작일 뿐이며, 현재 코드는 `Priming` 을 직접 조회한다.

## ✅ 저장소 이름 변경 완료 — `Priming-Maker` → `Priming` (2026-07-31, v0.2.87)
> 사용자가 GitHub 웹에서 이름 변경 완료 → 나머지 배관 정리.
- **실측 검증(중요)**: 변경 후 **두 raw URL 모두 200** — 새 이름은 물론 **옛 이름도 리다이렉트 없이 200** 응답.
  → 아직 재시작 안 한 PC(구버전 업데이터, 옛 URL 하드코딩)도 **업데이트가 끊기지 않음**을 확인.
- **정리 내역**: `git remote set-url` → `github.com/getwater-maker/Priming.git`(ls-remote 로 통신 확인) ·
  package.json `build.publish.repo` → `Priming` · light-updater 주석 갱신.
- **`REPOS = ['Priming', 'Priming-Maker']` 는 그대로 유지** — 옛 이름을 지우지 않는다. 이유: ① GitHub 리다이렉트가
  언제까지 유지되는지 보장이 없음 ② 구버전 코드가 남은 PC 를 위한 안전망. 비용은 최초 1회 404 시도뿐.
- ⚠ 아내 PC 는 git 을 안 쓰고 라이트 업데이터만 쓰므로 remote 관련 조치 불필요.
- ⚠ **다음 커밋부터는 새 저장소(`Priming`)로 푸시**된다. 기존 커밋·태그·이력은 그대로 보존됨.

## 🔀 저장소 이름 변경 준비 — light-updater 가 두 이름 모두 시도 (2026-07-31, v0.2.86)
> 요청(로이): GitHub 저장소명을 `Priming-Maker` → **`Priming`**(앱 이름과 동일)으로 변경. "문제 없다면".
- **문제가 있었다**: 라이트 업데이터가 `const REPO = 'getwater-maker/Priming-Maker'` 를 **하드코딩**해 raw URL 을 만든다.
  이름을 먼저 바꾸면 **아직 재시작 안 한 PC(예: 아내 PC)는 옛 URL 로 조회** → GitHub 리다이렉트에 의존하게 되고,
  실패 시 업데이터가 **조용히 스킵**(설계상 오프라인 취급)해 구버전에 머문다. 그래서 **순서가 중요**.
- **수정(1단계, 지금)**: `REPOS = ['Priming', 'Priming-Maker']` 로 **두 이름을 순서대로 시도** — 매니페스트가 받아지는
  base 를 찾아 이후 파일도 그 base 로 받는다. 폴백으로 성공하면 로그에 표시. **리다이렉트에 의존하지 않음.**
  실측: `Priming` → 404 · `Priming-Maker` → 200 (현재 상태에서 폴백이 정상 동작).
- **2단계(사용자)**: 모든 PC 가 한 번 재시작해 이 코드를 받은 뒤 **GitHub 웹 Settings 에서 이름 변경**.
  ⚠ `gh` CLI 가 이 PC 에 없어서 코드로는 변경 불가 — 계정 소유자가 웹에서 직접. 변경은 공개 저장소 대상이라 확인 후 진행.
- **3단계(변경 후)**: `git remote set-url` · package.json `build.publish.repo` 갱신 · REPOS 에서 옛 이름 제거(선택).
  ⚠ 아내 PC 는 git 을 안 쓰므로 remote 변경 불필요(라이트 업데이터만 사용).

## 🖱 팝업이 바깥 클릭으로 닫히던 것 차단 + 연결테스트 결과를 팝업 안에 표시 (2026-07-31, v0.2.85)
> 요청(로이): ① 마우스를 살짝만 잘못 옮겨도(=카드 밖 클릭) 팝업이 닫힌다 → **닫기 버튼·ESC 로만** 닫히게
>   ② 설정 팝업의 연결테스트 결과가 로그창에만 뜬다 → **그 팝업 안에서 바로** 보이게.
- **① 배경 클릭 닫힘 제거**: `classList.contains('modal-bg')` 로 닫던 모달 2개(**채널 순서**, **⚙ 설정**)의 onClick 제거.
  나머지 모달은 원래 배경 클릭으로 안 닫혔음(주석에 의도도 명시돼 있었는데 v0.2.75·0.2.66 에서 추가한 이 둘만 예외였음).
- **ESC 처리 누락 보완**: ESC 핸들러에 **settingsOpen·chOrderOpen·ttsSrvOpen·comfyOpen·cvidOpen·nameAsk** 추가
  (기존엔 이들이 빠져 ESC 로도 안 닫혔음). `nameAsk` 는 다른 모달 위에 뜨므로 **가장 먼저** 검사. deps 배열도 갱신.
- **② 연결테스트 결과 팝업 내 표시**: `settingsMsg` 상태 신설 — testComfy·비디오 테스트·testTtsSrv 가 결과를 여기에도
  기록. 팝업 하단에 **색상 박스**(✅ 초록 / ❌ 빨강 / ⏳ 회색)로 표시하고 **실패 시 주소·에러까지** 함께 보여준다.
  팝업을 열 때·탭을 바꿀 때 초기화(지난 결과 오해 방지). 로그창 표시는 그대로 유지(양쪽 다 남김).
- ⚠ JSX 함정: `{cond && ( {/* 주석 */} <div> )}` 는 **빌드 에러**(Expected ")" but found "className") — 주석은
  `{cond && (` **바깥**으로 빼야 한다. 처음에 안쪽에 넣어 빌드가 깨졌고 바로잡음.
- ⚠ 앱 재시작 반영. deps 무변경.

## 🧹 Supertonic TTS 전면 제거 + 아내 PC TTS 연결 장애 원인(Wi-Fi Public) (2026-07-31, v0.2.84)
> ① 아내 PC TTS 연결 실패 ② "슈퍼톤은 제거하자, 옴니보이스 하나면 충분" (로이 결정)
- **① 연결 장애 원인 = Windows 네트워크 프로필이 `Public` 로 바뀜 (앱 문제 아님)**. 점검 결과:
  서버 정상(`localhost:9881/health` 200) · **0.0.0.0 리스닝** · IP 그대로(192.168.219.157) · 9881 방화벽 **Allow 지만
  Profile=Private 전용** · 그런데 Wi-Fi `NetworkCategory=Public` → **Private 규칙이 적용 안 돼 LAN 인바운드 차단**.
  프로필 이름이 `U+NetC7AC_5G 2`(끝에 " 2")인 게 단서 — Windows 가 같은 SSID 를 **새 프로필로 재생성하며 기본값
  Public** 으로 잡음(어제까진 되던 이유). ⚠ 레지스트리 이력(NetworkList)은 관리자 권한 필요로 직접 확인 못 함.
  → **설정에서 Wi-Fi 를 '개인(사설)'로 변경**하니 즉시 정상화(사용자 확인). ⚠ 방화벽·네트워크 설정은 코드로 바꾸지 않음.
  ⚠ **아내 PC 엔 Tailscale 이 설치된 적 없음**(내 초기 추측은 오류 — `tailscale status` 의 offline 2대는 다른 기기).
  즉 **경로는 LAN 하나뿐** → 집 밖에서 쓰려면 Tailscale 설치가 별도로 필요.
- **② Supertonic 제거**: `tts/providers/supertonic-provider.js` **파일 삭제** · tts-manager `_connectSupertonic`·
  start() 호출·refreshProvider 분기 제거 · tts-config DEFAULTS 에서 supertonic 삭제 · main 의 `preview-supertonic` IPC
  + preload `previewSupertonic` 제거 · App.jsx 의 ttsSrv.supertonic 상태·설정탭 Supertonic 행·`previewSupertonicVoice`·
  채널모달 `engine==='supertonic'` 분기(사전정의 음성 select) 제거 → 참조음성 UI 를 무조건 표시.
  · preset-store: 시드 프리셋 2개(p_supertonic_ko_f1/m1) 삭제 + **loadAll 마이그레이션은 폐기 대신 `engine:
  supertonic → omnivoice` 이관**(채널을 filter 로 버리면 출력폴더·자막·스타일 설정까지 통째로 유실되므로).
  · gemini-provider 의 사용자 노출 에러문구에서 "Supertonic" 제거.
- 검증: 8개 파일 문법 OK · **채널 13개 그대로**(유실 0, omnivoice 12/gemini 1) · 매 실행마다 찍히던
  `[TTS] Supertonic 연결 중…` 로그 사라짐. ⚠ 앱 재시작 반영. deps 무변경. `~/.flow-app/tts-config.json` 의 supertonic
  항목은 코드가 더는 안 읽으므로 방치 무해.

## 🔤 부정 절(`no text`)을 맨 끝으로 모으고 중복 제거 + 긍정 억제 표현 추가 (2026-07-30, v0.2.83)
> 지적(로이): "no text 가 맨 앞에 있으면 부정문이 된다" → 위치/문구를 고쳐 이미지에 텍스트가 안 나오게.
- **코드로 확정한 사실(중요)**:
  · Krea2 워크플로 KSampler **`cfg = 1`** + negative=**`ConditioningZeroOut`** → **네거티브 프롬프트가 아예 없고
    있어도 무효**(Turbo). 즉 `no text` 를 네거티브로 옮기는 정석 해법이 **이 워크플로에선 불가능**.
  · `_buildWorkflow` 가 `CLIPTextEncode.text`(원래는 LLM 확장 체인 링크)를 **리터럴로 덮어씀** → 우리 프롬프트가
    **LLM 확장을 우회해 CLIP 으로 직행**. CLIP 은 부정을 이해 못 하므로 'text' 토큰이 그대로 활성화된다.
  · 실측: 대본 이미지 프롬프트 16/16 이 **끝에** `no text, no watermark` 를 이미 가짐 → 앱이 또 붙여 **중복**
    (='text' 토큰 가중치 배가). 대본 비디오 프롬프트는 과거 `NO text, no letters…` 로 **맨 앞** 시작 사례 있었음.
    사용자 수채화 스타일엔 `NOT digital illustration, NOT line art…` 가 중간에 있었음(스타일은 맨 앞에 붙음).
- **수정 `normalizePromptNegations(prompt)`**(pipeline, export): 쉼표 절 단위로 **부정으로 시작하는 절만**
  (`no|not|without` + 단어경계) 추출 → 원위치 제거 → **중복 없이 맨 끝에 재부착**. `NOT x`→`no x` 정규화.
  ⚠ 문장 중간의 not(`she does not look away`)은 절 시작이 아니라 **보존**된다.
- **`buildImagePrompt`**: `<스타일><본문>, plain unmarked surfaces, clean blank walls` + 부정 절 정리.
  **`POS_CLEAN` 신설** — CLIP 은 부정을 못 읽지만 **긍정 서술은 작동**하므로 실제 억제력은 여기서 나온다.
- **비디오도 동일 적용**(runComfyVideos) + 📝 팝업 미리보기도 같은 함수로 계산해 표시=실제 일치.
- 검증: 단위 5케이스(앞→끝 이동·중복제거·NOT정규화·부정없음 무변경·문장중간 not 보존) + 실제 프롬프트에서
  `no text` 1회·맨 앞 부정문 없음·첫 부정 절 61% 지점·수채화 스타일 NOT 절도 67% 지점으로 이동 확인.
- ⏳ **한계(정직히)**: cfg=1·네거티브 없음 구조에선 프롬프트로 텍스트를 100% 막을 수 없다. 가장 확실한 레버는
  **대본 프롬프트에서 글자가 있는 사물(간판·책 표지·신문·라벨)을 묘사하지 않는 것**. ⚠ 앱 재시작 반영.

## 📝 프롬프트 팝업에 '실제 전송되는 최종 프롬프트' 표시 (2026-07-30, v0.2.82)
> 요청(로이): 📝 버튼 팝업에 대본 프롬프트만 보인다 — **스타일까지 포함된 실제 생성용 프롬프트 전체**가 보이게.
- ⚠ **편집칸은 대본 원문(raw) 그대로 유지** — 이 팝업은 `set-group-prompt` 로 대본에 저장하는 편집기라, 결합본을
  편집칸에 넣으면 저장 시 대본이 스타일까지 먹은 채로 오염된다. → **읽기전용 '최종 프롬프트' 칸을 따로 추가**.
- **IPC `final-prompt-preview({styleId,imagePrompt,videoPrompt,motionNote})`** 신설 — 표시값이 실제와 어긋나지 않게
  **생성 코드와 같은 함수**로 계산: image=`P.buildImagePrompt(스타일프롬프트, 대본프롬프트)`(스타일 앞 + no text/
  no watermark + 얼굴 네거티브), video=`대본 비디오프롬프트 → 모션노트 → 기본 모션` 폴백 체인. 반환에 `styleName`·
  `styleHasPrompt`·`videoSrc`(어느 출처가 쓰였는지) 포함.
- **UI**: 이미지·영상 각각 아래에 회색 읽기전용 textarea + 「📋 최종 프롬프트 복사」. 편집 중에도 **300ms 디바운스로
  실시간 갱신**. 스타일 프롬프트가 비면 `⚠(스타일 프롬프트 비어있음)`, 영상은 `출처: 대본/모션노트/기본 모션` 표기.
  기존 '복사' 버튼(스타일 접두만 붙이던 것)은 최종본 복사로 대체.
- 영상엔 스타일을 붙이지 않는 현재 정책을 UI 에 명시(원본 이미지가 화풍을 가짐 — v0.2.63 방법B 미적용 상태).
- ⚠ 앱 재시작 반영. deps 무변경.

## 🎬 비디오(i2v)에도 검정 감지 + 순차 재생성 (v0.2.80 이미지 방어의 확장, A안) (2026-07-30, v0.2.81)
> 로이 선택 A: 이미지에서 확인된 동시 생성 부작용이 i2v 에도 있을 수 있으니 **선제 방어**(비디오 검정은 미확인 상태).
- **`looksBlankVideo(file)`**: 영상은 **파일 크기로 판정 불가**(검정도 수백KB) → **1·2·3초 지점 프레임을 실제 샘플링**
  (`-ss t -frames:v 1 -vf scale=1:1 rgb24`). **성공적으로 읽은 프레임이 전부** RGB합 <18 일 때만 검정으로 본다
  (페이드인으로 첫 프레임만 검은 경우 배제).
- **임계값 18** (이미지는 30보다 낮게) — 8분짜리 재생성 낭비를 막기 위해 오탐에 보수적. 실측 마진 확인:
  실제 정상 i2v 영상 RGB합 **140~507** · 완전 검정 **3**(→검정 판정 ✓) · 아주 어두운 밤 장면(#0d0f14) **49**(→정상 ✓).
- **복구 패스**: 검정이면 파일 삭제·`videoPath=null` → 동시 패스 후 **순차로 재생성. ⚠ i2v 는 건당 비용이 커서 1회만**
  (이미지는 2회). 실패 시 로그로 알리고 `videoPath` 없음 → **이미지로 대체되어 .vrew 는 계속 진행**
  (영상 없는 그룹은 이미지+켄번스로 나가므로 이미지처럼 .vrew 를 막지 않는다 — 의도된 차이).
- ⚠ 비디오 동시 개수는 **3 유지**(이미지와 달리 검정 발생 미확인). 실제로 검정이 잦으면 ⚙ 비디오 탭에서 1~2 로 낮출 것.
- ⚠ 앱 재시작 반영. deps 무변경.

## 🔴 동시 생성이 '검정(빈) 이미지'를 유발 → 검정 감지 + 순차 재생성 + 기본 동시 2 (2026-07-30, v0.2.80)
> 증상(로이): 이미지 대신 **검정 화면**이 나오고, **재생성해도 검정**. (v0.2.78 동시 4장 도입 직후)
- **원인 = 동시 생성(실측 대조로 확정)**. 출력 폴더의 png 크기로 대조:
  · 7/29 **순차**: 고전_0730 47장 + 품격_0731 27장 = **74장 중 검정 0개**
  · 7/30 **동시 4장**: 0729 8장 중 1 + 0730 16장 중 2 = **24장 중 3개(12.5%)**
  검정 파일은 **11KB**(정상 ~1.5MB), 1x1 평균 **RGB 4,0,5**, 해상도는 정상과 같은 1344×768.
  ⚠ **프롬프트 문제 아님** — 검정이 난 G7·G10 프롬프트는 "냉장고 앞 노인"·"책상에서 편지 쓰는 노인" 으로 완전 무해
  (더 민감해 보이는 G9 병상 장면은 정상). comfy.org 는 **모두 completed 로 보고**(4~5초, 정상과 동일)해서 앱이 몰랐음.
  comfy.org `/api/jobs` 조회로 확인: outputs_count 대부분 1(엉뚱한 출력 선택 아님), Krea2 워크플로는 22노드에
  **LLM 프롬프트 확장 노드 포함**. 동시 실행 시 서버측 경합/VRAM 압박으로 디노이즈가 사실상 안 된 것으로 추정.
- **수정 ① `looksBlankImage(file)`**: 받은 파일을 **반드시 검증** — 1차 파일크기(>120KB 면 즉시 통과) → 2차 ffmpeg
  `scale=1:1` 평균 RGB 합 <30 이면 검정. 판정 실패 시엔 정상 취급(오탐 방지). **실제 파일 4개로 4/4 정확 판정**.
- **수정 ② 검정 복구 패스**: 동시 패스 중 검정이면 파일 삭제·`imagePath=null` 후 `blanks[]` 에 모아두고, 동시 패스가
  끝난 뒤 **순차로(동시성 0) 그룹당 최대 2회 재생성**. 순차면 정상 생성되므로 대개 1회에 복구. 2회 실패 시 로그 경고
  + imagePath 없음 → 기존 `.vrew 게이트`가 미생성 그룹을 알려줌(검정 프레임이 영상에 실려 나가는 것 방지).
  ⚠ regen-group(🔄 단건 재생성)도 runComfyImages 를 타므로 **"재생성해도 검정" 문제까지 같이 해결**.
- **수정 ③ 기본 동시 4 → 2** (comfy-image DEFAULTS) + 사용자 설정파일의 4 도 2 로 낮춤. 복구 패스가 있지만 애초에
  덜 나오는 게 낫다. 여전히 순차보다 빠름. ⚠ 비디오(concurrency 3)는 이번에 **미검증** — i2v 도 같은 현상이 있으면
  같은 방식(검정 감지+순차 재생성)을 넣어야 함(현재 비디오엔 검증 없음).
- ⚠ 앱 재시작 반영. deps 무변경.

## ⚡ ComfyUI 클라우드 비디오(i2v)도 동시 3개 생성 (v0.2.78 이미지 검증 후 확장) (2026-07-30, v0.2.79)
> 이미지 동시 생성(v0.2.78)이 "순조롭고 빠르다" 확인 → 약속대로 비디오에도 동일 적용.
- **이미지와 이득 구조가 다름(중요)**: i2v 는 건당 수 분이라 **왕복 오버헤드 비중이 3% 남짓**(이미지는 67%).
  따라서 이득은 오버헤드 제거가 아니라 **여러 개를 동시에 올려 벽시계 시간을 줄이는 것**(5개×8분 순차 40분 →
  동시 3이면 ~14분). ⏳ 단 comfy.org 가 실제로 병렬 실행하는지(아니면 서버에서 직렬 대기) 미확인 — 직렬이면
  총 시간은 그대로다. 총 크레딧은 어느 쪽이든 동일(생성당 과금).
- **수정**: `comfy-video` DEFAULTS `concurrency: 3` + main `runComfyVideos` 를 **워커 풀**로(이미지와 동일 구조).
  `imageToVideo` 는 호출마다 업로드명·그래프·prompt_id·출력경로가 독립이라 동시 호출 안전(확인, 공유 상태 대입 없음).
  ⚠ **로컬은 VRAM 때문에 항상 1개씩 강제**. 429/동시제한 감지 → 순차 자동 강등 후 완주. 중단은 양쪽에서 즉시 이탈.
- **비디오 폴링은 2.5초 유지**(8분 작업에 0.8초로 줄이면 요청만 3배 늘고 이득 없음 — 이미지와 달리 안 건드림).
- ⚙ 설정 「🎬 ComfyUI 비디오」 탭에 **「동시」 입력(1~4)** 추가. 단위검증 6케이스 통과(클라우드 적용·대상수 상한·
  로컬 강제1·동시1 순차·상한4·**구 설정파일은 1=기존 동작**).
- ⚠ 앱 재시작 반영. deps 무변경. 업스케일(로컬 Real-ESRGAN)은 생성 전부 끝난 뒤 실행이라 무영향.

## ⚡ ComfyUI 클라우드 이미지 = 동시 4장 생성 (장당 12~18초 → 5~7초 목표) (2026-07-30, v0.2.78)
> 질문(로이): comfy.org 이미지 생성이 너무 느리다 — 프롬프트 문제인가?
- **프롬프트는 무관**(길이는 텍스트 인코딩 수십 ms 만 영향). **실측 원인 = 한 장씩 순차 왕복.**
  로그 계산: 45장 806.7s(17.9s/장) · 40장 630.0s(15.8s) · 47장 736.2s(15.7s). 그런데 **comfy.org 서버 보고 생성시간은
  5~6초** → 장당 10초 이상이 업로드·폴링·다운로드 왕복에서 샘(그 동안 클라우드 GPU 유휴). 해상도 1344×768 은 적정.
- **수정 ①** `comfy-image` DEFAULTS 에 `concurrency: 4` — main `runComfyImages` 를 **워커 풀**로 재작성(queue.shift()).
  `textToImage` 는 호출마다 그래프·seed·prompt_id·출력경로가 독립이라 **동시 호출 안전**(확인). ⚠ **로컬(cloud=false)은
  VRAM 때문에 항상 1장씩 강제** — concurrency 는 클라우드에만 적용. `1` 로 두면 기존 순차와 완전 동일.
- **수정 ②** 클라우드 폴링 **2초 → 0.8초**(5초 작업을 6초에 감지하던 낭비 제거).
- **안전장치**: 에러가 `429/too many/concurrent/rate limit` 류면 `degraded` → 워커 전원 종료 후 **남은 장을 순차로 완주**
  (+안내 로그). 중단(S.abort)은 워커 루프·순차 폴백 양쪽에서 즉시 이탈. ⚙ 설정에 **「동시 생성」 입력(1~8)** 추가.
- **비용**: comfy.org 는 생성당 크레딧이라 **총 크레딧 동일**(시간만 단축). RunPod(시간당)면 시간 단축 = 비용 절감.
- 단위검증 5케이스 통과(동시4 최대동시=4 · 동시1=순차 · 동시제한→순차 완주 · 장수<동시수 · 중단 조기이탈).
- ⏳ **실측 필요**: comfy.org 플랜의 동시 실행 허용치 미확인 — 첫 실행에서 `⚠ 클라우드가 동시 실행을 거부` 로그가
  뜨면 동시 장수를 2~3 으로 낮추면 됨. ⚠ 앱 재시작 반영. deps 무변경.

## 🔴 큐 제작 시 이미지 스타일이 안 붙어 실사 이미지 → 헤더 우선 (2026-07-30, v0.2.77)
> 증상(로이): 헤더 스타일이 `따듯한 회화풍 일러스트` 인데 생성된 이미지가 **실사**. 스타일 프롬프트가 안 들어간 듯.
- **원인 = v0.2.76 범위 사고와 완전히 동일한 구조**: run-batch 가 `styleId: s.styleId || null` 로 **항목 저장값만** 참조 +
  렌더러가 `common` 에 styleId 를 안 보냄. → **대본을 열고 바로 「만들기」**(디바운스 저장 전, 그 항목은 활성이었던 적
  없음) → `styleId=null` → `buildImagePrompt('', 대본프롬프트)` = **스타일 앵커 0개** → 모델 기본 성향(실사)로 나옴.
  (🖼 이미지 버튼·runStageQueue 는 헤더 styleId 를 직접 넘겨 정상이었음 — ⚡만들기 큐 경로만 사고.)
- **수정**: ① 렌더러 runBatchAll 이 `common.styleId` 전달 ② main 이 **헤더 우선 → 항목 폴백**
  (`common.styleId != null ? common.styleId : (s.styleId || null)`) — 도구·범위와 동일 정책(큐 단위 공통).
  ③ **2단계 로그에 실제 스타일 표기** — `🖼 2단계 — 이미지 일괄 생성… (스타일 따듯한 회화풍 일러스트)`,
  미지정이면 `⚠ 스타일 없음(실사로 나올 수 있음)`, 프롬프트 빈 스타일이면 `⚠(프롬프트 비어있음)` → 눈으로 즉시 확인.
- 실측 검증: `getById`(⚠ style-store 는 `list` 없음 — `loadAll`/`getById`)로 이름 조회 OK, 결합 프롬프트가
  `Warm painterly illustration, …` 로 시작함 확인 / 스타일 없을 때는 대본 프롬프트만 나감(사고 재현) 확인.
- ⚠ 앱 재시작 반영. deps 무변경. **이미 만든 실사 이미지는 X 로 지우고 재생성해야** 회화풍으로 바뀜.

## 🔴 큐 제작 시 영상 범위 미지정 → 전 그룹 영상 생성(비용 사고) → 헤더 우선 + 안전기본 (2026-07-29, v0.2.76)
> 증상(로이, **비용 직결 중대**): 헤더 범위 `1~7` 인데 큐 제작에서 `🎬 롱폼 비디오 생성 … 47개 그룹` — 47개 전부
>   i2v 생성(클라우드 GPU 시간 낭비). 같은 큐의 다른 대본들은 5~6개로 정상이었음.
- **원인(코드 확정 2단)**: ① `rangeNums(project, null, null)` 이 **전 그룹을 반환**(main.js:1682 — "미지정=전체" 설계).
  ② run-batch 가 범위를 **항목 저장값(`s.vidFrom/s.vidTo`)에서만** 읽고 헤더(common)는 안 봄. 렌더러도 common 에
  범위를 안 실어 보냈음. → **대본을 열고 곧바로 「만들기」를 누르면**(디바운스 저장 전, 그 항목은 활성이었던 적이
  거의 없음) `s.vidFrom/vidTo` 가 없어 `null` → ①에 걸려 **전 그룹**. 로그 시퀀스로 확인: 정상이던 대본들은 `↔ 대본
  선택` 을 거쳐 설정이 저장된 것들, 사고 난 고전_0730 은 2개 열기 직후 바로 제작(저장 전).
- **수정**: ① 렌더러 runBatchAll 이 `common` 에 **vidFrom/vidTo 포함**. ② main `_batchRange(common, s)` 신설 —
  **헤더 우선 → 항목 폴백 → 둘 다 없으면 `G1~G1`**(전체 아님!) + `⚠ 영상 범위 미지정 — 안전을 위해 G1 만 생성` 경고.
  영상은 건당 비용·시간이 크므로 **"미지정 = 전체" 를 큐 경로에서 원천 차단**. ③ 3단계 로그에 **실제 범위 표기**
  (`영상 범위 G1~G7`) → 사고를 눈으로 즉시 확인 가능.
- 단위검증 5케이스 통과(헤더우선·항목폴백·둘다없음→G1·빈문자열→G1·경고로그). ⚠ 앱 재시작 반영. deps 무변경.
- ⚠ 단건 경로(make-all·video-build)는 렌더러가 항상 숫자를 보내 무영향(기존 동작 유지).

## 🎛 ComfyUI 드롭다운 = 로컬/클라우드 2개 · ⚙ 버튼 1개 + 채널 순서 변경 + 대본 다중 열기 (2026-07-29, v0.2.75)
> 요청(로이): ① 컴피UI 클라우드·로컬을 각각의 항목으로 ② ⚙ComfyUI 버튼 1개로 통합(Krea2/Z-Image 선택은 설정 팝업에서)
>   ③ 채널 드롭다운 항목 순서 변경 ④ 대본 열기 다중 선택(2개 이상이면 작업큐에 순서대로).
- **① 드롭다운 단순화**: 워크플로마다 항목이 늘어나던 것(`ComfyUI: Krea2 Turbo`·`Z-Image Turbo`·`Wan2.2 5B`·`LTX2.3`)을
  **「ComfyUI 로컬」·「ComfyUI 클라우드」 2개**로. 엔진 값은 기존 `'comfy'` 그대로라 **main.js 무변경**(레거시 `comfy::<path>`
  도 계속 인식). 서버 프로필 기반 `comfy-profile::` 조합 옵션·`activeComfyChoice`·`parseComfyChoice` 제거.
  워크플로(Krea2/Z-Image·LTX/Wan) 선택은 **⚙ 설정 팝업의 「워크플로」 드롭다운**(이미 있던 UI)이 담당.
- **주소를 모드별로 기억**: comfy-image/comfy-video DEFAULTS 에 `localBaseUrl`(127.0.0.1:8188)·`cloudBaseUrl`
  (cloud.comfy.org) 추가. 로컬↔클라우드 전환 시 baseUrl 을 그 모드 값으로 복원 → 로컬 주소를 고쳐도 클라우드 갔다 와도 유지.
  설정 팝업의 주소칸·클라우드 체크박스도 이 필드를 같이 갱신.
- **② ⚙ ComfyUI 버튼 1개**: 이미지·비디오 각각 있던 두 개(`openComfy`/`openCvid`)를 **이미지 행의 1개**로 통합 —
  이미지가 comfy 면 이미지 탭, 아니면 비디오 탭으로 설정 팝업을 연다(둘 중 하나라도 comfy 면 표시).
- **③ 채널 순서 변경**: `preset-store.reorder(idsInOrder)` 가 **이미 있었으나 IPC 가 없었음** → IPC `reorder-presets({names})`
  신설(이름→id 매핑, 목록에 없는 채널은 뒤에 보존). 헤더 채널 ⚙ 옆 **「↕」 버튼** → ▲▼ 로 순서 조정 후 💾저장.
  ⚠ 드롭다운은 group 별로 다시 묶어 표시하므로, **그룹 순서는 그 그룹 첫 채널의 위치**로 정해짐(그룹 내 순서는 그대로 반영).
- **④ 대본 다중 열기**: `open-script` 에 `multiSelections` + 파일 루프. OS 선택 순서가 제각각이라 **파일명 자연정렬**
  (`localeCompare(ko,{numeric:true})`)로 큐 순서를 예측 가능하게. 파일별 모드 자동판별·outRoot 계산·addItem 그대로 재사용,
  **한 파일 실패해도 나머지 계속**(격리). 마지막 파일이 활성. 2개 이상이면 로그로 안내(`⚡ 만들기` 로 큐 순서 제작).
- 함께 발행(작업트리에 미커밋으로 남아 있던 UI 손질, v0.2.74 로 표시돼 있던 분): **미리보기 닫기(✕) 버튼을 이미지 우상단에
  원형 반투명 버튼으로**(`#previewBody` position:relative + `.close` absolute) · **이름입력 모달 z-index 100**
  (`.modal-bg.name-ask-layer`) — 다른 모달 위에서도 이름 입력이 보이게.
- ⚠ 앱 재시작 반영(라이트). deps 무변경.

## 🐞 추출한 mp3 제목이 스마트폰에서 `?????` → ID3v2.3 으로 저장 (2026-07-28, v0.2.73)
> 증상: 🎵 mp3 로 추출해 스마트폰(삼성 뮤직)으로 옮기니 곡 제목이 `*NEW* ?????? ????????? 3 | …` 로 깨짐
>   (아티스트는 "알 수 없는"). 한글이 전부 `?` 로.
- **원인(실측 확정)**: ffmpeg mp3 muxer 기본값이 **ID3v2.4 + UTF-8**(인코딩바이트 3). 안드로이드/삼성 뮤직 등 상당수
  플레이어가 v2.4 UTF-8 텍스트를 못 읽어 한글을 `?` 로 표시. 원본 영상(유튜브 등)의 title/artist 태그가 mp3 로
  그대로 복사되므로 **그 제목이 깨져 보인 것**(파일명 문제 아님 — 플레이어가 태그를 읽고 있었음).
  검증: 한글 title 넣은 테스트 mp4 → 추출 → `ID3 2.4 · TIT2 인코딩=3(UTF-8)` 확인.
- **수정**: `media-utils.extractAudioMp3(video, out, opts)` 에 `opts.compatTags` 추가 → `-id3v2_version 3` 으로 저장.
  **ID3v2.3 은 비-Latin1 텍스트를 ffmpeg 가 UTF-16 으로 자동 인코딩**(인코딩바이트 1) → 플레이어 호환. 원본 제목·
  아티스트 태그는 **보존**(덮어쓰지 않음), 오디오 스트림 동일(192k). extract-mp3 IPC 가 `{compatTags:true}` 전달.
  **STT 내부 임시 추출은 무옵션 = 기존 그대로(2.4)** — 전사만 하므로 태그 무의미(무영향).
  검증: compatTags → `ID3 2.3 · 인코딩=1(UTF-16)` · 제목/아티스트 보존 · STT 경로는 2.4 유지 확인.
- ⚠ **이미 만든 mp3 는 다시 추출해야** 고쳐짐(파일에 v2.4 태그가 이미 박혀 있음). 앱 재시작 반영(라이트). deps 무변경.

## 🎵 영상 → mp3 추출 버튼 신설 (STT 와 별개) (2026-07-28, v0.2.72)
> 요청: "이전에 영상에서 mp3분리하고 STT추출하는 기능이 있었는데 사라진것같네" → 확인 결과 STT(🎧)는 그대로 있었고,
>   mp3 는 STT 내부 단계로만 존재(임시폴더 생성 후 `fs.rmSync` 삭제 — main.js:611)해서 **파일이 남지 않았던 것.**
>   "난 mp3파일이 필요한데" → 독립 버튼 신설(로이 선택).
- **IPC `extract-mp3`**(stt-transcribe 바로 뒤): 파일 다중선택(영상 mp4·mov·mkv·webm·avi·m4v·ts·mpg·wmv + 음성 wav·m4a·
  flac·ogg·aac·wma) → `media-utils.extractAudioMp3`(**기존 함수 재사용**, libmp3lame 192k) → **원본과 같은 폴더에 같은 이름
  `.mp3`**(STT 가 .txt 만드는 것과 동일 규약). 끝나면 결과 폴더 `shell.openPath`.
  **덮어쓰기 안 함** — 같은 이름이 있으면 `" (2)"`, `" (3)"` … 로 피함(기존 파일 보호). 입력이 이미 .mp3 면 건너뜀.
  S.abort 로 중단 가능, 파일별 성공/실패 격리(한 파일 실패해도 다음 진행), 저장 크기(MB) 로그.
- **UI**: 헤더 「대본」 그룹의 🎧 STT 옆에 **「🎵 mp3」** 버튼(App.jsx `runExtractMp3`). preload `extractMp3`.
- **STT 와 완전 분리 — Whisper/OmniVoice 서버 불필요**(ffmpeg 만 사용) → 서버 꺼져 있어도 mp3 추출은 동작.
  기존 🎧 STT 는 무변경(임시 mp3 생성·삭제 그대로).
- ⚠ 앱 재시작 반영(라이트). deps 무변경.

## 🧹 RunPod 전면 제거 + 설정 통합 팝업(⚙ 설정) (2026-07-24, v0.2.71)
> 요청(로이): 런팟 안 씀 → 관련 기능 전부 제거. + 흩어진 API키/연결 버튼(제미나이·나노바나나·ComfyUI 이미지·비디오·
>   xAI·TTS서버)을 **한 버튼 → 통합 팝업**으로.
- **RunPod 제거**: `core/runpod.js` 삭제. main.js 의 `_runpodTarget`·`doStartPod/doStopPod`·`autoStartPodIfNeeded/
  autoStopPodIfNeeded`·IPC(get/set-runpod-key·config, runpod-start/stop/status) 제거 + make-all·run-batch 의 파드
  자동 시작/정지 훅 제거(try/finally 도 정리). preload 의 runpod API 7개 제거. App.jsx 의 rp 상태·openRunpod·rpStart/
  Stop/Check·⚡RunPod 버튼·모달·runStageQueue 의 rpAuto 블록 제거. (comfy-image/video 의 servers[] '서버 프로필'
  기능은 범용이라 유지 — 예시 텍스트에서 RunPod 문구만 정리. gemini-provider 의 'RunPodComfyProvider' 주석은 옛
  인터페이스명 언급일 뿐 기능 아님 — 유지.)
- **통합 설정 팝업**: 헤더에 단일 **「⚙ 설정」** 버튼(구 🖧TTS서버 자리) → 탭 4개 모달(`settingsOpen`·`settingsTab`):
  ① 🖼 ComfyUI 이미지 ② 🎬 ComfyUI 비디오 ③ 🔑 API 키(제미나이/나노바나나 키·모델 + Grok/xAI 키) ④ 🖧 TTS 서버.
  `openSettings(tab)` 이 5개 설정(comfy image/video·gemini key/cfg·xai key·tts servers)을 한 번에 로드. 기존 단독
  모달(comfyOpen/cvidOpen/ttsSrvOpen/xaiOpen) JSX 제거 — 본문은 탭 패널로 이관(로직·저장 함수 그대로 재사용).
  분산됐던 진입점 재배선: 이미지/비디오 드롭다운 옆 ⚙(openComfy/openCvid)→해당 탭, Grok API ⚙키→키 탭, 🖧→TTS 탭.
  이미지 순환(⚙, openImgRotation)은 순서·계정 전용으로 남기고 **거기 있던 Gemini 키 블록은 키 탭으로 이동**(중복 제거).
- ⚠ 앱 재시작 반영(라이트). deps 무변경. RunPod 홈 설정파일(`~/.priming-maker/runpod-config.json`)은 코드가 더는
  안 읽음(방치 무해). comfy 서버 프로필에 옛 RunPod 항목이 있으면 드롭다운에 남지만 선택 안 하면 무영향.

## ⚡ 「이미지+비디오」 큐 처리 = 전 항목 이미지 → 전 항목 비디오 (콜드스타트 최소화) (2026-07-24, v0.2.70)
> 지적(로이): 롱폼 작업큐 여러 개일 때 상단 🖼→🎬 를 누르면 항목마다 이미지→비디오→다음항목 이미지→비디오… 라
>   ComfyUI 가 이미지↔비디오 모델을 번갈아 로드(콜드스타트). 전 항목 이미지 먼저·그다음 전 항목 비디오가 스왑 최소.
- **원인**: `runStageQueue('imgvid')`(App.jsx)가 items 루프 안에서 한 항목의 image+video 를 함께 처리 → 항목마다
  ComfyUI 모델 스왑(이미지 단계는 `freeMemory()`로 비디오 모델 언로드까지) → 모델 재로딩 ~2×N 번.
- **수정**: imgvid 를 **2패스**로 — `phases = stage==='imgvid' ? ['image','video'] : [stage]`, 바깥 phase 루프 ×
  안쪽 items 루프. 즉 **전 항목 이미지 배치 → 전 항목 비디오 배치**. 모델 스왑이 사실상 1번(이미지→비디오)으로 감소.
  단일 stage(image/video/tts)는 phases=[stage] 라 기존과 동일(1패스). RunPod 파드 start/stop 은 바깥 finally 그대로 1회씩.
  진행 상태표시는 패스별 라벨(이미지 k/N → 비디오 k/N). 대본 위 단건 버튼(runImgVid)은 그 대본만이라 무변경.
- ⚠ 앱 재시작 반영(라이트). deps 무변경.

## 🧩 ComfyUI 워크플로(이미지·비디오) 번들 자동 등록 — PC 무관 정합 (2026-07-24, v0.2.69)
> 증상: 아내 PC(깃허브 설치본)는 버전(코드)은 업데이트되는데 비디오 도구 드롭다운에 **LTX 가 아니라 Wan** 만 뜸.
- **원인(코드 확정)**: 워크플로 목록(`workflows[]`)·활성(`workflowPath`)은 **홈 설정파일**(`~/.priming-maker/comfy-video-config.json`,
  `comfy-image-config.json`)에만 저장 — 라이트 업데이트는 설치폴더 코드만 교체하고 홈 설정은 안 건드림. 등록은 예전에 **내 PC
  홈파일 수동 기록**("초기값 직접 기록")으로만 했고 경로가 **내 PC 절대경로(`D:\Priming\comfy\...`)**라 아내 PC 엔 전달 안 됨.
  코드엔 워크플로 자동 등록 로직이 아예 없었음(DEFAULTS.workflows=[]). 반면 워크플로 JSON 파일(`comfy/*.json`)은 git-tracked
  라 설치·업데이트로 모든 PC 에 내려가 있었음 — **파일은 있는데 "등록"만 안 된 상태.**
- **수정(B안 = 번들은 항상 보장, 로이 결정)**: `comfy-video.js`·`comfy-image.js` 에 `_ensureBundled(cfg)` 추가 →
  `loadConfig()` 가 매번 실행. **설치폴더 기준**(`COMFY_DIR = path.join(__dirname,'..','comfy')` — asar:false 라 dev·설치본 양쪽
  정확) 으로 번들 워크플로(비디오=Wan2.2 5B·LTX2.3 / 이미지=Z-Image·Krea2)를 목록에 보장하고, **파일명(basename) 일치하는 기존
  항목은 제거 후 재구성**(절대경로 표류·중복 정리). 사용자 커스텀 워크플로는 보존. **활성 경로 복구**: basename 이 번들과
  일치하면 설치폴더 경로로 교정(사용자 선택 존중), 비었거나 실재하지 않으면(타 PC 절대경로 등) 기본(비디오 LTX2.3·이미지
  Z-Image)로. cloud/apiKey/서버프로필 등 나머지 설정은 그대로 보존.
- **효과**: 이제 **업데이트만으로 모든 PC 가 동일하게** Wan·LTX(및 Z-Image·Krea2)를 드롭다운에 갖게 됨. 아내 PC 는 기존 활성
  (Wan)이 유지되되 옛 절대경로가 자기 설치폴더 경로로 복구되고 **LTX 도 선택 가능**. 새 설치는 기본 LTX. 내 PC 도 D:\Priming
  경로 자동 정합(폴더 옮겨도 안 깨짐). loadConfig 는 in-memory 시드(파일 강제 재기록 안 함) — 다음 saveConfig 때 자연 영속.
- 검증: dev PC(Wan·LTX·활성 LTX) / 아내PC 시뮬(옛 Wan 절대경로 1개 → 목록 2개·중복0·활성 Wan 복구·exists·cloud/apiKey 보존) /
  새설치(빈 설정→LTX 기본) / 이미지(Z-Image·Krea2) 통과. ⚠ 앱 재시작 반영(라이트). deps 무변경.

## 🔼 상단 버튼 = 큐 전체 + 「이미지→비디오」 통합 버튼 (A안 ①②) (2026-07-23, v0.2.68)
> 요청: 상단 이미지·비디오·TTS 버튼은 작업큐의 모든 대본을 관할(대본 위 버튼은 그 대본만). + 이미지 먼저 다 만들고
>   비디오 만드는 통합 버튼을 상단·대본위 양쪽에 추가.
- **안정 최우선 = 기존 단건 핸들러(image-build/video-build/tts-build) 무변경, 렌더러 큐 루프로 재사용.**
  `runStageQueue(stage)`(App.jsx): 현재 모드 큐의 items 를 순회하며 각 항목 `selectQueueItem` → 해당 stage IPC 호출 →
  끝나면 원래 보던 대본으로 복원. stage='tts'|'image'|'video'|'imgvid'.
- **상단 버튼 재배선**: 🎤 TTS→runStageQueue('tts'), 🖼 이미지→('image'), 🎬 비디오→('video'), 신규 **🖼→🎬 이미지+비디오**
  →('imgvid', 이미지 전부→비디오). 큐 1개면 그 1개만(기존과 동일 동작).
- **대본 위 버튼**: 그대로 그 대본만(onImg/onVid). 신규 **🖼→🎬**(runImgVid) = 그 대본 이미지 전부→비디오.
- **파드 반자동 연동**: runStageQueue 가 autoManage ON + GPU 단계(image/video/imgvid)면 루프 전 runpodStart 1회 · 후 runpodStop 1회
  (TTS 는 OmniVoice 라 파드 무관 → 안 건드림). 헤더 이미지·비디오 도구가 큐 전 대본에 공통 적용(v0.2.61·v0.2.67 정합).
- ⚠ 현재 모드 큐만 관할(롱폼 탭이면 롱폼 큐). 교차 모드는 「만들기」가 담당. 앱 재시작 반영, deps 무변경.
- A안 ①②③ 완료. 남은 것: ③번(RunPod 작업을 TTS 와 분리해 연속 처리 — 큰 개조)은 별도 승인 후.

## 🎛 채널편집에 이미지·비디오 제작 도구 기본값 (③ of A안) (2026-07-23, v0.2.67)
> 요청: 채널편집 팝업에서 스타일 항목처럼 이미지·비디오 제작 도구를 골라 저장 → 그 채널 선택 시 헤더 기본값으로.
- **preset 에 `imgEngine`·`videoEngine` 필드 추가**(채널 단위, per-mode 아님). save-preset 이 patch 를 generic 병합(preset-store.update
  `{...old,...patch}`)이라 IPC 무변경. get-preset-detail 이 전체 반환 → 복원됨.
- **채널편집 모달**: 「🖼 이미지 도구 · 🎬 비디오 도구」 섹션(스타일 옆) — 이미지(순환/유료/ComfyUI)·비디오(ComfyUI/Grok/Grok
  API/없음) select. saveChannel patch 에 imgEngine/videoEngine, openChannelEditor setCh 에 기본값(rotate/grok).
- **채널 선택 시 헤더 세팅**: 프리셋→상태 effect(restoringItemRef 가드 안)에서 `p.imgEngine`/`p.videoEngine` 있으면
  setImgEngine/setVideoEngine(레거시 정규화). styleId 와 동일 패턴 — 항목 복원 중엔 항목값 우선(안 덮음).
- 구체 워크플로(Krea2/LTX)·서버(RunPod/comfy.org)는 헤더 ⚙ ComfyUI 설정을 따름(채널은 "어느 도구"만). ⚠ 앱 재시작 반영.
- 남은 것(A안): ①상단 버튼 큐 전체 + ②「이미지→비디오」 통합버튼(상단·대본위) — 다음 착수.

## ⚡ RunPod 파드 반자동 켜기/끄기 (과금 절감, 안정 우선 격리 구현) (2026-07-23, v0.2.66)
> 요청: 내가 쓸 때만 파드 켜고 끝나면 꺼서 시간·비용 낭비 막기. (A안: ①②먼저 → ③나중. 안정 최우선.)
- **`core/runpod.js` 신설**(격리): RunPod GraphQL(`api.runpod.io/graphql`)로 podResume/podStop/pod status. 키=secret-store
  'runpod'. config(`~/.priming-maker/runpod-config.json`) `autoManage(기본 false)`·podId·gpuCount·readyTimeoutSec·stopOnAbort.
  **podId 는 활성 ComfyUI 주소(`<podId>-8188.proxy.runpod.net`)에서 자동 추출.** waitComfyReady=주소 200 폴링(콜드스타트 대기).
- **main.js**: `autoStartPodIfNeeded()`/`autoStopPodIfNeeded()` — autoManage ON + 활성 comfy 서버가 RunPod 일 때만. make-all·
  run-batch 진입 시 자동 켜고(ComfyUI 뜰 때까지 대기, 실패면 제작 중단), 끝나면 finally 로 자동 정지. 수동 doStartPod/doStopPod +
  IPC(get/set-runpod-key·config, runpod-start/stop/status). **autoManage 기본 OFF → 켜기 전엔 기존 동작 완전 무영향(안전).**
- **App.jsx**: 헤더 「⚡ RunPod」 버튼 → 모달(API 키·반자동 토글·중단시끄기·podId(선택) + ▶켜기/⏹끄기/상태). 콜드스타트 경고 문구.
- ⚠ **RunPod GraphQL 스키마 실측 필요**: 키 넣고 ▶켜기/상태 1회 확인(응답 로그로 남김). podResume/podStop input 이 다르면 쿼리만
  보정. GPU 재고 없으면 켜기 실패로 표시. ⚠ 콜드스타트(부팅+모델로딩 ~1~3분)는 stop/start 의 본질적 대가 — 곧 재사용이면 켜둠 권장.
- 남은 것: ①틈 제거(큐 한꺼번에), ③RunPod 작업을 TTS와 분리해 연속 처리(대본 안 섞이게 항목별 상태 분리 선행). 둘 다 미착수.

## 🐞 업스케일 중 영상 썸네일이 하나씩 깜빡이며 사라짐 → 경로 교체 끝에 일괄 (2026-07-23, v0.2.65)
> 증상: 업스케일 단계에서 "하나 끝나면 하나 사라지고 계속 반복" — 그룹 영상 썸네일이 순차로 깜빡(빈 화면→재등장).
- **원인**: `maybeUpscale` 이 그룹마다 업스케일 직후 `g.videoPath = NN_1080.mp4` 로 교체 + pushDtoUpdate → 그 그룹
  `<video src>` 가 바뀌어 브라우저가 영상을 리로드(잠깐 빈 화면). 순차 처리라 그룹 수만큼 하나씩 깜빡임 반복.
  (`media()` 는 캐시버스터 없음 — 리로드는 오직 경로가 바뀔 때만 발생.)
- **수정**: 루프 중엔 **videoStatus(오버레이)만** 갱신(src 무변경 → 리로드 없음). 경로 교체(`NN.mp4`→`NN_1080.mp4`)는
  `swaps[]` 에 모아뒀다가 **모든 업스케일이 끝난 뒤 한 번에 반영 + pushDtoUpdate 1회**. → 순차 깜빡임 제거(있어도 끝에 1회).
- ⚠ 앱 재시작 반영(라이트). deps 무변경.

## 🔀 ComfyUI 서버 프로필(comfy.org ↔ RunPod) 드롭다운 전환 (2026-07-23, v0.2.64)
> 요청: comfy.org 클라우드와 RunPod(자가 GPU)를 골라 쓰며 몇 번 써보고 최종 결정하겠다. 지금 상태에서 comfy.org도
>   쓸 수 있게. (배경: RunPod RTX PRO 4500 이 comfy.org 대비 느림 — 이미지 ~30초/장, i2v 8분+. comfy.org 는 이미지 ~5초.)
- **comfy-image/comfy-video config 에 `servers[]`·`activeServer` 추가**(DEFAULTS). 엔진은 무변경 — 여전히 baseUrl/cloud/apiKey
  를 읽음. servers 는 저장된 프로필 목록 `[{name,baseUrl,cloud,apiKey}]`, 선택 시 그 값을 live 필드로 복사(saveCfg).
- **⚙ ComfyUI 이미지/비디오 모달에 「서버」 드롭다운** 추가(워크플로 드롭다운과 동일 패턴): 프로필 선택→주소·클라우드·키 한 번에
  전환, 💾저장(현재 설정을 이름 붙여 프로필로), 🗑삭제. App.jsx pickComfyServer/saveComfyServer/removeComfyServer(+Cvid 짝).
- **홈 config 두 개에 프로필 2개 선주입**: RunPod(cloud:false, `02kqro91c1m3q7-8188.proxy.runpod.net`) · comfy.org(cloud:true,
  cloud.comfy.org + apiKey). activeServer=RunPod. → 앱에서 드롭다운만 바꿔 comfy.org↔RunPod 즉시 전환.
- ⚠ 앱 재시작 반영(라이트). deps 무변경. RunPod URL 은 파드별 — 파드 바뀌면 프로필 주소 갱신 필요.

## 🎬 LTX2.3 i2v 실사화 → Prompt Enhance(Gemma 증강) 자동 OFF (2026-07-22, v0.2.63)
> 증상: 이미지는 수채화로 잘 나오는데 i2v 영상은 사진처럼 실사화됨. 사용자 비디오 프롬프트엔 "실사" 단어 없음.
- **원인 2가지**: ① 비디오 프롬프트에 화풍 앵커(watercolor 등)가 0개 — i2v 디노이즈가 텍스처를 다시 그릴 때 붙잡을 말이
  없음. ② 워크플로 `Boolean (Enable Prompt Enhance)=true`(노드 320:328) → LTX 가 Gemma(LLM)로 프롬프트를 영화적으로
  재작성하며 realism 어휘를 얹음 → 화풍이 사진처럼 변질.
- **수정(방법 A, comfy-video.js `_buildGraph`)**: 그래프를 comfy 로 보내기 직전, `_meta.title` 이 "prompt enhance"류인
  PrimitiveBoolean 노드를 찾아 **value=true → false 강제**(우리 프롬프트 그대로 사용). UI 토글이 아니라 **전송 시 제어** —
  워크플로 파일 수정 불필요. 실제 워크플로(320:328)로 감지 검증 통과. deps 무변경, 앱 재시작 반영(라이트).
- ⚠ 이 블록은 작업트리에 미커밋 상태로 남아 있던 것(v0.2.56 발행 때 누락) — 이번에 커밋·발행. 남은 개선(방법 B):
  i2v 프롬프트에 이미지 스타일(수채화 앵커) 자동 앞붙이기(미적용).

## 🐞 중단 눌러도 '이미지 생성 중' 스피너 고착 + 전 그룹 동시 표시 → 정리 + 배치만 표시 (2026-07-22, v0.2.62)
> 증상: 중단 눌러도 여러 그룹 카드가 계속 `🖼 이미지 생성 중…` 스피너를 돎. + 만들 때 지금 만드는 그룹만이 아니라
>   전 그룹이 한꺼번에 '생성 중'으로 뜸(v0.2.58 은 comfy 경로만 1개씩 적용).
- **Bug 1(스피너 고착)**: 중단(`abort` IPC)이 `S.abort=true` 만 하고 그룹의 `imageStatus/videoStatus`('generating'·
  'upscaling')는 안 건드림. genspark/flow 배치는 만들다 만 그룹이 'generating' 에 남아 카드 스피너가 영원히 돎.
  - 수정: `clearGeneratingStatus()` 신설 — 전 큐(longform/shorts)·활성 파싱본의 모든 그룹을 훑어 'generating'→
    (자산 있으면 'done', 없으면 'idle'), 'upscaling'→동일 정리. abort 핸들러가 이걸 호출 + pushDtoUpdate. 어느 엔진
    경로(comfy·genspark·flow·업스케일)든 중단 즉시 스피너 해제.
- **Bug 2(전 그룹 동시 표시)**: `pipeline.js generateImagesGenspark` 가 시작 시 `idx.forEach(...imageStatus='generating')`
  로 **대상 전체(예 33장)를 한꺼번에** 표시. → 그 선표시 제거하고, **배치 루프에서 지금 제출하는 6장만** 'generating'
  표시(Genspark 는 한 번에 최대 6장이라 실제 동시 생성분 만큼만). 이전/이후 배치는 done/idle. finalize(444)가 남은
  'generating'→'fail' 로 정리하는 건 그대로. (comfy 경로는 이미 v0.2.58 로 1개씩 — 무변경.)
- ⚠ 앱 재시작 반영(라이트). deps 무변경.

## 🐞 큐 제작 시 이어받은 대본이 헤더 이미지·비디오 도구를 무시 → 헤더(큐 단위) 우선 (2026-07-22, v0.2.61)
> 증상: 헤더에서 ComfyUI Krea2 로 골랐는데, 큐의 두 번째 대본(작업본 이어받기)이 `🔄 이미지 순환: genspark→flow`
>   로 돌아감. 비디오도 같은 현상.
- **원인**: run-batch 가 `ie = (s.imgEngine!=null) ? s.imgEngine : common.imgEngine` — **항목 저장값(s) 우선, 헤더(common)
  폴백**(v0.2.46 "항목별 도구" 설계). 이어받기한 대본은 저장된 옛 imgEngine(rotate/genspark)을 들고 있어, 헤더에서
  comfy 로 바꿔도 그 값은 **그 시점 활성 항목에만** 저장되고 이어받은 항목은 옛 값으로 실행됨. videoEngine 도 동일.
- **수정(로이 결정)**: 이미지·비디오 **생성 도구는 헤더(공통=큐 단위) 우선**으로 뒤집음 — `ie = (common.imgEngine!=null)
  ? common.imgEngine : (s.imgEngine||'genspark')`, videoEngine 도 동일. "어느 서비스/GPU로 만드냐"는 큐 전체 공통
  선택이므로 헤더에서 한 번 고르면 큐 전 항목에 적용. (스타일·배속·AI고지·영상범위·채널은 그대로 항목별 유지.)
  렌더러는 이미 `common`에 헤더 imgEngine/videoEngine 을 실어 보내고 있었음(App.jsx:507 의도와 일치하게 정렬).
- ⚠ 앱 재시작 반영(라이트). deps 무변경.

## 🐞 X로 지운 이미지가 만들기 때 캐시에서 부활 + 중단인데 폴더 열림 (2026-07-22, v0.2.60)
> 증상 ① 그룹 이미지를 X로 지운 뒤 만들기 → 지웠던 그 이미지가 그대로 다시 들어옴. ② 중단 눌러도 완료된 것처럼
>   출력폴더가 열림.
- **Bug 1 원인(이미지 전용)**: `clear-asset`(X)는 `imagePath=null` 만 하고 미디어캐시는 안 지움 → `prefillImageCache`
  가 만들기 시작 시 같은 키(prompt+style+aspect+engine) 캐시를 찾아 옛 이미지를 복사해 넣음. (비디오는 `MC.get(videoKey)`
  prefill 자체가 없어 부활 안 함 — 안전.)
  - 수정: clear-asset 이미지 삭제 시 `g.imageCleared=true` + 저장해둔 `g._imgCacheKey` 로 캐시파일 `MC.del`.
    prefillImageCache 는 `imageCleared` 그룹 건너뜀. cacheGeneratedImages 는 새 이미지 캐시 시 key 저장 + 플래그 해제
    (재생성 후엔 재활용 허용). → 지운 그룹은 캐시 무시하고 새로 생성. 순서: prefill(skip)→생성→cache(플래그 해제).
- **Bug 2 원인**: `runMakeAllCore` 4단계 vrew·폴더는 이미 `if(!S.abort)` 로 중단 시 생략됨. 그러나 **run-batch(큐 제작)
  핸들러 끝의 최종 폴더열기 `shell.openPath(S.outRoot)` 가 중단 무관하게 항상 실행**됨.
  - 수정: `if (!S.abort) { shell.openPath(...) }` 가드 추가. 이제 중단 시 폴더 안 열림(vrew 는 원래 생략됨).
- ⚠ 앱 재시작 반영(라이트). deps 무변경.

## 🐞 큐 항목 전환 시 영상범위·배속·스타일·AI고지 초기화 → 항목별 값 보존 (2026-07-22, v0.2.59)
> 증상: 헤더 영상범위(vidFrom~vidTo)를 바꾸고 다른 롱폼 큐를 클릭하면 초기화됨(1번 파일 1~5, 2번 파일 1~3 불가).
- **원인 2개(둘 다 기본값 useEffect 가 applySettings 복원값을 덮어씀)**:
  ① 범위 기본값 effect(deps: dto.fileTitle·isLf·introSig·lastNum) — 항목 전환 시 dto.fileTitle 바뀌면 `setVidFrom(1)
     ·setVidTo(도입부끝/마지막)` 로 복원된 범위를 덮음.
  ② 프리셋/모드 기본값 effect(deps: presetName·mode·modeProfiles) — 채널이 다른 항목으로 전환 시 fire 되어
     `setTtsSpeed·setStyleId·setAiNotice` 를 프리셋 기본값으로 덮음(같은 채널 항목끼린 안 걸림).
- **수정(App.jsx, ref 가드)**: `hasStoredRangeRef`(항목에 저장된 범위 있으면 범위 effect skip)·`restoringItemRef`
  (복원 중이면 프리셋 effect 가 배속·스타일·AI고지 덮어쓰기 skip, 단 자막·분할은 채널값이라 무조건 적용). applySettings
  진입 시 두 ref set, **사용자가 채널 직접 선택(switchModeForChannel)·새 대본 열기(openScript)·모드 전환(switchMode)**
  때만 clear → 그때만 기본값 적용. → 항목별 범위·배속·스타일·AI고지가 큐 전환에도 유지됨. 빌드 통과.
- ⚠ 앱 재시작 반영(라이트). deps 무변경.

## 🖼 이미지 '생성 중' 스피너 — 만드는 그룹 카드에만 표시 (2026-07-22, v0.2.58)
> 요청: 배치 이미지 생성 시 "이미지 생성 중"이 안 뜨거나 엉뚱하게 떴음 → 실제로 그 순간 만드는 그룹 카드에만.
- 원인: `runComfyImages`(comfy 배치)가 그룹 생성 직전 `g.imageStatus='generating'` 을 안 켜고, 실패 시에도
  상태를 안 바꿔서 comfy 경로엔 스피너가 아예 안 떴음(빈 '+'). (UI App.jsx:2348 은 imageStatus==='generating'
  일 때만 `🖼 이미지 생성 중…` 오버레이 렌더 — regen-group 단건만 이걸 설정하고 있었음.)
- 수정(main.js runComfyImages 루프): 각 그룹 textToImage 직전 `imageStatus='generating'`+pushDtoUpdate,
  끝나면 성공 'done'/실패 'fail'+pushDtoUpdate. **순차 루프라 항상 1개 그룹만 'generating'** → 딱 그 카드에만
  스피너. 실패에도 해제해 고착 방지. (genspark/flow 는 6장 배치 병렬이라 '1개만'이 애매 — comfy 경로만 적용,
  사용자가 comfy Krea2 로 이전 중이라 실사용 경로.)
- ⚠ 앱 재시작 반영(라이트). deps 무변경.

## 🐞 TTS 동시 실행 → `TTS provider 'omnivoice' not available` 크래시 → TTS 직렬 큐 (2026-07-22, v0.2.57)
> 증상: 아내와 동시 작업(또는 한 사람이 🎤 그룹재생성 + 전체 TTS빌드 동시 클릭) 시 `tts G1 컷N`·`tts 롱폼 컷N`
>   이 번갈아 찍히다 `⚠ 컷N TTS 실패 — TTS provider 'omnivoice' not available` + 전체빌드의 `🗑 전체삭제`가
>   다른 작업 음성파일까지 지움.
- **원인(코드 확정)**: `tts/tts-manager.js` `getInstance()` 가 **모듈 싱글톤** 반환 → tts-build·tts-group·make-all·
  run-batch 가 **공용 TTS 매니저 하나를 공유**. 한 작업의 `refreshProvider`(provider stop+delete 후 재연결)·
  `mgr.stop()`(providers 맵 전체 clear)가 **동시에 도는 다른 작업의 provider 를 없애** → `synthesize` 가
  `providers.get('omnivoice')` 실패 → tts-manager.js:200 `not available` throw. (앱 내 동시 실행만 해당 —
  두 PC 는 각자 매니저라 무관, 서버 쪽은 이벤트루프 블로킹으로 이미 직렬이라 OOM 무관.)
- **수정(main.js)**: `enqueueImageJob` 패턴을 본떠 **`enqueueTtsJob(label, fn)` 직렬 큐** 신설(프로미스 체인 +
  대기 카운터). tts-build·tts-group·make-all·run-batch 네 핸들러를 전부 이 큐로 감쌈(여는 줄 `=> enqueueTtsJob(
  '…', async () => {` + 닫는 줄 `}))` — 본문 무변경). 한 번에 하나만 싱글톤을 건드림 → 충돌 0. 뒤 작업은 앞
  작업 끝나면 자동 실행(손실 0). 실패 격리(앞 작업 실패해도 다음 실행).
- **트레이드오프(수용)**: make-all/run-batch(긴 파이프라인)도 같은 큐라, 파이프라인 실행 중 클릭한 수동 🎤/빌드는
  파이프라인이 끝날 때까지 대기(`⏳ … 큐 대기` 로그로 가시화). 크래시·손실보다 훨씬 나음. 단위검증: 겹친 3작업이
  최대동시=1·A→B→C 순서·B실패에도 C실행 확인.
- ⚠ 설치본 반영은 **앱 재시작**(라이트 업데이트). deps 무변경.

## 🌡 OmniVoice 서버 GPU 온도 기록 로거 (2026-07-22)
> 요청: 동시 TTS 시 GPU 온도 급상승 우려 → 실제 온도를 기록으로 남기게. (GPU=메인 PC=OmniVoice 서버에만 있음)
- **위치**: OmniVoice 서버 `D:\Work\TTS_Engine\06_OmniVoice\api.py`(Priming 저장소 밖 — git/매니페스트 무관, **서버
  재시작 필요**). Priming 앱은 무수정. 의존성 추가 없음(nvidia-smi 서브프로세스, venv 무수정).
- **구현**: 별도 데몬 스레드가 3초마다 `nvidia-smi` 로 온도·전력·사용률·메모리·팬·클럭을 읽어 **작업 중일 때만**
  `logs/gpu_YYYYMMDD.csv` 에 기록(유휴 잡음 제외, 작업 종료 직후 1줄=쿨다운 시작점 포함). `_model.generate()` 가
  이벤트루프를 막아도 OS 스레드라 계속 측정됨. `/tts` 에 `active_tts` 카운터(inc/finally dec), `/gpu` 조회
  엔드포인트(현재값+오늘 피크). 문법·nvidia-smi 파싱·CSV 단위검증 통과.
- **실측 발견**: 부하 시 84°C·100%·150W(전력상한 이미 150)·팬 nvidia-smi상 0%. 팬 0%는 유휴 제로-RPM 모드거나
  nvidia-smi가 이 카드 RPM 미판독일 가능성 큼(부하 때 실물 팬 회전 육안확인 필요). 84°C는 RTX3060 사양(93°C)
  내 안전·스로틀 미발동. 전력상한은 이미 150이라 온도 저감 효과 없음 → 필요 시 팬 커브가 답.

## 🐞 LTX2.3 i2v — 사용자 이미지 대신 "이집트 여왕" 영상이 나옴 → 프롬프트·해상도·길이 주입 실패 (2026-07-21, v0.2.56)
> 증상: LTX2.3 비디오 생성 시 그룹 이미지가 아니라 LTX 샘플이미지(egyptian queen)가 i2v 되는 것처럼 보임.
- **실측 진단(클라우드 `/api/jobs` 로 실행된 그래프 회수 — 강력한 디버깅 수단)**: 이미지 업로드·주입은 구버전에서도
  정상이었음(실행 그래프 LoadImage=사용자 이미지 해시, `/api/view` 로 내려받아 육안 확인 = 조선 수채화).
  진짜 원인 = **프롬프트 미주입**: LTX 워크플로는 프롬프트가 PrimitiveStringMultiline("Prompt")→TextGenerateLTX2Prompt
  (Gemma 증강)→CLIPTextEncode(text=**링크**) 구조라, 구코드(CLIPTextEncode 리터럴만 주입)가 대상 못 찾고 **기본 프롬프트
  ("Egyptian royal in blue-and-gold headdress…" = 샘플이미지 묘사문)가 그대로 실행** → 2단계 i2v(2단 strength 0.7)+
  프롬프트 증강(EnablePromptEnh=true)이 영상 내용을 프롬프트 쪽으로 끌고 가 샘플이미지가 쓰인 것처럼 보임.
  해상도(1280×720 기본)·길이(5초 기본, TTS 무시)도 미주입이었음.
- **수정(comfy-video.js — 작업트리에 있던 미발행분 검증 후 발행)**: 프롬프트→문자열 Primitive("Prompt") value(네거티브
  제외), 해상도→Width/Height PrimitiveInt, 길이→Duration Primitive(초, 없으면 Length/Frames=프레임) 주입 + 업로드·큐
  진단 로그. 실워크플로 단위검증(이미지·프롬프트·1280×704·6초 주입 확인). `comfy/video_ltx2_3_i2v.json` 저장소 추가.
- **폴더명 이관**(D:\Priming-Maker→D:\Priming): 코드 폴백 경로(ace-step/qwen-design/lora-collect)·홈 설정파일
  (comfy-image-config·lora-collect·tts-presets scriptFolder)·문서(cd 경로) 갱신. GitHub 저장소명은 그대로(발행 무영향).
- ⚠ 사용자는 설치본(라이트 업데이트) 사용 — **발행 후 앱 재시작해야 반영**. 실행 그래프 확인법: `/api/jobs?limit=N` →
  `/api/jobs/{id}`.workflow.prompt (X-API-Key 헤더).

## 🐞 큐 만들기가 항목 이미지도구(comfy)를 무시하고 순환 실행 → 서버 it.settings 우선 (2026-07-19, v0.2.55)
> 증상: 이미지 도구를 ComfyUI Z-image 로 바꿨는데 「만들기(큐)」 시 Genspark→Flow(순환)로 만듦. workspace.json 의
>   큐 항목들은 모두 `imgEngine:"comfy"` 였음(서버 저장은 정상).
- **원인**: `set-queue-settings`(디바운스)는 서버 `it.settings` 만 갱신하고 **렌더러로 DTO 를 안 되돌림** →
  렌더러 `queue.items[].settings` 가 stale. run-batch 가 렌더러 plan 의 `entry.settings`(=stale, imgEngine 옛값)를
  써서 순환 실행. 서버 it.settings(comfy)는 무시됨.
- **수정**: run-batch 가 `const s = { ...entry.settings, ...it.settings }` — **서버쪽 it.settings(진짜 최신) 우선**.
  imgEngine 뿐 아니라 videoEngine·presetName·배속·스타일 등 항목별 설정 전부 최신값으로 실행됨.
- make-all(단건)은 헤더 state 를 직접 넘겨 무관(스테일 없음). 큐 경로만 영향.

## ⬆ 업스케일 중인 그룹 시각 표시 (2026-07-18, v0.2.54)
> 요청: 업스케일 시 어느 그룹 영상을 처리 중인지 화면에 보이게.
- maybeUpscale(main.js): 각 그룹 업스케일 전 `g.videoStatus='upscaling'`+pushDtoUpdate, 끝나면 'done'+pushDtoUpdate.
  로그도 `⬆ [i/N] G{num} 영상 업스케일 → WxH…` + 종료 `⬆ 업스케일 완료 (n/N)`.
- toDTO videoStatus 에 'upscaling' 추가(주석). App.jsx Thumb: videoPath 있고 videoStatus==='upscaling' 이면
  영상 썸네일 위에 genOv('⬆ 업스케일 중…') 스피너 오버레이(이미지 'generating' 오버레이와 동일 패턴).

## 🐞 서버 주소 오타(포트 중복·스킴 누락) → 자동 보정 (2026-07-18, v0.2.53)
> 증상: 로컬 GPU 이미지 `X ComfyUI 연결 실패 (127.0.0.1:8188:8188)` — 주소칸에 포트가 두 번 + http:// 누락.
>   ComfyUI 자체는 127.0.0.1:8188 정상 리스닝 중이었음(주소 오타가 유일한 원인).
- comfy-image/comfy-video 생성자 + set-tts-server: `trim` → 스킴 없으면 `http://` 자동 추가 →
  `(:\d+)(?::\d+)+$` 로 **꼬리 포트 중복 오타 접기**(`host:8188:8188`→`host:8188`). 실측 5케이스 통과.
- 사용자 설정파일(comfy-image-config.json)의 오타 값도 직접 교정. ⚠ PowerShell 로 JS 원라이너 테스트 시
  쌍따옴표 안 `$1` 이 PS 변수로 삼켜짐 — replace 콜백 검증은 bash 로.

## ⏸ 한도 배지 통계줄 앞 이동 + Grok 수동 버튼 쿨다운 스킵 (2026-07-18, v0.2.52)
> 요청: Genspark "7월 18일 오전 11:08에 재설정" 팝업 시 그 시각을 **capbar(분할·AI고지·⏱통계 줄) 맨 앞**에
>   "젠스파크 이미지 생성가능시간: 7월 18일 오전 11:08" 로 표시 + 순환이면 Genspark 건너뛰고 Flow 직행. Grok 도 동일.
- **이미 있던 것(확인)**: 한도 메시지 파싱→계정 쿨다운 기록(v0.2.19)·순환의 Genspark 무접속 건너뜀→Flow 직행,
  Grok 쿨다운 기록+make-all 3단계 생략(v0.2.30). 이번 팝업도 정상 기록돼 있었음(cooldowns.default=7/18 11:08).
- **바꾼 것**: ① 헤더 구석의 gsCool/grokCool 배지를 **capbar 맨 앞**으로 이동, 문구 "🖼 젠스파크 이미지 생성가능시간:
  7월 18일 오전 11:08"(fmtKoTime — 12시간제 한국어) / "🎬 Grok 비디오 생성가능시간: …". 60초 폴링·재시작 유지 그대로.
  ② **video-build/video-group**(수동 🎬 버튼)에도 grok/grok10 이면 grokCoolUntil() 스킵 가드 추가(브라우저 안 띄움) —
  기존엔 make-all 파이프라인에만 있었음. grok-api/comfy 는 쿨다운 무관(가드 제외).

## 🐞 ComfyUI 클라우드 체크 + LAN IP 혼합 → fetch failed → 클라우드면 항상 comfy.org (2026-07-17, v0.2.51)
> 증상: 아내 PC 이미지 `fetch failed` 연발. 로그 "클라우드(Krea2) … http://100.112.7.63:8188" = 클라우드 체크 켠 채
>   주소를 LAN(Tailscale) IP로 둠. 클라우드 방식(/api·X-API-Key)을 로컬 IP로 보내 실패. health()는 클라우드면
>   apiKey만 확인(실접속 X)이라 "연결 OK" 오탐.
- comfy-image/comfy-video 생성자: `cloud && localhost`일 때만 치환 → **`cloud && !cloud.comfy.org`면 항상 치환**.
  클라우드 체크 시 주소칸(로컬/LAN IP)과 무관하게 cloud.comfy.org 로. 혼합 오설정 원천 차단.
- 선택: **클라우드**=주소 무관·comfy.org(간단, 메인 PC 불필요) / **로컬 via Tailscale**=클라우드 해제+주소
  100.x:8188+메인 PC ComfyUI `--listen`+방화벽 8188(현재 미가동·규칙 없음).

## 🐞 워크플로 추가 시 `prompt() is not supported` — window.prompt → 이름 모달 (2026-07-16, v0.2.50)
> 증상: 아내 PC ⚙ ComfyUI ＋추가 시 `워크플로 추가 오류: prompt() is not supported`. (메인 PC는 설정파일 직접
>   기록이라 안 겪음.) 원인: pickComfyWf/pickCvidWf 가 `window.prompt()` 사용 — 일부 Electron 렌더러에서 미지원/예외.
- 프로미스 기반 **이름 입력 모달**(`askName`/`nameAsk`) 추가로 window.prompt 2곳 대체. Enter=확인/Esc=취소.
- 이 로그로 확인된 정상 동작: OmniVoice **Tailscale 연결됨**(tts 컷 생성) + ComfyUI **클라우드 연결 OK**.

## 🖧 TTS 서버 주소 설정 UI 추가 (다른 PC=Tailscale/LAN) (2026-07-16, v0.2.49)
> 배경: 아내 PC를 Tailscale 씬클라이언트로. OmniVoice 주소를 앱에서 바꿀 UI가 없어(설정파일만) 추가.
- IPC get/set/test-tts-server (tts-config setProvider). preload getTtsServers/setTtsServer/testTtsServer.
- 헤더 「🖧 TTS서버」 버튼 → 모달(OmniVoice·Supertonic baseUrl + 연결테스트). 이 PC에만 저장(~/.flow-app/tts-config.json).
- OmniVoice 는 방화벽 Private 규칙이 **Tailscale 인터페이스(Private)**까지 커버 → Tailscale IP(100.x)로 이미 접속됨.
  ComfyUI 는 클라우드가 다PC에서 더 간단(메인 8188 미가동). Voice Design(qwen-design)은 로컬 spawn 전용 — 원격은 추후 코드.
- ⚠ 다른 PC TTS 실패 주원인: OmniVoice 주소가 그 PC에서 안 닿는 IP. Public 프로필이면 방화벽 차단(Private로 바꾸거나 Tailscale).

## 🐞 TTS OmniVoice 콜드스타트(모델 재로딩) 연결 실패 → 재시도 창 확대 (2026-07-16, v0.2.48)
> 증상: 아내 PC에서 `omnivoice 미가동`. 그러나 브라우저로 `192.168.219.157:9881/health` = `{"status":"ok"}` (닿음).
- **원인**: 네트워크·방화벽 아님. OmniVoice 서버가 유휴 후 **모델 재로딩 중**(로딩 시 /health 503)이라 앱의 재시도 창
  (v0.2.41: 3회×5초 + 3초 타임아웃 ≈ 24초)을 넘김. 로딩 완료 후엔 200(그래서 브라우저는 정상).
- **수정**: makeTtsManager 재시도 **3→6회, 5→6초**(≈36초+타임아웃)로 콜드스타트(15~40초) 견딤. omnivoice init
  헬스 타임아웃 3→5초. 서버가 진짜 꺼져 있으면 연결거부는 즉시 실패라 최악 대기만 늘 뿐(수용).

## 🖼 Genspark 침묵 정체(무응답) 감지 → 추정 쿨다운·조기 Flow 전환 (2026-07-16, v0.2.47)
> 증상: Genspark가 한도 메시지 없이 조용히 정체(제출은 되는데 0장, 75초 정체 반복)해도 다음 이미지 가능시간이
>   표시 안 됨. 기존 표시는 명시적 "…재설정됩니다" 메시지 파싱에만 의존했는데, 이 경우 메시지가 안 뜸.
- pipeline `generateImagesGenspark`: 단건 재생성이 **연속 3장 무응답(비차단·비한도 실패)**이면 `limitReached='__STALL__'`
  세팅 후 조기 종료(배치 성공·단건 성공 시 카운트 리셋). → 남은 배치·단건 헛시도(장당 ~75초) 안 함.
- main run-batch genspark 분기: `'__STALL__'` 이면 **30분 추정 쿨다운** setCooldown + "침묵 정체(추정, 메시지 없음)" 로그
  → 순환이 즉시 다음 엔진(Flow)으로. genspark-cooldown 배지에 추정 시각 표시(앱 재시작해도 유지).
- 명시적 한도 메시지가 뜨는 경우는 기존대로 정확한 재설정 시각 파싱(무변경). ⚠ 추정이라 과부하로 더 빨리 풀리면 30분 뒤 재시도.

## 🐞 큐 항목 전환 시 이미지·비디오 엔진 초기화 → 항목별 도구 보존 (2026-07-16, v0.2.46)
> 증상: 다중 큐에서 "이 작업엔 이 도구, 저 작업엔 저 도구"를 지정해도 항목 이동 사이 엔진이 초기화됨.
- **원인 ①(UI 리셋)**: App.jsx applySettings 가 `imgEngine==='comfy'` 를 **`rotate` 로 강제 마이그레이션**(구 comfy 이미지
  제거 시절 잔재). comfy 이미지(z-image/Krea2) 부활했는데 이게 남아 있어, 항목 전환마다 comfy→순환으로 되돌리고
  디바운스 저장이 그 값을 **항목에 영구 덮어씀**(진짜 초기화). → 마이그레이션 제거(레거시 genspark/flow 단독만 rotate 로).
- **원인 ②(런타임 무시)**: run-batch 가 v0.2.24에서 **헤더(common) 엔진을 전 항목에 강제** 적용 → 항목별 도구가 실행 때
  무시됨. → **항목별 값(s.videoEngine/s.imgEngine) 우선, 헤더는 폴백**으로 변경(제거된 flow/wan/grok10 만 grok 보정).
- 이제 각 큐 항목이 자기 이미지·비디오 도구를 기억하고 만들기 때도 그대로 사용. 헤더 드롭다운은 **활성 항목의 도구를 편집**
  (디바운스로 그 항목에 저장). 모든 항목을 한 번에 바꾸는 건 각 항목 선택해 변경(추후 '전체 적용' 옵션 여지).

## 🐞 화면 검색(Ctrl+F) 프리징 — 타이핑마다 findInPage → 디바운스 (2026-07-15, v0.2.45)
> 증상: 검색 기능 쓰면 프리징. 원인: 검색 input onChange 가 **글자마다** `find-in-page` 호출.
- Electron `findInPage` 는 호출당 전체 DOM 스캔+강조인데, 화면이 대본 수십 컷+영상 수십 개라 DOM 이 거대 →
  타이핑 한 글자마다 풀스캔이 연달아 큐잉돼 렌더러가 밀려 멈춤.
- **수정**: `runFind` 에 디바운스 — 타이핑(findNext=false)은 280ms 멈춘 뒤 1회만, Enter/화살표(findNext=true)는 즉시.
  `findTimerRef` 로 이전 타이머 취소. closeFind 도 타이머 정리. (검색어 지우면 즉시 stopFindInPage)

## 🗂 채널 드롭다운 그룹 구분선 (2026-07-15, v0.2.44)
> 요청: 채널이 많아질 예정 → 구분자(-----)로 구분. 결정: **그룹 이름으로 자동 구분**(사용자 선택).
- preset 에 `group` 필드 추가. listPresets·save-preset 반환에 group 포함. ⚙ 채널편집 모달에 **「그룹(구분)」 입력란**
  (datalist 로 기존 그룹 자동완성) → saveChannel patch 에 group.
- 헤더 채널 드롭다운: group 별로 묶고 그룹마다 `──── 그룹명 ────` **disabled option 구분선**(선택 불가) + 그룹 내
  채널은 앞에 전각공백 들여쓰기. 그룹 없는 채널은 맨 위에. 빈 그룹이면 구분 없이 그대로.

## ✏ 채널(프리셋) 이름 변경 기능 추가 (2026-07-15, v0.2.43)
> 요청: 채널명을 수정할 방법이 없었음(추가·삭제만 있었음).
- IPC `rename-preset({oldName,newName})` — preset id 유지, name 만 교체. 중복·빈이름 방어. 현재 세션 큐 항목
  `settings.presetName`·활성 `S.preset.name` 도 옛→새 이름으로 갱신(참조 깨짐 방지). preload `renamePreset`.
- ⚙ 채널편집 모달 상단에 **채널 이름 입력란** 추가. `saveChannel` 이 origName(`ch._raw.name`)과 다르면
  renamePreset 먼저 호출 후 새 이름으로 savePreset. 저장 시 presetName state 도 새 이름으로.

## 🐞 큐 만들기 시 완료(done) 항목 재작업·vrew 재열림 → 완료 건너뜀 (2026-07-15, v0.2.42)
> 증상: 완료(86·88)·실패(87)·진행중(89)·대기(90) 섞인 큐에서 「만들기」 → 완료된 86·88도 다시 만들고
>   그 .vrew 를 또 엶. 게다가 완료된 60컷+영상 대본으로 활성 전환·재렌더·vrew 재열기까지 겹쳐 앱이 잠깐 멈춤.
- **원인**: run-batch(main)·plan 빌드(App.jsx)가 큐의 **모든** 항목을 status 무시하고 재실행.
- **수정**: ① App.jsx plan 빌드에서 `status==='done'` 항목 제외(정확한 개수·미전송) ② main run-batch 에서도
  `it.status==='done'` 이면 활성전환/실행/ vrew 재열기 없이 스킵(`⏭ 이미 완료 — 건너뜀`, 안전망). 종료 로그에 `완료건너뜀 N`.
  → 완료 대본(60컷+영상) 재렌더·vrew 재열기가 사라져 만들기 시 멈춤도 크게 완화. 모두 완료면 "만들 대본이 없습니다".
- done 은 워크스페이스에 유지(재시작해도 완료 스킵). 진짜 다시 만들려면 그 큐를 지우고 다시 열기.

## 🐞 큐에서 TTS 서버 순간 미응답 = 대본 통째 스킵 → 재시도 추가 (2026-07-15, v0.2.41)
> 증상: 5개 큐 중 87회만 실패 — `TTS 엔진 'omnivoice' 미가동`. 직후 88·89회는 정상 연결.
- **원인**: `makeTtsManager`(pipeline.js)가 대본 시작마다 `refreshProvider`→OmniVoice `/health`(3초 타임아웃) **1회**만 확인.
  직전 86회의 대형 작업(TTS 983초+비디오 2418초, 중단) 직후 LAN OmniVoice(192.168.219.157:9881) 서버가 부하 회복/모델
  재로딩 중이라 그 **3초 창**을 놓침 → 재시도 없이 그 대본 실패 처리(`다음 대본 계속`). 일시적(88·89회 즉시 정상)이었음.
- **수정**: `makeTtsManager(logger, engine, opts)` 에 재시도 추가 — 실패 시 5초 간격 **3회** 재연결 후에만 미가동 판정.
  모든 호출 경로(make-all·run-batch·tts-build·tts-group) 공통 적용. gemini(키기반)는 즉시 성공/실패라 영향 미미.
  interactive 단건에서 서버가 진짜 꺼져 있으면 ~18초 대기 후 실패(수용 가능), 배치 무인 실행은 blip 견딤.

## 🐞 일괄첨부 시 앱 멈춤 — media:// 프로토콜 동기 파일읽기 → 스트리밍 (2026-07-15, v0.2.40)
> 증상: 대본 선택 후 「📎 일괄첨부」로 여러 영상 첨부하면 프로그램이 멈춤(freeze).
- **원인**: `protocol.handle('media')` 가 파일을 **동기**(`fs.readFileSync`/`fs.readSync`)로 통째 읽음(main.js:242).
  영상 `<video>` 요소는 초기 로드 때 `Range: bytes=0-`(=전체)로 요청 → **206 분기도 파일 전체를 동기 읽기**.
  일괄첨부로 여러 영상 썸네일이 동시에 media:// 를 요청 → **메인 프로세스가 동기 I/O 에 줄줄이 묶여 IPC 응답 불가 = 앱 전체 멈춤**.
  (큰 mp4 여러 개 = 통째 버퍼 할당까지 겹쳐 메모리 급증.)
- **수정**: `fs.createReadStream` + `require('stream').Readable.toWeb(...)` 로 **비동기 스트리밍** 응답(206/200 양쪽).
  파일을 통째 메모리로 안 올리고 청크로 흘려보냄 → 동시에 여러 영상이 로드돼도 메인 프로세스가 안 멈춤.
  Electron 33(Node 20)에서 `Readable.toWeb` + protocol.handle 스트림 바디 지원. 자막·미리보기 재생도 동일 경로라 함께 개선.
- ⚠ bulk-attach 는 g.videoPath 를 **원본 선택 경로**로 지정(미디어폴더 복사 안 함) — 파일을 지우면 끊김(현행 유지, 별도).

## 🎬 ComfyUI i2v 비디오 엔진 (Wan 2.2 5B / LTX · 로컬·클라우드) 추가 (2026-07-15, v0.2.38)
> 요청: Grok API 무료크레딧 불가+건당 과금 → ComfyUI i2v 로 전환. 사용자가 `D:\Priming\comfy\video_wan2_2_5B_ti2v.json`
> 다운(일단 comfy 클라우드). 이미지 comfy 경로(comfy-image.js)를 비디오용으로 확장.
- **엔진** `core/comfy-video.js`(구 comfy-engine.js i2v 로직 이식): `/upload/image` 로 그룹 이미지 업로드 →
  워크플로 LoadImage.image=업로드명. **LoadImage 없으면 자동 주입** → i2v latent 노드(class_type ~/ImageToVideo/)의
  `start_image` 에 연결(이 Wan JSON 은 LoadImage 미포함이라 필수). 프롬프트(Positive CLIPTextEncode, Negative 회피)·
  길이(초→`length`=4n+1 프레임, fps·videoMaxSec 반영)·해상도(비율)·seed 주입 → /prompt → 폴링 → /view 다운로드(webm→mp4 ffmpeg).
  로컬(/history)·클라우드(cloud.comfy.org /api + X-API-Key + extra_data.api_key_comfy_org, /job/{id}/status→/jobs/{id}) 양쪽.
- **설정** `~/.priming-maker/comfy-video-config.json`(이미지 comfy 와 별개): baseUrl·cloud·apiKey·workflows[]·workflowPath·
  fps(24)·videoMaxSec(8, 0=제한없음)·sendDims·timeoutSec. IPC get/set-comfy-video-config·pick-comfy-video-workflow·test-comfy-video.
  초기값 직접 기록(cloud=true, 이미지 config 의 apiKey 복사, Wan2.2 5B 등록).
- **비디오 드롭다운**: Grok(브라우저)/Grok API(유료)/**ComfyUI: <워크플로>**(value `comfy::<path>`)/없음. comfy 선택 시 「⚙ ComfyUI」 →
  설정 모달(로컬·클라우드/키/워크플로 추가·전환/최대길이·fps/타임아웃). main `runComfyVideos`(이미지 있는 그룹만 i2v,
  duration=그룹 TTS초, 출력 media-N/NN.mp4) + `genGroupVideos` 디스패치(grok-api / comfy[::path] / 브라우저 Grok).
- comfy 는 브라우저 아님 → grokVideoPipeline=false → make-all 순차 3단계 실행. 저장된 videoEngine 정규화에서 'comfy' 제외 유지.
- ⚠ **실측 필요**: ① comfy.org 클라우드가 `/api/upload/image`(i2v 소스 업로드)를 받는지 ② Wan22ImageToVideoLatent 에
  start_image 자동주입이 실제로 먹는지(안 되면 ComfyUI 에서 Load Image→start_image 연결 후 API 포맷 재저장) ③ 클라우드 length·해상도 한계.

## 🎬 Grok Imagine 비디오 API (image→video, 브라우저 없이) 추가 (2026-07-15, v0.2.36)
> 요청: "비디오에서 Grok api를 이용할수 있도록 해줘". 브라우저 Grok(SuperGrok 구독, 주간한도)과 별개의 **유료 사용량 과금** API.
- **엔진** `core/grok-api.js`: xAI 공식 REST. 제출 `POST https://api.x.ai/v1/videos/generations`
  (`Authorization: Bearer <키>`, body `{model:'grok-imagine-video', prompt, image(base64 data URI), duration(1~15), aspect_ratio, resolution:'720p'}`)
  → `{request_id}` → 폴링 `GET /v1/videos/{id}` (3초 간격, ≤7분) → `{status: done|pending|failed|expired, video:{url}}` →
  `video.url` 다운로드. `generateVideo`(url) / `generateVideoToFile`(mp4). 키=secret-store `'xai'`.
- **비디오 엔진 드롭다운**: `Grok (브라우저)` / **`Grok API (유료)`**(value `grok-api`) / `없음`. grok-api 선택 시 「⚙ 키」 버튼 →
  xAI 키 입력 모달(console.x.ai 발급 안내). IPC `get-xai-key`/`set-xai-key`.
- **디스패치**: main.js `genGroupVideos(pr, mediaDir, onlyNums, videoEngine)` — `grok-api`면 `runGrokApiVideos`(이미지 있는 그룹만
  i2v, prompt=videoPrompt‖motionNote‖기본, duration=그룹 TTS초 clamp1~15‖6, 출력 media-N/NN.mp4, 성공 g.videoPath·실패 g.videoStatus='fail'),
  그 외는 기존 `P.generateHookVideosGrok`(브라우저). video-build·make-all 3단계·video-group 3곳이 이 래퍼 사용.
- grok-api 는 브라우저가 아니라 **파이프라인/쿨다운 제외**(grokVideoPipeline=false → _grokCool=0, videoPipeline=false) →
  make-all 에서 순차 3단계로 실행. 429는 사용량/결제 한도로 중단(브라우저 주간한도 쿨다운과 무관).
- ⏳ **실측 필요**: ① `image` 필드 형식(base64 data URI 로 보냄 — 공식 문서 명시 약함, 실패 시 로그의 `xAI 4xx:` 응답 보고
  URL 방식/필드명 조정) ② 폴링 응답 실제 shape(status/video.url) 확인. xAI 키+결제 설정 후 영상 1개로 검증.

## 🖼 Flow 계정 라운드로빈 + 한도 판정 계정별로 (2026-07-14, v0.2.20)
> 증상: 그룹 단건 재생성마다 항상 같은(첫 가용) Flow 계정만 사용 → 한 계정에 시도 집중.
>       "일일 한도 50회 초과 (현재 92회)" 경고는 **모든 계정 합계**(anti-detect todayCount) 기준이라 오해 유발.
- **라운드로빈**: flow-accounts 에 `lastUsedId` 저장(markUsed 시) + `pickNext(excludeIds)` —
  마지막 사용 계정의 '다음'부터 available(캡·쿨다운 통과) 계정 선택. runFlowImages nextAcc 가 pickNext 사용.
  단건 재생성 연타 → 계정 1→2→3→4→1… 분산 (단위검증: 순환·제외·캡소진·쿨다운 4케이스).
- **한도 판정 계정별**: anti-detect checkDailyLimit/beforeNextGeneration 판정을 todayCount(전역)에서
  **profileCount(현재 계정 성공 장수)** 기준으로. 메시지 "이 계정 N회 · 오늘 전체 M회" 병기.
  flow-engine 로그도 동일 표기. stop 모드가 멀쩡한 계정을 차단하던 여지 제거.
- ⚠ anti-detect profiles 카운트는 '성공 장수'(registerGenerationSuccess) — 시도 카운트 아님(사용자 정책).

## 🖼 이미지 한도 쿨다운 기억 + 재생성 직렬 큐 (2026-07-14, v0.2.19)
> 증상 ① 그룹 단건 재생성마다 한도 걸린 Genspark 에 접속→제출→한도 확인→Flow 폴백(시간 낭비)
>      ② 재생성 진행 중 다른 그룹 생성 클릭 → 같은 브라우저를 잡아 먼저 작업이 죽음("Target page ... closed")
- **한도 쿨다운**: genspark 한도 메시지("…7월 14일 오후 3:39에 재설정됩니다")에서 재설정 시각 파싱
  (`parseLimitResetTime` main.js — 파싱불가/오파싱(24h 초과)은 지금+60분) → `accounts.js setCooldown/cooldownUntil`
  (계정별, `~/.priming-maker/genspark-accounts.json` cooldowns) → activeAccounts/pickActive 가 쿨다운 계정 제외
  → 순환이 접속 시도 없이 "⏭ Genspark 건너뜀(재설정 HH:MM 이후)" 로그 후 다음 엔진 직행.
  pipeline.js limitReached 가 bool 대신 **감지 메시지 문자열**을 담아 반환(파싱용, truthy 호환).
  ⚠ 한도 후 첫 1회는 여전히 접속함(감지해야 시각을 앎) — 그 1회가 쿨다운을 기록.
- **직렬 큐**: main.js `enqueueImageJob` — regen-group·image-build 를 하나의 promise 체인으로 순차 실행.
  뒤 클릭은 "⏳ 큐 대기 (앞에 N건)" 로그 후 자기 차례에 실행. 앞 작업 실패해도 다음 실행(체인 유지).
  검증: 순차성·실패격리·반환값 단위테스트 + 쿨다운 스토어/파서 단위테스트(실메시지 7/14 15:39 정파싱) 통과.
- make-all 의 이미지 단계는 큐 미적용(범위 밖 — 필요시 추후).

## 📖 러닝헤드 — 장 시작 페이지 밑줄 제거 + '제N회' 뒤 공백 (2026-07-13, v0.2.15)
> 요청: ① 회차 시작 페이지의 헤더 밑줄(글자 없이 떠 있는 줄) 제거 ② 러닝헤드 "제16회여포의…"→"제16회 여포의…"
- **② 공백**: `string-set: chapter-title content()` 를 h2 에서 뽑으면 h2 가 `<span.ch-no>제16회</span>여포의…`
  로 쪼개져 사이 공백이 사라짐 → 장마다 **숨김 앵커** `<span class="ch-rh">${c.title}</span>`(display:none)
  를 h2 앞에 두고 거기서 string-set. 원문(공백 포함) 그대로 러닝헤드에 들어감.
- **① 밑줄**: vivliostyle 은 **내용이 비어도 @top-center 의 border-bottom 을 그림**(실측) → first-except 로
  글자만 비는 장 시작 페이지에 '떠 있는 줄'이 남았음. `page: chapstart` 로 그 페이지만 명명하려 했으나
  **h2 에 page 를 주면 강제 개행으로 제목만 홀로 떨어짐**(제목+리드+본문 분리 = 실패, 폐기).
  → **해결: 밑줄을 상자 테두리(border-bottom)가 아니라 '글자 밑줄'(text-decoration:underline +
  text-underline-offset:5pt + color #ccc)로** 변경. 글자가 있을 때만 그려지므로 장 시작 페이지(글자 빔)엔
  자동으로 안 나옴. 계속 페이지엔 러닝헤드 글자 밑에 밑줄. 정렬(짝=왼/홀=오 바깥)·간격 유지.
  ⚠ 부작용: 밑줄이 판면 전폭이 아니라 **글자 폭에 맞음**(러닝헤드가 길어 실제론 거의 전폭). headerLine 옵션 유지.
- 실측: 장시작 p9 밑줄 없음 / 계속 p10·p11 러닝헤드+밑줄 정상 / 쪽수 안 늘어남(제목 안 떨어짐). 테스트 4종 통과.

## 📖 미리보기 목차 쪽번호 '??' 최종 해결 — 캐시버스터 폐기, 조판마다 새 파일명 (2026-07-13, v0.2.14)
> 증상: 앱 미리보기에서만 목차 쪽번호가 전부 `??`(CLI PDF 는 정상). v0.2.10 의 '#t='→'?t=' 수정으로도 미해결.
- **원인(iframe 실측)**: previewUrl 에 `?t=<ts>` 쿼리 캐시버스터 → CoreViewer 로드 URL 은 쿼리 포함인데
  vivliostyle 이 목차 `#ch-N` href 를 절대화한 값은 **쿼리 없음** → 같은문서 판정 실패 → 외부 문서 취급
  → target-counter 해석 포기 = `??`. **URL 에 쿼리든 프래그먼트든 붙이면 깨진다.**
- **수정**: main `book-preview` 가 조판마다 `_preview/book-<Date.now()>.html` **새 파일명**으로 저장
  (기존 book*.html 삭제) + BookView 는 쿼리 없이 URL 그대로 로드 — 새 URL 이라 캐시 무효 + anchor 일치.
  수정 후 href 가 `#viv-id-…` 내부 anchor 로 정상 변환, 쪽번호 실측(11/17/23/…).
- 미리보기 목차 번호는 표지 안내 페이지(미리보기 전용) 때문에 PDF 보다 +2 — 미리보기 하단 폴리오와는
  일치(자기일관), PDF 는 정확. book-ui.smoke 에 「목차 ?? 회귀」 단언 추가. 테스트 4종 통과.

## 📖 판권 [자유문] 중복 제거 — 표는 메타, 고지문만 자유문 (2026-07-13, v0.2.13)
> 증상: 사용자가 [판권] 섹션에 판권 전체(제목·원작·편역·펴낸이·펴낸곳·초판발행·ISBN·ⓒ)를 산문으로
>   다시 써넣으니, 상단 `> 메타`로 만든 자동 표 + 그 자유문이 **둘 다** 나와 발행일·ISBN·저자가 중복 인쇄.
- **결정(사용자 확정, Option 1)**: 표는 `> 메타`로 자동 생성, [판권] 자유문은 **표에 없는 고유 정보(고지문)만** 남김.
- **구현**(html-builder colophonHtml): `filterColophonSection` — [판권] 각 문단을 줄 단위로 훑어
  `colophonLineRedundant` 가 **표·제목·ⓒ에 이미 있는 줄**이면 버림. 규칙 = ① 제목/부제 재기술 ②
  `ⓒ/©`(author·translator·copyright 있으면 — 하단 cp-legal 자동) ③ `라벨+값`(원작·지은이·편역·펴낸이·
  펴낸곳·발행일/초판·ISBN·정가·부가기호·주소·전화·이메일 등)이 **해당 메타가 실제로 채워져 있을 때만**.
  → 메타에 없는 고유 정보는 보존(무손실). ⚠ 정규식 `\b` 는 한글 경계 미인식 → 초판/발행 라벨은 `\s+` 사용.
- 검증: 천하대란 원고(자유문에 전부 재기술) → 표 + 고지문 2줄(편집저작권·AI활용)만, 되풀이 8줄 제거.
  고전의뜰 목표 원고(고지문=문장뿐) → 5문장 전부 보존(안 걸림). 테스트 4종 통과.
- ⚠ **G: 원고는 Google Drive 스트리밍** — 동기화 중이면 읽을 때마다 옛/새 버전이 오락가락(파싱은 새 내용,
  PDF 는 옛 내용으로 나온 원인). 검증 땐 로컬로 스냅샷 후 작업. [[gpu-server-ollama-architecture]]

## 📖 출판 조판 = 목표 최종본 정합 (2026-07-13, v0.2.11)
> 기준: 사용자 확정본 `G:\...\02_출판용\POD_최종\[POD] 원고_고전의뜰 삼국지_01.pdf`(구 앱 산출, 208쪽 A5).
> 원고 = `02_출판용\출판_삼국지_1권_부속물.md` + `출판_삼국지_제NNN회.md`(구 앱 형식). 실측 재현 완료.
- **파서 강화(구 앱 회차파일)**: detectBookFileKind 에 `# 제N회` 우선 판정(내부 `## [목차]` 마커에 속아
  native 오판하던 것) · normalizeChapterFile 이 헤딩 전 평문 `라벨: 값`(META_KEYS)을 `>` 메타로,
  구 마커(`# [목차]` 등) 제거, 짧은 첫 평문 → `## ` 승격 · normalizeEssentialFile 은 비예약 H1 도
  `## [이름]` 커스텀 섹션으로(lastZone 상속: 에필로그 뒤 '다음 이야기' = back).
- 🔴 **인용으로 새던 메타**: 장 헤딩 아래 `> 플랫폼: 교보` 류(연속 여러 줄이 한 quote 로 병합)가
  본문에 인쇄되고 **장의 첫 블록을 차지해 리드문(*이탤릭*) 감지까지 깨뜨림** → quote 의 전 줄이
  META_KEYS 매칭이면 통째로 meta 흡수(기존값 우선). 리드문 중앙 배치 복구.
- **편역 라벨**: META_KEYS 에 편역/편역자/편역이/번역/엮은이 → translator + **translatorLabel('편역이') 보존**.
  판권 행 라벨·속표지(`N 편역`)·ⓒ 저작권자(translator 우선 — 편역서 편집저작권) 모두 반영.
- **목차**: 본문 장(제N회)만 + flex 점선 리더 + target-counter 쪽번호 — 목표본과 동일(13/29/45/…).
- **장 표제**: `제N회`(윗줄 .ch-no)/제목(아랫줄) 2줄 중앙, 위 16% 여백. 문단간격 기본 5pt.
- **판권**: 앞 판권(판권위치=앞)은 **속표지 뒷면(verso)** — cp-front 의 break-before recto→page.
  하단 배치 = `.cp-wrap margin-top 44%`(⚠ %마진은 판면 **너비** 기준; flex height:100% 하단정렬은
  vivliostyle 조각화에서 미해석이라 폐기). 라벨 min-width 제거(라벨 바로 뒤 ｜). **[판권] 자유문은
  flushPara 가 개행 보존**(`join('\n')` + cp-notes p pre-line) — 고지문 줄 단위 조판.
- 테스트 4종 통과(parser 44단언 · multi 260쪽+ePub · pdf 39쪽+표지 · UI E2E). deps 0a1409dd 유지(라이트 업데이트 OK).
- ⏳ 발행일·출판등록·주소·ISBN·가격·QR 은 원고에 없으면 빈 행 생략 — 앱 책정보 폼 입력 시 목표본과 완전 동일.

## 📖 출판(POD) 실입고 점검 — 표지검증 사각·바코드 규격·판형 불일치 수정 (2026-07-12, v0.2.6)
> 실제 부크크/교보POD 입고 전 전수 점검(테스트 4종 + 멀티에이전트 감사 20건 → 검증 후 반영). 수정 후 4종 재통과.
- 🔴 **표지 치수 검증이 죽어 있었음**: readImageSize 는 `{w,h}` 반환인데 book-attach-cover·epub
  cropFrontCover 가 `dim.width` 를 읽어 **검증·크롭이 한 번도 실행된 적 없음** → dim.w/h 로 수정.
  표지 첨부 필터에서 TIFF 제거(Chromium 렌더 불가 → 빈 표지 인쇄 위험).
- **판형 폴백 통일**: html-builder(내지)가 미등록 판형이면 A5, main(표지·책등)은 플랫폼 기본으로
  제각각 폴백 → 내지/표지 판형 불일치 입고거절 위험. main 과 동일식(TRIM_SIZES 검증)으로 통일.
- **바코드 GS1 규격화**: 표지 자동조판 svg 폭 32mm 고정 → 부가기호(EAN-5) 포함 시 배율 ~57%(스캔불가).
  모듈수×0.264mm(80% 배율)로 동적 계산(부가기호 포함 ≈45mm) + 바높이 200px + quiet 11모듈.
  ISBN 하이픈 병기는 사용자 입력 표기 우선(한국 발행자번호 2~6자리 가변). 체크섬 오류 시 무경고→경고.
- **빌드시 표지 재검증**: 첨부 후 쪽수가 변해도 재검증 없이 object-fit:fill 스트레치되던 것 →
  book-build-pdf 가 최종 스프레드로 validateCoverImage 재실행+경고. 표지 실패가 '완료'로 보이던 것
  → coverError 반환+BookView 구분 표시. [판권] 섹션 부재 시 경고(법정 기재사항 누락 방지).
- **UI 안전망**: 미리보기 실패 시 '조판 중…' 고착 해제, 표지가이드/바코드 저장실패 통보, 쪽수 미확정
  (책등 0mm) 상태에서 가이드 생성 차단, 원고 전환 시 lastPagesRef 리셋, ePub 쪽수미확정 크롭 생략.
- 남은 개선(미적용·제안): 표지만 재생성 버튼(현재 내지 선행 필수), spine ratio 허용 1%→mm 환산 경고.

## 🧹 코드 정비 — ComfyUI 완전 제거·BGM ACE-Step 이관 (2026-07-12, v0.2.3)
> 전수 감사(IPC 사슬·죽은 코드·낡은 문구) 후 확정분만 반영. IPC main↔preload↔renderer 115:115 일치 확인.
- **BGM 3.5단계 → ACE-Step 독립 서버**(core/ace-step.js, 플리 음악과 동일 경로): 기존엔 comfy-engine
  textToAudio(127.0.0.1:8188)로 가서 **항상 "음악 서버 연결 실패"로 죽던 상태**를 해소. 생성 중
  `S.musicActive` 뮤텍스(TTS·보이스디자인 차단), 출력 `bgm_*.wav` → loopAudioTo. resolveBgmPath 폴백이
  `.wav` 도 잡도록 확장.
- **ComfyUI 흔적 완전 제거**: comfy-engine.js·core/comfy-config.js **파일 삭제**(참조 0 확인),
  get/set/test-comfy IPC + preload API + ⚙ Comfy 모달(도달 불가 죽은 UI 4천자) 제거.
  ENGINE_META 의 comfy 항목, "(ComfyUI는 순환과 별개)" 등 낡은 문구 정리.
- **가드 보강**: `tts-group`(그룹 단건 재변환)에 gpuBusyReason 뮤텍스 추가(음악/디자인 중 차단).
- **잔재 정리**: 채널편집의 ch.gemini/gkey 데이터 배관(UI 필드는 이미 제거됨), runTts 의 사장된
  force 파라미터(구 '다시 변환' 전용), make-all 1·2단계 주석의 "로컬 ComfyUI 순차" 서술 갱신.
- ⚠ 남은 것: ace-step/qwen-design 폴더의 venv 는 로컬 전용(매니페스트·git 제외). BGM 이 이제
  ACE-Step 를 쓰므로 첫 BGM 생성 시 모델 로딩(~30초) 후 곡 생성. 미설치면 "BGM 없이 진행" graceful.

## 🎞 프리미어 XML 3종 수정 (2026-07-04, v0.1.79)
- **오디오**: `s.ttsAudioPath` 가 캐시/임시 경로면 프리미어가 엉뚱한 파일을 물거나(존재 시) 조용히 건너뜀(부재 시 무음).
  → `buildPremiereXml(a.ttsDir)` 로 **tts-N 폴더 정본(`<num>.mp3/wav`) 우선** 참조 + 못 찾으면 컷 번호 경고 로그.
- **이미지**: Premiere 는 스틸을 원본 픽셀 그대로 배치(1376×768 → 화면에 안 참) → 파일 실치수(readImageSize, **{w,h} 반환** 주의)
  + `Basic Motion` scale 키프레임(xmeml)로 **cover 배율 → ×1.1 켄번스**(그룹 홀짝 줌인/줌아웃).
- **자막**: export-premiere 가 XML 옆에 `_premiere.srt` 동시 생성(Premiere 캡션 가져오기용, captionMaxChars 반영).

## 🎵📖 BGM .vrew 삽입 확정 + 발음사전 UI + 그룹TTS 재변환 + BGM표시 (2026-07-04, v0.1.73)
- 🐞 **BGM 이 .vrew 에 안 들어가던 문제 해결 — 수동 삽입 .vrew 샘플 분석으로 형식 확정**:
  Vrew BGM = ① `files[]` 에 `sourceFileType:'BGM'` 파일 + ② `props.tracks[tid]` 에 **`type:'bgm'`** 트랙
  (`fade:{in,out}`, `loop:true`, `sourceOut=파일길이`) + ③ `props.assets[aid]={trackIds:[tid],role:'sub'}` +
  ④ **그 aid 를 전 clip 의 assetIds 에 추가**(수동샘플 실측: 731 clip 전부 참조). ①②③④ 다 있어야 Vrew 가 BGM 렌더.
  ⚠ v0.1.73 에서 ③④(asset·clip링크)를 "전역이라 불필요"로 오판해 제거 → 트랙만 있고 asset 없어 **안 들림**.
  v0.1.77 에서 asset + 전 clip 링크 복원(zip 오디오는 `.mp3` 그대로, TTS 와 동일해 Vrew 로드 OK). 볼륨 0.15.
- 🎤 **그룹 TTS 재변환("🎤")이 같은 음성 나오던 문제**: `tts-group` 이 force 없이 호출→기존 음성 건너뜀 + seed 고정.
  → `fillTtsList(...force=true)` + **seed 매 클릭 랜덤화**(같은 seed=결정적 동일). 매번 새 take 로 뽑힘.
- 📖 **발음사전 UI 추가**(백엔드 omnivoice-dict-store 는 이미 있음, UI 만 없었음): IPC `dict-list`/`dict-save`(+
  tts-manager.invalidateDict) + preload + 「📖 발음사전」 모달. entry `{source,pron,enabled}` — **자막은 대본 그대로,
  TTS 만 교정**(applyOmniVoiceDict 가 synthesize 전 치환). 예: 정약용→정냐굥. 저장 후 TTS 재변환해야 반영.
- 🎵 **BGM 프롬프트 표시**: toDTO 에 `bgmMood`(대본 지정) + `bgmUsed`(실제 사용 무드, 생성 후) → 첫 그룹 카드에
  「🎵 BGM:」 줄 + 복사 버튼(bgmOn 일 때).

## 🧹 엔진 단순화 — Krea2·LTX·Wan·Flow영상 제거 (2026-07-04, v0.1.71)
> 사용자 요청: 이미지·영상 엔진을 브라우저 기반만 남기고 ComfyUI 이미지/영상 엔진 제거. **ACE-Step 오디오(플리·BGM)는 유지.**
- **이미지 엔진** = `순환(Flow+Genspark)` 하나만. `comfy`(Krea2_Turbo) 옵션·`runComfyImages`·순환 루프의 comfy 슬롯 제거.
  UI 드롭다운 → 「이미지: 순환」 라벨 + ⚙ 순환 버튼만.
- **영상 엔진** = `Grok` / `없음` 만. `flow`(runFlowVideos)·`comfy`(LTX)·`wan`(runComfyVideos) 옵션·함수·디스패치 전부 제거.
  make-all 파이프라인/3단계·video-build·video-group 모두 Grok 단일 경로로 단순화. `clipMaxOf`=10 고정, `noLocalGpuImg`=true,
  `videoPipeline`=canParallel&&grok. 레거시 마이그레이션: 저장된 comfy/wan/flow 영상 → grok, comfy 이미지 → rotate.
- **유지**: `comfy-engine.js`·`comfy-config.js`·⚙ Comfy 모달(서버/클라우드/오디오 섹션만) — ACE-Step 음악(플리)·BGM 이 씀.
  comfy-engine 의 imageToVideo/textToImage 는 이제 미사용(무해한 dead 메서드, 나중 정리 가능). comfy-config 의 wan*·video* 필드도 무해하게 잔존.
- ⚠ 이번 세션에서 붙였던 Wan i2v(아래 v0.1.68 항목)는 이 릴리스로 **제거됨**(설명은 이력용으로 남김).

## 📖 출판 독립화·표지 안내 페이지·구조 체크박스 개편·작성 가이드 (2026-07-03, v0.1.69)
> 사용자 리뷰 2차 반영 — 출판을 영상과 완전 분리된 개별 기능으로.
- **롱폼↔출판 분리**: 롱폼 헤더 「📖 출판편집」 버튼·openBookFromLongform 제거. 출판은
  「📖 원고 열기」로만 진입(open-book-path IPC 는 E2E 용으로 유지). longform-parser 의
  예약섹션 스킵은 안전망으로 유지.
- **미리보기 1쪽 = 표지 안내 페이지**(coverInfoHtml, **미리보기 전용 — 내지 PDF 미포함**):
  치수 표(판형/스프레드 mm·px/책등/날개/재단·안전여백) + 축소 다이어그램(구획·재단선 빨강·안전선
  초록). **표지 이미지 첨부 시 다이어그램 배경에 깔려 치수 정합을 눈으로 확인**. book-preview 가
  opts.coverInfo 전달(build-pdf 는 안 함). 쪽수 보고·표시는 -1 보정("표지 / N쪽").
- **구조 체크박스 개편**: 원고에 쓴 섹션 = 자동 체크. **체크 해제 = 원고 보존한 채 출력에서만
  제외**(layout.excluded — html-builder front/back/목차/판권/표지 covers 전부 필터). 원고에 없는
  섹션 체크 시에만 템플릿 삽입(기존). 「판권 구성」 패널(17종 체크박스) 삭제 — 판권은 [판권] 섹션
  자유문이 정본(마커만 있으면 메타 자동 생성 폴백), 위치 select 는 규격 패널.
- **특별 섹션(반복 코너)**: layout.specialKeyword(쉼표 복수) — 일치하는 소제목(###) 구간을
  `div.special-sec`(옅은 배경 노트 박스, 0.93em, 들여쓰기 없음)로 조판. 삼국지 '역사 노트' 용.
- **관행 보정**: 문단 간격 기본 10pt→**0**(관행=들여쓰기만), 소제목 아래 여백 10→8pt(위25:아래8),
  반표제지·속표지 제목에 fileTitle 폴백(메타 없을 때 1쪽이 빈 페이지로 나오던 문제).
- **원고 작성 가이드**: `docs/출판-원고-가이드.md` — 규약 설명이 HTML 주석으로 들어 있는 "살아있는
  예시"(파서로 규약 검증 통과 확인). IPC `book-save-guide`(showSaveDialog 복사) + 헤더 「📄 작성
  가이드」 버튼. 구 출판-원고-예시.md 삭제. h1=책제목 고정(가이드에 명시).
- .bkactions 버튼 3개 크기 통일(flex:1).

## 🍌 Nano Banana 2 Lite — Flow 이미지 모델 선택 추가 (2026-07-03, v0.1.68)
- **배경**: 2026-06-30 Google 이 신모델 `gemini-3.1-flash-lite-image`(Nano Banana 2 Lite) 정식 출시
  — Nano Banana 2 대비 절반 가격·약 2.7배 빠름(공식 발표). Genspark/Flow 양쪽 다 지원 가능성 있으나,
  **Genspark 는 코드에 "Nano Banana 2"가 하드코딩**(검증만, 선택 로직 없음)돼 있어 변경 위험이 크고,
  **Flow 는 이미 `_selectModel()` 로 모델을 텍스트 매칭 선택하는 인프라가 있어** 이쪽으로 추가.
- **구현**: `core/image-rotation.js` DEFAULTS 에 `flowImageModel`(기본 'Nano Banana 2') 추가(load/save
  이미 patch 를 그대로 병합하는 제네릭 구조라 IPC 변경 불필요) → `main.js runFlowImages()` 가
  `Rot.load().flowImageModel` 을 읽어 `eng.run({..., model})` 로 전달(flow-engine.js 는 이미
  `opts.model` 을 image/video 공통으로 처리 — **엔진 코드 변경 없음**). UI: 「⚙ 순환」 모달의 Flow 행에
  select(Nano Banana 2 / Nano Banana 2 Lite) 추가.
- **안전장치**: `_selectModel()` 이 드롭다운에 해당 텍스트가 없으면 **조용히 무시하고 기존 모델 유지**
  (에러 없음) — Flow 웹 UI 에 아직 이 옵션이 안 떴어도 앱이 깨지지 않음.
- ⏳ **실측 필요(중요)**: Google 공식 발표는 Flow 를 롤아웃 대상으로 명시했으나, **Flow 웹 UI 드롭다운에
  "Nano Banana 2 Lite" 라는 정확한 라벨이 떠 있는지는 미확인**(genspark.ai/Flow 상세 UI가 자동 조사에서
  차단됨). 사용자가 Flow 로 이미지 1장 만들 때 로그의 "모델 Nano Banana 2 Lite" 표시 후 실제로 다른
  모델이 적용됐는지(속도·화질 차이) 1회 확인 필요. 라벨이 다르면(예: "Nano Banana 2 Flash Lite") select
  의 value 문자열만 그에 맞게 고치면 됨.
- Genspark 는 이번 범위 제외(하드코딩된 모델 검증 로직을 실제 선택 로직으로 바꿔야 해서 더 위험) —
  Flow 경로가 실측으로 확인되면 다음으로 고려.

## 🎬 Grok 새 UI 대응·비디오 파이프라인·프리미어 XML + 📖 행간 관행 (2026-07-03, v0.1.67)
- **Grok 새 UI(2026-07) 대응**: 제출 후 "이미지" 화면에 머물고 생성물은 **사이드바 진행률(N%)
  썸네일**로만 표시 → video 요소 못 찾아 5분 타임아웃 → 실패 → 재시도(크레딧 추가 차감)되던 문제.
  폴링 루프에 ① 진행률 배지(N% 텍스트) 감지+로그 ② 썸네일 좌표 클릭으로 비디오 화면 진입(최대 4회)
  ③ 진행률 보이는 동안 타임아웃 연장(절대 상한 15분) 추가 (grok-engine.js 폴링부). ⏳ 실전 1회 실측 필요.
- **이미지→비디오 파이프라인 Grok 확장**: 기존 videoPipeline(Comfy 클라우드 한정)을
  `videoEngine==='grok'` 에도 — Grok 은 별도 크롬 프로필이라 Genspark/Flow 이미지와 비충돌.
  videoStage 를 "준비된 그룹 모아서 편별 배치 호출"로 개편(Grok 은 호출당 브라우저 기동이라 단건 비효율).
  Grok 'auto' 길이는 그룹 TTS 필요 → ttsReady 요구. **Flow 비디오는 이미지와 같은 브라우저라 파이프라인 제외**.
- **프리미어 Pro 임포트**: `core/premiere-xml.js` — FCP7 XML(xmeml v4) 시퀀스(V1=그룹 비디오/이미지
  스틸, A1=문장 TTS 순차, 30fps, 16:9/9:16/1:1). 비디오가 그룹보다 짧으면 잔여를 이미지로 채움.
  IPC `export-premiere` + 카드 「🎞 프리미어」 버튼(.vrew 뒤). Premiere: 파일>가져오기, 자막은 .srt 캡션.
  pathurl=`file://localhost/D%3a/...`. 스모크: 더미 자산 165프레임 검증.
- 카드 「🎬 영상」 라벨 → 「🎬 비디오」.
- **📖 행간 관행**: 기본 1.65(구 앱) → **1.8** (한국 단행본 관행 = 글자크기의 1.7~2.0배, 10pt→18pt).
  삼국지 261→275쪽. 기존 원고의 저장된 layout 은 유지(새 원고부터 1.8).

## 📖 머리글 정렬·내용 확장 + 휠 넘기기 / 🎵 BGM·플리 코드 점검 (2026-07-03, v0.1.66)
- **머리글**: 내용 5종(책제목/책부제/장제목/소제목(절)/없음) × 정렬 3종(왼쪽/가운데/오른쪽) —
  짝/홀 각각 선택. 기본 = 짝수쪽 책제목 왼쪽 / 홀수쪽 장제목 오른쪽(바깥 정렬 관행).
  부제=`.book-subtitle-anchor` string-set, 절=`section.chapter h3` string-set(sec-title).
  ⚠ **vivliostyle 마진박스 실측**: `@top-left` 단독=글자 세로 쌓임 / `@top-left`+`@top-center`
  병용=공간 분할로 왼쪽 글이 중앙에 밀림 → **`@top-center` 단일 박스 + width(판면폭 mm 명시) +
  text-align** 이 정답(좌표 실측: 짝수쪽 x0=판면 왼쪽 끝, 홀수쪽 x1=판면 오른쪽 끝 정확).
- **미리보기 휠 넘기기**: iframe doc 'wheel' 리스너(250ms 스로틀) → navigateToPage NEXT/PREV.
- **🎵 BGM 점검 4건 수정**: ① addBgmTrack 의 `assetEffectInfo:{type:'none'}` 제거(제목 web 트랙에서
  Vrew 내보내기 실패시켰던 패턴 — startDelay>0 일 때만 검증된 fade-in) ② sourceOut 을
  min(총길이, 파일실측길이)로(소스 범위 초과 방어) ③ **💾 재export 에도 BGM 포함**(pr._bgmPath 재사용,
  스냅샷 bgmPath/bgmVolume 저장·복원 — 재시작 후에도 유지) ④ 플리 곡 길이를 mp3 실측으로 보정
  (make-playlist 생성 직후 + make-playlist-video 입구 — 스펙 180초 ≠ 실제 출력이면 배경 루프·자막 어긋남).
  textToAudio·makeBoomerang/loopBoomerangTo·loopAudioTo 는 이상 없음(리뷰 완료).
  BGM 트랙이 Vrew 에서 실제로 들리는지는 여전히 실측 필요(안 들리면 수동 .vrew 샘플로 필드 확정).

## 📖 출판 조판 8종 개선 — 사용자 리뷰 반영 (2026-07-03, v0.1.65)
> 삼국지 미리보기 검토 피드백 8건 반영. 조판 기본값 = **구 Book Publishing 앱 사용자 설정 그대로**
> (defaults.ts 실측: KoPub바탕 Light 10pt/1.65/자간-0.4pt/들여15pt/문단간격10pt/여백 20·15·20·17).
- **① 폰트**: KoPubWorld 바탕·돋움 이식. ⚠ **woff2·otf(CFF)는 Chromium PDF 가 Type3 로 구움** →
  fonttools(cu2qu)로 **TrueType 변환**해 동봉(Light/Bold만, 56MB) = Type0(CID) 임베딩 실측.
  본문 폰트 select(KoPub바탕/돋움/나눔명조/고딕) + 굵기 300/400/700. `assets/fonts/` 는 라이트업데이트
  제외라 기존 설치본은 나눔 폴백 — 다음 설치본 재배포에 포함.
- **② 표지 구성**: 예약 섹션 zone 'cover' — `## [뒷표지]`(소개글) `## [앞날개]` `## [뒷날개]` `## [책등]`
  (구조 패널 「표지 구성」). 표지 PDF 가 배경 이미지(선택) 위에 조판: 앞표지 제목·저자 오버레이(옵션),
  책등 세로쓰기(자동=제목·저자·출판사), 뒷표지 소개글(위 10%부터)+**ISBN 바코드+정가**(우하단),
  날개 글. 이미지 없이 텍스트만으로도 표지 PDF 생성 가능. buildCoverPdf compose 파라미터.
- **③ 여백**: 기본 20/15/20/17(구 앱) + UI 4필드. **④ 목차 쪽번호**: leader() 가 코어(미리보기)와 CLI 에서
  달라 점선 리더 폐기 → **absolute 우측 고정 쪽번호**로 통일(양쪽 동일 실측).
- **⑤ 머리글·쪽번호 선택**: 짝/홀 각각 책제목/장제목/없음, 구분선 on/off(9pt 회색 고딕), 쪽번호
  바깥하단/가운데/숨김. **⑥ 문단**: 문단 간격(pt)·들여쓰기(pt) UI. **⑦ 소제목(## = h3)**: 크기·고딕/본문체·
  굵기·정렬·장식문자(❖ 기본)·위아래 여백. **⑧ 판권**: 자동 항목 17종 체크박스(colophonFields)+위치(앞/뒤)
  — 「판권 구성」 패널. 자유문([판권] 내용) 있으면 체크박스 무시 안내.
- 구현: html-builder pageCss 가 옵션 전부 반영(CSS 순서 = 폰트→테마→pageCss 오버라이드),
  layout 옵션은 item.settings.book 영속(bookDTO.layoutSaved 로 복원). BookView LAYOUT_DEFAULTS.

## 📖 출판 모드 확장 — 다중 파일 원고·ePub·바코드·표지가이드 (2026-07-03, v0.1.64)
> D:\PrimingBook(구 Book Publishing 앱) 기능 이식. **사용자 실원고 = 삼국지연의 1권**
> (`D:\PrimingBook\book-publishing\data\삼국지연의_1권_필수파일.md` + `출판_삼국지_제001~015회.md`)로 실측 검증.
- **다중 파일 원고**: 「📖 원고 열기」 multiSelections — 필수파일(평문 `책제목:` 메타 + `===앞부속물===` 구분 +
  `# 헌사`/`# 판권` H1 부속물) + 회차 파일(H1(또는 첫 줄 평문)=장, `*이탤릭*`=리드문, H2=절) N개 → 한 권.
  구현: book-parser `parseBookFiles` — 파일별 **라인수 보존 변환**(`# 헌사`→`## [헌사]`, 평문 메타→`> 메타`,
  회차 헤딩 +1레벨) 후 결합 → 기존 parseBookText. `_files[{path,kind,startLine,lineCount}]` 오프셋으로
  결합 라인↔원본 파일 역매핑(`resolveSourceLine`) → 클릭-편집이 **원본 파일의 올바른 줄**을 수정
  (IPC `book-get-lines`/`book-apply-edit`). 메타·섹션 토글은 essential 파일에 기록(스타일 보존: 평문/`>`).
  정렬 = essential 먼저 + 파일명 숫자 정렬. 워크스페이스 `settings.book.files` 복원. `# 속표지/# 표제지`
  지시 섹션은 RESERVED zone:'ignore'(내용 버림 — 앱이 자동 조판). 다중일 때 ✏대본수정(전체 텍스트)은 차단.
- **판권 자유 문구**: `[판권]` 섹션에 내용이 있으면(AI 활용 고지 등) 자동 테이블 대신 그대로 조판(cp-free).
- **장 리드문**: 장 첫 블록이 `*...*` 전체 이탤릭이면 type 'lead' → 제목 아래 이탤릭 부제.
- **ISBN 바코드**: `core/book/isbn-barcode.js` — 의존성 없는 EAN-13+부가기호(EAN-5) SVG(인코딩 테이블 직접).
  pyzbar 디코딩 실측 통과. IPC `book-export-barcode`(SVG 저장) + BookView 「🏷 바코드」가 PNG 변환도 저장.
  메타 `> 부가기호: 03910`(isbnAddon).
- **표지 가이드**: BookView 「📐 표지 가이드」 — 300dpi 투명 PNG(재단선 빨강/안전선 초록/책등·날개 구분+치수)
  를 canvas 로 그려 IPC `book-save-asset` 로 저장. 캔바 밑그림 레이어용.
- **ePub 3.0**: `core/book/epub-builder.js` + IPC `book-build-epub` + 「📱 ePub」. ⚠ **adm-zip 은 writeZip 때
  이름순 정렬**해 mimetype=첫 엔트리 규격을 못 지킴 → zlib+CRC32 **MiniZip**(직접 조립, mimetype STORED) 사용.
  각주=epub:type noteref/footnote(팝업). 전자책 표지 = `> 전자책표지:` 경로 우선, 없으면 인쇄 표지 스프레드에서
  **앞표지를 ffmpeg 크롭**. 판권 자유문 재사용.
- 🐞 **미리보기 쪽수 왜곡(255→660쪽) 해결**: CoreViewer 를 앱 document 에 직접 렌더하면 **앱 전역 CSS
  (p 마진·14px 폰트)가 조판 DOM 에 캐스케이드**돼 실측 페이지 분할이 부풀음 → **iframe 격리**
  (`viewportElement`=iframe 내 div + `window: iframe.contentWindow`, 클릭은 iframe doc 리스너). 미리보기=PDF 쪽수 일치 실측.
  같은 원인 계열로 html-builder 는 CSS 변수(:root/var()) 대신 값 직접 치환.
- 🐞 **모드 전환 크래시 수정**: 출판→롱폼 탭 전환 시 한 프레임 동안 book dto 가 Cards 에 들어와
  `dto.projects.length` TypeError → 가드 보강 (App.jsx:1585).
- 테스트: `test/book-multi.smoke.js`(삼국지 16파일 → 255쪽 PDF+ePub 구조 검증) + book-ui.smoke 에
  다중 파일 케이스(255쪽 조판 확인). `npm run test:book` 에 포함.

## 📖 출판(POD) 모드 — MD 원고 → 내지·표지 PDF (2026-07-02, v0.1.62)
- 모드 토글 **4-way**: 롱폼/쇼츠/🎵플리/**📖출판**. 롱폼과 원고(.md) **공유** — 롱폼 헤더 「📖 출판편집」
  버튼(IPC `open-book-path`, 무인자=현재 대본)으로 전환. 출판에서 고친 텍스트는 같은 .md 에 저장돼 롱폼에도 반영.
- **원고 규약** (`docs/출판-원고-예시.md`): `# 제목` + `> 라벨: 값` 메타(저자/ISBN/정가/출판등록…) +
  **`## [예약섹션]`**(헌사·추천사·역자서문·서문·일러두기·목차·프롤로그/에필로그·후기·감사의글·부록·참고문헌·
  저자소개·판권 — 별칭 허용, 관행 순서로 자동 재배열) + 일반 `## N장.` = 본문 장(`## N부.` = 부 표제지),
  H3=절/H4=소절, `>`=인용, ` ```시 `=시(행 보존), `![캡션](경로)`=삽화, `[^1]`=각주. [목차]·[판권]은 마커만
  (자동 생성 — 목차 쪽번호 target-counter, 판권지는 메타로 조판+법정 7필드 미입력 경고).
  **롱폼 파서는 `## [예약]` 섹션을 스킵**(book-parser.stripBookSections — longform-parser 전처리).
- **모듈**: `core/parsers/book-parser.js`(MD→BookModel, lineStart/End 소스매핑) ·
  `core/book/platform-presets.js`(부크크〔공식 규격도구 실측: 46판/A5/B5/A4·bleed3·날개100 고정·min50p〕/
  교보POD〔신국판 등 6종〕/작가와〔베타-부크크 준용〕+ 용지 낱장두께) · `spine-calc.js`(책등=(쪽÷2)×두께,
  표지 스프레드=`bleed3+[날개100]+뒤+책등+앞+[날개100]+3`×`높이+6`) · `html-builder.js`+`book-theme.css`
  (CSS Paged Media) · `pdf-builder.js`(**@vivliostyle/cli 자식 프로세스**, ELECTRON_RUN_AS_NODE=1 +
  playwright chromium `--executable-browser` 재사용, PDF 쪽수는 zlib 로 ObjStm 해제 카운트).
- **조판 확정 사항(스모크 실측)**: 목차 쪽번호(`target-counter`+`leader(dotted)`) ✓ · 러닝헤드 짝수쪽=책제목/
  홀수쪽=장제목(**`string(chapter-title, first-except)`** — `@page name:first` 는 vivliostyle 에서 장별로 안 먹혀
  first-except 로 장 시작 러닝헤드 생략) ✓ · `break-before: recto`+`@page :blank` 백면 ✓ · `float: footnote` 각주 ✓
  · 판형 MediaBox 127×188mm 정확 ✓. **폰트: 가변(variable) TTF 는 Chromium 이 Type3 로 구움** → 정적
  나눔명조/고딕(OFL, assets/fonts/book/) 동봉 = CIDFontType2 정상 임베딩. **CLI 는 HTML 을 HTTP 서빙하므로
  file:/// 서브리소스 차단** → `prepareWorkAssets()`가 폰트·이미지를 _work 로 복사해 상대경로 참조.
- **UI**: `renderer/src/BookView.jsx` 3-pane — 좌 책구조(섹션 체크=MD 템플릿 삽입/삭제 IPC `book-toggle-section`) /
  중앙 **실제 페이지 미리보기**(@vivliostyle/core CoreViewer, media:// 로 조판 HTML 로드, 렌더 완료 시
  `book-report-pages` 로 쪽수 보고→책등 계산) + **문단 클릭-편집**(data-src-line → `book-apply-edit` 로 원본 .md
  줄 치환) / 우 설정(규격·조판·표지 첨부〔치수검증 validateCoverImage〕·책정보 폼=`book-set-meta` 가 `> 메타줄` 갱신).
- **출력**: `<채널 outLong||outputFolder>/출판/<원고명>/` 에 `<제목>_내지.pdf` + `<제목>_표지.pdf`(표지 이미지
  첨부 시). press-ready(PDF/X-1a)는 Ghostscript 필요라 옵션(기본 off — 부크크·교보는 일반 PDF 입고 가능).
- 테스트: `npm run test:book` = book-parser.test(44단언) + book-pdf.smoke(헤드리스 PDF) + book-ui.smoke(Playwright
  _electron E2E: 부팅→로드→미리보기 조판→클릭-편집→.md 저장 왕복).
- ⚠ **deps 변경**(@vivliostyle/cli·core, markdown-it) → **이 릴리스는 설치파일 재배포 필요**(라이트업데이트는
  "재설치 필요" 안내가 뜸). markdown-it 은 현재 미사용(인라인 렌더 자체 구현) — 향후 확장용.
- ⏳ **실측 필요**: 부크크 규격체크 도구에 실제 내지·표지 PDF 업로드 1회 확인(사용자). 대형 원고(300p+)의
  미리보기 렌더 속도(renderAllPages) 체감 확인.

## 🎬 Wan i2v 영상 엔진 추가 (2026-07-02)
- 영상 엔진 드롭다운에 **`Wan (i2v)`** 추가 (Grok/Flow/LTX2.3/**Wan**/없음). LTX·Wan **병행** — 각자 워크플로 슬롯.
- 구조: `comfy` 영상 엔진은 워크플로 JSON 종속이 없는 범용 i2v 라, Wan 은 **다른 워크플로만 갈아끼움**. `videoEngine==='wan'`
  이면 `runComfyVideos(...,{wan:true})` 가 cfg 를 `{workflowPath:wanWorkflowPath, videoFps:wanFps, videoMaxSec:wanMaxSec, timeoutSec:wanTimeoutSec}`
  로 오버라이드. 캐시 태그 `comfy-wan`. **클라우드(comfy.org) 설정(cloud/apiKey)은 그대로 물려받음** — Wan 도 클라우드에서 동작.
- comfy-config: `wanWorkflowPath`·`wanFps(16)`·`wanMaxSec(0=videoMaxSec)`·`wanTimeoutSec(0=timeoutSec)`·`videoFps(0=초)` 추가.
- **클라우드 Wan = 단일 API 노드 `Wan2ImageToVideoApi`** (예: `D:\Priming\Wan2.7 I2V\api_wan2_7_i2v.json`).
  로컬 Wan(CLIPTextEncode+WanImageToVideo.length 프레임)과 다름 → 엔진이 둘 다 처리:
  - 프롬프트 주입(`_buildGraph`): CLIPTextEncode.text 없으면 **`model.prompt`/prompt/positive** 키 자동 주입(부정 프롬프트 보존).
  - 길이 주입(`_setVideoDuration`): 필드명으로 초/프레임 자동 판별 — **`model.duration`/duration/seconds=초**, `length`/`num_frames`=프레임(초×videoFps).
    클라우드 Wan 은 model.duration(초)라 fps 무시됨. 로컬 Wan 은 fps=16 로 프레임 변환.
- **워크플로 2종 — 노드 종류로 유·무료 갈림**:
  - ❌ `Wan2ImageToVideoApi`(API 노드) = comfy.org 가 파트너(Alibaba) 유료 API 를 중계 → **API 크레딧 과금** + 헤드리스 REST 는 계정 인증 필요
    (`_queue` 가 `extra_data.api_key_comfy_org` 전달로 "Unauthorized" 대응). 길이=`model.duration`(초), 프롬프트=`model.prompt`.
  - ✅ **로컬노드 워크플로**(`UNETLoader`+`WanImageToVideo`+`CLIPTextEncode`+`KSamplerAdvanced`+`SaveVideo`) = 클라우드 GPU 에서
    직접 실행 = **구독 GPU 시간만(추가 크레딧 X)**. 예: `D:\Priming\Wan2.2 I2V\video_wan2_2_14B_i2v.json`(Wan 2.2 14B, 검증됨).
- **로컬노드 Wan 2.2 주입 특이점**(comfy-engine 이 처리, 실측 검증):
  - 프롬프트: CLIPTextEncode 가 **긍정/부정 2개** → `_buildGraph` 가 **제목(positive/negative)으로 긍정 노드 선택**(부정에 주입 금지).
  - 길이: `Float (Duration)`(초) 프리미티브 → 워크플로가 `floor(초×fps+1)`로 프레임 자동계산. `_setVideoDuration` 브랜치③ 이
    **제목으로 초/프레임 판별**해 Duration 엔 '초'를 넣음(프레임값 넣으면 폭발). WanImageToVideo.length 는 링크라 안 건드림.
  - 해상도: `WanImageToVideo` 의 width/height(640² 고정) → `_setVideoDims` 자동탐지②가 **width/height 리터럴 직접 주입**(16:9→832×480).
  - seed: 클라우드 API 노드가 int32 검증 → `_buildGraph` seed 를 **0..2147483647** 로 제한.
- ⏳ **실측 필요**: (API 노드) 출력이 `_scanMedia`/`_waitCloud` 로 잡히는지 · (로컬노드 Wan 2.2) 실제 생성 mp4/업스케일 완주 1회 확인.

## 🎵 '플리' 모드 — ACE-Step 음악 생성 (2026-06-30)
- 모드 토글이 **롱폼/쇼츠/🎵플리** 3-way. 라벨에서 "16:9·9:16" 표기 제거. 플리는 ACE-Step(ComfyUI) 음악 전용.
- **흐름**: 클로드가 채팅에서 **플리 스펙(.md)** 생성 → 앱 「🎵 플리 스펙 열기」로 로드 → ⚡ 음악 전체 생성 →
  곡마다 **로컬 ComfyUI ACE-Step API**(comfy-engine `textToAudio`) → `<출력폴더>/플리/<스펙명>/NN_제목.mp3`. TTS·자막·.vrew 없음.
- 스펙 형식: `core/parsers/playlist-parser.js`(`## NN · 제목` + tags/스타일 · lyrics/가사 · length/길이). 예시: `docs/플리-스펙-예시.md`.
  연주곡 = 가사 빈값/`(instrumental)`/`(연주곡)`. `instrumental` 태그는 tags 에 포함.
- ComfyUI: `comfy-config` 에 `audioWorkflowPath`·audioTagsNodeId·audioLyricsNodeId·audioDurationNodeId·audioTimeoutSec 추가.
  comfy-engine `_buildAudioWorkflow`(tags→TextEncodeAceStepAudio.tags, lyrics, seconds 자동탐지) + `_waitAudio` + `_downloadAudio`(출력 확장자 보존).
  ⚙ Comfy 모달에 "🎵 음악(ACE-Step)" 섹션. **이미지(Krea2)·영상(LTX)와 같은 REST 패턴** — 워크플로 JSON 1개만 "저장(API 포맷)"으로 지정.
- main.js: `S.modes.playlist` 큐 추가, `normMode`(3-way), `currentDTO`/`playlistDTO`, IPC `open-playlist-spec`·`make-playlist`,
  워크스페이스 직렬화/복원 3-way. 플리는 .smproj 스냅샷 스킵(워크스페이스+스펙.md 가 진실). App.jsx `PlaylistView` + `isPl` 분기.
- ⏳ **실측 필요**: ACE-Step "API 포맷" 워크플로 JSON 이 있어야 실제 생성됨(없으면 graceful 에러). 노드 자동탐지 구조는 실제 워크플로로 1회 검증 필요.
  미설정/실패는 이미지·영상과 동일하게 {success:false} 로 안전 처리. 음악 품질 검증(ACE-Step 1곡) 후 워크플로 export 가 선행.
- 🎬 **플리 배경(무한루프 영상)+.vrew (2026-07-01, v0.1.47)**: 음악만 만들던 플리를 **영상화**까지. PlaylistView 헤더
  「🎬 배경+vrew」(IPC `make-playlist-video`). 흐름: 스펙 `> 배경:`(없으면 컨셉/제목) → Krea2 이미지(16:9)
  → LTX i2v 짧은 클립 → **부메랑(정방향+역방향)으로 seamless** → 곡마다 곡 길이(+1s)로 stream-copy 루프
  (`core/playlist-video.js` makeBoomerang/loopBoomerangTo, ffmpeg=media-utils.getFfmpegPath). 그 뒤
  `buildPlaylistProject`(곡=그룹[배경 루프 videoPath]+문장[곡제목=자막, mp3=오디오], aspect 16:9) → `P.buildProjectVrew`
  로 `<출력>/플리/<제목>.vrew`. 배경 1개 공통 + 곡 제목 자막(YouTube 챕터·단속 차별화). vrew-builder 는 16:9+videoPath
  면 영상 트랙 사용(line 844, 비율 무관). 영상 실패 시 이미지 배경으로 폴백. playlist-parser 에 bgPrompt 파싱 추가.
  ⏳ **실측 필요**: Vrew 에서 .vrew 정상 로드(배경 영상 트랙·곡별 길이 매칭)·LTX 480p 배경 화질(필요 시 업스케일 추가).

## 🎵 대본 지정 BGM 프롬프트 + BGM 기본 ON (2026-07-04, v0.1.72)
- **대본에 배경음악 프롬프트**: 메타 줄 `> 🎵 배경음악: <영문 ACE-Step 태그>`(배경음악/배경음/BGM, `·`·`|`·개행 앞 정지).
  longform-parser·cut-script-parser 가 파일 전체 스캔 → `proj.bgmMood`. **쇼츠는 3편 공통**(파일 상단 1줄).
  ⚠ cut 파서는 루프에서 `meta=parseMeta()` 재할당되므로 bgmMood 는 **별도 지역변수**로 보관(meta 에 안 얹음).
- **우선순위**: `deriveBgmMood` = 작업바 moodOverride → **proj.bgmMood(대본 지정)** → Ollama → Gemini → 기본값.
- **BGM 기본 ON**: App.jsx `bgmOn` 기본값 false→**true**(저장된 설정에 값 있으면 존중). 음악 서버 미설정이어도 graceful(로그만).
- 가이드: `docs/대본-작성-가이드.md` 공통원칙 #4 + 롱폼/쇼츠 템플릿에 배경음악 줄 추가.

## 🎵 롱폼/쇼츠 BGM(ACE-Step 배경음) 자동 삽입 (2026-07-01, v0.1.50)
- 작업바 「🎵 BGM」 체크 + 무드칸(빈값=대본 자동). ⚡만들기 시 **3.5단계**(비디오 뒤·.vrew 앞, main.js runMakeAllCore)에서
  편별 총길이(=ttsDurationSec 합) 계산 → `deriveBgmMood`(대본 무드 → ACE-Step 태그: **moodOverride 우선 → Ollama → Gemini →
  기본값** `calm, cinematic, ambient, soft piano, slow tempo, warm, instrumental`) → `comfy-engine.textToAudio`(≤180s 생성)
  → `media-utils.loopAudioTo`(총길이로 재인코딩 루프) → `pr._bgmPath`.
- **음악만 로컬 서버**: `audioBaseUrl` 있으면 그 주소로(cloud:false), 이미지/영상은 클라우드 그대로 (플리 패턴 재사용).
- .vrew 삽입: 4단계 ep 에 `bgm:{enabled,audioPath,volume(기본0.15),loop}` 부착 → `pipeline.buildProjectVrew` 가 `opts.bgm` 전달
  → **`vrew-builder.addBgmTrack`**: AVMedia(sourceOrigin USER, sourceFileType ASSET_AUDIO) 등록 + `videoAudio` 타입 트랙
  (volume 0.15, loop, sourceOut=총길이, assetEffectInfo.startDelay 0) + asset(role sub) → **clip[0] 에만 링크**(AI고지 절대배치와 동일).
- UI(App.jsx): bgmOn/bgmMood 상태 + capbar 컨트롤 + make args `bgm`. 💾 재export 경로는 미포함(⚡만들기 전용). 롱폼·쇼츠 공통.
- ⏳ **실측 검증 필요(중요)**: Vrew 가 **독립 오디오 BGM 트랙**에 기대하는 정확한 JSON 미확정 — `videoAudio` 는 원래 영상에 묶인
  타입이라 best-effort. Vrew 에서 BGM 이 안 들리면 **BGM 수동 삽입한 .vrew 샘플을 받아 files/tracks/assets 필드 확정**
  (자막·제목·도형 잡던 "사용자 .vrew 분석" 방식). 계획서: `~/.claude/plans/ace-step-prancy-planet.md`.
- 🔜 **SFX(효과음)**: 이번 범위 제외. ACE-Step 은 음악 전용이라 효과음 불가 → 추후 로컬 SFX 라이브러리 + 대본 마커([효과음: …])
  + `addSfxTrack`(startDelay 타임스탬프)로 별도 구현 예정.

## 🐞 쇼츠 9:16 .vrew = 화면비 가로로 표시 + 내보내기 실패 — 실제 원인 2가지 (2026-06-24)
> ⚠️ 이전의 "videoRatio 역수" 진단은 **오진**이었음. videoRatio 는 화면비와 무관한 상수(정상 .vrew 는
>   16:9·9:16 모두 **0.5625**). 사용자 정답본(make1.vrew = 프로그램 .vrew 를 9:16 으로 직접 수정) 정밀 비교로 확정.

**① 화면비가 9:16 이 아니라 16:9(가로)로 열림 — 원인: 오버레이 트랙 X좌표계**
- Vrew 는 videoSize/videoRatio 가 아니라 **web/shape(제목·도형·AI고지) 트랙의 xPos/width 좌표계**로 캔버스
  화면비를 인식한다. 우리는 이 좌표를 16:9 기준 0..1(예 제목 width 0.96)로만 써서 Vrew 가 가로로 펼침.
- 수정(vrew-builder.js buildVrew 끝 "2.7"): web/shape 트랙만 xPos/width 를 **×_overlayScale**(중앙 보존).
  `_overlayScale = (16/9)×(canvasH/canvasW)` → **16:9=1.0(무변경=롱폼 안전) · 9:16=(16/9)²=3.1605 · 1:1=1.7778**.
  **미디어(video/image)는 그대로**(width≈1 + fillType:cut = 커버 — ×스케일 하면 캔버스에서 사라짐).
  **자막(transcript.clips[].captions[].style)도 그대로**(별도 좌표계). videoRatio 는 상수 **0.5625** 로 통일.
- 검증: make1 의 영상 z=1~3 은 ×3.16(자동변환 잔재)이지만 사용자가 직접 고친 z=0·z=4 는 `width=1+fillType:cut`.
  자막·도형·제목만 ×3.16. → 미디어 유지 + 오버레이만 스케일 = 9:16 + 미디어 보임 + 내보내기 정상(실측 확인).

**② 내보내기 자체가 실패(제목 텍스트가 원인) — 원인: 제목 web 트랙의 assetEffectInfo**
- 정상 .vrew 의 제목 web 트랙엔 `assetEffectInfo` 필드가 **없음**. 우리는 `assetEffectInfo:{type:'none',...}` 를
  넣어 둠 → Vrew 내보내기 실패(type:'none' 이라도 web/textbox 트랙에 있으면 안 됨).
- 수정(vrew-builder.js addTitleTrack ~585): `assetEffectInfo` 줄 **삭제**(필드 생략 = 즉시 표시). 실측 내보내기 성공.

## 라이트 자동 업데이트 — 변경 파일만 교체 (2026-06-23)
- 요청: 설치는 1회, 이후엔 바뀐 파일만 설치폴더에 받아 즉시 최신으로 실행. (기존 팝업→다운→인스톨→재시작 단계 제거)
- 구조: **asar 비활성**(package.json `build.asar:false` — 개별 파일 교체 가능) + `light-updater.js` + `scripts/gen-manifest.js`.
  - `light-updater.applyUpdates()` 가 **bootstrap.js 에서 `require('./main.js')` 하기 전에** await 호출 →
    GitHub(main) 의 `update-manifest.json`(파일별 sha1)과 설치폴더 파일 해시 비교 → 다른 파일만 raw URL 로
    받아 원자적 교체(tmp→rename) → 그 실행이 곧바로 최신. 오프라인/실패면 조용히 현재 버전 진행. dev 는 건너뜀.
  - **deps(node_modules) 변경**은 파일 교체로 불가 → manifest.deps 해시 다르면 적용 멈추고 "재설치 필요" 안내(앱 ready 후 dialog).
    즉 package.json dependencies 바꾼 릴리스는 설치파일 재배포 필요. 그 외 JS/렌더러 변경은 전부 이 방식.
  - 오래된 렌더러 에셋(이전 빌드 해시) 자동 정리(manifest 에 없는 renderer/dist/assets 삭제).
- 발행: 코드 변경 후 **`npm run update:publish`**(vite build + gen-manifest) → `update-manifest.json`+변경파일 **git commit + push**.
  raw.githubusercontent.com 은 public repo 라 토큰 불필요(CDN 캐시 ~5분).
- .gitignore: `dist/`→`/dist/`(루트 설치산출물만 무시). **renderer/dist 와 update-manifest.json 은 커밋 대상**(raw 로 받기 때문).
- 기존 `auto-updater.js`(electron-updater/NSIS)는 bootstrap 에서 호출 제거(파일은 남김). electron-updater 의존성은 미사용.

## AI 고지 = 양쪽 모드 선택형, 기본값만 모드별 (2026-06-23)
- 요청: 롱폼·쇼츠 **둘 다 선택 가능**. 강제 아님. 기본값만 다름 — **롱폼 표시 ON / 쇼츠 미표시 OFF**.
- main: `forceAiNoticeIfLongform` → **`resolveAiNotice(preset, want)`**. 모드 무관하게 `enabled:!!want`(사용자 선택).
  롱폼은 켜질 때만 5초후 5초 타이밍 적용, 쇼츠는 preset 타이밍 유지. export-vrew·runMakeAllCore 4단계·run-batch
  (s.aiNotice) 호출부가 `aiNotice` 전달. run-batch 는 `clipMaxOf(videoEngine)` 도 전달(쇼츠 재구성).
- 렌더러: capbar 'AI 고지' 체크박스 **양쪽 모드 노출**. 기본값은 preset 로드 effect 에서 `setAiNotice(mode==='longform')`
  로 세팅(롱폼 true/쇼츠 false). make/export 가 `aiNotice` 그대로 전달(강제값 제거). currentSettings/applySettings 에 포함.
  채널편집 모달 체크박스는 안내문만(실제 표시는 작업바 토글이 결정).

## Flow 창 닫기 + 이미지 미생성 시 .vrew 차단 (2026-06-23)
- **Flow 크롬 창 마무리**: 기존엔 S.flowEng 를 재사용하려고 앱 종료(before-quit) 때만 닫아 작업 끝나도 창이 남음.
  → `closeFlowEng()`(main.js, `_closeContextAndCleanup` 호출) 추가 + runMakeAllCore 끝 / image-build / video-build
  종료 시 호출. 재사용은 한 번의 실행 안에서만, 끝나면 창 닫음. (다음 실행은 getFlowEng 가 새로 띄움)
- **이미지(쇼츠는 영상) 미생성 그룹 있으면 .vrew 생성 차단 + 팝업**: `missingVisualGroups(pr)` =
  imagePrompt 있는데 imagePath·videoPath 둘 다 없는 그룹. 있으면 그 편 .vrew 를 건너뛰고
  `warnIncompleteVisuals()` 가 `dialog.showMessageBox` 로 어느 편 G몇이 빠졌는지 팝업. runMakeAllCore 4단계 +
  export-vrew(💾) 양쪽 적용. (쇼츠는 영상이 있으면 이미지가 있었던 것 → image|video 둘 중 하나면 OK)
- 참고: vrew-builder 의 `Image file 수 0 < 이미지 그룹 수 N` self-check 경고는 **이미지 파일 개수만** 세서
  영상으로 대체된 그룹을 못 알아보는 false alarm(경고 로그일 뿐, 차단 아님). 실제 차단은 위 main 게이트가 담당.

## 전체만들기 = 단계별 일괄 처리 (2026-06-23)
- 요청: 대본 1개(쇼츠1·2·3)를 **하나의 덩어리**로 — 쇼츠마다 TTS→이미지→영상→vrew 를 끝내고 다음으로
  가지 말고, **전 쇼츠 TTS(쇼츠1 1그룹~쇼츠N 마지막)→ 전 쇼츠 이미지 → 전 쇼츠 영상 → 전 쇼츠 .vrew** 순서로.
- `runMakeAllCore`(main.js) 재구성: `projects` 한 번 필터 후 1·2·3·4단계 루프로 분리. 각 단계는 전 쇼츠를 돈다.
  - 1단계 TTS: fillTts + (쇼츠 모드면) `mergeGroupsByTts(clipMaxSec)` 그룹 재구성까지 — tts-build 와 동일.
    이제 make-all 이 TTS 버튼 없이도 그룹 재구성을 포함(원클릭 정합성). clipMaxSec 는 renderer makeAll 이 전달.
  - 부수효과: 단계 완전 순차 → ComfyUI(로컬 GPU) 이미지와 OmniVoice TTS 가 안 겹쳐 VRAM 충돌 자동 해소
    (예전 comfy 전용 순차 분기 제거). 트레이드오프: Genspark/Flow 의 'TTS∥이미지' 동시 실행은 사라짐(의도).
  - 중단(S.abort): 단계 사이/내부에서 멈추되 **4단계 .vrew 는 항상 실행**(만들어진 자산으로 best-effort 패키징).
  - opts 에 `clipMaxSec` 추가. run-batch(큐)도 shortsNum 필터만 다를 뿐 동일 경로라 영향 없음.

## 버그픽스 2건 (2026-06-23)
- 🐞 **쇼츠2부터 이미지가 Genspark→Flow 로 빠짐**: 원인은 Genspark 서비스 한도가 아니라 **앱 자체 캡**
  (`core/genspark-accounts.json` dailyCap=45)에 오늘 카운트 45 도달 → `activeAccounts()` 빈 배열 → 순환이
  Flow 로 폴백(main.js:548). 사용자 의도 = "Genspark 일일한도는 알 수 없고, Genspark 가 보내는 휴식/한도
  **메시지**(genspark-engine `_detectLimitMessage`)를 감지했을 때만 Flow 전환". → **Genspark 캡을 무제한으로**:
  `core/accounts.js` 에 `defaultCap` 인자 + `dailyCap<=0` = 무제한(pickActive/activeAccounts 항상 활성, setCap 0 허용),
  `genspark-accounts.js` 가 `makeAccountStore('genspark','gs',0)` 로 호출. 기존 상태파일도 dailyCap:0·count 초기화.
  UI: Genspark 계정 모달 "0=무제한" 안내 + "오늘 N/무제한" 표시. (tts/genspark-store 의 maxDaily 는 이미 0=무제한)
- 🐞 **쇼츠2 Grok 영상 실패 후 .vrew 만들고 다음 쇼츠로 + G1 "영상 변환 중" 고착**: `start()` 의 첫 진입이
  `waitUntil:'networkidle'`(grok-engine.js:302)이라 grok.com SPA 에서 30초 타임아웃(특히 Flow 크롬 동시 실행 시).
  게다가 `await this.start()` 가 generateVideoFromImage 의 try **밖**이라 예외가 그대로 위로 던져져 → 그룹 status
  가 'generating' 에 고착 + make-all(main.js:1332)이 삼키고 다음 쇼츠로. → ① 302 를 `'load'` 로(399 와 동일 정책)
  ② start() 를 try 안으로 이동(실패도 {success:false} 반환) ③ pipeline `generateHookVideosGrok` 에 **그룹별 1회
  자동 재시도**(실패 시 eng.stop() 후 재진입) + try/catch 로 항상 'done'/'fail' 종결(배지 고착·전체중단 방지).

## ComfyUI 클라우드(comfy.org) 지원 (2026-06-23)
- RunPod 설치/GPU 할당이 어려워 **comfy.org 공식 클라우드**로 전환 옵션 추가. 로컬/RunPod 경로는 그대로.
- comfy-config: `cloud`(bool)·`apiKey` 필드 추가. cloud=true 면 baseUrl 이 로컬이면 `https://cloud.comfy.org` 로 보정.
- comfy-engine: 클라우드 분기 — ① 모든 경로에 `/api` 접두(`_url`) ② 모든 요청에 `X-API-Key` 헤더(`_headers`)
  ③ health=API키 존재확인 ④ 폴링을 `/history/{id}` 대신 `/api/job/{id}/status`(상태) + `/api/jobs/{id}`(outputs)
  로 전환(`_waitCloud`) ⑤ `/api/view` 는 서명 URL 302 → fetch 자동추적. 업로드/큐/다운로드/이미지·비디오 wait 공용화(`_scanMedia`/`_extractOutputs`).
- UI(App.jsx ⚙ ComfyUI 모달): '클라우드 모드' 체크박스 + (체크 시) 'API 키' 입력란. 워크플로는 동일하게 '저장(API 포맷)' JSON.
- 플랜: API 접근은 Standard($20)+. 직접 학습 LoRA 가져오기는 Creator($35)+. 현재 사용자는 기본 모델만 → Standard 가정.
- ⏳ **실측 검증 필요**: `/api/jobs/{id}` 의 outputs 구조·`/api/view` 동작은 실제 키로 한 번 돌려 확인(엔진 셀렉터 반복법과 동일).
  응답 구조가 다르면 `_extractOutputs`/`_scanMedia` 만 보정하면 됨(로그에 응답 일부 출력하도록 처리됨).

## 최근 버그픽스 (2026-06-17)
- 🐞 **Genspark 이미지가 UI에 안 붙음**: `generateImagesGenspark`는 `g.imagePath`를 정상 매핑하지만,
  make-all/image-build가 중간에 `pushDtoUpdate()`를 안 보내 작업 끝까지 썸네일이 안 보였음(Flow만 실시간).
  → make-all에서 `Promise.allSettled` 직후 + 영상 후, image-build/video-build 각 쇼츠 후 `pushDtoUpdate()` 추가.
  `generateHookVideosGrok`에 `onProgress` 콜백 추가 → 그룹별 영상 완성 시마다 UI 갱신(main이 pushDtoUpdate 전달).
- 🐞 **Grok 영상 완료 감지 실패(화면 구성 변경)**: `downloadButton` 셀렉터(`div.absolute.-right-14 button:nth-child(4)`)가
  새 UI와 안 맞아 `dlEnabled`가 계속 false → 실제 https videoUrl을 41초에 잡고도 5분 타임아웃 fallback까지 대기.
  → grok-engine 폴링을 **다운로드 버튼 비의존**으로 변경: 실제 https videoUrl + 비디오 ready(readyState≥2 & dur>1)가
  **2회 연속(≈10초 안정)** 잡히면 즉시 URL 직접 다운로드. 다운로드 버튼은 blob/실패 시 폴백으로만 사용.

## 최근 기능 추가 (2026-06-17, v0.1.5)
- **도형 위치·크기 고정**: vrew-builder `addShapeTrack` 폰트기반 계산 제거 → 사용자 .vrew 분석값 고정
  (xPos 0 / yPos 0.012 / width 1 / height 0.203). 배경 도형 **기본 ON**(parser `proj.bgEnabled=true`).
- **그룹 8초 재구성**(`P.mergeGroupsByTts`, IPC `merge-groups`, UI 🔗합치기 + **TTS 후 자동**): v0.1.9에서
  **문장 단위 그리디 패킹**으로 변경 — 모든 문장을 순서대로 8.0초 캡으로 다시 묶음. **큰 그룹은 쪼개지고
  (예 훅강화 12.6s→2그룹) 작은 그룹은 합쳐져** 결과 그룹 전부 <8.0초. 단일 문장>8초면 단독. 첫 문장 원본
  그룹의 phase/프롬프트 보존. tts-build 가 음성변환 직후 자동 호출(`pushDtoUpdate`). 워크플로우: TTS(자동 재구성)
  → 내보내기/가져오기로 프롬프트 → 이미지.
- **이미지 프롬프트 내보내기/가져오기/API**(`core/prompt-io.js`, IPC export-prompts/import-prompts/
  generate-prompts-api, UI 📤내보내기·📥가져오기+모달): PrimingFlow 포팅. 편-그룹 복합 라벨 `## [쇼츠-그룹]`로
  3편 한 번에. 안전치환(PROMPT_SAFE_RULES) 포함. API는 secret-store 키(gemini/claude/openai).
- **음성 배속을 MP3 에 직접 굽기 (Vrew 배속 폐기)**(v0.1.10): Vrew playbackRate 방식은 폐기.
  대신 `fillTts` 가 정속 WAV 합성 → ffmpeg `atempo=배속(기본1.15)` 로 **배속 MP3** 생성(피치 유지),
  `ttsDurationSec = 원본/배속`. 8초 그룹·.vrew 모두 이 배속 MP3 사용. vrew-builder `_playbackRate` 기본 1.
  capbar '음성배속'(기본 1.15) → ttsBuild/make-all 의 speed → fillTts(speedFactor). 검증: 2.30s→2.04s MP3,
  vrew playbackRate 전부 1.
  - 🐞 **(v0.1.11) 설치판 배속 미적용 버그**: pipeline.js 의 ffmpeg-static 경로가 asar 보정 없어 패키지 앱에서
    실행 실패 → atempo 실패 → 정속 WAV 폴백(평상시 속도로 들림). vrew-builder 처럼 `app.asar`→`app.asar.unpacked`
    보정 추가로 해결. fillTts 시작 시 ffmpeg 사용가능 여부 로그(🔊 적용 / ⚠ 미적용).

## 긴 문장 자동 분리 (2026-06-17, v0.1.16)
- 한 줄/컷에 마침표로 끊기는 문장 여러 개가 붙어 있으면(예 CTA 17.4초) 8초 그룹이 못 쪼개 초과 → 해결.
- 파서 `splitNarration(text, maxChars=28)`: ① `.?!。` 로 문장 분리(종결부호 유지) ② 그래도 28자(유효
  글자) 넘으면 쉼표로 절 분리. buildProjectModel(cut)·buildProjectModelGrouped(prose/grouped) 둘 다 적용.
  TTS 전 분리라 각 문장이 별도 음성 → 오디오 자르기 불필요. 이후 8초 패킹이 ≤8초 그룹으로 묶음.
- 검증: 17.4초 CTA → 4문장(~3-5초)으로 분리. 회귀 407단언 통과.

## 줄글(prose) 대본 형식 추가 (2026-06-17, v0.1.8)
- 신규 3번째 형식: `## 쇼츠 N` 안에 `제목:`(2줄) + `[훅]`/`[본론 심화]`/`[CTA]` 같은 **대괄호 그룹 헤더** + 그 아래 줄들=문장.
  `★CTA:` 같은 별표 라벨도 그룹 헤더로 인식. **이미지 프롬프트 없음** → TTS→합치기→내보내기/가져오기로 프롬프트 생성.
- 파서: `parseShortsBlockProse` + `buildProjectModelGrouped` 재사용. 감지 `format='prose'`(① 없고 `[..]`+`제목:` 있으면).
  proj.titleLine1/2 자동 세팅. 검증: 0617 파일 = prose 3편·6그룹·16/17/15문장, 회귀 407단언 통과.
- 테스트: prose 는 imagePrompt 단언 생략(없는 게 정상).

## Flow 이미지+영상 통합 (2026-06-17, v0.1.7 — i2v 첨부는 반복 보정 중)
- 이미지 엔진 select=**Google Flow** 선택 시 이미지·영상 **모두 Flow**로 (Grok 대신). Genspark 선택 시 이미지=Genspark/영상=Grok 유지.
- 엔진은 이미 이미지/동영상·비율·매수·모델(Veo) 선택 지원(`_configureSettings`/`_selectModel`/`_clickTab`). run()에
  **`frameImages` 옵션** 추가 → 영상 모드에서 단락마다 소스 이미지를 프레임/애셋으로 첨부(i2v).
- 신규 `_attachFrameImage(imagePath,num)`(flow-engine): **best-effort** 셀렉터(프레임/애셋 버튼+`input[type=file]`)
  + 실패 시 `_dumpFrameAttachUI()`로 버튼/입력 후보 로그 → **실제 로그 보고 셀렉터 고정 예정**(Grok/자막과 동일 반복법).
- main `runFlowVideos(project,mediaDir,logger,{videoCount,model,count})`: 앞 N개(이미지 있는) 그룹 → eng.run(video,
  frameImages) → 출력 .mp4 매핑. video-build/make-all 이 engine==='flow'면 이 경로, 아니면 Grok.
- UI: 헤더에 `#flowVideoModel`(Veo 3.1 Lite/Fast/3.1)·`#flowCount`(1x~4x) — 엔진=Flow일 때만 노출(`_toggleFlowOpts`).
- ⏳ **검증 필요**: 실제 Flow 영상 UI에서 프레임/애셋 첨부 셀렉터. 사용자가 한 번 돌려 `[i2v DUMP]` 로그 주면 고정.

## PrimingFlow와의 관계 (중요)
- 엔진(flow/genspark/grok-engine, vrew-builder, tts, video-renderer, anti-detect, project-model)은
  `D:\PrimingFlow\rebuild` 에서 **복사**해 사용. 수정 시 원본과 갈라짐을 인지할 것.
- 차이: PrimingFlow는 텍스트→자동 문장분리/그룹화/프롬프트생성. 본 앱은 **컷이 이미 확정**되어
  파서가 Sentence/Group을 직접 만들고 그 단계들을 건너뜀.
- 설정/상태 디렉토리: PrimingFlow=`~/.flow-app/`, 본 앱=**`~/.shots-maker/`** (절대 공유 금지).

## 실행
```powershell
cd D:\Priming
npm install   # 첫 실행 시
npm start     # → electron .
```

## ⚠[폐기 2026-08-22] 대본 형식 (입력 계약) — 쇼츠 2형식 (core/cut-script-parser.js 는 삭제됨. 현행 형식은 docs/대본-작성-가이드.md)
공통: H1 제목 + `>` 메타(목소리/9:16) + `## 쇼츠 N`(편) + `- 훅 자막(첫 프레임): ...`.
감지: 본문에 `- 음성/자막:` 또는 `**…그룹 N`이 있으면 **신규(grouped)**, 없으면 **구(cut)**.

▸ **신규(grouped) — 권장** (0611이 이 형식):
```
**🎬 그룹 1 ｜훅 (이미지 → 비디오)**       ← 그룹 헤더: 번호·단계·모드
- 음성/자막:
  - 문장1
  - 문장2                                  ← 그룹당 문장 여러 개
- 🖼️ 이미지: `image prompt`
- 🎬 → 비디오(I2V): `i2v video prompt`     ← 🎬 그룹(훅·절정)만
**🎞️ 그룹 2 ｜본론 (이미지 + 모션)**
- 🖼️ 이미지: `...`
- 🎞️ 모션: slow zoom-in …                  ← 켄번스 힌트(영상 아님, group.motionNote)
```
단계: 훅/본론/재훅·심화/절정 직전/CTA. 모드: "이미지 → 비디오"(I2V) / "이미지 + 모션".

▸ **구(cut) — 하위호환**: `① (훅) 나레이션` + 다음 줄 백틱 이미지 프롬프트 (컷=문장=그룹 1:1:1).
- **(2026-06-17 추가) 컷별 비디오 프롬프트**: 이미지 백틱 줄 다음에 `🎬 \`video prompt\`` 를 쓰면
  그 컷이 Grok 영상이 될 때 **기본값 대신** 그 프롬프트 사용(+isI2V 표시). `🎞 모션설명`은 videoPrompt
  없을 때 폴백/켄번스. 파서 `parseShortsBlock`(🎬/🎞 줄 인식)→`buildProjectModel`(g.videoPrompt/motionNote).
  ⚠️ 영상이 되는 컷은 **영상 개수(앞에서 N개)** 로 정해짐 — 특정 컷을 영상화하려면 N이 그 컷을 포함해야 함.
- 검증: 0611=grouped, 0612~0619=cut. `node test/parser.test.js` 419 단언 통과.

## 데이터 매핑
- 그룹(또는 컷) → **Group**: imagePrompt(🖼️ 백틱 **그대로**), videoPrompt(🎬 I2V 백틱),
  phase, mode('i2v'|'motion'), isI2V, motionNote. 음성/자막 줄(또는 컷 나레이션) → **Sentence**(그룹에 다수 가능).
- `## 쇼츠 N` → Project 1개(aspect '9:16'). 한 파일 → Project N개 → .vrew N개.
- 영상: **사용자 입력 개수 N(헤더 🎬[N])만큼 앞 그룹부터** Grok 영상화(isI2V 고정 폐지, PrimingFlow 개수방식).
  모션 프롬프트 = group.videoPrompt(I2V) || motionNote || Grok 기본. 나머지 그룹은 이미지+켄번스.
  생성·삽입은 PrimingFlow 엔진(grok/flow/genspark)+vrew-builder 그대로(세로영상=비디오트랙).
- `splitIntoSentences`/`buildGroups` 미사용 (대본이 그룹·문장을 이미 명시).

## 핵심 모듈
- `core/cut-script-parser.js` — ★신규 파서 (가장 자주 수정).
- `core/project-model.js` — Sentence/Group/Project (PrimingFlow에서 복사, 무수정).
- `vrew/vrew-builder.js` — .vrew 4.0.1 생성 (복사). 형식 변경 시 PrimingFlow CLAUDE.md의
  "4.0.1 호환 변경표" 참조. 검증본: `D:\PrimingFlow\test.vrew` / `01.vrew`.
- `tts/` — OmniVoice(근간, 포트 9881, 중후한 남성)+Gemini 폴백.
- `flow-engine/genspark-engine/grok-engine.js` + `anti-detect.js` — 자동화·봇회피
  (일일한도 PER_PROFILE_DAILY_CAP=45, 상태는 `~/.shots-maker/anti-detect-state.json`).
- `main.js` — IPC 오케스트레이션(파서→TTS→이미지→비디오→편별 .vrew). 편별 N회 호출 래퍼 포함.

## 출력
- 1차: 편별 .vrew (Vrew에서 마무리). 2차: 편별 MP4(ffmpeg, video-renderer).

## 추가 기능 (최신)
- 썸네일 **✕ 삭제**(IPC clear-asset): 그룹 image/video 비움.
- 영상 개수 = **셀렉트(기본 '랜덤')**. 랜덤이면 쇼츠마다 1~min(3,그룹수) 무작위(main `resolveVideoCount`). 숫자 선택 시 그 값.
- 자막 옵션(위치/미세조정/정렬/크기) 변경 시 **미리보기 재생 중이면 즉시 반영**(applyCaptionStyle 재호출).
- 도형 가로 = **영상 전체 폭(width 1)**, 세로만 텍스트 크기 따라 가변.
- 비GPU PC: TTS 멀티엔진(OmniVoice 원격GPU / **Gemini API** / **Supertonic 로컬CPU 9882**) 이미 포함 →
  채널 engine을 gemini/supertonic으로 하면 GPU 없이 동작(PrimingFlow 구조 그대로).
  **Gemini API 키 입력**: ⚙ 채널편집 모달의 'Gemini 키' → IPC get/set-gemini-key → `tts/secret-store`('gemini'.key).
  gemini-provider.init()이 그 키를 읽음. 비GPU PC는 gemini 채널 선택 + 키 입력하면 음성 생성.
- 패키징: `npx electron-builder --win nsis --x64 --publish never` → dist/Shots-maker Setup 0.1.0.exe(+latest.yml).
  자동업데이트는 그 둘을 GitHub Releases에 올리면 동작(토큰 없이 드래그업로드 가능). repo: getwater-maker/Shots-maker(푸시됨).
- GitHub 자동업데이트: main에 `electron-updater` `checkForUpdatesAndNotify`(패키징 시만), package.json
  `build.publish`(github getwater-maker/Shots-maker) + `npm run dist`. 발행은 repo 생성 + GH_TOKEN 필요(수동).

## 구현 순서 (계획서: ~/.claude/plans/...0611-glistening-lagoon.md)
0. 이 CLAUDE.md 작성 ✅
1. cut-script-parser.js + node 단위 테스트 (0611 → projects 3·컷 5·imagePrompt 매핑)
2. PrimingFlow 모듈 복사 + package.json + 앱 부팅
3. 파서 → .vrew 직결(빈 자산)으로 출력 파이프라인 선검증
4. TTS 연결 → 5. 이미지 연결 → 6. 편별 .vrew 3개 완성
7. (2차) Grok 비디오, MP4 편별 렌더

## 디버깅 팁 (PrimingFlow에서 계승)
- 로그 `[Vrew] (4.0.1 호환)` 보이면 정상. `.vrew.debug.json` 옆에 생성됨 → test.vrew와 라인 비교.
- main process(엔진) 변경은 앱 완전 재시작 필요. ui/index.html은 Ctrl+R 반영.

## 진행 상황
- ✅ 0단계: CLAUDE.md 작성
- ✅ 1단계: `core/cut-script-parser.js` + `test/parser.test.js`. **2형식(grouped/cut) 자동 감지** 파서.
  실제 대본 9개 파싱, 단언 419개 통과. (0611=grouped: 5그룹·9문장·I2V 플래그, 나머지=cut)
- ✅ 그룹 형식 대응: 그룹당 문장 다수 → DTO sentences[], UI 문장목록·I2V/모션 배지·그룹시간,
  I2V 영상은 isI2V 그룹만(group.videoPrompt 사용). vrew-builder는 다중문장 그룹을 이미 지원.
- ✅ 2단계(부분): vrew 파이프라인 모듈 복사(vrew-builder, long-sentence-splitter, media-utils,
  vrew-template.json, vrew/dummy) + package.json + `npm install adm-zip ffmpeg-static`.
  (electron/playwright/wavesurfer 미설치 — 엔진·UI 단계에서 설치)
- ✅ 3단계: `build-shorts.js` 헤드리스 CLI — 파서 → (DRY: ffmpeg 무음) → vrew-builder 편별 호출.
  0611 대본 → 편별 .vrew 3개 생성 검증 완료. videoRatio 0.5625/1080×1920, 자막에 실제 나레이션
  박힘, .vrew=유효 zip(project.json+mp3). **남은 검증: 사용자가 Vrew 4.0.1에서 직접 열어보기.**
- ✅ 4단계: 실제 TTS 연결. tts/ 모듈 복사(파이썬 백엔드 제외), build-shorts.js에 OmniVoice 연결.
  기본 프리셋 "역사이야기"(중후한 남성, ref=02_저음 2단계.wav, speed 1.2, seed 5697) 재사용.
  프리셋의 captionStyle·aiNotice·disableLongSplit도 .vrew opts로 전달 → 채널과 동일한 자막/AI고지.
  0611 쇼츠1 실측: 5컷 합성(4.0/3.68/3.52/3.36/5.70s), wav→mp3 5/5, AI고지 자막 삽입, .vrew 생성 OK.
- ✅ Electron UI 셸: electron 33 설치, `bootstrap.js`(엔트리, userData=~/.shots-maker/electron 격리)
  + `preload.js`(contextBridge api) + `main.js`(창+IPC, 권위 데이터 S 보유, DTO 전달)
  + `ui/index.html`(모닝커피 팔레트, 편 카드·컷별 표시·전체/편별 TTS·.vrew 버튼·로그콘솔).
  공유 로직은 `core/pipeline.js`로 추출(CLI build-shorts.js와 공용). `npm start` 부팅 성공(4프로세스).
  IPC: list-presets / open-script / tts-build({shortsNum,dry,presetName}) / export-vrew / open-folder.
- ✅ 5단계: 이미지 연결 (Genspark + Flow 둘 다). 엔진 복사(genspark-engine, flow-engine, anti-detect,
  style-store, image/), playwright 설치(시스템 Chrome 사용, 브라우저 다운로드 생략).
  - Genspark: `pipeline.generateImagesGenspark()` — `generateImagesBatch({prompts,outputPaths})`에
    group.imagePrompt 그대로 투입, `_aspectRatio='9:16'`, 결과를 group.imagePath에 매핑. 출력 `쇼츠N_images/cutM.png`.
  - Flow: `main.js runFlowImages()` — FlowAutomator.run({paragraphs, customPrompts=imagePrompt 그대로,
    ratio '9:16'}) 이벤트 기반, win 필요. images/NN*.png(num=01..)를 group에 매핑. 출력 `쇼츠N_flow/images/`.
  - 브라우저 엔진은 `~/.flow-app` 프로필/anti-detect 재사용(기존 로그인 유지 + 일일한도 정확 공유).
  - IPC `image-build({shortsNum, engine})`, UI: 이미지 엔진 select + 🖼 전체/편별 버튼.
  - ⚠️ main process 변경 → **앱 완전 재시작 필요**. 실제 생성은 브라우저+로그인 필요(사용자 검증).
  - 🐞 **함정(해결)**: Genspark는 `chromium.launchPersistentContext`로 **Playwright 내장 Chromium**을 씀
    (시스템 Chrome 채널 미지정). 그래서 `npx playwright install chromium` 필수 — 안 깔면 "이미지 안 생성".
    (Flow는 channel:'chrome' 시스템 크롬). 현재 chromium-1223 설치됨(playwright 1.60.0).
  - UI: 로그콘솔에 **📋 복사** 버튼 추가(navigator.clipboard, execCommand 폴백).
- ✅ 6단계(부분): 훅 컷 Grok image-to-video. grok-engine.js 복사, `pipeline.generateHookVideosGrok()`
  — phase==='훅' & imagePath 있는 컷만 `generateVideoFromImage({imagePath,prompt,outputPath})`,
  `_aspectRatio='9:16'`(세로 6초). 결과를 group.videoPath에 매핑. 출력 `쇼츠N_video/cutM.mp4`.
  IPC `video-build({shortsNum})`, UI 🎬 전체/편별 훅영상 버튼. Grok은 X(트위터) 로그인 필요.
- ✅ vrew-builder 9:16 영상 지원: 기존엔 9:16이면 영상 무시(이미지만)였으나, **세로 영상이면 비디오
  트랙 사용**하도록 보정(`_useVideo = _aspect!=='9:16' || vertical`). Grok 세로영상 → .vrew 훅 컷 애니메이션.
  ffmpeg 1080x1920 합성영상으로 비디오트랙 삽입 검증 완료.
- ✅ UI: 컷마다 들어간 **이미지/비디오 파일명 표시**(🖼/🎬, prompt는 숨김). 로그 📋 복사 버튼.
- ⏭ 남음: MP4 편별 렌더(video-renderer 편별 N회 래퍼) — 선택.

## UI (index.html) 현재 구성
- 레이아웃: 상단 헤더 + 좌(본문 카드)/우(로그 패널) 2분할. (로그를 하단→우측 이동)
- 편별 그룹 카드: 헤더에 편 제목·그룹수·**합계시간**, 내부 그룹들은 **2열 그리드**(1,2/3,4/5,6, `.cuts-grid`).
- 그룹 행: **3배 썸네일(150×264)** + 헤더(G번호·단계배지·🎬I2V/🎞️모션 배지·그룹시간) + 문장 목록(각 ▶시간) + 자산명.
- 영상 썸네일은 **autoplay muted loop**로 실제 재생되어 보임. 썸네일 클릭 → 크게보기 모달(showPreview).
- **미리보기 재생 플레이어**(#player, playShorts): 편별 ▶미리보기 / 헤더 ▶전체 미리보기 →
  9:16 스테이지에서 그룹마다 이미지/영상 + 문장 TTS 오디오(media://) + 자막을 **순서대로 재생**.
  오디오 없으면 그룹시간/2.5s 타이머. DTO sentences에 audio(ttsAudioPath) 포함.
- 썸네일/자산 미리보기는 `media://<encoded-abs-path>` 커스텀 프로토콜로 로컬 파일 로드.
  🔴 **비디오 검정화면 버그**: `net.fetch(file://)`가 Range 요청에 ERR_UNEXPECTED → protocol.handle에서
  **Range 직접 처리**(fs.readSync 슬라이스 → 206 Content-Range)로 교체. 이미지는 200 전체. 비디오 미리보기 정상화.
  썸네일 클릭 → 모달 크게보기. 미리보기 음성은 IPC read-audio(base64).
- UI 정리: 배지 이모지 제거(I2V/모션), 제목 1줄 라벨 🔖 제거, capbar 안내문구 제거.
- 무음(DRY) 체크박스 = TTS 없이 무음으로 .vrew 구조 검증. 채널 select = 목소리·자막·AI고지 프리셋 선택.
- 작업목록 **3열 그리드**. 그룹 헤더줄(.narr) 배경색 + 본문(.sents) 사이 빈 줄. 자산 파일명 표시 제거.
- 썸네일 클릭 → **이미지/영상 첨부·교체**(IPC `attach-asset`, 파일 대화상자, 확장자로 image/video 판별).
- 로그창 = **우하단 작은 떠있는 창**(fixed, 330×230), 바 클릭으로 접기/펼치기(.collapsed).
- 채널 편집: 헤더 ⚙ → 모달(#chmodal). 속도/참조음성/참조텍스트/시드/AI고지 편집 → `save-preset`(preset-store.update).
- 자막 설정 바(#capbar): 크기·**상하위치(기본 '가운데'=-0.5)**·**미세조정 px 입력(+아래/−위, /1920)**·정렬·**🎤속도**.
  capOverride yOffset = base + px/1920. 💾 .vrew 시 `exportVrew({captionStyle})`, 🎤 TTS 시 `ttsBuild({speed})`.
- 🔴 **자막 위치/크기 — 사용자 .vrew 분석으로 확정**: 위치는 **클립별 `captions[].style`가 지배**
  (전역 globalCaptionStyle 아님). 가운데=`yAlign:'middle', yOffset:0`, 미세조정 N → `yOffset=N*0.0025`
  (예 80→0.2, +아래/−위). 좌우 가운데=`--textbox-align:'center'`. 폰트 `size`(기본 90, 옵션 25~300).
  vrew-builder CAPTION_STYLE 기본을 middle/0/center/size90 으로 변경. 검증: 가운데+80 → middle/0.2/90 일치 OK.
- 자막 줄 분할: `core/caption-splitter.js` — **공백무시 8자 + 쉼표에서 끊기**. vrew-builder가 이걸로 sub-clip
  생성(20자 algo 대체). DTO sentences[].lines = 편 전체 이어지는 넘버링(01|,02|…). UI에 번호+줄 표시.
- 미리보기: 이미지 그룹 **켄번스 CSS 애니메이션**, 영상 그룹 재생. 자막은 capbar 위치(yAlign middle, top%=50+yOffset*50),
  제목(훅 자막)은 **상단 고정**(#stageTitle, titleSize/titleColor).
- 출력경로(재설정): **`<채널 outputFolder>/<대본파일명>/`** + **쇼츠별 폴더 `media-N`(이미지+영상)·`tts-N`(음성)·
  `subtitles-N`(SRT)** + 루트에 `쇼츠N.vrew`. main `shortsDirs(outRoot,N)`. .vrew 생성 즉시 `shell.openPath`.
- 파일명: media-N/`{그룹2자리}.ext`(01.png,01.mp4), tts-N/`{문장num}.wav`. Flow는 임시폴더 생성→번호매칭(실패시 순서)
  복사. Flow 내부 vrew 빌드용 더미 `dummy-tts.mp3`(무음) 추가로 ENOENT 로그 제거.
- 이미지 스타일: `core/style-store.js`(28종) → 헤더 `#styleSel`. **PrimingFlow 방식: `<stylePrompt>, <대본 imagePrompt>`**
  (스타일을 앞). 채널편집 모달 `이미지 스타일` 사전설정(preset.styleId), 임의 변경 가능.
- Flow: **FlowAutomator 단일 인스턴스 재사용**(S.flowEng) — 매번 new 하면 같은 프로필에 크롬창 중복 실행/"비정상 종료".
  run()이 기존 브라우저 health-check 후 재사용. before-quit에서 context.close. (Genspark는 매 호출 stop()로 정상 종료)
- Flow **실시간 첨부**: 생성 중 work폴더 2.5s 폴링(`mapFlowImagesOnce` 멱등) → 새 이미지를 media-N/NN로 복사·그룹에
  즉시 매핑 → `win.webContents.send('dto-update', toDTO)` → 렌더러 `onDtoUpdate`가 DTO 교체·재렌더(썸네일 라이브).
  종료 시 순서폴백 최종 매핑.
- 🐞 **Flow 크롬창 누적/about:blank/이미지 안만들어짐**: 앱이 비정상 종료(force-kill 등)되면 Flow 크롬이 정리 안 돼
  프로필 락(Singleton*)이 남음 → 다음 실행 시 "복원하시겠습니까" + 빈 창이 쌓이고 Flow 진입 실패. 해결:
  `cleanChromeProfile(profileDir)`로 첫 실행 전 **Singleton 락 삭제 + Preferences exit_type=Normal/exited_cleanly** 세팅.
  before-quit에서 context.close. **남은 stray 크롬창은 사용자가 한 번 모두 닫아야 함**(락 점유 중이면 새 실행 충돌).
- 파일명(#ftitle)은 **자막설정 줄 왼쪽**(grow로 자막항목은 우측). 긴 이름은 줄임표+title 호버. 헤더는 버튼 한 줄.
- 자막 줄 글자수 **기본 7자**, UI(capbar #capChars)에서 조절. 글자수 카운트 = **공백·쉼표·마침표·느낌표·물음표 제외**(한글·영숫자만).
  export/make에 captionMaxChars 전달. 렌더러도 동일 splitLines로 넘버링 표시(문장별 시간표시 제거).
- 미리보기: 클립(7자)별로 자막을 TTS 길이에 비례해 순차 표시(stepCaptions). **전체 미리보기=편 사이 1초 검은화면**.
  그룹 시간 옆 **▶(단일 그룹 미리보기)** 버튼(data-prevgroup). 편별/그룹별/전체 재생(playProjects/playGroup).
- ⚡ 전체만들기/💾.vrew: 새 폴더구조·SRT·captionMaxChars 반영 + .vrew 자동 열기.
- 자막 분할 v2 (`core/caption-splitter.js`): **어절(조사 포함) 안 쪼갬**(긴 단어는 넘쳐도 1줄 → "다." 고아 방지),
  쉼표 끊기, **균형 DP**(최대 줄길이 최소화), **접속부사(그런데/그리고…) 단독 줄**. 사용자 그룹3 예시와 정확 일치.
  렌더러 splitLines도 동일. 기본 7자, 카운트는 한글·영숫자만(공백·문장부호 제외).
- 제목(훅) = **각 쇼츠 카드에서 2줄 편집**(텍스트+줄별 크기·색상·정렬, 기본 가운데). project.titleLine1/2 + t1*/t2*,
  IPC set-title({shortsNum,field,value}). DTO·save/load 포함. 미리보기 상단 표시 + **.vrew 번인**(vrew-builder
  `addTitleTrack`: 줄마다 web/textbox 트랙 상단 고정, durationSeconds 0=전체, 줄별 정렬). 검증: 2줄 트랙 OK.
- 일괄첨부 = **파일 다중선택**(openFile+multiSelections, 폴더선택 폐지). 파일명 앞 숫자=그룹, 같은번호 영상우선.
- Genspark는 **6장씩 배치 청크**(generateImagesGenspark, 한 장씩 X). 미리듣기 오디오는 fetch→blob(media:// 직접재생 실패 우회).
- 앱 표시: 그룹헤더(G·배지·모션·시간) 15px 동일크기, 자막(.sent) 14px. 문장별 시간표시 제거.
- 자산 파일명 `{그룹번호2자리}_s{N}.ext`(예 `01_s1.mp4`) — 일괄첨부 `^0*(\d+)`가 그룹번호로 매핑(훅=01).
- 미리보기 TTS: IPC `read-audio`(파일→base64 data URL)로 재생(media:// fetch가 렌더러에서 막히는 문제 우회).
- 제목 배경 도형: 사용자 .vrew 분석 → `type:'shape'`(dimensionType2/shapeType0/square) + `files[].Svg` + zip `media/<id>.vbin`
  (EJS 템플릿, `vrew/dummy/shape-square.vbin`). plane.color=`#RRGGBBAA`(채우기색+불투명도), stroke(테두리 색/불투명도/두께/점선),
  cornerRounding(0~1). vrew-builder `addShapeTrack`(제목보다 아래 zIndex, 전체 clip 링크). 세로=제목 덮음, 가로=폰트·글자수 비례.
  카드 제목영역에 도형 컨트롤(채우기/테두리/모서리/점선), project.bg* 필드, set-title/DTO/save 포함. 검증: shape+Svg+1:1 OK.
- 이미지/영상 비율: 헤더 `#aspectSel`(9:16 기본/1:1) → IPC set-aspect로 전 프로젝트 aspect 설정.
  Genspark/Flow/Grok `_aspectRatio=project.aspect`, vrew-builder 1:1(캔버스 1080×1080, ratio 1.0) 지원.
- **이미지 비율 불일치 시 중앙 배치**: vrew-builder `readImageSize`(PNG/JPEG 헤더)로 실제 비율 측정 →
  캔버스와 다르면(예: 1:1 이미지를 9:16에) **늘리지 않고 contain 중앙 배치(fillType 'fit', 켄번스 없음)**.
  비슷하면 기존처럼 꽉채움+켄번스. 검증: 1024² → width1/height0.563/yPos0.219 OK.
- 일괄첨부(IPC bulk-attach): 폴더 선택 → 파일명 앞 숫자=그룹번호 매핑, 같은 번호면 **영상 우선**.
- 채널 편집(⚙): 속도·참조음성·참조텍스트·**출력폴더·대본폴더**·이미지스타일·시드·AI고지 (save-preset).
- 대본 열기: 선택 채널의 `scriptFolder`가 dialog 기본 경로(open-script에 presetName 전달). 출력경로도 그 채널 outputFolder 기준.
- 헤더 1줄: 좌(🎬·채널명만·⚙·대본열기·프로젝트·불러오기) + grow + 우(스타일·엔진·비율·미리보기·TTS·이미지·I2V·만들기·.vrew·출력폴더).
  파일명은 그 아래 줄(#ftitle 볼드). 무음(DRY) 제거. 채널 select는 채널명만 표시(★·엔진 suffix 제거).
- 프로젝트 저장/불러오기: `~/.shots-maker/projects/<base>.smproj.json` 스냅샷(IPC save/load-project).
- ⚡ 전체 만들기(IPC make-all): TTS+이미지 동시(Promise.allSettled) → I2V영상 → .vrew → 출력폴더 열기.
- 헤더 파일명 전체표시+볼드. 그룹 헤더줄 배경+우측정렬 시간+폰트확대. 자산 파일명줄 제거.
- ⏭ **미해결**: 제목(훅)을 .vrew 영상 상단에 **번인**(현재 앱/미리보기만). Vrew 상단 텍스트트랙 형식 필요 →
  사용자가 Vrew에서 상단 제목 넣은 .vrew 샘플 주면 그 형식 맞춰 구현(자막 위치 잡은 방식과 동일).

## 앱 구조 (현재)
- 엔트리 `bootstrap.js` → `main.js`(IPC) ↔ `preload.js` ↔ `ui/index.html`(렌더러).
- `core/pipeline.js` = parseScript/toDTO/getPreset/listPresets/makeTtsManager/fillTts/fillSilent/buildProjectVrew.
- CLI `build-shorts.js`도 동일 파이프라인 사용. UI/CLI 어느 쪽이든 같은 결과.
- 실행: `npm start`(UI) 또는 `node build-shorts.js "<대본.md>" [--only N] [--dry]`(헤드리스).

## TTS 연결 핵심 (4단계에서 확정)
- **설정 재사용**: TTS는 `~/.flow-app/`의 tts-presets.json/tts-config.json/ref-audio/dict를 **그대로 재사용**
  (격리 예외 — 동일 목소리·재설정 불필요). 격리는 브라우저/봇회피 상태에만 적용.
- baseUrl: `http://192.168.219.157:9881` (LAN GPU PC, tts-config.json). /health 200 확인.
- ⚠️ **버그 주의**: `TTSManager.start()`는 omnivoice/supertonic 연결을 await하지 않음(Gemini만 await).
  헤드리스에서는 `await ttsMgr.refreshProvider(engine)`로 연결 완료를 기다린 뒤 사용해야 함.
- 프리셋 선택: `presetStore.getDefault()` 또는 `--preset <name>`. OmniVoice는 Voice Clone =
  refAudioPath+refText 필요(프리셋에 경로 들어있음). provider.synthesize → {mp3Buffer(=wav), durationSec}.

## build-shorts.js (헤드리스 CLI)
- `node build-shorts.js "<대본.md>" [--out <dir>] [--no-dry] [--only N]`
- 기본 DRY: TTS 없이 무음 오디오로 .vrew 구조 검증. 출력: `output/<파일베이스>/쇼츠N.vrew`(+.debug.json).
- `--no-dry`: 실제 자산 경로(아직 TTS/이미지 미연결 — 4·5단계에서 구현).

## 미해결/다음 작업
- 사용자 Vrew 4.0.1에서 output 의 .vrew 3개 정상 로드 확인 (자막·9:16·타임라인).
- OmniVoice 백엔드(포트 9881) 이 PC에서 가동 가능한지 확인 후 4단계 TTS 연결.
- 이미지/비디오 엔진(flow/genspark/grok)은 playwright 설치 + 로그인 세션 필요 → 5단계+.
